# VERIFY q251-q300 audit - 2026-05-18

Scope: generated published variants `q251` through `q300` in
`data/questions.ts` / `data/additionalQuestions.ts`, derived from source
questions `q027` through `q040`, on current `origin/main` commit `0478154`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Result: facts, citations, correct answers, and distractors are supported for
the slice. Fifteen generated true/false rows still fail the natural-language
bar and are mirrored in `site/questions.js`, so the defect belongs to
DATA-INTEGRITY rather than a hand edit to exported rows.

## Defects

- `q253`, `q254`, `q265`, `q266`: the generated true/false template embeds a
  full statement after `Det stämmer... att` / `It is ... that` without adapting
  capitalization or sentence flow. The facts are supported, but the visible
  prompts read like template scaffolding.
- `q261`, `q262`: the generator splices answer fragments into the voting-right
  stem: `...har fyllt 18 år gäller för...` / `...at least 18 years old applies
  to...` and the same pattern for the false home-ownership option.
- `q270`: the English false prompt says `One reason is that so votes are
  counted faster`, which is not idiomatic English.
- `q273`, `q274`: the political-party true/false variants splice fragments
  into `...gemensamt` / `...in common`, producing duplicate or ungrammatical
  prompts such as `have they replace the courts in common`.
- `q285`, `q286`: the constitutional-law list variants use singular list
  predicates in English and awkward Swedish `är listan som innehåller...`
  phrasing.
- `q289`, `q290`: the Instrument of Government variants append `om offentlig
  makt i Sverige` / `about public power in Sweden` after a complete clause.
- `q297`, `q298`: the right-of-public-access variants publish `innebär att
  ger...` / `means it gives...` splices.

## Source Coverage

- `q251`: generated from `q027`, `Så här styrs Sverige` / `Sveriges
  statsskick`, printed page 13, covering Sweden as a constitutional monarchy
  where the king or queen is head of state without political power.
- `q252`-`q255`: generated from `q028`, `Så här styrs Sverige` / `Staten`,
  printed page 12, covering the opposition's role in scrutinizing the
  government's work and proposing alternative policy.
- `q256`-`q263`: generated from `q029` and `q030`, `Politiska val och
  partier` / `Val och röstning`, printed page 14, covering four-year national,
  regional, and municipal elections plus Swedish citizenship and age 18 for
  Riksdag voting.
- `q264`-`q267`: generated from `q031`, `Politiska val och partier` /
  `Folkomröstningar`, printed page 14, covering advisory referendums.
- `q268`-`q271`: generated from `q032`, `Politiska val och partier` / `Så här
  går det till att rösta`, printed page 14, covering secret voting behind a
  screen.
- `q272`-`q283`: generated from `q033` through `q035`, `Politiska val och
  partier` / `Politiska partier` and `Proportionella val`, printed page 15,
  covering shared party ideas, proportional seat allocation, and the Riksdag
  four-percent threshold.
- `q284`-`q295`: generated from `q036` through `q038`, `Lag och rätt` /
  `Grundlagarna`, `Regeringsformen`, and `Successionsordningen`, printed page
  16, covering Sweden's four constitutional laws, public power from the people,
  and succession to the throne.
- `q296`-`q300`: generated from `q039` and `q040`, `Lag och rätt` /
  `Allemansrätten` and `Rättsväsendet`, printed page 17, covering public access
  with responsibility and the authorities in the Swedish justice system.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q251`-`q300`:

- `Last verified commit`: `0478154`
- `Status`: `defect` for `q253`, `q254`, `q261`, `q262`, `q265`, `q266`,
  `q270`, `q273`, `q274`, `q285`, `q286`, `q289`, `q290`, `q297`, and `q298`
- `Status`: `ok` for the other rows in the slice
- `Next recheck`: `rolling-source-slice` for accepted rows and
  `queued-data-integrity-residual` for defect rows
