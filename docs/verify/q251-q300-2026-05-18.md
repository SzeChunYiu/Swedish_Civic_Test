# VERIFY q251-q300 current-output refresh - 2026-05-18

Scope: generated published variants `q251` through `q300` in
`content/question-bank.csv` and `site/questions.js`, derived from source
questions `q027` through `q039`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Current output checked: `origin/main` commit `6ef2182`, after the later
generated true/false prefix, false-explanation, q601/q651 cleanup work, and
partial generated single-choice filler cleanup PR #788.

Result: facts, citations, answer keys, explanations, and ordinary distractors
remain supported for the slice. Current canonical CSV and static-site output
clear the stale q251-q300 true/false prefix defects, old q255 false-answer
explanation mismatch, generic fallback options, and true/false judgement-shell
option rows. Remaining learner-visible defects are generated wording/template
issues owned by DATA-INTEGRITY.

## Current Defects

- Ordinary generated single-choice meta-stem rows: `q252`, `q260`, `q264`,
  `q272`, `q276`, `q280`, `q284`, `q288`, `q292`, `q296`, and `q300` still
  begin with `Vilket svar är korrekt?` / `Which answer is correct?`. This
  folds into the remaining generated single-choice cleanup after PR #788.
- Generated true/false standalone-stem defects: `q270` and `q271` begin
  `En anledning är...` / `One reason is...` without naming that the reason is
  for voting behind a screen in a polling station. The underlying fact and
  answer keys are supported, but the stems do not stand alone naturally.

## Cleared Since The Earlier Report

- The stale q251-q300 true/false prefix surface is clean: 0/25 true/false rows
  in this slice begin with `Sant eller falskt:` / `True or false:` in
  canonical CSV or `site/questions.js`.
- `q255` now has an aligned false-answer explanation: it states that the
  opposition should scrutinize the government's work and propose alternative
  policies, then says the prompted negative statement is false and `False` is
  correct.
- PR #788 cleared the q251-q300 generic fallback options and true/false
  judgement-shell option rows. `q253`, `q256`, `q265`, and `q268` now use
  statement-comparison options instead of `Sant` / `Falskt` plus generic
  filler options.
- The old q253/q254, q261/q262, q265/q266, q273/q274, q285/q286, q289/q290,
  and q297/q298 splice/meta-stem defects remain cleared in current output.

## Source Coverage

- `q251`: generated from `q027`, `Så här styrs Sverige` / `Sveriges
  statsskick`, printed page 13, covering Sweden as a constitutional monarchy
  where the king or queen is head of state without political power.
- `q252`-`q255`: generated from `q028`, `Så här styrs Sverige` / `Staten`,
  printed page 12, covering the opposition's role in scrutinizing the
  government's work and proposing alternative policy.
- `q256`-`q263`: generated from `q029` and `q030`, `Politiska val och
  partier` / `Val och röstning`, printed page 14, covering four-year national,
  regional, and municipal elections plus Swedish citizenship and age 18 for
  Riksdag voting.
- `q264`-`q267`: generated from `q031`, `Politiska val och partier` /
  `Folkomröstningar`, printed page 14, covering advisory referendums.
- `q268`-`q271`: generated from `q032`, `Politiska val och partier` /
  `Så här går det till att rösta`, printed page 14, covering secret voting
  behind a screen.
- `q272`-`q283`: generated from `q033` through `q035`, `Politiska val och
  partier` / `Politiska partier` and `Proportionella val`, printed page 15,
  covering shared party ideas, proportional seat allocation, and the Riksdag
  four-percent threshold.
- `q284`-`q295`: generated from `q036` through `q038`, `Lag och rätt` /
  `Grundlagarna`, `Regeringsformen`, and `Successionsordningen`, printed page
  16, covering Sweden's four constitutional laws, public power from the people,
  and succession to the throne.
- `q296`-`q300`: generated from `q039`, `Lag och rätt` / `Allemansrätten`,
  printed page 17, covering public access with responsibility.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q251`-`q300` at `6ef2182`:

- `defect` / `queued-data-integrity-single-choice-meta-stem`:
  `q252`, `q260`, `q264`, `q272`, `q276`, `q280`, `q284`, `q288`, `q292`,
  `q296`, and `q300`.
- `defect` / `queued-data-integrity-standalone-stem`: `q270` and `q271`.
- `ok`: the other 37 rows in the slice.
