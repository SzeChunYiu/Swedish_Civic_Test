# VERIFY q351-q400 current-output refresh - 2026-05-19

Scope: generated published variants `q351` through `q400` in
`content/question-bank.csv` and `site/questions.js`, derived from source
questions `q051` through `q063`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Current output checked: `origin/main` commit `cd44093`, after generated
true/false closures, generated single-choice filler cleanup, rebrand work, and
later release/static/native copy changes.

Result: facts, citations, answer keys, explanations, and ordinary distractors
remain supported for this slice. The previous `120d440` q351-q400 report is
stale: its named residuals no longer describe the current q351-q400 output.
Current defects are generated-language surfaces, not source-support failures.

## Current Defects

- Generated single-choice meta prompts: `q353`, `q354`, `q357`, `q358`,
  `q361`, `q362`, `q365`, `q366`, `q369`, `q370`, `q373`, `q374`, `q377`,
  `q378`, `q381`, `q382`, `q385`, `q386`, `q389`, `q390`, `q393`, `q394`,
  `q397`, and `q398` still begin with `Välj rätt alternativ:` /
  `Choose the correct option:` or `Vilket svar stämmer bäst?` /
  `Which answer best matches?`. These fold into the existing generated
  single-choice meta-prompt cleanup tasks already queued in `codex-tasks/open.txt`.
- Generated true/false Swedish naturalness: `q367`, `q368`, `q387`, and
  `q388` are factually supported and have usable English stems, but the Swedish
  stems duplicate the infinitive phrase after the subject, e.g. `Att köpa sex i
  Sverige är olagligt att köpa sex...` and `Äktenskap mellan personer av samma
  kön i Sverige är tillåtet att gifta sig...`. These need a focused
  DATA-INTEGRITY generator/validator/static-mirror cleanup.

## Cleared Since The Earlier Report

- The old q358/q359 standalone-subject defects are not present in current
  output: the current rows now belong to the gender-equality source question
  and state the concept directly.
- The old q371 answer-ambiguity defect is not present in current output:
  current q371 asks about the Convention on the Rights of the Child being law
  in Sweden since 2020 and marks `Sant` / `True` as correct.
- The old q374/q375 mid-sentence minority-name capitalization defects are not
  present in current output: current q374-q375 are the 1979 child-violence-ban
  rows and are factually supported.
- The old q398/q399 pronoun-only trade-union defects are not present in
  current output: current q398-q399 are the private-sector workforce-share rows
  and name the concept directly.

## Source Coverage

- `q351`-`q353`: generated from `q051`, `Mänskliga rättigheter` /
  `Mänskliga rättigheter gäller alla`, printed page 22, covering the creation
  of the United Nations after the Second World War to prevent war and protect
  human rights.
- `q354`-`q357`: generated from `q052`, `Mänskliga rättigheter` /
  `FN:s förklaring om de mänskliga rättigheterna`, printed page 22, covering
  the 1948 declaration and its 30 articles.
- `q358`-`q361`: generated from `q053`, `Mänskliga rättigheter` /
  `Jämställdhet mellan könen`, printed page 23, covering equal rights, duties,
  and power for women and men to influence society and their own lives.
- `q362`-`q365`: generated from `q054`, `Mänskliga rättigheter` /
  `Könsrelaterat våld och förtryck`, printed page 23, covering violence in
  close relationships and honour-related violence and oppression as crimes
  under Swedish law.
- `q366`-`q369`: generated from `q055`, `Mänskliga rättigheter` /
  `Sexköpslagen`, printed page 24, covering that buying sex is illegal in
  Sweden while the person selling sex is not punished.
- `q370`-`q373`: generated from `q056`, `Mänskliga rättigheter` /
  `Barns rättigheter`, printed page 24, covering the Convention on the Rights
  of the Child as Swedish law since 2020.
- `q374`-`q377`: generated from `q057`, `Mänskliga rättigheter` /
  `Barns rättigheter`, printed page 25, covering Sweden's 1979 decision to ban
  hitting children.
- `q378`-`q381`: generated from `q058`, `Mänskliga rättigheter` /
  `Nationella minoriteter och urfolk`, printed page 25, covering Jews, Roma,
  Sami, Sweden Finns, and Tornedalians as Sweden's five national minorities.
- `q382`-`q385`: generated from `q059`, `Mänskliga rättigheter` /
  `Nationella minoriteter och urfolk`, printed page 25, covering the Sami
  Parliament's role representing the Sami population on language, culture, and
  identity.
- `q386`-`q389`: generated from `q060`, `Mänskliga rättigheter` /
  `Hbtqi-personer`, printed page 26, covering that marriage between people of
  the same sex is allowed in Sweden and that relevant discrimination is
  prohibited.
- `q390`-`q393`: generated from `q061`, `Mänskliga rättigheter` /
  `Arbetet mot diskriminering`, printed page 26, covering DO's role working
  for equal rights and ensuring the Discrimination Act is followed.
- `q394`-`q397`: generated from `q062`, `Arbetsmarknad och privatekonomi` /
  `Så fungerar arbetsmarknaden`, printed page 27, covering the public sector as
  activities for which the state, regions, and municipalities are responsible.
- `q398`-`q400`: generated from `q063`, `Arbetsmarknad och privatekonomi` /
  `Så fungerar arbetsmarknaden`, printed page 27, covering about 70 percent of
  Sweden's workforce working in the private sector.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q351`-`q400` at `cd44093`:

- `defect` / `queued-data-integrity-single-choice-meta-prompt`: `q353`,
  `q354`, `q357`, `q358`, `q361`, `q362`, `q365`, `q366`, `q369`, `q370`,
  `q373`, `q374`, `q377`, `q378`, `q381`, `q382`, `q385`, `q386`, `q389`,
  `q390`, `q393`, `q394`, `q397`, and `q398`.
- `defect` / `queued-data-integrity-tf-sv-naturalness`: `q367`, `q368`,
  `q387`, and `q388`.
- `ok`: the other 22 rows.

Focused CSV/static scan on current output reported 50 checked rows, 50
generated rows, 24 single-choice meta-prompt rows, four true/false Swedish
naturalness defects, no missing static rows, and no static text mismatches.
