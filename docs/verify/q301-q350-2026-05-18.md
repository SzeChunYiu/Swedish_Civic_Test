# VERIFY q301-q350 current-output refresh - 2026-05-18

Scope: generated published variants `q301` through `q350` in
`content/question-bank.csv` and `site/questions.js`, derived from source
questions `q040` through `q052`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Current output checked: `origin/main` commit `8bb2ccf`, after DATA-INTEGRITY
removed generated true/false meta-stem wrappers and stripped redundant
true/false UI prefixes.

Result: facts, citations, correct answers, and ordinary distractors remain
supported for the slice. The earlier q301-q350 splice/meta and true/false
prefix defects are cleared in current output. The remaining defects are seven
generated true/false wording issues owned by DATA-INTEGRITY.

## Current Defects

- `q331` and `q339` remain false-answer explanation mismatches: their visible
  questions are false statements and the correct answer is `False`, but the
  generated explanations still begin by telling the learner that the underlying
  positive statement is true.
- `q318` and `q319` still have non-native English age phrasing (`from 15 years`
  / `from 13 years`) instead of natural `from age 15` / `from age 13`.
- `q346`, `q347`, and `q350` still are not standalone propositions after
  prefix stripping: `One reason is...` does not say it is a reason the UN was
  created, and `It was presented...` does not name the UN declaration.

## Cleared Since The Earlier Report

- The 25 true/false rows in this slice no longer begin with redundant
  UI-afforded `Sant eller falskt:` / `True or false:` prefixes.
- `q305` and `q306` no longer append the source question after an answer
  fragment.
- `q313`, `q314`, `q321`, and `q322` no longer use infinitive or option
  fragments before `beskriver` / `describes`.
- `q317` and `q318` no longer drop the English verb before `criminally
  responsible`, though `q318` still needs the age-phrase cleanup noted above.
- `q325` and `q326` no longer turn `Genom att...` / `By...` fragments into the
  full true/false stem.
- `q329`, `q330`, `q337`, and `q338` no longer publish stem-level
  `Det stämmer... att` / `It is factually true that` scaffolding.
- `q333` and `q334` no longer publish bare organization lists without a
  predicate.
- `q345` and `q346` no longer publish the ungrammatical `One reason is that
  prevent/decide` pattern, though `q346` still needs a standalone referent.

## Source Coverage

- `q301`-`q303`: generated from `q040`, `Lag och rätt` / `Rättsväsendet`,
  printed page 17, covering the Police, Swedish Prosecution Authority, courts,
  Crime Victim Authority, and Prison and Probation Service as parts of the
  Swedish justice system.
- `q304`-`q307`: generated from `q041`, `Lag och rätt` / `Rättssäkerhet`,
  printed page 17, covering equal treatment before the law, fair trial,
  independent courts, defence with a lawyer, and appeal rights.
- `q308`-`q311`: generated from `q042`, `Lag och rätt` / `Domstolar`, printed
  page 18, covering presumption of innocence, courts' role in deciding guilt
  and sanctions, civil disputes, appeals, and court levels.
- `q312`-`q315`: generated from `q043`, `Lag och rätt` / `Polisen`, printed
  page 18, covering police responsibility for law and order, preventing and
  investigating crime, protection, passports, national ID cards, and permits.
- `q316`-`q319`: generated from `q044`, `Lag och rätt` /
  `Straffmyndighet och belastningsregister`, printed page 19, covering
  criminal responsibility from age 15 and the 2026 proposal about age 13 for
  serious crimes.
- `q320`-`q331`: generated from `q045` through `q047`, `Mediernas roll` /
  `Fria medier`, printed page 20, covering free media, scrutiny of people with
  power, public access to official documents, secrecy limits, source anonymity,
  responsible publishers, and checking information with independent sources.
- `q332`-`q339`: generated from `q048` and `q049`, `Mediernas roll` /
  `Public service`, printed page 21, covering SR, SVT, and UR as public
  service companies, their special mission, independence from political and
  other interests, impartial reporting, advertising limits, and tax-fee
  financing.
- `q340`-`q343`: generated from `q050`, `Mediernas roll` / `Källkritik`,
  printed page 21, covering source criticism as questioning and checking
  whether information is correct.
- `q344`-`q347`: generated from `q051`, `Mänskliga rättigheter` / `Mänskliga
  rättigheter gäller alla`, printed page 22, covering the creation of the UN
  after the Second World War to prevent war and protect human rights.
- `q348`-`q350`: generated from `q052`, `Mänskliga rättigheter` / `FN:s
  förklaring om de mänskliga rättigheterna`, printed page 22, covering the
  1948 declaration and its 30 articles.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q301`-`q350` at `8bb2ccf`:

- `ok`: all rows except the seven current generated true/false residuals.
- `defect` / `queued-data-integrity-false-explanation`: `q331` and `q339`.
- `defect` / `queued-data-integrity-residual-grammar`: `q318`, `q319`,
  `q346`, `q347`, and `q350`.
