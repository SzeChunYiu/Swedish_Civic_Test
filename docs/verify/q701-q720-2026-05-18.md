# VERIFY q701-q720 audit - 2026-05-18

Scope: current exported rows `q701` through `q720`, first audited on the
content-equivalent `origin/main` commit `332bfc9` and rebased over
`3e63c23`, plus the ledger inventory drift introduced by the new authored
source question `q144`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Result: source facts, citations, answer keys, and ordinary distractors are
supported for the slice. The q144 insertion expanded the exported bank from
715 to 720 rows, so `docs/verify/ledger.md` was refreshed to match the current
question bank and to realign generated rows by stable source question and
variant kind.

## Defects

- `q706` and `q707`: the generated true/false prompts publish answer fragments
  about Lucia food as complete statements. The facts are supported by the UHR
  Lucia section, but the visible prompts are not natural propositions.
- `q714` and `q715`: the generated true/false prompts for the
  Götaland/Svealand/Norrland statement use meta wording, `Det stämmer i sak
  att` / `It is factually true that` and `Det stämmer inte att` / `It is not
  true that`, instead of direct learner-facing propositions.
- `q718` and `q719`: the generated true/false prompts publish bare answer
  fragments (`25 landskap` / `21 landskap`) as complete statements.
- `q704`, `q708`, `q712`, `q713`, `q716`, and `q720`: generated single-choice
  variants include the already-routed fallback option `Det går inte att avgöra
  av materialet` / `It cannot be determined from the material`. This belongs
  to `REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1`, not a hand edit to
  generated rows.

## Source Coverage

- `q701`-`q704`: generated from `q140`, `Traditioner och högtider` / `Jul`,
  printed page 47, covering Christmas as both a Christian-rooted holiday and a
  major family holiday in Sweden, including non-religious celebration.
- `q705`-`q708`: generated from `q141`, `Traditioner och högtider` / `Lucia`,
  printed page 47, covering Lucia on 13 December and saffron Lucia buns.
- `q709`-`q712`: generated from `q142`, `Landet Sverige` / `Skogar, sjöar och
  öar`, printed page 6, covering Sweden having about 250,000 islands.
- `q713`-`q716`: generated from `q143`, `Landet Sverige` / `Sveriges
  indelning`, printed page 6, covering Sweden's usual division into Götaland,
  Svealand, and Norrland.
- `q717`-`q720`: generated from new source `q144`, `Landet Sverige` /
  `Sveriges indelning`, printed page 6, covering Sweden's 25 historical
  provinces and the distinction from counties, municipalities, and major parts.

## Ledger Update

Updated `docs/verify/ledger.md`:

- Coverage summary now inventories 720 exported rows, 144 authored source
  questions, and 576 generated published variants.
- Inserted `q144` as an authored source row and shifted generated rows to match
  current `data/questions.ts` output by source question and variant kind.
- Added generated rows `q716`-`q720`.
- Marked `q701`, `q702`, `q703`, `q705`, `q709`, `q710`, `q711`, and `q717` as
  `ok`.
- Marked `q706`, `q707`, `q714`, `q715`, `q718`, and `q719` as `defect` with
  `queued-data-integrity-residual`.
- Marked `q704`, `q708`, `q712`, `q713`, `q716`, and `q720` as `defect` with
  `queued-data-integrity-unknown-option`.
