# VERIFY q701-q720 audit - 2026-05-18

Scope: current exported rows `q701` through `q720` in
`data/questions.ts` / `data/additionalQuestions.ts`, verified against current
`origin/main` commit `d1753ae`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

PDF evidence: `pdfinfo` reported the current official file as `Sverige i
fokus`, 48 pages, modified 2026-05-07. `pdftotext -layout` confirmed the cited
printed pages and sections used by this slice.

Result: q701-q720 facts, citations, answer keys, explanations, and ordinary
distractors are supported on current output. The older q714/q715 meta-stem and
false-answer explanation defects are now stale: current canonical CSV and
`site/questions.js` render direct q714/q715 propositions and aligned true/false
explanations. Current remaining learner-visible defects are the generated
single-choice judgement-template rows `q713` and `q716`.

Those q713/q716 defects are already queued as
`REVIEWER-GENERATED-JUDGEMENT-Q701-Q720-CURRENT-1`; no additional
DATA-INTEGRITY queue item was filed from this VERIFY slice.

## Defects

- `q713` and `q716`: generated single-choice rows ask the learner to choose or
  match an option for a statement, but the options are `Sant`, `Falskt`,
  `Inget av alternativen stämmer`, and `Endast ibland` / `True`, `False`,
  `None of the options is correct`, and `Only sometimes`. This recreates a
  true/false judgement prompt inside a single-choice shell.

## Current Output Check

- Checked 20 rows (`q701`-`q720`) in `content/question-bank.csv`.
- Checked the same 20 rows in `site/questions.js`.
- Found 10 true/false rows in the slice.
- Found zero redundant `Sant eller falskt:` / `True or false:` prefixes in
  current CSV/static output for the slice.
- Found zero true/false stem meta wrappers such as `Påståendet är sant`,
  `The statement is true`, `Det är inte sant att`, or `It is not true that` in
  the current question stems for the slice.
- Found zero generated unknown-material fallback options in the slice.
- Found two generated single-choice judgement-template defects: `q713` and
  `q716`.
- Confirmed no missing static rows and no CSV/static text mismatches.

## Source Coverage

- `q701`-`q704`: generated from `q140`, `Traditioner och högtider` / `Jul`,
  printed page 47. The source says Christmas is traditionally Christian,
  is a major family holiday in Sweden, and is also celebrated by many people
  without religious meaning. The correct answers and false alternatives align.
- `q705`-`q708`: generated from `q141`, `Traditioner och högtider` / `Lucia`,
  printed page 47. The source says Lucia is on 13 December and that special
  saffron Lucia buns are commonly served that day. The correct answers and
  false alternatives align.
- `q709`-`q712`: generated from `q142`, `Landet Sverige` / `Skogar, sjöar och
  öar`, printed page 6. The source says Sweden has about 250,000 islands, more
  than any other country. The correct answers and false alternatives align.
- `q713`-`q716`: generated from `q143`, `Landet Sverige` / `Sveriges
  indelning`, printed page 6. The source says Sweden is usually divided into
  Götaland, Svealand, and Norrland, and describes where each major part is.
  The source facts and answer keys align, and current q714/q715 stems are
  direct propositions. Current q713/q716 learner-visible option surfaces are
  defective as described above.
- `q717`-`q720`: generated from `q144`, `Landet Sverige` / `Sveriges
  indelning`, printed page 6. The source says Sweden is divided into 25
  historical provinces, and also into 21 counties and 290 municipalities. The
  correct answers and false alternatives align.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q701`-`q720`:

- `Last verified commit`: `d1753ae`
- `Status`: `defect` with `queued-data-integrity-q701-q720-current` for
  `q713` and `q716`
- `Status`: `ok` with `rolling-source-slice` for the other rows in the slice

The previous `q714` and `q715` defect markings are cleared for current output;
the current q701-q720 residuals are `q713` and `q716`.
