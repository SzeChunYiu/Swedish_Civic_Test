# VERIFY q301-q350 current-output refresh - 2026-05-19

Scope: generated published variants `q301` through `q350` in
`content/question-bank.csv` and `site/questions.js`, derived from source
questions `q040` through `q052`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Current output checked: `origin/main` commit `e90f82a`, after the
DATA-INTEGRITY q301-q350 true/false wording cleanup, later generated-output
cleanup, and partial generated single-choice filler cleanup PR #788.

Result: facts, citations, answer keys, explanations, and ordinary distractors
remain supported for the slice. Current canonical CSV and static-site output
clear the stale q301-q350 true/false prefix defects, q318/q319 age-phrase
defects, q331/q339 false-answer explanation mismatches, and q346/q347/q350
standalone-stem defects. Remaining learner-visible defects are generated
single-choice meta-stem rows owned by DATA-INTEGRITY.

## Current Defects

- Ordinary generated single-choice meta-stem rows: `q304`, `q308`, `q312`,
  `q316`, `q320`, `q324`, `q328`, `q336`, `q344`, and `q348` still begin with
  `Vilket svar är korrekt?` / `Which answer is correct?`. These fold into the
  remaining generated single-choice cleanup already queued after PR #788.

## Cleared Since The Earlier Report

- `q318` and `q319` now use natural English age phrasing (`from age 15` /
  `from age 13`) and supported Swedish stems.
- `q331` and `q339` now have aligned false-answer explanations that state the
  prompted negative statement is false and `False` is correct.
- `q346`, `q347`, and `q350` are now standalone propositions naming the UN or
  the UN Universal Declaration of Human Rights directly.
- The slice has zero redundant `Sant eller falskt:` / `True or false:`
  prefixes in canonical CSV or `site/questions.js`.
- The prior splice/list/predicate defects in q305/q306, q313/q314, q321/q322,
  q333/q334, q345/q346, and related rows remain cleared in current output.

## Source Coverage

- `q301`-`q303`: generated from `q040`, `Lag och rätt` / `Rättsväsendet`,
  printed page 17, covering the Police, Swedish Prosecution Authority, courts,
  Crime Victim Authority, and Prison and Probation Service as parts of the
  Swedish justice system.
- `q304`-`q307`: generated from `q041`, `Lag och rätt` / `Rättssäkerhet`,
  printed page 17, covering equal treatment before the law, fair trial,
  independent courts, defence with a lawyer, and appeal rights.
- `q308`-`q311`: generated from `q042`, `Lag och rätt` / `Domstolar`, printed
  page 18, covering presumption of innocence, courts' role in deciding guilt
  and sanctions, civil disputes, appeals, and court levels.
- `q312`-`q315`: generated from `q043`, `Lag och rätt` / `Polisen`, printed
  page 18, covering police responsibility for law and order, preventing and
  investigating crime, protection, passports, national ID cards, and permits.
- `q316`-`q319`: generated from `q044`, `Lag och rätt` /
  `Straffmyndighet och belastningsregister`, printed page 19, covering
  criminal responsibility from age 15 and the 2026 proposal about age 13 for
  serious crimes.
- `q320`-`q331`: generated from `q045` through `q047`, `Mediernas roll` /
  `Fria medier`, printed page 20, covering free media, scrutiny of people with
  power, public access to official documents, secrecy limits, source
  anonymity, responsible publishers, and checking information with independent
  sources.
- `q332`-`q339`: generated from `q048` and `q049`, `Mediernas roll` /
  `Public service`, printed page 21, covering SR, SVT, and UR as public
  service companies, their special mission, independence from political and
  other interests, impartial reporting, advertising limits, and tax-fee
  financing.
- `q340`-`q343`: generated from `q050`, `Mediernas roll` / `Källkritik`,
  printed page 21, covering source criticism as questioning and checking
  whether information is correct.
- `q344`-`q347`: generated from `q051`, `Mänskliga rättigheter` /
  `Mänskliga rättigheter gäller alla`, printed page 22, covering the creation
  of the UN after the Second World War to prevent war and protect human
  rights.
- `q348`-`q350`: generated from `q052`, `Mänskliga rättigheter` / `FN:s
  förklaring om de mänskliga rättigheterna`, printed page 22, covering the
  1948 declaration and its 30 articles.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q301`-`q350` at `e90f82a`:

- `defect` / `queued-data-integrity-single-choice-meta-stem`: `q304`, `q308`,
  `q312`, `q316`, `q320`, `q324`, `q328`, `q336`, `q344`, and `q348`.
- `ok`: the other 40 rows.
