# VERIFY q201-q250 current-output refresh - 2026-05-18

Scope: generated published variants `q201` through `q250` in
`content/question-bank.csv` and `site/questions.js`, derived from source
questions `q015` through `q027`.

Primary source opened: UHR, `Sverige i fokus`, official PDF:
https://www.uhr.se/globalassets/_uhr.se/medborgarskapsprovet/utbildningsmaterial/sverige-i-fokus.pdf

Current output checked: `origin/main` commit `719cb3b`, after DATA-INTEGRITY
removed the generated true/false prefix surface, aligned false-answer
explanations, and accepted later generated true/false wording cleanups.

Result: facts, citations, answer keys, and ordinary distractors remain
supported for the slice. The earlier q201-q250 splice defects in `q206`,
`q237`, and `q238`, all true/false prefix defects, and the `q235`
false-answer explanation mismatch are stale on current output. The remaining
defects are generated single-choice answer-quality surfaces owned by
DATA-INTEGRITY.

## Current Defects

- `q204`, `q208`, `q212`, `q216`, `q220`, `q224`, `q228`, `q232`, `q240`,
  `q244`, and `q248`: generated single-choice rows use generic filler options
  `Inget av alternativen stämmer` / `None of the options is correct` and
  `Endast ibland` / `Only sometimes` instead of useful, stem-specific
  distractors. These belong with the generated single-choice filler-option
  cleanup.
- `q233` and `q236`: generated single-choice rows ask the learner to choose or
  match an option for the statement `Riksdagen väljer statsminister` / `The
  Riksdag chooses the prime minister`, but the options are `Sant`, `Falskt`,
  `Inget av alternativen stämmer`, and `Endast ibland` / `True`, `False`,
  `None of the options is correct`, and `Only sometimes`. This recreates a
  true/false judgement prompt inside a single-choice shell and belongs with
  `REVIEWER-GENERATED-JUDGEMENT-TRUEFALSE-FILLER-ALL-1`.

## Cleared Since The Earlier Report

- All 25 true/false rows in this slice now have direct stems without redundant
  `Sant eller falskt:` / `True or false:` prefixes.
- `q235` now has answer `False` and explains that the prompted statement is
  false, so the earlier false-answer explanation mismatch is stale.
- `q206` is now a grammatical direct proposition about citizens choosing
  Riksdag members by voting in general elections.
- `q237` is now a single-choice prompt asking which statement describes
  government agencies; the prior true/false splice defect no longer applies.
- `q238` is now a grammatical direct proposition about government agencies
  implementing decisions and following laws and government instructions.

## Current Output Check

- Checked 50 rows (`q201`-`q250`) in `content/question-bank.csv`.
- Checked the same 50 rows in `site/questions.js`.
- Found 25 true/false rows in the slice.
- Found zero redundant `Sant eller falskt:` / `True or false:` prefixes.
- Found zero true/false stem meta wrappers such as `Påståendet är sant`,
  `The statement is true`, `Det är inte sant att`, or `It is not true that`.
- Found zero false-answer explanation mismatches in the slice, including the
  prior offender `q235`.
- Found 13 generated single-choice filler/judgement defects: `q204`, `q208`,
  `q212`, `q216`, `q220`, `q224`, `q228`, `q232`, `q233`, `q236`, `q240`,
  `q244`, and `q248`.
- Confirmed no missing static rows and no CSV/static text mismatches.

## Source Coverage

- `q201`-`q203`: generated from `q015`, `Sveriges demokratiska system` / `Hot
  mot demokratin`, printed page 11, covering how low voter turnout can reduce
  people's ability to influence political decisions and increase differences
  between groups.
- `q204`-`q207`: generated from `q016`, `Så här styrs Sverige` / `Staten`,
  printed page 12, covering parliamentary representative democracy, general
  elections, Riksdag members, laws, and the state budget.
- `q208`-`q215`: generated from `q017` and `q018`, `Så här styrs Sverige` /
  `Staten`, printed page 12, covering the Riksdag's 349 members, four-year
  elections, and the Riksdag's role in choosing the prime minister.
- `q216`-`q219`: generated from `q019`, `Politiska val och partier` / `Val och
  röstning`, printed page 14, covering the age threshold for voting and Swedish
  citizenship for Riksdag elections.
- `q220`-`q223`: generated from `q020`, `Politiska val och partier` /
  `Folkomröstningar`, printed page 14, covering advisory referendums and the
  fact that politicians do not have to follow the result.
- `q224`-`q227`: generated from `q021`, `Så här styrs Sverige` / `Landet styrs
  på olika nivåer`, printed page 12, covering national, regional, and municipal
  political responsibility.
- `q228`-`q235`: generated from `q022` and `q023`, `Så här styrs Sverige` /
  `Staten`, printed page 12, covering Riksdag decisions about laws and the
  state budget plus the Riksdag's role in choosing the prime minister.
- `q236`-`q239`: generated from `q024`, `Så här styrs Sverige` /
  `Myndigheter`, printed page 13, covering government agencies implementing
  decisions and following laws and government instructions.
- `q240`-`q243`: generated from `q025`, `Så här styrs Sverige` / `Regioner och
  kommuner`, printed page 13, covering regions' responsibility for health care,
  public transport, and county museums.
- `q244`-`q247`: generated from `q026`, `Så här styrs Sverige` / `Kommunernas
  ansvar`, printed page 13, covering municipal services such as water, sewage,
  care, snow removal, parks, and adult education.
- `q248`-`q250`: generated from `q027`, `Så här styrs Sverige` / `Sveriges
  statsskick`, printed page 13, covering Sweden as a constitutional monarchy
  where the king or queen is head of state without political power.

## Ledger Update

Updated `docs/verify/ledger.md` rows `q201`-`q250` at `719cb3b`:

- `defect` / `queued-data-integrity-single-choice-filler-options`: `q204`,
  `q208`, `q212`, `q216`, `q220`, `q224`, `q228`, `q232`, `q240`, `q244`,
  and `q248`.
- `defect` / `queued-data-integrity-judgement-truefalse-filler-all`: `q233`
  and `q236`.
- `ok` / `rolling-source-slice`: every other row in the slice, including all
  true/false rows.
