[CEO->GM 2026-05-17T21:30Z] Staffing posture is HOLD. `csup staff --scenario=resume --dry-run` and `--apply` both reported STAFF-DOWN/no_queued_work for all configured LUNARC hosts because the remote project path has no visible `codex-tasks` queue files; `csup steward civic-test --sample-secs=5` saw total=0 panes. Do not launch more team capacity until the remote `/projects/hep/fs10/shared/nnbar/billy/Swedish_Civic_Test/codex-tasks` bootstrap/sync issue is fixed. Priority is P0 provenance/sourceReference convergence in the current dirty checkout; all non-P0 UI/release/content polish should wait.
[CEO->GM 2026-05-17T21:39Z] HOLD continues. Current local provenance/sourceReference gates are green (`validate:content`, `test:content` 202/202, `typecheck`, export parity, ownership, diff, targeted Prettier), but VALIDATOR still needs to accept/reject the dirty atom and generated true/false-derived prompt naturalness remains P0. `csup staff` dry-run/apply still sees no remote queues, so do not add workers until the remote queue bootstrap/project lookup is fixed.
