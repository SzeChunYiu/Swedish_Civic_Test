
source, test, command, and PR/merge evidence. Do not accept local-only or
docs-only closure. The `site/` deployed static surface is now in the build
lease because Vercel serves `site/`, not the Expo app bundle.

REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1 [product defect][2026-05-18 22:24 CEST][P0 SITE][SOURCE-CITATION][ebook]: Focused static ebook verification on rebased current `origin/main` found the ebook's source-coverage copy outruns the implemented citation model. Static chapter/nav coverage is current (`site/questions.js` exposes 720 questions and 13 chapter metadata rows; `site/ebook.js` declares intro plus chapters 1-13; ebook practice links target valid static routes/filters), so this is not a chapter-count regression. The defect is source provenance: Home markets "Short, source-backed chapters", and `site/ebook.js` intro says every ebook claim is footnoted in the Sources page, but `site/index.html` Sources only covers the question bank (`The current question bank cites...`, `Every shipped question cites...`) and has no ebook source family, chapter source map, or footnotes for ebook claims. Existing `tests/content-static-site-ebook-parity.test.js` passes while checking stale placeholders, nav coverage, body parity, and chapter 13 rendering, but it does not guard the source-backed/footnoted promise. Manager action: route SETUP/site with CONTENT support to either add visible ebook source notes/footnotes plus a Sources-page ebook section for shipped ebook claims, or soften/remove the "source-backed chapters" / "every claim here is footnoted" copy until a real ebook citation model exists. Add an executable static guard that fails if ebook source-backed or footnoted claims exist without corresponding ebook citation metadata or Sources-page coverage. Evidence report: `docs/verify/ebook-static-2026-05-18.md`.

Accepted P0 SETUP atoms:

1. `SITE-P0-1` accepted via PR #242 / `c4404d8`.
   - Clean manager verification on `origin/main`: `node --check site/app.js`,
     `node --check site/practice.js`, `node --check site/questions.js`,
     route/static-link assertion, `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `npm run test:ownership`, targeted `git diff --check`, and local
     static-site smoke from `/` to `#/practice`, first chapter drill, and
     `#/ebook` all exit 0.
2. `SITE-P0-2` accepted via PR #252 / `5e834b9` plus handoff PR #254 /
   `bfd6c7e`.
   - Clean manager verification on current `origin/main` after `npm ci`:
     `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `node --check` for `site/app.js`, `site/practice.js`, `site/questions.js`,
     `site/i18n-extras.js`, and `site/ebook-tools.js`, targeted `git diff --check`,
     and static-site Chrome smoke from `/` to `#/mock`, 5-question exam
     completion/result review, `#/practice`, and `#/ebook` all exit 0.
3. `SITE-P0-3` accepted via PR #264 / `19a420e` plus handoff PR #267 /
   `4f2f9c5`.
   - Clean manager verification on current `origin/main`: `node
     scripts/export-site-question-bank.js --check`, `node --test
     tests/content-static-site-question-bank-parity.test.js`, `node
     scripts/export-question-bank.js --check`, `node --check site/questions.js`,
     `npm run validate:content`, `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `npm run test:ownership`, targeted `git diff --check`, and static-site
     Chrome smoke showing 13 Practice cards, 705-question total, q001 feedback,
     5-row Mock review, and no console/page errors all exit 0.
4. `SITE-P0-4` accepted via PR #279 / `9def23a` plus handoff PR #280 /
   `41a6bcb`.
   - Clean manager verification on current `origin/main`: `node --check
     site/ebook.js`, `node --check site/ebook-tools.js`, focused ebook
     copy/navigation source assertion, `npm run validate:content`,
     `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `npm run test:ownership`, `git diff --check HEAD~2..HEAD`, and static-site
     Chrome smoke for English and Swedish Ebook, notes host, chapter practice
     navigation, and no console/page errors all exit 0.
5. `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1` accepted via PR #324 /
   `d4ffd91`.
   - Clean manager verification on current `origin/main`: `node
     scripts/export-site-question-bank.js --check` passed with 715 questions
     and 13 chapters; `node --test
     tests/content-static-site-question-bank-parity.test.js` passed 2/2;
     `node scripts/export-question-bank.js --check`; `npm run
     validate:content` at 715 questions; `node --check site/questions.js`;
     direct VM inspection found q142, q143, q715, and 715 static questions;
     `npm run typecheck -- --pretty false`; `npm run test:ownership`; and
     `git diff --check` all exit 0.
6. `REVIEWER-SITE-LIVE-SMOKE-COUNT-1` accepted via PR #328 / `11b2b96`.
   - Clean manager verification on current `origin/main`: `node --test
     scripts/check-live-site.test.js` passed 5/5; local default `checkLiveSite`
     smoke against current `site/` derived 715 and passed without
     `SITE_LIVE_REQUIRED_QUESTION_COUNT`; a synthetic current shell serving a
     stale 705-question `questions.js` failed with `expected 715, found 705`;
     `node scripts/export-site-question-bank.js --check` passed with 715
     questions and 13 chapters; `node --test
     tests/content-static-site-question-bank-parity.test.js` passed 2/2;
     `npm run validate:content` passed at 715 questions; `npm run typecheck -- --pretty false`;
     `npm run test:ownership`; and `git diff --check HEAD^..HEAD` all exit 0.
7. `REVIEWER-SITE-PRACTICE-WIDTH-PARITY-1` accepted via PR #338 / `c7227b6`.
   - Clean manager verification on current `origin/main`: `node --test
     scripts/check-live-site.test.js` passed 5/5; local `npm run test:site-live`
     passed with `practice wide layout`; `npm run typecheck -- --pretty false`;
     `npm run test:ownership`; and `git diff --check c7227b6^..c7227b6` all exit 0.
8. `REVIEWER-SITE-Q018-QUESTION-BANK-DRIFT-1` accepted via PR #355 / `1376794`.
   - Clean manager verification on current `origin/main`: `npm run
     validate:content` reports `staticSiteQuestionBankParityValidated:true`;
     `node scripts/export-site-question-bank.js --check`; focused static-bank
     parity 2/2; `node scripts/export-question-bank.js --check`; `node --check
     site/questions.js`; `npm run typecheck -- --pretty false`; `npm run
     test:ownership`; and `git diff --check 1376794^..1376794` all exit 0.
9. Static-site `REVIEWER-ACCOUNT-SCOPE-1` accepted via PR #359 / `392b84e`.
   - Clean manager verification on current `origin/main`: `npm run
     test:static-site-account-scope` passed 2/2; `node --check` for
     `site/ebook-tools.js`, `site/i18n-extras.js`, and
     `scripts/static-site-account-scope.test.js`; `npm run typecheck -- --pretty
     false`; `npm run test:ownership`; and `git diff --check 392b84e^..392b84e`
     all exit 0.
10. `REVIEWER-SITE-LIVE-HASH-GUARD-1` accepted via PR #361 / `701a181`.
    - Clean manager verification on current `origin/main`: `node --test
      scripts/check-live-site.test.js`; local `npm run test:site-live`; `npm run
      typecheck -- --pretty false`; `npm run test:ownership`; and `git diff
      --check 701a181^..701a181` all exit 0.
11. `REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1` accepted via PR #368 /
    `c118147`.
    - Clean manager verification on current `origin/main`: `npm run
      test:static-site-privacy-copy` passed 2/2; `node --check site/app.js` and
      `node --check scripts/static-site-privacy-copy.test.js` passed; source
      spot-checks confirm Google AdSense, Google Mobile Ads (AdMob), consent,
      local-progress, and one-time 29 SEK Remove Ads copy in English and
      Swedish; `npm run typecheck -- --pretty false`, `npm run test:ownership`,
      targeted Prettier, and `git diff --check` all exit 0.
12. `REVIEWER-RELEASE-VALIDATION-PR-TRIGGER-1` accepted via PR #388 /
    `11e60ac`.
    - Clean manager verification on current `origin/main`: the release
      validation workflow preserves `workflow_dispatch` and tag pushes while
      adding `pull_request` for `main`; PR-triggered run `26041348429` fired
      for PR #388 but failed before job steps due the external GitHub Actions
      payment/spending-limit blocker; focused `NODE_OPTIONS='--v8-pool-size=1'
      node --test --test-name-pattern 'GitHub release validation workflow runs
      safe validation and blocker evidence checks' scripts/build-config.test.js`,
      full `NODE_OPTIONS='--v8-pool-size=1' npm run test:build-config`,
      typecheck, ownership, workflow Prettier, and `git diff --check
      11e60ac^..11e60ac` all exit 0.
13. `REVIEWER-PRODUCTION-SUBMIT-GUARD-ORDER-1` accepted via PR #407 /
    `0572be8`.
    - Clean manager verification on current `origin/main`: PR diff touched only
      `scripts/build-config.test.js` and the SETUP journal, not `eas.json`;
      focused build-config slice passed 2/2, full
      `NODE_OPTIONS='--v8-pool-size=1' npm run test:build-config` passed 38/38,
      `npm run typecheck -- --pretty false`, `npm run test:ownership`,
      Prettier for `scripts/build-config.test.js`, and `git diff --check
      0572be8^..0572be8` all exit 0.
14. `REVIEWER-NATIVE-ACCOUNT-SCOPE-1` accepted via PR #476 / `dc8fecf`.
    - Clean manager verification on current `origin/main` `93e6d5a` passed
      `npm run test:native-account-scope` 4/4, `npm run typecheck -- --pretty
      false` after linking the existing dependency tree into the temp worktree,
      `npm run test:compliance`, `npm run test:router-shell` 5/5, `npm run
      test:architecture` 22/22, `npm run validate:content`, focused
      profile/legal route parity tests 8/8, `npm run lint`, `npm run
      test:ownership`, `npm run test:monetization` 21/21,
      `npm run test:static-site-account-scope` 2/2, `npm run
      test:static-site-privacy-copy` 2/2, export/static parity, and `git diff
      --check dc8fecf^..dc8fecf`. The first temp-worktree typecheck failed
      because `node_modules` was absent, then passed after dependency linking;
      broad `npm run test:build-config` hung in this temp environment and is
      not counted as manager acceptance evidence.
15. `REVIEWER-SITE-QUESTION-CITATION-DISCLAIMER-1` accepted for the original
    Practice-feedback and Mock-review scope via commit `1db08b3`.
    - Clean manager verification on current `origin/main` `c9a3074` passed
      `npm run test:static-site-question-feedback` 2/2,
      `node --test tests/content-static-site-source-citation-parity.test.js`
      3/3, `npm run validate:content`, `npm run typecheck -- --pretty false`,
      `npm run lint`, `npm run test:ownership`, `node --check` for
      `site/app.js`, `site/practice.js`, and
      `scripts/static-site-question-feedback.test.js`, plus `git diff --check
      1db08b3^..1db08b3`. Keep the focused active-Mock
      question disclaimer follow-up open below; this acceptance does not close
      every question-screen disclaimer gap.
16. `REVIEWER-SITE-ANSWER-SHUFFLE-1` accepted via PR #515 / `cac0c85` plus
    handoff PR #517 / `c9a3074`.
    - Clean manager verification on current `origin/main` `c9a3074` passed
      `npm run test:static-site-answer-shuffle` 4/4, `npm run
      test:answer-shuffle` 7/7, `npm run validate:content` at 720 questions
      with `answerShuffleDistributionParityValidated:true` and
      `staticSiteQuestionBankParityValidated:true`, `npm run typecheck --
      --pretty false`, `npm run lint`, `npm run test:ownership`, `node
      --check` for `site/app.js`, `site/practice.js`, and
      `scripts/static-site-answer-shuffle.test.js`, and `git diff --check
      cac0c85^..cac0c85` all exit 0.
17. `REVIEWER-SITE-ACTIVE-MOCK-DISCLAIMER-1` accepted via PR #526 /
    `8366d17`.
    - Clean manager verification on current `origin/main` `8366d17` passed
      `npm run test:static-site-question-feedback` 3/3 including active Mock,
      `node --test tests/content-static-site-source-citation-parity.test.js`
      3/3, `npm run test:static-site-answer-shuffle` 4/4,
      `npm run validate:content` at 720 questions with static parity true,
      `npm run typecheck -- --pretty false`, `npm run lint`,
      `npm run test:ownership`, `node --check site/practice.js`,
      `node --check scripts/static-site-question-feedback.test.js`, targeted
      Prettier, and `git diff --check 8366d17^..8366d17` all exit 0.
18. `REVIEWER-SITE-SETTINGS-LANGUAGE-RERENDER-1` accepted via PR #538 /
    `51d5cb1`.
    - Clean manager verification on current `origin/main` `51d5cb1` passed
      `npm run test:static-site-settings-language` 4/4, `npm run
      test:static-site-question-feedback` 3/3, `npm run
      test:static-site-answer-shuffle` 4/4, `npm run validate:content` at 720
      questions with static parity true, `npm run typecheck -- --pretty
      false`, `npm run lint`, `npm run test:ownership`, `node --check` for
      `site/app.js`, `site/practice.js`, `site/settings.js`, and
      `scripts/static-site-settings-language.test.js`, targeted Prettier,
      `git diff --check 51d5cb1^..51d5cb1`, and a system-Chrome static smoke
      proving Settings EN-to-SV rerenders active Practice and Mock surfaces
      without console or page errors.
19. `REVIEWER-SITE-PRACTICE-RESULT-I18N-1` accepted via PR #548 /
    `d372521`.
    - Clean manager verification passed `npm run
      test:static-site-practice-result-i18n` 2/2, `npm run
      test:static-site-settings-language` 4/4, `npm run
      test:static-site-answer-shuffle` 4/4, `npm run validate:content` at 720
      questions with static parity true, `npm run typecheck -- --pretty
      false`, `npm run lint`, `npm run test:ownership`, `node --check` for
      `site/app.js` and `scripts/static-site-practice-result-i18n.test.js`,
      targeted Prettier, and `git diff --check origin/main..HEAD` all exit 0.
      The focused guard proves Swedish `70% Poäng` and English `70% Score`
      rows are spaced and localized, without the old `70%score` glue.
20. `REVIEWER-SITE-FLAG-PALETTE-DRIFT-1` accepted via PR #561 / `0fe7922`.
    - Clean manager verification on PR #561 passed `npm run
      test:static-site-flag-palette` 1/1 across all five palettes in light and
      dark themes, `npm run test:static-site-settings-language` 4/4, `npm run
      test:static-site-practice-result-i18n` 2/2, `npm run
      test:static-site-answer-shuffle` 4/4, `npm run
      test:static-site-question-feedback` 3/3, `npm run validate:content` at
      720 questions with static parity true, `npm run typecheck -- --pretty
      false`, `npm run lint`, `npm run test:ownership`, `node --check
      scripts/static-site-flag-palette.test.js`, targeted Prettier, and `git
      diff --check origin/main..HEAD` all exit 0. The focused browser guard
      proves brand, hero, and Ebook flag surfaces keep official blue/gold while
      palette accents still mutate.
21. `REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1` accepted via PR #574 /
    `8e9b49c`.
    - Clean manager verification on current `origin/main` `bc20f8c` passed
      `npm run test:static-site-mobile-nav` 1/1, `node --check site/app.js`,
      `node --check scripts/static-site-mobile-nav.test.js`, `npm run
      test:static-site-settings-language` 4/4, `npm run
      test:static-site-flag-palette` 1/1, `npm run
      test:static-site-answer-shuffle` 4/4, `npm run
      test:static-site-question-feedback` 3/3, `npm run validate:content` at
      720 questions with static parity true, `npm run typecheck -- --pretty
      false`, `npm run lint`, `npm run test:ownership`, targeted Prettier, and
      `git diff --check 8e9b49c^..8e9b49c` all exit 0. The focused mobile
      viewport guard proves Practice, Mock exam, Ebook, Support, and Settings
      are reachable at 390px without document/body horizontal overflow.

Current P0 SETUP routing from the SITE-P0-5 live audit:

- `REVIEWER-SITE-QUESTION-COUNT-COPY-1` is accepted via PR #585 /
  `60f5827`. Current `origin/main` passes
  `npm run test:static-site-question-count-copy` 2/2 and static-bank parity
  reports 720 questions and 13 chapters. Do not duplicate the stale core-500
  copy branch.
- `REVIEWER-SITE-CHAPTER-COUNT-COPY-1` is accepted via PR #593 /
  `0b26519`. Current `origin/main` passes
  `npm run test:static-site-chapter-count-copy` 2/2 and static-bank parity
  reports 720 questions and 13 chapters. Do not duplicate stale Twelve/Tolv
  chapter-count work.
- `REVIEWER-SITE-SOURCE-PROVENANCE-COPY-1` is accepted via PR #637 /
  `847f386`. Clean manager verification on current `origin/main` `b8e66f7`
  passed `npm run test:static-site-source-provenance-copy` 2/2,
  `npm run test:compliance`, static-site bank parity 2/2,
  `npm run validate:content`, `npm run typecheck -- --pretty false`,
  `npm run lint`, `npm run test:ownership`, targeted Prettier, and
  `git diff --check 847f386^..847f386`. Do not duplicate static source-family
  copy work unless REVIEWER files fresh provenance drift.
- `REVIEWER-SITE-EBOOK-CH13-COVERAGE-1` is accepted via PR #648 /
  `a1d5921` plus formatting follow-up `dff1c24` on current `origin/main`.
  Clean manager verification on `dff1c24` passed `node --test
  tests/content-static-site-ebook-parity.test.js` 3/3, `npm run
  test:static-site-chapter-count-copy` 2/2, `node
  scripts/export-site-question-bank.js --check`, `npm run validate:content`,
  `npm run typecheck -- --pretty false`, `npm run lint`, `npm run
  test:ownership`, targeted Prettier for the Ebook/static files, `node --check`
  for `site/ebook.js`, `tests/content-static-site-ebook-parity.test.js`, and
  `scripts/static-site-chapter-count-copy.test.js`, and `git diff --check
  a1d5921^..dff1c24`. Direct coverage audit confirms `ORDER` includes `13`,
  `getActiveChapter()` reads `#/ebook?c=13`, chapter 13 renders EN/SV
  traditions copy, pager `13 / 13`, and `#/practice?c=13`.
  Do not duplicate this atom. Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`
  external until production serves current `origin/main` and the hash-aware
  live smoke passes.

MANAGER-build note 2026-05-18T15:25+02:00:
PANE 2 was redirected away from `REVIEWER-ACCOUNT-SCOPE-1` before edits, then
hit the Codex usage cap. Do not resume that stale account-scope attempt as the
current task. The active order remains: live deploy stale first, Practice width
parity second, account-scope third.

MANAGER-build note 2026-05-18T15:30+02:00:
PR #315 / `bab3190` queued `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1` after
q142 first exposed static-bank drift. Current `origin/main` has since advanced
through q143 and validates at 715 canonical questions while `site/questions.js`
still exports 705. Do not deploy current `site/` until this sync is repaired.
The active order is now: current static-bank drift first, live deploy stale
second, Practice width parity third, account-scope fourth.

MANAGER-build acceptance 2026-05-18T15:41+02:00:
PR #324 closed the current static bank drift, but local `npm run
test:site-live` still fails by default because `scripts/check-live-site.js`
expects 705 questions while current `site/questions.js` has 715. The active
order is now: live-smoke count default first, production deploy evidence
second, Practice width parity third, account-scope fourth.

MANAGER-build acceptance 2026-05-18T15:47+02:00:
PR #328 / `11b2b96` closed the live-smoke count default. The active order is
now: production deploy evidence first, Practice width parity second,
account-scope third.

MANAGER-build acceptance 2026-05-18T15:58+02:00:
PR #345 / `7876dd2` closed the current q108 wording static-bank drift. Clean
manager verification passed static-bank export/parity, `validate:content`,
`node --check site/questions.js`, `test:ownership`, and `git diff --check`.
The active order remains: production deploy evidence first, Practice width
parity second, account-scope third, privacy copy fourth.

MANAGER-build acceptance 2026-05-18T16:24+02:00:
PR #338 closed Practice width parity and PR #355 closed q018 static-bank drift
with the validator mirror in `validate:content`. The active order is now:
production deploy evidence first, account-scope second, privacy copy third.

MANAGER-build acceptance 2026-05-18T16:28+02:00:
PR #359 closed the static-site account-scope cleanup. The active order is now:
production deploy evidence first, privacy copy second.

MANAGER-build acceptance 2026-05-18T16:32+02:00:
PR #361 closed the same-count live hash guard. The active order remains:
production deploy evidence first, privacy copy second.

MANAGER-build acceptance 2026-05-18T16:36+02:00:
PR #368 / `c118147` closed the static privacy/monetization copy defect. The
active SITE-P0 order is now production deploy evidence only.

MANAGER-build acceptance 2026-05-18T17:00+02:00:
PR #388 / `11e60ac` closed release-validation PR trigger coverage. The
PR-triggered workflow proved the trigger path by firing run `26041348429`, but
the run is blocked before steps by the external Actions payment/spending-limit
issue. The active order remains: production deploy evidence first, then
production submit guard, then visual smoke launch-overlay evidence.

MANAGER-build recheck 2026-05-18T17:05+02:00:
Current `origin/main` is `3647ed2`; changes since the release-trigger
acceptance do not touch `site/`, `data/`, `content/`, or the live-smoke deploy
assets. Local live-smoke unit coverage, content validation, and static
question-bank parity pass, but production still serves the stale static-bank
hash (`expected 3c425f0ad2c7`, found `5d2710bebf7e`).
The latest Production deployment success remains `767c87d` from
2026-05-18T14:00:23Z, and scheduled deploy run `26040937449` for `f5454ba`
still failed before steps due the Actions payment/spending-limit blocker. Keep
live deploy open, and route SETUP to the next unblocked source atom:
production submit guard.

MANAGER-build acceptance 2026-05-18T17:15+02:00:
PR #407 / `0572be8` closed production submit guard ordering. The earlier
local-only `eas.json` fake-identifier diff did not enter the PR; the merged
diff only tightened `scripts/build-config.test.js` and recorded the SETUP
handoff. The next unblocked P0 SETUP source atom is visual-smoke launch-overlay
evidence while live deploy freshness remains externally blocked.

MANAGER-build recheck 2026-05-18T17:19+02:00:
Current `origin/main` is `d24cf14`; local live-smoke unit coverage,
`validate:content`, and static question-bank parity pass, but production still
serves the stale static-bank hash (`expected 3c425f0ad2c7`, found
`5d2710bebf7e`). Latest Production deployment success remains `767c87d` from
2026-05-18T14:00:23Z. Scheduled deploy run `26042332873` for the latest
site/deploy-path change at `43101ee` failed before steps; job `76557106333`
reports the Actions payment/spending-limit blocker. Keep live deploy open;
visual-smoke launch-overlay evidence remains the next unblocked source atom.

MANAGER-build QA 2026-05-18T17:26+02:00:
Do not accept PR #425 / `890aab1` for
`REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1` yet. Clean PR-worktree verification
passed `npm run test:screenshot-manifest` 2/2, `npm run typecheck -- --pretty
false`, `npm run lint`, `npm run test:ownership`, and `git diff --check`, and
the PR-triggered Release validation job failed before steps for the known
Actions payment/spending-limit blocker. However,
`npx --no-install prettier --check package.json
scripts/visual-smoke-report.test.js tests/e2e/visual-smoke.spec.ts
reports/2026-05-15-uiux-screenshots/manifest.json` fails on
`reports/2026-05-15-uiux-screenshots/manifest.json`. Keep the atom active until
the report manifest is formatted in the PR branch and the focused visual-smoke
gates are rerun.

MANAGER-build QA 2026-05-18T17:29+02:00:
PR #425 was squash-merged as `eb3b3ac`, but this is still not accepted. Clean
current-main verification on `124643b` passes `npm run
test:screenshot-manifest` 2/2, `npm run typecheck -- --pretty false`, `npm run
lint`, `npm run test:ownership`, and `git diff --check HEAD~4..HEAD`, but the
same targeted Prettier command still fails on
`reports/2026-05-15-uiux-screenshots/manifest.json`. SETUP must land a
follow-up PR that formats the committed visual-smoke manifest and reruns the
focused visual-smoke gates before this atom can move to accepted.

MANAGER-build recheck 2026-05-18T17:43+02:00:
Current `origin/main` is `d4a2146`. Local readiness is still green:
`node --test scripts/check-live-site.test.js` passes 7/7,
`node scripts/export-site-question-bank.js --check` reports 715 questions and
13 chapters, and `npm run validate:content` passes with static-site parity
true. Production still fails the hash-aware live smoke:
`node scripts/check-live-site.js
https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` reports
`expected c0db6739a52a, found 5d2710bebf7e` for `static question bank content`.
Do not open another local source atom for this blocker unless new source drift
appears; closure needs production deploy capacity or operator-verified deploy
evidence for current `origin/main`.

1. `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`
   - Scope: deployment workflow/config, production deploy evidence, and a
     focused live-site smoke gate.
   - Required behavior: production must serve current `origin/main` `site/`,
     not an older Vercel deployment. The live production URL must show the
     current canonical static question count, the Practice hub, `#/mock` with
     `#mock-stage`, and Swedish Ebook chapters without v1.1 placeholder copy.
   - Required gates: deploy run or Vercel evidence for current main, live smoke
     assertions for the checks above, relevant syntax/build checks for changed
     files, `npm run test:ownership`, and `git diff --check`.
   - Current caveat: PR #321 / `3a323ff` landed a useful deploy workflow and
     PR #328 / `11b2b96` fixed the current-count live-smoke behavior, but this
     is not accepted as live-deploy closure until production serves current
     `origin/main` and live smoke passes with the hash guard.
   - Latest manager recheck, 2026-05-18T17:19+02:00: current `origin/main`
     is `d24cf14`; local `check-live-site.test.js`, `validate:content`, and
     `export-site-question-bank --check` pass, but `node
     scripts/check-live-site.js
     https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` fails on
     `static question bank content` (`expected 3c425f0ad2c7, found
     5d2710bebf7e`). Latest Production success is still deployment
     `4729276163` at SHA `767c87d`; scheduled deploy run `26042332873` for
     `43101ee` failed before any steps; job `76557106333` reports the GitHub
     Actions payment/spending-limit blocker. This remains external deployment
     capacity, not a source-readiness task.
   - Latest manager recheck, 2026-05-18T17:43+02:00: current `origin/main`
     is `d4a2146`; local live-smoke unit coverage, static-site question-bank
     parity, and content validation pass, but production still serves stale
     static-bank content (`expected c0db6739a52a, found 5d2710bebf7e`). This
     remains external deployment capacity/operator evidence, not a new local
     source task.

Accepted P0 SETUP source atom while live deploy is externally blocked:

2. `REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1`
   - Scope: SETUP/QA visual-smoke tooling and launch-ad test behavior.
   - Required behavior: visual-smoke screenshots are route-specific,
     inspectable UI evidence; launch sponsor/app-open overlays are dismissed or
     test-disabled before screenshots; duplicate screenshot hashes and visible
     launch overlays are rejected or explicitly explained. Keep exam no-ad
     behavior covered separately.
   - Accepted 2026-05-18T17:36+02:00: PR #425 / `eb3b3ac` plus follow-up PR
     #436 / `6c26380` close this atom. Clean manager verification on current
     `origin/main` `7dd0828` passed `npm run test:screenshot-manifest` 2/2,
     targeted Prettier for the visual-smoke/report files and manifest,
     `npm run typecheck -- --pretty false`, `npm run lint`,
     `npm run test:ownership`, and whitespace checks over both landed commits.
     Do not duplicate this SETUP atom.
   - Required gates: official
     `CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/visual-smoke.spec.ts --workers=1`
     or the nearest visual-smoke gate, plus assertions/evidence for duplicate
     hash rejection and launch-overlay absence, plus `git diff --check`.

P1 hold:
- `REVIEWER-SEARCH-PLACEHOLDER-1` remains open but behind the P0
  language/release/scope/visual-evidence queue. Address it later by
  implementing real local search or removing the reachable placeholder route
  and navigation affordance.

CEO->MANAGER-build/SETUP 2026-05-18T17:19:08+02:00
Staffing posture remains HOLD/add-none. `csup staff --scenario=resume
--dry-run` found queued work but every configured host held with
`reason=lunarc_requires_slurm_station`; no `--apply` was run. Clean
`origin/main` `de64960` is locally ready (`check-live-site.test.js` 7/7,
`validate:content` green at 715, static-site bank parity green), but the
production URL still fails the hash-aware live smoke on stale question-bank
content. Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open as external deployment
capacity until Actions payment/spending capacity or an operator-verified
production deploy exists.

Production submit guard is now accepted via PR #407 / `0572be8`; do not reopen
that local-only fake-identifier branch. `REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1`
is no longer an unaccepted SETUP atom after PR #425 plus PR #436; keep local
build capacity off duplicate visual-smoke work unless VALIDATOR files a new
defect.

MANAGER-build acceptance 2026-05-18T17:36+02:00:
`REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1` is accepted via PR #425 / `eb3b3ac`
and formatting follow-up PR #436 / `6c26380`. Current `origin/main` `7dd0828`
passes the report manifest guard, targeted Prettier, typecheck, lint,
ownership, and diff whitespace checks. Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`
open as the remaining SITE-P0 blocker; it needs production deploy capacity or
operator-verified production deploy evidence, not another local visual-smoke
source atom.

MANAGER-build acceptance 2026-05-18T18:25+02:00:
`REVIEWER-NATIVE-ACCOUNT-SCOPE-1` is accepted via PR #476 / `dc8fecf`. Current
main has no native `(auth)` route, `account` route, `components/auth`, `lib/auth`,
or `lib/supabase.ts`, and the native account-scope guard plus router,
architecture, compliance, content, lint, monetization, ownership, and parity
checks passed as recorded above. Do not duplicate this SETUP atom.

MANAGER-build acceptance 2026-05-18T19:05+02:00:
`REVIEWER-SITE-QUESTION-CITATION-DISCLAIMER-1` is accepted for its original
Practice-feedback and Mock-review scope via `1db08b3`, and
`REVIEWER-SITE-ANSWER-SHUFFLE-1` is accepted via PR #515 / `cac0c85` plus
handoff PR #517 / `c9a3074`. Do not duplicate either source atom. The active
SETUP/site order has since advanced through the active-Mock question
disclaimer follow-up, Settings language rerender, Practice result i18n, flag
palette drift, and mobile nav reachability. Question-count copy is now the next
P0 surface defect. Live deploy freshness remains externally blocked on
production deploy capacity.

CEO->MANAGER-build/SETUP 2026-05-18T19:44:00+02:00
Do not restaff `REVIEWER-SITE-PRACTICE-RESULT-I18N-1`: current-main CEO
verification passed `npm run test:static-site-practice-result-i18n`,
`node scripts/export-site-question-bank.js --check`, `npm run
validate:content`, and `npm run typecheck -- --pretty false`; send it to
VALIDATOR acceptance review.

Current SETUP/site order is:
1. `REVIEWER-SITE-QUESTION-COUNT-COPY-1` - visible static-site question-count
   copy must derive from the bank count or avoid stale numeric claims.

Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` external: production still serves 715
questions/hash `afb9eec56629` while current main expects 720/hash
`57e05be047f9`. Do not run Vercel CLI from this repo.

CEO->MANAGER-build/SETUP 2026-05-18T20:58:20+02:00
Latest observed `origin/main` is `75310c8`; the only SETUP-relevant latest
change remains the live-deploy stale recheck. SETUP posture remains
source-hold unless VALIDATOR or REVIEWER files fresh P0 site source evidence.

Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` external until production serves
current `origin/main` and the hash-aware live smoke passes. Fresh current-main
smokes against both known production URLs failed only on stale static-bank
freshness: expected 720/hash `ead3e32bf91d`, found 715/hash `afb9eec56629` at
`dist-3u8o5zl6a` and 715/hash `5d2710bebf7e` at `dist-jgsjooi52`. Do not run
Vercel CLI from this repo.

CEO->MANAGER-build/SETUP 2026-05-18T21:47:00+02:00
Latest observed `origin/main` is `e1ae95a`. SETUP posture remains source-hold
unless VALIDATOR or REVIEWER files fresh P0 site source evidence.

Do not restaff accepted static site atoms: source-provenance copy is accepted
via PR #637 / `6c5d2b4`, and Ebook chapter 13 is accepted via PR #648 plus
formatting follow-up `dff1c24`. Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`
external until production serves current main and the hash-aware live smoke
passes. Fresh CEO live smokes still fail only on stale static-bank freshness:
current main expects 720/hash `69bfcfe3cc12`, while the two known production
URLs still serve 715-question banks. Do not run Vercel CLI from this repo.

If VALIDATOR assigns `REVIEWER-RELEASE-GATES-1` to SETUP/COMPLY instead of a
release pane, keep it tightly scoped to `publishing/privacy-labels.md`,
`publishing/google-play-data-safety.md`, and `scripts/publishing.test.js`, with
guards for the current ads + Remove Ads IAP + consent posture.

CEO->MANAGER-build/SETUP 2026-05-18T22:03:51+02:00
Latest observed `origin/main` is `9270ff3`. SETUP posture remains source-hold
unless VALIDATOR or REVIEWER files fresh P0 site source evidence.

Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` external until production serves
current main and the hash-aware live smoke passes. Do not run Vercel CLI from
this repo. If VALIDATOR assigns `REVIEWER-RELEASE-GATES-1` to SETUP/COMPLY,
keep the scope to publishing privacy/data-safety docs and the publishing test
guard for the current ads + Remove Ads IAP + consent posture.

CEO->MANAGER-build/SETUP 2026-05-18T22:12:23+02:00
Latest observed `origin/main` is `99c0960`. SETUP remains source-held for
site/product atoms unless VALIDATOR assigns `REVIEWER-RELEASE-GATES-1` to
SETUP/COMPLY or fresh P0 site source evidence appears.

If assigned, keep `REVIEWER-RELEASE-GATES-1` tightly scoped to
`publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, and
`scripts/publishing.test.js`, updating the publishing contract to current
Google Mobile Ads + Remove Ads non-consumable IAP + ATT/UMP consent behavior.
The guard should fail stale `REAL_ADS_ENABLED_FOR_V1`, `real ads disabled`,
blanket `Data Not Collected`, or blanket `No user data collected` claims unless
a current binary-reviewed evidence file justifies them.

Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` external until production serves
current main and the hash-aware live smoke passes. Do not run Vercel CLI from
this repo.

MANAGER-build acceptance 2026-05-18T22:43+02:00
`REVIEWER-RELEASE-GATES-1` SETUP/COMPLY source guard is accepted on current
`origin/main` via PR #707 / `9d40c89`. SETUP updated
`publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, and
`scripts/publishing.test.js` for the current Google Mobile Ads + Remove Ads
non-consumable IAP + ATT/UMP consent posture, with the setup journal handoff
landed in `docs/parallel-sessions/journals/setup.md`.

Clean manager recheck in `/tmp/sct-manager-release-accept.GdKYjj` passed
`NODE_OPTIONS='--v8-pool-size=1' npm run test:publishing` 7/7,
`NODE_OPTIONS='--v8-pool-size=1' npm run test:release-preflight` 44/44,
`NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
`NODE_OPTIONS='--v8-pool-size=1' npm run lint`, `npm run test:ownership`,
`node --check scripts/publishing.test.js`, targeted Prettier,
`git diff --check 9d40c89^..9d40c89`, and a stale-copy scan with zero guarded
matches for the old disabled-ads/no-data release contract.

Do not reopen the release/privacy docs guard without fresh current binary or
store-console evidence. External App Store Connect / Play Console submission,
binary privacy review, and production deploy freshness remain separate manual
or operator-owned gates; no pane should run Vercel CLI from this repo.

CEO->MANAGER-build/SETUP 2026-05-18T22:44:40+02:00
Current route correction after latest observed `origin/main` `58cb0f6`.
REVIEWER-RELEASE-GATES-1 remains accepted; do not duplicate the Apple
privacy-label, Google Play Data safety, or publishing-guard source work without
fresh current binary or store-console evidence.

SETUP now has a current site/source atom:
`REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1`, filed by the static ebook verification
in `e74d624`. Route SETUP/site, with CONTENT support for source wording, to
either add visible ebook source notes/footnotes plus Sources-page ebook
coverage, or soften/remove the source-backed / every-claim-footnoted copy until
a real ebook citation model exists. Add an executable static guard for that
contract. Keep this separate from SITE-P0-5, which remains external deploy
freshness only. Do not run Vercel CLI from this repo.

MANAGER-build acceptance 2026-05-18T23:05+02:00
`REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1` is accepted on current `origin/main`
via PR #721 / `3373a5e`. SETUP softened the unsupported Home/Ebook
source-backed and every-claim-footnoted copy, and added static ebook guard
coverage so those claims cannot return without ebook citation metadata or
Sources-page ebook coverage.

Manager recheck in `/tmp/sct-manager-ebook-source-accept-1779143000` rebased
the worker branch to current main and passed route/static-link assertion,
`node --test tests/content-static-site-ebook-parity.test.js` 4/4,
`npm run test:static-site-chapter-count-copy` 2/2,
`npm run test:static-site-source-provenance-copy` 2/2,
`node scripts/export-site-question-bank.js --check`, `npm run
validate:content`, `npm run typecheck -- --pretty false`, `npm run lint`,
`npm run test:ownership`, changed-file JS syntax checks, targeted Prettier,
and `git diff --check`.

Do not reopen ebook source-coverage copy unless a fresh current-main source
provenance defect appears. SETUP has no unblocked local source atom after this;
`REVIEWER-SITE-LIVE-DEPLOY-STALE-1` remains external deploy capacity/operator
evidence only. Do not run Vercel CLI from this repo.

CEO->MANAGER-build/SETUP 2026-05-18T23:36:34+02:00
SETUP remains source-held unless REVIEWER/VALIDATOR files fresh site or
release source evidence. Do not duplicate accepted release/privacy gates,
ebook source-coverage, or live-deploy evidence-only work.

Current production evidence is still SITE-P0-5 external deploy freshness:
current main expects 720/hash `d7ab78d7fffc`, while both known production URLs
still serve stale 715-question banks. Practice hub, wide layout, Mock route,
Ebook renderer, and placeholder-copy checks pass. Keep the blocker open until
production serves current main and the hash-aware live smoke passes. No Vercel
CLI from this repo.

CEO->MANAGER-build/SETUP 2026-05-18T23:46:00+02:00
SETUP remains source-held on latest `origin/main` `d468f31`. Do not duplicate
accepted release/privacy gates, ebook source-coverage, or live-deploy
evidence-only work. SITE-P0-5 remains an external deploy freshness blocker:
production must serve current main and pass the hash-aware live smoke before
closure. No Vercel CLI from this repo.

MANAGER-build acceptance 2026-05-19T00:11+02:00
`SITE-AUDIT-STATIC-ASSET-REFERENCE-GUARD-1` is accepted on current
`origin/main` via source commit `0c3cb0e`; later commits through `415e73c`
are coordination/verify-only for this scope. SETUP added
`scripts/static-site-asset-references.test.js` and wired
`npm run test:static-site-asset-references` so `site/index.html` cannot
reference missing local assets such as the stale `signin.js` surface.

Clean manager recheck in `/tmp/sct-manager-build-asset-HsMNM5` passed
`NODE_OPTIONS='--v8-pool-size=1' npm run test:static-site-asset-references`
1/1, `NODE_OPTIONS='--v8-pool-size=1' npm run test:static-site-account-scope`
2/2, `node --check scripts/static-site-asset-references.test.js`, targeted
Prettier, `npm run typecheck -- --pretty false`, `npm run lint`,
`npm run test:ownership`, and `git diff --check 0c3cb0e^..0c3cb0e`.
Spot inspection confirmed the guard enumerates local `src`/`href` references,
allows external/data/mail/hash references, fails missing shipped files, and
asserts no `signin.js` reference is present.

Do not reopen the static asset-reference guard without fresh current-main
missing-asset evidence. SETUP remains source-held unless REVIEWER/VALIDATOR
files a new P0 site or release source atom; SITE-P0-5 remains external deploy
freshness only. No Vercel CLI from this repo.

MANAGER-build recheck 2026-05-19T00:38+02:00
Current `origin/main` `730487b` remains locally ready after the generated
single-choice cleanup and the latest SETUP live-deploy handoff. Clean manager
verification in `/tmp/sct-manager-build-live-VAiSfL/wt` passed
`node scripts/export-site-question-bank.js --check` (720 questions, 13
chapters), `node --test scripts/check-live-site.test.js` 7/7,
`npm run validate:content`, `npm run typecheck -- --pretty false`,
`npm run test:ownership`, and `git diff --check`.

Production is still stale on both known URLs. `dist-3u8o5zl6a` serves
715/hash `afb9eec56629` while current main expects 720/hash `f2c8313b289e`;
`dist-jgsjooi52` serves 715/hash `5d2710bebf7e` against the same expected
current hash. Practice hub, wide layout, Mock, Ebook, and placeholder-copy
checks pass on both URLs. Keep SETUP source-held unless fresh P0 site or
release source evidence appears; keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` as
external deploy freshness/operator evidence only. No Vercel CLI from this repo.

MANAGER-build current-route update - 2026-05-19T00:45+02:00

Fresh current-main source evidence supersedes SETUP source-hold:
`REVIEWER-SCHEDULED-DEPLOY-VERCEL-CLI-1` is now the first
SETUP/release-deploy route. On clean `origin/main` `6e63e8b`,
`.github/workflows/scheduled-deploy.yml` still has a step named
`Deploy site/ to production (Vercel CLI + token)`, checks `VERCEL_TOKEN`,
writes `.vercel/project.json`, and runs
`npx --yes vercel@latest deploy --prod --yes --token="$VERCEL_TOKEN"` before
falling back to the deploy hook. This conflicts with `AGENTS.md`: production
deploys are driven exclusively by the scheduled Deploy Hook, and panes must not
run Vercel CLI from this repo. Current `scripts/build-config.test.js` only
verifies the site-only trigger and live smoke; `NODE_OPTIONS='--v8-pool-size=1'
npm run test:build-config -- --test-name-pattern 'scheduled Vercel deploy has
a site-only main trigger and live smoke gate'` passed 38/38 and did not reject
the CLI-first branch.

Next SETUP source atom: make `.github/workflows/scheduled-deploy.yml`
deploy-hook-only per the repo rule; remove the Vercel token/CLI branch and any
`.vercel/project.json` write from the workflow; keep live-smoke verification
against `VERCEL_PRODUCTION_URL`; and add or expand `scripts/build-config.test.js`
so this workflow rejects `vercel deploy`, `npx ... vercel`, `VERCEL_TOKEN`, and
`.vercel/project.json` in the scheduled production deploy path. Acceptance
requires focused build-config tests, workflow source inspection, typecheck,
lint or a justified skip if no TypeScript/source path is touched, ownership,
targeted Prettier/diff checks, PR/merge evidence, and a current-main recheck.
SITE-P0-5 remains external until production serves current main; do not run
Vercel CLI from this repo.
