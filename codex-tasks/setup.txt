MANAGER-build queue - 2026-05-18T15:30+02:00

P0-first rule: while `codex-tasks/P0.md` has unchecked launch/release/product
items, SETUP should take only atoms that close validator/reviewer defects with
source, test, command, and PR/merge evidence. Do not accept local-only or
docs-only closure. The `site/` deployed static surface is now in the build
lease because Vercel serves `site/`, not the Expo app bundle.

Accepted P0 SETUP atoms:

1. `SITE-P0-1` accepted via PR #242 / `c4404d8`.
   - Clean manager verification on `origin/main`: `node --check site/app.js`,
     `node --check site/practice.js`, `node --check site/questions.js`,
     route/static-link assertion, `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `npm run test:ownership`, targeted `git diff --check`, and local
     static-site smoke from `/` to `#/practice`, first chapter drill, and
     `#/ebook` all exit 0.
2. `SITE-P0-2` accepted via PR #252 / `5e834b9` plus handoff PR #254 /
   `bfd6c7e`.
   - Clean manager verification on current `origin/main` after `npm ci`:
     `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `node --check` for `site/app.js`, `site/practice.js`, `site/questions.js`,
     `site/i18n-extras.js`, and `site/ebook-tools.js`, targeted `git diff --check`,
     and static-site Chrome smoke from `/` to `#/mock`, 5-question exam
     completion/result review, `#/practice`, and `#/ebook` all exit 0.
3. `SITE-P0-3` accepted via PR #264 / `19a420e` plus handoff PR #267 /
   `4f2f9c5`.
   - Clean manager verification on current `origin/main`: `node
     scripts/export-site-question-bank.js --check`, `node --test
     tests/content-static-site-question-bank-parity.test.js`, `node
     scripts/export-question-bank.js --check`, `node --check site/questions.js`,
     `npm run validate:content`, `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `npm run test:ownership`, targeted `git diff --check`, and static-site
     Chrome smoke showing 13 Practice cards, 705-question total, q001 feedback,
     5-row Mock review, and no console/page errors all exit 0.
4. `SITE-P0-4` accepted via PR #279 / `9def23a` plus handoff PR #280 /
   `41a6bcb`.
   - Clean manager verification on current `origin/main`: `node --check
     site/ebook.js`, `node --check site/ebook-tools.js`, focused ebook
     copy/navigation source assertion, `npm run validate:content`,
     `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
     `npm run test:ownership`, `git diff --check HEAD~2..HEAD`, and static-site
     Chrome smoke for English and Swedish Ebook, notes host, chapter practice
     navigation, and no console/page errors all exit 0.

Active P0 SETUP atoms from the SITE-P0-5 live audit:

MANAGER-build note 2026-05-18T15:25+02:00:
PANE 2 was redirected away from `REVIEWER-ACCOUNT-SCOPE-1` before edits, then
hit the Codex usage cap. Do not resume that stale account-scope attempt as the
current task. The active order remains: live deploy stale first, Practice width
parity second, account-scope third.

MANAGER-build note 2026-05-18T15:30+02:00:
PR #315 / `bab3190` queued `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1` after
q142 first exposed static-bank drift. Current `origin/main` has since advanced
through q143 and validates at 715 canonical questions while `site/questions.js`
still exports 705. Do not deploy current `site/` until this sync is repaired.
The active order is now: current static-bank drift first, live deploy stale
second, Practice width parity third, account-scope fourth.

1. `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1`
   - Scope: `site/questions.js`, static question-bank generator/parity tests,
     `scripts/check-live-site.js`, `scripts/check-live-site.test.js`, and any
     focused static/live smoke count assertions.
   - Required behavior: regenerate the deployed static bank from current
     canonical content so all latest generated variants are present and static
     site question count matches the canonical published count. Do not hard-code
     stale 705/710-question expectations; derive the expected count or update it
     to the current canonical count at execution time.
   - Required gates: `node scripts/export-site-question-bank.js --check`,
     `node --test tests/content-static-site-question-bank-parity.test.js`,
     `node scripts/export-question-bank.js --check`, `npm run validate:content`,
     `node --check site/questions.js`, relevant static smoke through Practice
     and Mock using the current count, `npm run test:ownership`, and
     `git diff --check`.

2. `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`
   - Scope: deployment workflow/config, production deploy evidence, and a
     focused live-site smoke gate.
   - Required behavior: production must serve current `origin/main` `site/`,
     not an older Vercel deployment. The live production URL must show the
     current canonical static question count, the Practice hub, `#/mock` with
     `#mock-stage`, and Swedish Ebook chapters without v1.1 placeholder copy.
   - Required gates: deploy run or Vercel evidence for current main, live smoke
     assertions for the checks above, relevant syntax/build checks for changed
     files, `npm run test:ownership`, and `git diff --check`.
   - Current caveat: PR #321 / `3a323ff` landed a useful deploy workflow and
     live-smoke harness, but it is not accepted as live-deploy closure because
     there is no production deploy evidence yet and the live-smoke default still
     accepts/requires 705 questions. The static-bank repair above must fix that
     count behavior before this atom can close.

3. `REVIEWER-SITE-PRACTICE-WIDTH-PARITY-1`
   - Scope: `site/index.html`, `site/styles.css`, and focused site layout
     assertion.
   - Required behavior: port the draft wide Practice hub shell so desktop
     Practice uses the intended wide layout instead of the old 720px container,
     while preserving current question data and Mock exam behavior.
   - Required gates: static/browser assertion for the wide Practice container,
     `node --check` for changed site JS if any, relevant smoke through
     `#/practice` and `#/mock`, `npm run test:ownership`, and
     `git diff --check`.

4. `REVIEWER-ACCOUNT-SCOPE-1`
   - Scope: SETUP/build product shell, route manifests, dependencies, static
     `site/` account prompts, and scaffold/router-shell tests.
   - Required behavior: the MVP has no reachable account/authentication
     surfaces, no Supabase/OAuth runtime dependency path, and local-profile /
     privacy/static Ebook copy says progress, highlights, and notes work
     locally without an account.
   - Acceptance caveat: validator/reviewer saw promising local cleanup, but do
     not accept this defect until the scoped source atom lands from a clean
     latest-`origin/main` branch with green whitespace gate, PR self-review,
     squash-merge, and handoff evidence.
   - Keep `REVIEWER-SEARCH-PLACEHOLDER-1` separate; do not expand this atom
     into Search implementation/removal.

Existing P0 SETUP atoms behind the live-site P0 queue:

5. `REVIEWER-RELEASE-VALIDATION-PR-TRIGGER-1`
   - Scope: release-config/tooling.
   - Required behavior: `.github/workflows/release-validation.yml` runs on
     `pull_request` targeting `main`, while preserving the existing manual/tag
     release validation paths.
   - Required gates: focused
     `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/build-config.test.js --test-name-pattern 'GitHub release validation workflow runs safe validation and blocker evidence checks'`,
     then `NODE_OPTIONS='--v8-pool-size=1' npm run test:build-config` or the
     nearest full release-validation gate, plus `git diff --check`.

6. `REVIEWER-PRODUCTION-SUBMIT-GUARD-ORDER-1`
   - Scope: release-config/tooling.
   - Required behavior: `submit-production-guard --check-only` reports
     placeholder Apple submit identifiers (`appleId`, `ascAppId`,
     `appleTeamId`, `TBD`) before release-preflight blockers can mask them.
   - Required gates: focused
     `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/build-config.test.js --test-name-pattern 'EAS build and submit profiles are configured|production submit guard blocks placeholder Apple identifiers'`,
     then `NODE_OPTIONS='--v8-pool-size=1' npm run test:build-config` or the
     nearest release validation gate, plus `git diff --check`.

7. `REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1`
   - Scope: SETUP/QA visual-smoke tooling and launch-ad test behavior.
   - Required behavior: visual-smoke screenshots are route-specific,
     inspectable UI evidence; launch sponsor/app-open overlays are dismissed or
     test-disabled before screenshots; duplicate screenshot hashes and visible
     launch overlays are rejected or explicitly explained. Keep exam no-ad
     behavior covered separately.
   - Required gates: official
     `CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/visual-smoke.spec.ts --workers=1`
     or the nearest visual-smoke gate, plus assertions/evidence for duplicate
     hash rejection and launch-overlay absence, plus `git diff --check`.

P1 hold:
- `REVIEWER-SEARCH-PLACEHOLDER-1` remains open but behind the P0
  language/release/scope/visual-evidence queue. Address it later by
  implementing real local search or removing the reachable placeholder route
  and navigation affordance.
