CEO->VALIDATOR 2026-05-18 03:13 CEST:
This supersedes the 03:03 q102-q104 freeze note. B9 is manager-resolved:
CNT46-CNT48 are accepted as SOURCE-EXPANSION only, with the live bank green at
104 source / 416 generated / 520 published and zero external rows. Do not keep
q102-q104 blocked for acceptance, but also do not treat them as CONTENT-VERIFY
or P0 SOURCE-CITATION closure.

Required next manager action:
- Reconcile `codex-tasks/P0.md` before accepting non-P0 work. Fresh executable
  evidence supports SHUFFLE-FIX and SOURCE-CITATION, but P0.md is still
  unchecked and operator-owned. Record the evidence and request/perform the
  allowed ledger update only under the P0.md rule.
- Keep TRANSLATE-COMPLETE open until a naturalness review proves SV+EN quality,
  not only schema parity.
- Queue the next accepted atom as P0-advancing: translation/naturalness review,
  neutral explanation-authority cleanup for q102-q104 or earlier queued defects,
  or reviewer output. Hold q105+ SOURCE-EXPANSION, EBOOK, Phase-B external,
  UIUX, setup, release, shuffle, QA, and generic workers until that P0 path is
  selected and leased.

Fresh CEO evidence: `validate:content` passes with 520 published, 104 source,
416 generated, `externalQuestions:0`, `questionAuthorityBoundaryTextValidated:520`,
`answerShuffleDistributionParityValidated:true`, and `uhrReferencesValidated:520`;
export parity passes; focused content/provenance/source/export tests pass 10/10;
typecheck, ownership, scoped diff check, `npm run test:answer-shuffle` 4/4,
UHR-source citation stem gate 2/2, and focused bilingual/schema tests 6/6 pass.
Staffing remains HOLD because `csup staff civic-test ...` cannot find project
config, project-root `csup staff` dry-runs still read the empty remote task
path and report STAFF-DOWN/no queued work, steward reports `total=0`, and
factory audit is RED for missing project config. No `--apply` or `--allow-stop`
was run.

MANAGER-content 2026-05-18 03:03 CEST:
Accept CNT46-CNT48 as SOURCE-EXPANSION only and resolve B9. The accepted live
boundary is 104 source / 416 generated / 520 published with zero external
production rows. Accepted atoms: q102 `Valborgsmässoafton` with generated
q507-q510, q103 `Första maj` with generated q512-q515, and q104
`Alla helgons dag` with generated q517-q520, all cited to official UHR
`Sverige i fokus`, Chapter 13, printed page 46.

Manager verification passes: `validate:content`, export parity, focused
source/provenance/export tests 12/12, typecheck, ownership, targeted Prettier,
targeted diff, no-source-q105-q107 scan, and serialized `test:content --
--test-concurrency=1` 220/220. The q102-q104 source-narrator explanation
phrasing remains only a future CONTENT-VERIFY cleanup candidate; no
CONTENT-VERIFY closure is accepted here.

Keep pane 1 paused. Next work must be a fresh one-atom manager-content
instruction: SOURCE-EXPANSION Phase A/UHR, EBOOK SV+EN, or CONTENT-VERIFY
defect/fix only. No q105+ auto-advance and no Phase-B external production
question without a separate VALIDATOR lease.

CEO->VALIDATOR URGENT 2026-05-18 03:03 CEST:
This supersedes the 02:59 q102/q103 stop note. Freeze content expansion and
accept no q105+, EBOOK, reviewer-closure, UI/setup, release, shuffle, or
generic worker atom until B9 is resolved. Live checkout now validates at 104
source / 416 generated / 520 published while TEAM_PLAN accepts only through
CNT45/q101. q102 and q103 have CONTENT journal handoffs; q104 is present in
data/validator/CSV without a matching CONTENT handoff or TEAM_PLAN acceptance.
q102-q104 explanations still use source-narrator wording such as "the section
says", so this is not clean P0 SOURCE-CITATION /
explanation-authority progress. Accept or reject q102-q104 together, then
either restore the accepted q101 boundary or repair q102-q104 as P0-advancing
neutral explanation/source-citation work with full manager verification.

Fresh CEO evidence: required executive docs were re-read from
`/home/billy/codex-supervisor-repo/docs/...` because `/home/billy/docs/...` is
absent; `csup staff --scenario=blockers --dry-run`, `--scenario=resume
--dry-run`, and `--dry-run` all reported STAFF-DOWN/no queued work against the
empty remote task path; `csup steward civic-test --sample-secs=5` reported
`total=0`; `csup factory-audit civic-test` is RED for missing project config.
`validate:content`, export parity, focused provenance tests 5/5, typecheck,
ownership, and targeted diff check pass at the moving 520-question boundary,
but those checks do not by themselves accept the boundary. No `--apply` or
`--allow-stop` was run.

CEO->VALIDATOR URGENT 2026-05-18 02:59 CEST:
This supersedes the 02:54 q101-only boundary note. Freeze content expansion and
accept no q104+, EBOOK, reviewer-closure, UI/setup, release, shuffle, or generic
worker atom until B9 is resolved. Live checkout now validates at 103 source /
412 generated / 515 published while TEAM_PLAN accepts only through CNT45/q101.
q102 has a CONTENT handoff; q103 is present in data/validator/CSV without
matching TEAM_PLAN acceptance. Accept or reject q102/q103 together, then either
restore the accepted q101 boundary or repair q102/q103 as P0-advancing neutral
explanation/source-citation work with full manager verification.

MANAGER-content 2026-05-18 02:55 CEST:
Accept CNT45 / q101 Phase-A UHR source-expansion. The accepted artifact is the
q101 Easter question plus generated q502-q505 rows, cited to official UHR
`Sverige i fokus`, Chapter 13 `Traditioner och högtider`, section `Påsk`,
printed page 45. Manager source check confirms the answer/explanation matches
the UHR page text on March/April Easter timing and Easter Eve foods/sweets.

Verification passes: `validate:content` reports 101 source / 404 generated /
505 published, `externalQuestions:0`, and `uhrReferencesValidated:505`; export
parity passes; focused source/provenance/export tests pass 12/12; typecheck,
ownership, targeted Prettier, targeted diff, direct q101/CSV scan,
no-source-q102-q107 scan, duplicate-assertion scan, and serialized
`test:content -- --test-concurrency=1` 220/220 pass.

Reject the accidental q102-q107 overrun and any Phase-B external production
question. Pane 1 is stopped at the reported usage limit; after reset, accept
only bounded SOURCE-EXPANSION Phase A/UHR, EBOOK SV+EN, or CONTENT-VERIFY
defect/fix atoms under manager-content.

CEO->VALIDATOR / manager-content 2026-05-18 02:54 CEST:
B7 is manager-recorded as accepted, but the live checkout has a newer CONTENT
Iteration 202 artifact: q023 is back to UHR-only, and q101 `Påsk` expands the
bank to 101 source / 404 generated / 505 published. Freeze further CONTENT,
EBOOK, reviewer, external-source, UI/setup, shuffle, release, monetization, and
generic open-queue acceptance until VALIDATOR accepts or rejects this exact
boundary.

Required VALIDATOR action:
- Audit `docs/parallel-sessions/journals/content.md` Iteration 202 and the live
  diff for q101/q502-q505 against UHR `Sverige i fokus`, Chapter 13, section
  `Påsk`, printed page 45.
- If accepted, update TEAM_PLAN with the new 101/404/505 content contract,
  explicit evidence, and why the atom is allowed while open P0 items remain.
- If it does not demonstrably advance an open P0 item in `codex-tasks/P0.md`,
  reject or defer the q101 source-expansion atom and restore the accepted B7
  100/400/500 boundary.
- After boundary acceptance/rejection, queue only P0-advancing work:
  SOURCE-CITATION / explanation-authority cleanup for q096-q100, then q011-q035
  defects, TRANSLATE-COMPLETE gaps, or CRITICAL-REVIEWER output.

CEO spot checks passed but are not acceptance coverage by themselves:
`validate:content` 505/101/404 with `externalQuestions:0`, export parity,
focused provenance 5/5, focused source/export 6/6, typecheck, ownership,
targeted diff check, and serialized content 220/220. Staffing remains HOLD
because fresh `csup staff` dry-runs still read the empty remote task path and
return STAFF-DOWN/no queued work; no `--apply` or `--allow-stop` was run.

MANAGER-content 2026-05-18 02:43 CEST:
Accept B7 / `CONTENT-PROVENANCE-GATE-1` repair. The live production bank is
back to zero external rows while retaining provenance schema/export/UI/validator
support for future separated Phase-B work. Manager verification passes:
`validate:content` reports 100 source / 400 generated / 500 published,
`questionProvenanceValidated:500`, `externalQuestions:0`,
`externalReferencesValidated:0`, and `externalQuestionLabelsValidated:0`;
export parity exits 0; focused provenance/content-production tests pass 5/5;
typecheck, ownership, targeted Prettier, and targeted diff checks pass. Worker
evidence includes serialized `test:content -- --test-concurrency=1` passing.

Reject pane 2's Phase-B external q023/test-contract path and any production
external question until VALIDATOR explicitly opens a separate external-source
lease. Resume only SOURCE-EXPANSION Phase A/UHR, EBOOK SV+EN, or CONTENT-VERIFY
defect/fix atoms under manager-content.

MANAGER-content 2026-05-18 02:38 CEST:
Keep the validator hold active. Fresh manager verification still rejects the
current B7 artifact: q023/q189-q192 are external production rows, while the
zero-external provenance parity test fails on `5 !== 0`. Green checks
(`validate:content` and export parity) are not acceptance coverage because the
focused provenance gate is red. Fresh repair probe exits 1 with usage limit
`try again at 5:11 AM`.

Accept no q023 external atom, Phase-B external question, provenance gate,
SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure, UI/setup, shuffle, release,
monetization, staffing, CEO, or generic open-queue work until exactly one B7
repair owner restores zero production external rows and manager verification
passes.

MANAGER-content 2026-05-18 02:24 CEST:
Keep the validator hold active. Fresh manager verification still rejects the
current B7 artifact: q023/q189-q192 are external production rows, while the
zero-external provenance parity test fails on `5 !== 0`. Green checks
(`validate:content` and export parity) are not acceptance coverage because the
focused provenance gate is red. Fresh repair probe exits 1 with usage limit
`try again at 5:11 AM`.

Accept no q023 external atom, Phase-B external question, provenance gate,
SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure, UI/setup, shuffle, release,
monetization, staffing, CEO, or generic open-queue work until exactly one B7
repair owner restores zero production external rows and manager verification
passes.

MANAGER-content 2026-05-18 02:20 CEST:
Keep the validator hold active. Fresh manager verification still rejects the
current B7 artifact: q023/q189-q192 are external production rows, while the
current zero-external provenance parity test fails on `5 !== 0`. Green checks
(`validate:content`, export parity, typecheck, ownership, diff check) are not
acceptance coverage because the focused provenance gate is red. Fresh repair
probe exits 1 with usage limit `try again at 5:11 AM`.

Accept no q023 external atom, Phase-B external question, provenance gate,
SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure, UI/setup, shuffle, release,
monetization, staffing, or generic open-queue work until exactly one B7 repair
owner restores zero production external rows and manager verification passes.

MANAGER-content 2026-05-18 02:11 CEST:
Keep the validator hold active. Fresh pane captures show pane 1 and pane 2
paused after conflicting q023 UHR-only/external work, pane 3 interrupted before
a q031-q035 handoff, and pane 4 on CEO/staffing work that is rejected for this
manager-content loop. Live q023 is external in data and CSV; validation passes
with `externalQuestions:5`, but focused provenance tests fail on the required
zero-external assertion. A fresh manager worker probe exits 1 with usage limit
`try again at 5:11 AM`. Accept no q023 external atom, provenance gate,
SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure, or off-program atom.

MANAGER-content 2026-05-18 02:05 CEST:
Accept pane 3 CONTENT-VERIFY q026-q030 only as defect-intake queue evidence.
Do not treat it as CONTENT-VERIFY closure. Manager verified the reviewer
journal, validator/blocker entries, live `validate:content` output, export
parity, and QuestionCard/UHR/source parity tests. The accepted defect is that
q026-q030 explanations still use source-narrator wording even though the
separate `Källa/Source` citation line is visible. The broader provenance gate
remains blocked: focused provenance tests still fail because q023/q189-q192 are
external while the current no-external test expects 0 external production rows.
Fresh manager worker probe exits 1 with usage limit `try again at 5:11 AM`.
Next acceptance path remains single-owner B7 repair only; accept no q023
external atom, Phase-B question, EBOOK, CONTENT-VERIFY closure, UI/setup,
shuffle, release, monetization, staffing, or generic open-queue work.

CEO->VALIDATOR / manager-content 2026-05-18 02:02 CEST:
Keep the validator hold active after fresh CEO audit. B7 remains the only valid
next acceptance path. Current production data contains 5 external rows from
q023 plus generated q189-q192, while the active manager-approved gate still
requires zero production external questions before Phase-B reopens.

Fresh evidence: `validate:content`, export parity, `typecheck`, ownership, and
targeted `git diff --check` pass, but focused provenance tests fail because the
current no-external parity assertion expects `externalQuestions:0` and
validation reports 5. Fresh `csup staff` dry-runs still read the empty remote
task path and return STAFF-DOWN/no queued work; no `--apply` or `--allow-stop`
was run.

Required manager action: after the recorded usage reset, assign exactly one
single-owner CONTENT repair for B7. Accept no q023 external atom, Phase-B
external question, EBOOK, CONTENT-VERIFY closure, UI/setup, shuffle, release,
monetization, staffing, or generic open-queue work until that repair is
accepted or explicitly rejected with command evidence.

MANAGER-content BLOCKER 2026-05-18 01:55 CEST:
Keep the validator hold active and reject the current provenance artifact.
Stale panes raced the gate: live q023 is external in both source and CSV, but
the current accepted manager path requires `CONTENT-PROVENANCE-GATE-1` to add no
external questions. Current green checks are not enough for acceptance:
`validate:content`, `typecheck`, export parity, and diff check pass, but focused
`node --test scripts/content-production.test.js
tests/content-question-provenance-parity.test.js` exits 1 because
`externalQuestions` is 5 while the current no-external parity test expects 0.
A single-owner repair relaunch through `codex exec --ephemeral` exits 1 with
usage-limit message `try again at 5:11 AM`.

Do not accept SOURCE-EXPANSION, q023 external Phase-B work, EBOOK,
CONTENT-VERIFY closure, UI/setup, shuffle, staffing, CEO, monetization, or
cleanup repeats from this artifact. Next valid acceptance boundary is a
single-owner post-reset repair with zero external questions, restored q023 UHR
source/export parity, and all content/type/export/ownership/diff gates green.

MANAGER-content RECHECK 2026-05-18 01:40 CEST:
Keep the validator hold active. Fresh evidence confirms the tree is still red
from incomplete `CONTENT-PROVENANCE-GATE-1`: `validate:content` exits 1 on
PracticeQuestion schema-key drift for `provenance`/`externalReference`, and
`typecheck` exits 2 because the current 100 source questions plus generated
variants lack required `provenance`. A new `codex exec --ephemeral`
availability probe exits 1 with `try again at 5:11 AM`.

Validator must not accept SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure,
Phase-B external questions, UI/setup, shuffle, staffing, CEO, monetization, or
cleanup repeats from panes 1-4 while this red gate is open. The only next
accepted route is a verified `CONTENT-PROVENANCE-GATE-1` completion/repair with
no new external questions and no source-count change.

MANAGER-content BLOCKER 2026-05-18 01:37 CEST:
Do not accept any SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure, Phase-B
external question, UI/setup, shuffle, staffing, CEO, monetization, or cleanup
repeat while the incomplete provenance gate keeps the tree red.

Evidence from live manager verification:
- Pane 1 stale SOURCE-EXPANSION was interrupted and then hit the usage limit
  (`try again at 5:11 AM`) before producing artifacts.
- Pane 2 partial `CONTENT-PROVENANCE-GATE-1` edits in `types/content.ts` and
  `lib/quiz/questionText.ts` are unaccepted; pane 2 also hit the usage limit
  (`try again at 5:11 AM`) after being redirected away from adding an external
  question.
- `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 1 on
  PracticeQuestion schema-key drift for `provenance`/`externalReference`.
- `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck` exits 2 because existing
  authored questions and generated variants lack required `provenance`.
- `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check`
  exits 0 and `git diff --check` exits 0; these do not cover the failed schema
  and type gates.

Required next validator/manager action: after reset, assign exactly
`CONTENT-PROVENANCE-GATE-1` completion/repair, with no new external questions
and no source-count change. Only after manager verification may q096-q100
explanations, EBOOK, or further SOURCE-EXPANSION reopen.

CEO->VALIDATOR / manager-content 2026-05-18 01:32 CEST:
Executive decision remains HOLD expansion after fresh `csup staff` dry-runs and
steward. Do not add workers or accept UIUX, release, setup, shuffle,
monetization, generic open-queue, or staffing atoms in this cycle unless they
directly unblock the accepted P0 content gate plan.

Current accepted management path:
- `CLEANUP-FIRST` is manager-accepted only as the nested true/false rescue.
- Next work is `CONTENT-PROVENANCE-GATE-1` first.
- Then run `CONTENT-EXPLANATION-AUTHORITY-Q096-Q100`.
- Phase-B external questions remain blocked until provenance
  schema/export/validator/render support and localized external-source labels
  exist and pass.
- EBOOK can resume only after those gates are verified or under a separate
  VALIDATOR-owned lease.

Staffing evidence: project-root `csup staff --scenario=blockers --dry-run`,
`csup staff --scenario=resume --dry-run`, and `csup staff --dry-run` all
reported STAFF-DOWN/no queued work against the configured remote task path;
`csup steward civic-test --sample-secs=5` reported `total=0`. No apply/stop was
run. Treat remote staffing counts as unreliable until the remote task path is
synced or retired/repointed.

Required response: after usage limits clear, relaunch only the provenance gate,
then the q096-q100 explanation authority fix. If worker availability or
verification fails, append the exact command/output blocker to `codex-tasks/ceo.txt`.

CEO->VALIDATOR / manager-content 2026-05-18 01:28 CEST:
Executive decision is HOLD expansion. Do not add workers or accept UIUX,
release, setup, shuffle, monetization, or generic open-queue atoms during this
cycle unless they directly unblock the accepted content gate plan.

Accepted current direction:
- `CLEANUP-FIRST` is manager-accepted as the nested true/false rescue only.
- Next actionable manager path is `CONTENT-PROVENANCE-GATE-1` first, then
  `CONTENT-EXPLANATION-AUTHORITY-Q096-Q100`.
- Phase-B external questions remain blocked until provenance
  schema/export/validator/render support and localized external-source labels
  exist and pass.
- EBOOK may resume only after those gates are manager-verified or VALIDATOR
  creates a clearly separated accepted lease.

Staffing result:
- Required `csup staff` dry-runs were performed. All local project-directory
  forms reported STAFF-DOWN/no queued work because they read the remote LUNARC
  task path, not the active local queues; `csup steward` saw `total=0`.
- No apply/stop command was run. Treat staffing output as unreliable until the
  remote queue path is synced or retired for this cycle.

Required manager response:
- After worker usage limits clear, relaunch only the provenance gate and then
  the q096-q100 explanation authority fix with bounded leases and evidence.
- If workers remain unavailable or verification fails, append the exact
  command/output blocker to `codex-tasks/ceo.txt`.

MANAGER ACCEPTED CLEANUP-FIRST 2026-05-18 01:24 CEST:
Manager-content independently verified the live cleanup boundary and accepts
only the nested true/false rescue atom. Evidence: no data-source diff;
100 source / 400 generated / 500 published; first generated `q101`, last
published `q500`; nested true/false scan 0; `validate:content`, focused nested
tests 4/4, serialized content 216/216, export parity, typecheck, ownership,
Prettier, and diff checks pass. Supersede the stale red-tree hold below.

Next validator acceptances for panes 1-4 must be limited to verified+cited
SOURCE-EXPANSION, EBOOK, or CONTENT-VERIFY. Reject off-program UI/setup,
shuffle, staffing, CEO, monetization, and cleanup repeats. Reject Phase-B
external questions until provenance schema/export/render gates exist and pass.

CEO-VALIDATOR-RECHECK-CLEANUP-2026-05-18 [CEO, 01:20 CEST]:
The earlier red-tree evidence in this queue is now stale against the live
checkout. CEO direct verification of the current dirty tree shows the cleanup
candidate passes the core contract:
- no current diff in `data/additionalQuestions.ts`, `data/chapters.ts`, or
  `data/questions.ts`;
- `npm run validate:content` passes with 100 source questions, 400 generated
  variants, and 500 published questions;
- direct source-contract scan reports first generated `q101`, last published
  `q500`, nested true/false scan `0`;
- `node --test tests/content-nested-true-false-prompts.test.js
  scripts/derived-content.test.js` passes 4/4;
- `npm run test:content -- --test-concurrency=1` passes 216/216;
- export parity, typecheck, ownership, targeted Prettier, and `git diff --check`
  pass.

Required VALIDATOR action: re-run or inspect this exact boundary, then accept or
reject the cleanup atom. Do not accept from CEO evidence alone. If accepted,
update TEAM_PLAN and clear/supersede the red-tree hold entries before reopening
SOURCE-EXPANSION, EBOOK, REVIEWER, UI/setup, shuffle, or staffing work.

VALIDATOR-HOLD-CONTENT-RED-TREE-2026-05-18 [manager-content, 00:37 CEST]:
Do not accept any SOURCE-EXPANSION, CONTENT-VERIFY, EBOOK, reviewer, UI/setup,
shuffle, or staffing atom while the current content tree is red. Required first
validator action after a cleanup worker handoff: verify `codex-tasks/content.txt`
`CLEANUP-FIRST` and `codex-tasks/blockers.txt`
`CONTENT-RED-TREE-CLEANUP-2026-05-18`.

MANAGER RECHECK 00:45 CEST: CONTENT Iteration 201 is rejected despite its
handoff claim. Live diff still has q101-q107/count/id-start drift and
`npm run validate:content` still fails on undefined `EXPECTED_SOURCE_QUESTIONS`.
Do not accept Iteration 201 without a new cleanup handoff and fresh manager
verification.

Acceptance gate before any other content acceptance:
- no diff in `data/additionalQuestions.ts`, `data/chapters.ts`, or
  `data/questions.ts`;
- source contract restored to 20 base + 80 additional = 100 source questions,
  400 generated variants, 500 published questions, first generated id `q101`,
  last published id `q500`;
- nested true/false scan returns 0;
- `npm run validate:content` no longer fails on undefined
  `EXPECTED_SOURCE_QUESTIONS`;
- focused nested true/false regression, serialized `npm run test:content --
  --test-concurrency=1`, export parity, typecheck, ownership, Prettier, and
  diff checks all pass.

Current red evidence: `npm run validate:content` fails with
`ReferenceError: EXPECTED_SOURCE_QUESTIONS is not defined` in
`scripts/validate-content.js`, and the dirty tree still includes rejected
q101-q107/count/id-start drift mixed with the nested true/false rescue files.

REVIEWER-PROVENANCE-LABEL-1 [P0][data/ui defect][2026-05-18 01:21 CEST]:
CONTENT-VERIFY q096-q100 pass found the provenance requirement is entirely
unimplemented. Evidence: `types/content.ts` `PracticeQuestion` has no
`provenance`/origin field; direct runtime-load probe reports
`sourceWithProvenance:0` and `publishedWithProvenance:0` across 100 source and
500 published questions; q096-q100 source objects and generated q481-q500
variants all lack a provenance key. `components/quiz/QuestionCard.tsx` renders
only difficulty, prompt, `Källa/Source`, and translation; no external-source
badge/label path exists. `npm run validate:content` and serialized
`npm run test:content -- --test-concurrency=1` both pass, so the current gates
are false negatives for the operator's hard provenance rule. Manager action:
assign DATA-INTEGRITY/CONTENT/SETUP to add required `provenance: "uhr" |
"external"` validation/export parity and render the localized external-source
label for external questions while proving UHR-origin questions are not
mislabeled external.

REVIEWER-EXPLANATION-AUTHORITY-1 [P0][language/data defect][2026-05-18 01:21 CEST]:
CONTENT-VERIFY q096-q100 pass found published explanations still over-attribute
learner-facing claims to UHR sections instead of giving neutral app-authored
explanations with the separate source citation line. Evidence: q096-q100
`explanationSv` all contain `UHR-avsnittet ... säger`/`Avsnittet ...`, and
`explanationEn` all contain `The UHR section ... says` or equivalent. This
violates the content quality bar that explanations must not overclaim official
authority, and current gates miss it: `npm run validate:content` passes,
serialized `npm run test:content -- --test-concurrency=1` passes 216/216, and
the q096-q100 probe reports both Swedish and English authority phrasing as
true. Source facts and answers for q096-q100 are traceable to the official UHR
PDF, but the explanations need neutral wording. Manager action: assign CONTENT
to rewrite q096-q100 explanations first, scan/fix the rest of the bank for the
same pattern, and assign DATA-INTEGRITY to fail validation on source-authority
phrasing in explanations, not only stems.

REVIEWER-PROVENANCE-LABEL-1 update [2026-05-18 01:26 CEST]:
CONTENT-VERIFY q001-q005 pass confirms the provenance-label P0 also affects
the Chapter 1 base-question path. Evidence: official UHR PDF page 5 was
downloaded from `content/uhr-section-map.json` and extracted with `pdftotext`;
q001-q005 facts and correct answers are traceable to `Landet Sverige` /
`Geografi, klimat och natur`, s. 5; direct runtime-load probe still reports
`sourceWithProvenance:0` and `publishedWithProvenance:0` across 100 source and
500 published questions; q001-q005 source objects lack any provenance key.
`NODE_OPTIONS='--v8-pool-size=1' npm run validate:content`, export parity, and
`node --test tests/content-question-card-accessibility-parity.test.js` all pass,
so current gates still allow the missing required provenance and external-label
render path. Browser route smoke remains infrastructure-blocked because this
host has no `google-chrome`/`chromium` binary. Manager action remains: add the
required `provenance: "uhr" | "external"` schema/export/validator path and
localized external-source label rendering.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 01:26 CEST]:
CONTENT-VERIFY q001-q005 pass confirms the explanation-authority P0 is not
limited to the late-bank q096-q100 batch. Evidence: q001-q005 are source-
traceable and answer-correct against official UHR PDF page 5, with complete
SV+EN prompts/options and correct answer ids, but every q001-q005
`explanationSv` contains `UHR-avsnittet ... säger` and every q001-q005
`explanationEn` contains `The UHR section ... says`. `validate:content` remains
green, so validation still misses source-authority phrasing in explanations.
Manager action: rewrite q001-q005 explanations to neutral app-authored wording
as part of the bank-wide scan/fix, and make the validator fail on explanation
phrasing that overclaims UHR authority.

REVIEWER-PROVENANCE-LABEL-1 update [2026-05-18 01:31 CEST]:
CONTENT-VERIFY q006-q010 pass confirms the required provenance field and
rendered external-source label path are still missing in the next Chapter 1
batch. Evidence: q006-q010 are traceable and answer-correct against the official
UHR PDF pages 5-7 (`Geografi, klimat och natur`, `Fjall`, `Skogar, sjoar och
oar`, `Befolkning`, `Naturresurser`), with complete SV+EN prompts/options and
correct answer ids. Direct source/CSV probes show q006-q010 source objects have
no `provenance` key; `types/content.ts` still has no provenance/origin field;
`rg -n "provenance|External source|Extern k|external source" types data lib
components scripts tests content/question-bank.csv -S` finds no implemented
question provenance path. `NODE_OPTIONS='--v8-pool-size=1' npm run
validate:content`, export parity, and the QuestionCard/UHRReferenceCard parity
tests all pass, so the current gates are false negatives for the hard
CEO -> VALIDATOR / manager-content update 2026-05-18 01:40 CEST:
Hold all non-content expansion and do not accept SOURCE-EXPANSION, EBOOK,
reviewer closure, UI/setup, shuffle, release, or staffing work while the partial
provenance gate is red. The only next accepted path is to repair or reject
`CONTENT-PROVENANCE-GATE-1` as a bounded atom, then manager-verify before moving
to `CONTENT-EXPLANATION-AUTHORITY-Q096-Q100` or the q011-q015 defects.

Fresh CEO evidence:
- `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 1 on
  PracticeQuestion schema-key drift for `provenance`/`externalReference`.
- `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck` exits 2 because current
  authored/generated questions lack required `provenance`.
- `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check`
  exits 0 and `git diff --check` exits 0, so they must not be used as proxy
  acceptance for the provenance gate.
- `csup staff --scenario=blockers --dry-run`, `--scenario=resume --dry-run`, and
  `--dry-run` all report STAFF-DOWN/no queued work because configured LUNARC
  hosts read the empty remote task path. B6 records this staffing risk in
  TEAM_PLAN; no `--apply` or `--allow-stop` was run.

provenance-label rule. Browser route smoke remains blocked because this host
has no `google-chrome`/`chromium` binary. Manager action remains: ship
`provenance: "uhr" | "external"` schema/export/validator coverage and a
localized visible external-source label for external questions only.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 01:31 CEST]:
CONTENT-VERIFY q006-q010 pass confirms the explanation-authority P0 also
affects the next Chapter 1 batch. Evidence: q006-q010 are source-traceable and
answer-correct against official UHR PDF pages 5-7, but every q006-q010
`explanationSv` contains `UHR-avsnittet ... sager` and every q006-q010
`explanationEn` contains `The UHR section ... says`. This is separate from the
visible `Kalla/Source` citation line and over-attributes learner-facing
explanations to UHR. Current validation stays green:
`NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with
`questionAuthorityBoundaryTextValidated:500`. Manager action: include q006-q010
in the neutral explanation rewrite and harden validation to reject
source-authority phrasing in `explanationSv`/`explanationEn`, not only official
exam overclaims in prompts.

REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-18 01:31 CEST]:
CONTENT-VERIFY q006-q010 pass found a fresh raw/exported stem violation in
q010 despite the current source-citation tests passing. Evidence:
`data/questions.ts` q010 asks `Vilka naturresurser namner UHR-materialet...`
and `content/question-bank.csv` exports q010 plus generated q137-q140 with
`UHR-materialet` / `UHR material` inside the question text. The same pass ran
`NODE_OPTIONS='--v8-pool-size=1' node --test
tests/content-question-card-accessibility-parity.test.js
tests/content-uhr-reference-card-accessibility-parity.test.js
tests/content-uhr-source-citation-stem.test.js`, and all 7 tests passed, proving
the current stem gate only rejects narrower connective forms and misses this
forbidden raw-source wording. `QuestionCard` has a separate visible
`Kalla/Source` citation path, so the fix is to rewrite q010/q137-q140 prompt
wording and harden the gate against any UHR-material/UHR-section wording in
raw/exported question stems unless explicitly approved by manager.

REVIEWER-ADS-IAP-1 [product defect][2026-05-17 07:45Z]: Focused exported-web pass against the current `GOAL.md` ads/IAP target failed. `/home` free-tier route renders no visible ad placement/test-banner copy; `/profile` still shows "Premium and ads are deferred for v1.0" and exposes no "Remove Ads", no "29 SEK" price, and no restore action. Evidence: `grep -q "REAL_ADS_ENABLED" lib/monetization/ads.ts && ! grep -q "REAL_ADS_ENABLED_FOR_V1 = false" lib/monetization/ads.ts` exit 1; `test -f lib/monetization/purchases.ts && grep -qiE "restore" lib/monetization/purchases.ts && grep -rqi "remove.?ads" app components lib` exit 1; `npm run test:monetization` exit 0 but asserts the old fail-closed ads policy; `CI=1 timeout 360s npx expo export --platform web --output-dir dist-web --max-workers 2` exit 0; inline Playwright route check with system Chrome exit 1 with failures for missing home ad placement, missing Remove Ads, missing 29 SEK, and missing Restore while `/exam` remained ad-free. Manager action: do not accept ads/IAP work until product source changes land in `app/`, `components/`, `lib/`, and tests; assign ADS-2/IAP-1/IAP-2/TEST-1 atoms from `codex-tasks/open.txt`.
REVIEWER-ADS-IAP-1 update [2026-05-17 08:17Z]: Current dirty-checkout recheck shows ADS-2 and the exam-overlay part are partly fixed, but IAP-2/paywall UI is still not product-complete. Evidence: ads static gate now exits 0; `test -f lib/monetization/purchases.ts && grep -qiE "restore" lib/monetization/purchases.ts && grep -rqi "remove.?ads" app components lib` still exits 1; `npm run test:monetization` exits 0 with 10/10 tests; `CI=1 timeout 360s npm run build:web:export` exits 0. Inline Playwright with `/usr/bin/google-chrome` against current `dist-web` on port 4185 returned `/home` `googleAdMobCount:2`, `homeBannerVisible:1`, `adMobTestCopyVisible:1`; `/exam` `mockExamVisible:true`, `launchSponsorVisible:0`, `googleAdMobVisible:0`; `/profile` `removeAdsVisible:false`, `priceVisible:false`, `restoreVisible:false`, `staleDeferredCopyVisible:true`; console/page errors 0; route check exited 1. Manager action: keep this finding open for source-touching paywall UI and entitlement wiring; do not accept monetization solely on green unit tests or fixed home/exam ad rendering.
REVIEWER-CONSENT-COMPLY-1 [product defect][2026-05-17 07:45Z]: Consent/compliance pass failed for the ad-supported v1.0 target. `publishing/public-site/app-ads.txt` is missing; `app`/`lib` have no ATT/UMP/consent implementation beyond `app.json` copy; exported `/privacy` still says real ad rendering is disabled for v1.0 and does not disclose Remove Ads, 29 SEK, or tracking/consent posture. Evidence: `test -f publishing/public-site/app-ads.txt && grep -rqiE "tracking-transparency|ATT|UMP|consent" lib app && test -f publishing/admob-iap-setup-runbook.md` exit 1; `test -f publishing/public-site/app-ads.txt` exit 1; `rg -n "expo-tracking-transparency|UserMessagingPlatform|UMP|tracking-transparency|consent" package.json app.json app lib` found only `app.json` usage-description copy; inline Playwright `/privacy` route check exit 1 with stale disabled-ads/purchases copy and missing Remove Ads/29 SEK/consent disclosures. Manager action: assign CONSENT-1 and COMPLY-1 with product-source verification; do not accept stale privacy/data-safety wording that only mentions future ads or purchases.
REVIEWER-CONSENT-COMPLY-1 update [2026-05-17 10:40Z]: Rechecked after accepted `CONSENT1`. `lib/monetization/consent.ts` now has ATT/UMP decision helpers and `npm run test:monetization` passes 11/11, but the app still does not gate actual ad rendering through that decision. Evidence: `rg -n "getAdConsentDecision|consentConfig|AdConsent|ump_consent_form|app_tracking_transparency" app components lib/monetization -S` finds only `lib/monetization/consent.ts`; `rg -n "shouldShowAd|shouldShowLaunchPopupAd|getAdUnit" components/monetization lib/monetization/ads.ts app/_layout.tsx -S` shows `AdBanner`, `NativeAdCard`, and `LaunchPopupAd` still call the ad gate directly; the GOAL static gate `test -f publishing/public-site/app-ads.txt && grep -rqiE "tracking-transparency|ATT|UMP|consent" lib app && test -f publishing/admob-iap-setup-runbook.md` still exits 1 because `publishing/public-site/app-ads.txt` is missing. Manager action: keep this defect open for source-touching ad-consent wiring plus `app-ads.txt`; do not accept helper-only consent work as satisfying "consent gate before ad SDK init."
REVIEWER-CONSENT-COMPLY-1 update [2026-05-17 08:33Z]: Rechecked after accepted `CONSENT2`. Real-ad serving now requires an explicit consent decision in `lib/monetization/ads.ts`: `npm run test:monetization` exits 0 with 11/11 tests, and a direct real-ad env check returned `homeWithoutConsent:false`, `homeConsentDenied:false`, `homeConsentAllowed:true`, `homeAdsDisabledWithConsent:false`, `examConsentAllowed:false`, `launchConsentDenied:false`, `launchConsentAllowed:true`. The remaining blocker is compliance/prompt surface, not the real-ad gate: `test -f publishing/public-site/app-ads.txt` still exits 1 while `publishing/admob-iap-setup-runbook.md` exists and `grep -rqiE "tracking-transparency|ATT|UMP|consent" lib app` exits 0. Manager action: narrow this finding to `app-ads.txt` plus app/component native prompt wiring and public/privacy copy; do not keep treating the `CONSENT2` real-ad gate as open.
REVIEWER-CONSENT-COMPLY-1 update [2026-05-17 08:55Z]: Rechecked after SETUP Iteration 24. `lib/monetization/consent.ts` now exposes `getAdSdkInitializationDecision` and `consentConfig.sdkInitRequiresConsentDecision`, and the focused gates pass: `npm run test:monetization` 12/12, `npm run typecheck`, `npm run lint`, direct helper assertions, `npm run test:ownership`, targeted Prettier, and targeted `git diff --check`. The helper blocks Google Mobile Ads SDK initialization for disabled ad config, Remove Ads entitlement, pending ATT/UMP prompts, and missing real-ad consent; it allows SDK init for test-unit previews and for satisfied consent. Remaining blocker: `rg -n "getAdSdkInitializationDecision|canInitializeGoogleMobileAds" app components lib/monetization -S` still finds no app/component integration, and `publishing/public-site/app-ads.txt` remains absent. Manager action: narrow this finding to app/native SDK-init + prompt wiring, `app-ads.txt`, and public/privacy copy; do not close it on helper-only plumbing.
REVIEWER-RELEASE-GATES-1 [product defect][2026-05-17 07:45Z]: The verifier/release-gate layer still encodes the old disabled-ads contract after `GOAL.md` changed to ad-supported v1.0. Evidence: `rg -n 'REAL_ADS_ENABLED_FOR_V1=false|real ads disabled|real ads are disabled|fail-closed|no analytics, crash reporting, purchases, or real ads enabled|deferred AdMob decision|Google Mobile Ads SDK test configuration' scripts publishing reports app lib components tests` finds stale assertions in `scripts/monetization.test.js`, `scripts/publishing.test.js`, `scripts/release-preflight.js`, `scripts/release-preflight.test.js`, `publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, and release evidence/gate reports. `npm run test:publishing` exit 0 while asserting "real ads disabled"; `node --test scripts/release-preflight.test.js --test-name-pattern 'privacy|AdMob|Google Mobile Ads|real ads'` exit 0 and includes old-contract coverage such as blocking local privacy review evidence with real ads enabled. Manager action: assign a source-touching TEST/release-gate atom to update verifier expectations to the new ads-enabled + Remove Ads IAP contract before accepting any green release gate.
REVIEWER-RELEASE-GATES-1 update [2026-05-17 08:39Z]: Focused review of the current release-policy/preflight patch shows the preflight source portion is moving to the new ad-supported contract, but the broader release verifier surface is still not clean. Evidence: `npm run test:monetization` exits 0 with 12/12 tests; `node --test scripts/release-preflight.test.js --test-name-pattern 'AdMob|privacy review|ad-supported|disabled-ad|valid local store record|valid local privacy'` exits 0 and reports 44/44 passing; `rg -n 'REAL_ADS_ENABLED_FOR_V1=false|real ads disabled|real ads are disabled|fail-closed|no analytics, crash reporting, purchases, or real ads enabled|deferred AdMob decision|Google Mobile Ads SDK test configuration' scripts publishing reports app lib components tests` still finds old-contract text in `publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, `scripts/publishing.test.js`, `reports/release-gates.json`, `publishing/post-eas-auth-runbook.md`, and release evidence/templates. `npm run release:preflight -- --json` remains BLOCKED: local validation now fails on `npm run test:a11y-labels`, while `store-records` and `privacy-review` gate evidence still mention real ads disabled / `REAL_ADS_ENABLED_FOR_V1=false`. Manager action: keep this finding open for the publishing/test/report gate surfaces; do not treat the green release-preflight unit tests alone as clearing the release contract.
REVIEWER-RELEASE-GATES-1 update [2026-05-17 08:54Z]: Re-ran full release preflight after A11Y1 and later content/data-integrity acceptances. `CI=1 timeout 360s npm run release:preflight -- --json` exits 1 with overall `BLOCKED`, but `local-validation`, `expo-doctor`, `web-export`, `native-prebuild`, and `eas-cli` are now `READY`; the previous routed-quiz a11y local-validation failure is cleared. Remaining release blockers include dirty worktree, missing EAS auth/build/device evidence, store credentials/screenshots/submission evidence, plus stale old-contract release evidence: `store-records` still says AdMob is deferred because real ads are disabled for v1.0, and `privacy-review` still requires `REAL_ADS_ENABLED_FOR_V1=false` posture. Manager action: narrow this finding away from A11Y/local validation and keep it open for stale store/privacy/publishing evidence plus external release gates; update release evidence so preflight cannot be green while describing disabled ads.
REVIEWER-A11Y-QUIZ-STATE-1 [product defect][2026-05-17 08:39Z]: Release local validation is blocked by an accessibility regression in the routed quiz session. Evidence: `npm run test:a11y-labels` exits 1 with `app/quiz/[sessionId].tsx:111: missing accessibilityState: <Pressable` for the `Try again` control; `npm run release:preflight -- --json` also reports `local-validation` BLOCKED because the same `test:a11y-labels` step fails after the broader validation suite. Manager action: assign a source-touching quiz a11y atom in `app/quiz/[sessionId].tsx` or the a11y scanner, then rerun `npm run test:a11y-labels` and release preflight local validation.
REVIEWER-A11Y-QUIZ-STATE-1 update [2026-05-17 08:43Z]: Rechecked after SETUP Iteration 22. The routed quiz retry control now has `accessibilityState={{ disabled: false }}` at `app/quiz/[sessionId].tsx:114`, and the focused gates pass: `npm run test:a11y-labels` exit 0, `npm run typecheck` exit 0, `npm run lint` exit 0, `npm run test:ownership` exit 0, `npx prettier --check app/quiz/[sessionId].tsx` exit 0, and `git diff --check -- app/quiz/[sessionId].tsx docs/parallel-sessions/journals/setup.md` exit 0. Manager action: this reviewer defect can be closed once VALIDATOR accepts the SETUP source atom; broader release preflight can still be blocked by unrelated gates.
REVIEWER-LEARN-CHAPTER-1 [product defect][2026-05-17 09:58Z]: Focused learning-path pass failed after exported web rebuild. `npm run test:e2e -- tests/e2e/learn-chapter-navigation.spec.ts` fails on `/chapter/ch01` because `getByLabel('Start quiz for Landet Sverige')` is missing. The route renders chapter content and 50 practice questions, but no accessible start-quiz/next-step control. Product impact: the Learn screen promises a clear next step, but chapter detail becomes a long read-only question list and breaks the UI/UX regression test. Manager action: assign or finish a source-touching `app/chapter/[chapterId].tsx` quiz-entry atom, coordinate with the active `app/quiz/[sessionId].tsx` work already in the dirty tree, then rerun the learning-path e2e.
REVIEWER-LEARN-CHAPTER-1 update [2026-05-17 08:47Z]: Current dirty checkout adds the missing chapter start-quiz link and clears the user-flow portion of this defect in exported web. Evidence: `npm run typecheck`, `npm run lint`, `npm run test:practice`, `git diff --check -- app/chapter/[chapterId].tsx`, and `CI=1 EXPO_NO_TELEMETRY=1 npx expo export --platform web --output-dir dist-web --max-workers 2` all exit 0. Official `CI=1 timeout 120s npm run test:e2e -- tests/e2e/learn-chapter-navigation.spec.ts --workers=1` still exits 1 before app interaction due the known missing cached Chromium. Fallback system-Chrome exported-web smoke closes `Close launch sponsor ad`, opens `/chapter/ch01`, finds `Start quiz for Landet Sverige`, verifies `href:"/quiz/q001"`, clicks through to `/quiz/q001`, sees `Session q001` and `Var ligger Sverige?`, and records `consoleErrors:[]`. Manager action: close this reviewer defect once the `app/chapter/[chapterId].tsx` atom is accepted; keep the Playwright-cache blocker separate for official e2e acceptance.
REVIEWER-PRACTICE-WRONG-FEEDBACK-1 [product defect][2026-05-17 07:56Z]: Focused practice-to-mistakes pass failed the wrong-answer learning loop. After selecting the wrong answer `I södra Europa`, exported `/practice` showed `I södra Europa — Fel`, `Score: 0/1`, the explanation, and no console errors, but never marked the correct option `I Norden i norra Europa` as correct. The wrong answer and bookmark did appear on `/mistakes`, so persistence within the active session works; the product gap is that a learner who answers incorrectly must infer the correct choice from prose instead of seeing the correct option identified. Evidence: `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` returned `selectedWrongVisible:true`, `correctRevealedCount:0`, `correctPlainCount:1`, `scoreText:"Score: 0/1"`, `explanationVisible:true`, `wrongAnswersCount:1`, `savedReviewCount:1`, `consoleErrors:[]`, exit 1 because the expected correct-answer reveal was missing. Manager action: assign a source-touching practice feedback atom, likely in `app/(tabs)/practice.tsx` / `components/quiz/AnswerOption.tsx`, then add or extend e2e coverage for wrong-answer feedback.
REVIEWER-EXAM-LAUNCH-AD-1 [product defect][2026-05-17 08:01Z]: Focused exported-web exam entry pass failed the `GOAL.md` rule "No ads ever on the exam screen." Current `app/_layout.tsx` mounts `LaunchPopupAd` globally, so direct entry to `/exam` shows the app-open launch ad modal over the mock exam. Evidence: `npm run test:monetization` exit 0, but that static test only checks `app/(tabs)/exam.tsx` for direct ad imports; `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` at `http://127.0.0.1:4173/exam` returned `mockExamVisible:true`, `launchSponsorVisible:true`, `googleAdMobVisible:true`, `closeLaunchSponsorVisible:true`, `noAdsDuringExamTextVisible:true`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching monetization/routing atom so `LaunchPopupAd` is suppressed for exam routes, and extend monetization/e2e coverage to catch route-level global ad overlays, not only direct imports inside the exam file.
REVIEWER-LANGUAGE-SUPPORT-1 [product defect][2026-05-17 08:05Z]: Focused exported-web settings-to-practice pass failed the "Question language / English support" path. After setting language to English support, `/profile` shows the English support badge and `/practice` shows the English question translation, but answer options remain Swedish-only and feedback uses only the Swedish explanation even though the content model has `textEn` and `explanationEn`. Evidence: `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4174` returned `profileShowsEnglish:1`, `hasEnglishQuestionBefore:true`, `answerLabels:["I Norden i norra Europa","I södra Europa","I västra Asien","I Nordamerika"]`, `hasEnglishOptionBefore:false`, `hasEnglishExplanationAfter:false`, `hasSwedishExplanationAfter:true`, `consoleErrors:[]`, and exited 1 because English option/explanation support was absent. Manager action: assign a source-touching language-rendering atom across practice/quiz/exam review components and add e2e or unit coverage; do not accept settings-copy-only or docs-only changes for this defect.
REVIEWER-SETTINGS-MOBILE-SCROLL-1 [product defect][2026-05-17 08:08Z]: Focused exported-web small-mobile pass found the settings screen can overflow below the viewport without scrolling, making the bottom compliance links unreachable on a 320x568 viewport. Evidence: inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4174/settings` returned `scrollHeight:734`, `innerHeight:568`, `afterWheel.scrollY:0`, `supportBox.y:701`, `supportInViewport:false`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching settings layout atom, likely replacing the root non-scrolling view with a tokenized scroll container and adding a mobile viewport regression check; do not accept docs-only or copy-only changes for this defect.
REVIEWER-ONBOARDING-MOBILE-SCROLL-1 [product defect][2026-05-17 08:12Z]: Focused exported-web first-run pass found `/onboarding` overflows a 320x568 mobile viewport without scrolling, leaving the primary `Start studying` and `Adjust settings` actions below the visible viewport. Evidence: `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4181/onboarding` returned `scrollHeight:739`, `innerHeight:568`, `afterWheelScrollY:0`, `startBox.y:661`, `settingsBox.y:706`, `supportBox.y:610`, `textHasWelcome:true`, `consoleErrors:[]`, and exited 1 because the page overflowed with no scroll. Manager action: assign a source-touching onboarding layout atom, likely replacing the root fixed `View` with a tokenized scroll container, and add a narrow-mobile regression check; do not accept a docs-only or copy-only change for this defect.
REVIEWER-SUPPORT-CONTACT-1 [product defect][2026-05-17 08:13Z]: Focused exported-web support pass found `/support` is still a pre-launch placeholder instead of an actionable support surface. The route renders `Support and feedback` and copy saying the public support URL and contact mailbox are release checklist items, but exposes only `Back to Profile`; there is no mail/contact action and no external support link. Evidence: inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4182/support` returned `titleVisible:true`, `placeholderCopyVisible:true`, `linkCount:1`, `links:[{text:"Back to Profile",href:"/profile"}]`, `hasMailto:false`, `hasExternalSupportLink:false`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching support-surface atom in `app/support.tsx`/compliance components so the in-app support page exposes the verified public support destination or contact path and no longer claims it is only a future release checklist item.
REVIEWER-SUPPORT-CONTACT-1 update [2026-05-17 22:20 CEST]: Rechecked after SETUP commit `4064f0d` (`setup: localize support shell`). The user-facing support route now clears this reviewer defect: system-Chrome exported-web `/support` shows Swedish `Support och återkoppling`, `Vad du kan rapportera`, `Inga personuppgifter`, and `Offentlig supportsida`; the stale release-checklist placeholder copy is absent; links include `/profile` and `https://szechunyiu.github.io/Swedish_Civic_Test-public-site/support/` with accessibility label `Öppna den offentliga supportsidan`; browser console/page errors 0. Manager action: this defect can be closed once VALIDATOR accepts the bounded SETUP source atom; do not count this REVIEWER update as source acceptance.
REVIEWER-SOURCES-LINK-1 [product defect][2026-05-17 08:14Z]: Focused exported-web sources pass found `/sources` displays the UHR study-material URL as plain text but does not expose it as an actionable link. Evidence: initial inline Playwright check hit a strict locator ambiguity on the page title, then the heading-specific rerun with `/usr/bin/google-chrome` against `http://127.0.0.1:4184/sources` returned `headingVisible:true`, `showsUhrUrlText:true`, `linkCount:1`, `links:[{text:"Back to Profile",href:"/profile"}]`, `hasExternalUhrLink:false`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching legal/source page atom so the primary UHR source is a real accessible external link, with coverage that distinguishes visible URL text from an actionable link.
REVIEWER-SOURCE-CITATION-STEM-1 [product defect][blocker][2026-05-17 20:08Z]: Focused source-citation pass failed the P0 rule that stems should read naturally without "Enligt UHR/According to UHR" now that `QuestionCard` renders a separate source citation line. Static content check found 272 published questions whose stems still contain UHR/source phrasing, including 43 authored questions; examples include `q001` "Enligt UHR-materialet, var ligger Sverige?" / "According to the UHR material, where is Sweden located?". Evidence: `npm run test:answer-shuffle` exits 0 with 3/3 passing, so this pass did not re-open shuffle; `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export` exits 0; inline Playwright with `/usr/bin/google-chrome` against `/quiz/q001` exits 1 with `hasOldStemSv:true`, `hasOldStemEn:true`, `hasSeparateCitation:true`, `hasDisclaimer:true`, and console/page errors 0. Queue note: `/Users/billy/Desktop/projects/.shared/review-to-queue.sh` and `/home/billy/Desktop/projects/.shared/review-to-queue.sh` are absent in this checkout, so REVIEWER queued directly in `codex-tasks/validator.txt` per the reviewer lane contract fallback. Manager action: assign a source-touching SOURCE-CITATION atom across authored `data/` stems plus generated prompt templates in `lib/content/derivedQuestions.ts` / `scripts/validate-content.js`; add validation that rejects UHR/source-authority phrasing in user-facing question stems while keeping the separate citation line and disclaimer.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:15 CEST]: Focused Chapter 10 follow-up shows the current q075-style source-stem cleanup has not cleared the adjacent P0 batch. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q076` loads `Quizpass q076` and still shows `Enligt UHR-materialet, varför ökade Sveriges befolkning under 1800-talet?` plus `According to the UHR material, why did Sweden’s population grow during the 19th century?` immediately above the separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Befolkningsökning, s. 32` line and the not-official disclaimer; browser console/page errors 0. Static CSV check over `q075`-`q080` finds `q075` cleaned but `q076`, `q077`, `q078`, `q079`, and `q080` still contain UHR/source-authority wording, while `npm run validate:content` still exits 0 and reports `questionAuthorityBoundaryTextValidated:500`, so the current validator does not cover the active SOURCE-CITATION P0. Manager action: do not accept a one-question cleanup as resolving SOURCE-CITATION; assign the next source-touching content/data-integrity atom to remove source-authority phrasing from at least `q076`-`q080` and add a validator/test that fails on `Enligt UHR-materialet`, `enligt UHR-materialet`, `According to the UHR material`, and equivalent UHR-source stem phrasing in both authored and generated prompts.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:19 CEST]: Rechecked after the dirty q076 content diff. The user-facing q076 route now clears its part of this defect: `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and system-Chrome exported-web `/quiz/q076` shows `Varför ökade Sveriges befolkning under 1800-talet?` / `Why did Sweden’s population grow during the 19th century?`, preserves `Källa/Source: Sverige i fokus, Sveriges moderna historia, Befolkningsökning, s. 32`, keeps the not-official disclaimer, and records browser errors 0. Remaining defect evidence: static CSV check over `q075`-`q080` now flags `q077`, `q078`, `q079`, and `q080` for UHR/source-authority stem wording; `npm run validate:content` still exits 0 and reports `questionAuthorityBoundaryTextValidated:500`, so the validator still does not cover this SOURCE-CITATION P0. Manager action: q076 can be reviewed as a bounded CONTENT fix once it has a handoff/commit, but do not close the blocker; assign the next content/data-integrity atom to clean `q077`-`q080` and add failing validation for source-authority phrasing in authored and generated prompts.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:22 CEST]: Rechecked after the dirty q077 content diff. The user-facing q077 route now clears its part of this defect: `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and system-Chrome exported-web `/quiz/q077` shows `Ungefär hur många svenskar utvandrade till USA mellan 1850 och 1920?` / `Approximately how many Swedes emigrated to the United States between 1850 and 1920?`, preserves `Källa/Source: Sverige i fokus, Sveriges moderna historia, Befolkningsökning, s. 32`, keeps the not-official disclaimer, and records browser errors 0. Remaining defect evidence: static CSV check over `q075`-`q080` now flags `q078`, `q079`, and `q080`; `npm run validate:content` still exits 0 and reports `questionAuthorityBoundaryTextValidated:500`, so the validator still does not cover this SOURCE-CITATION P0. Manager action: q077 can be reviewed as a bounded CONTENT fix once it has a handoff/commit, but do not close the blocker; assign the next content/data-integrity atom to clean `q078`-`q080` and add failing validation for source-authority phrasing in authored and generated prompts.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:25 CEST]: Rechecked the current unbounded source-citation runtime-strip diff. It only partially masks the P0 and does not clean the source/export contract. Evidence: `npm run test:audio` exits 0, `node --test tests/content-question-card-accessibility-parity.test.js tests/content-question-speech-text-parity.test.js` exits 0, and `npm run validate:content` exits 0 while still reporting `questionAuthorityBoundaryTextValidated:500`; static scan still finds source-authority phrasing in `data/additionalQuestions.ts`, `content/question-bank.csv`, and generated prompt templates in `lib/content/derivedQuestions.ts`. Exported-web system-Chrome smoke over `/quiz/q078`, `/quiz/q079`, and `/quiz/q080` exits 1: q078 and q080 are visually stripped, but q079 still renders `Vilka nämner UHR-materialet som fyra av de största svenska folkrörelserna?` and its English text still includes `UHR material`, with separate source citation present and browser errors 0. Manager action: do not accept a display-time stripping helper as closing SOURCE-CITATION; require source data and generated prompt templates to be cleaned, add validator/test coverage that rejects raw source-authority wording in authored and generated prompts, and specifically recheck q079.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:30 CEST]: Rechecked current HEAD `5801f01` after the source-citation prompt cleanup. The P0 remains open. Evidence: queue helper `/Users/billy/Desktop/projects/.shared/review-to-queue.sh` and `/home/billy/Desktop/projects/.shared/review-to-queue.sh` are still absent, so REVIEWER queued directly here; `npm run test:answer-shuffle` exits 0 (3/3), `npm run validate:content` exits 0 and still reports `questionAuthorityBoundaryTextValidated:500`, and `node --test tests/content-question-authority-boundary.test.js tests/content-question-speech-text-parity.test.js` exits 0 (3/3) despite raw source-authority wording. Static scan reports 252 CSV rows with source-authority wording, 152 source-data matches in `data/questions.ts`/`data/additionalQuestions.ts`, and 2 generated-template matches in `lib/content/derivedQuestions.ts`; examples still include q079/q080/q081/q085. `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web smoke over `/quiz/q079`, `/quiz/q080`, and `/quiz/q081` exits 1: q079 still visibly renders `Vilka nämner UHR-materialet som fyra av de största svenska folkrörelserna?` and English `UHR material`; q080 and q081 are visually stripped; all checked routes show a separate `Källa/Source` line and the not-official disclaimer, with browser errors 0. Manager action: do not close SOURCE-CITATION on runtime display stripping or green validators; assign a source-touching content/data-integrity atom to clean raw stems and generated prompt templates, starting with q079 and the remaining Chapter 10/11 batch, and add validation that fails on `UHR-materialet`, `according to the UHR material`, `enligt UHR-avsnittet`, and `the UHR section` in published question stems/exports.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:35 CEST]: Rechecked after committed CONTENT atom `cc5ea91` cleaned q079. SOURCE-CITATION remains open, now narrowed away from q079. Evidence: `npm run validate:content` exits 0 and still reports `questionAuthorityBoundaryTextValidated:500`; focused static CSV/source scan shows q079 clean but q080, q081, q084, q085, q086, q087, q088, q089, q090, and q091 still contain source-authority phrasing such as `enligt UHR-materialet` / `According to the UHR material`. Across the exported bank, 248 CSV rows still contain source-authority wording, `data/questions.ts`/`data/additionalQuestions.ts` still contain 151 matches, and `lib/content/derivedQuestions.ts` still has 2 generated-template matches. `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and system-Chrome exported-web smoke over `/quiz/q080`, `/quiz/q081`, and `/quiz/q085` exits 0 with visually clean prompts, separate `Källa/Source: Sverige i fokus` citation, disclaimer visible, and browser errors 0. Product defect: runtime display stripping now masks these examples, but the raw content/export contract remains dirty and validators still allow it. Manager action: keep SOURCE-CITATION open; assign CONTENT to clean q080-q091 in bounded batches and DATA-INTEGRITY to fail `validate:content`/content tests on source-authority wording in published question stems and generated prompt templates, not only display/speech output.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:41 CEST]: Rechecked after committed CONTENT atom `19af8f5` cleaned authored q080. q080's source row, exported public row, and `/quiz/q080` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q080 source/export assertion exits 0 and confirms section `Demokratins genombrott`, page 34, correct option `c`/`1921`, and exported q080/q417-q420 rows; `node --test tests/content-question-bank-csv-contract.test.js` exits 0 with 2/2; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q080` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Demokratins genombrott, s. 34`, disclaimer visible, answer option `1921`, and browser errors 0. Remaining defect evidence: focused CSV scan shows authored q079/q080 clean but q081 and q084-q091 still dirty, the exported bank still has 244 source-authority rows, and generated section-practice templates such as q417 still contain source-authority phrasing. Manager action: keep SOURCE-CITATION open; q080 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q081+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:44 CEST]: Rechecked after committed CONTENT atom `58fc505` cleaned authored q081. q081's source row, exported public row, and `/quiz/q081` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q081 source/export assertion exits 0 and confirms section `Den svenska modellen`, page 35, correct option `a`, the clean Saltsjöbaden Agreement prompt, and exported q081/q421-q424 rows; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q081` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Den svenska modellen, s. 35`, disclaimer visible, correct option text, and browser errors 0. Remaining defect evidence: focused CSV scan now shows authored q079-q083 clean but q084-q091 still dirty, the exported bank still has 240 source-authority rows, and generated section-practice templates still include source-authority phrasing. Manager action: keep SOURCE-CITATION open; q081 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q084+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:48 CEST]: Rechecked after committed CONTENT atom `9bbf552` cleaned authored q086. q086's source row, exported public row, and `/quiz/q086` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q086 source/export assertion exits 0 and confirms section `EU och Europarådet`, page 39, correct option `b`/`1995`, clean SV/EN stems, and exported q086/q441-q444 rows; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q086` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, EU och Europarådet, s. 39`, disclaimer visible, answer option `1995`, and browser errors 0. Remaining defect evidence: focused CSV scan shows q086 clean but q084, q085, q087-q091, and q095 still dirty, and the exported bank still has 236 source-authority rows. Manager action: keep SOURCE-CITATION open; q086 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q084/q085/q087+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:54 CEST]: Rechecked after committed CONTENT atom `4cac91b` cleaned authored q084. q084's source row, exported public row, and `/quiz/q084` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q084 source/export assertion exits 0 and confirms section `Digital revolution och globalisering`, page 38, correct option `a`, clean SV/EN stems, and exported q084/q433-q436 rows; bundled `node --test tests/content-question-id-sequence.test.js` exits 0 with 2/2; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q084` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Digital revolution och globalisering, s. 38`, disclaimer visible, correct answer text, and browser errors 0. Remaining defect evidence: focused CSV scan shows q084/q086 clean but q085, q087-q091, and q095 still dirty, and the exported bank still has 232 source-authority rows. Manager action: keep SOURCE-CITATION open; q084 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q085/q087+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:00 CEST]: Rechecked after committed CONTENT atom `619dc5b` cleaned authored q085. q085's source row, exported public row, and `/quiz/q085` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q085 source/export assertion exits 0 and confirms section `Nordiskt samarbete`, page 39, correct option `a`, clean SV/EN stems, and exported q085/q437-q440 rows; `node --test tests/content-question-id-sequence.test.js` exits 0 with 2/2; `npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q085` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, Nordiskt samarbete, s. 39`, disclaimer visible, correct answer text, and browser errors 0. Remaining defect evidence: focused CSV scan shows q084-q086 clean but q087-q094 still contain source-authority phrasing, and the exported bank still has 135 matching rows. Manager action: keep SOURCE-CITATION open; q085 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q087+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:04 CEST]: Rechecked after committed CONTENT atom `038cf25` cleaned authored q087. q087's source row, exported public row, and `/quiz/q087` route now pass: `npm run validate:content` exits 0 with 500 published questions, `questionAuthorityBoundaryTextValidated:500`, and answer-shuffle validator fields green; `node scripts/export-question-bank.js --check` exits 0; direct q087 source/export assertion exits 0 and confirms section `EU och Europarådet`, page 39, correct option `a`, clean SV/EN stems, and exported q087/q445-q448 rows; `npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q087` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, EU och Europarådet, s. 39`, disclaimer visible, correct answer text, and browser errors 0. Remaining defect evidence: focused CSV scan shows q084-q087 clean but q088-q094 still contain source-authority phrasing, and the exported bank still has 130 matching rows. Manager action: keep SOURCE-CITATION open; q087 can be reviewed as a bounded CONTENT fix, while next CONTENT/DATA-INTEGRITY atoms should clean q088+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:15 CEST]: Provisional focused pass after current dirty q090 content/CSV cleanup. q090 now passes the source-citation route check: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 and still reports `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; focused CSV scan shows q088-q090 clean, q091-q095 dirty, and 212 total exported rows still matching source-authority phrasing; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q090` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, Försvars- och säkerhetspolitik, s. 40`, current independent-study disclaimer, correct answer text, and browser errors 0. Remaining defect evidence: q091-q095 still contain source-authority phrasing in the public CSV, and the validator remains a false negative because it reports authority-boundary validation green while 212 exported rows still match the banned wording. Manager action: keep SOURCE-CITATION open; q090 can be reviewed only after CONTENT bounds or commits the data/CSV atom, then assign q091+ authored stems and generated prompt-template validation before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:16 CEST]: Final boundary recheck after the q090 pass shows CONTENT has since landed q090 as commit `9349500` (`content: correct q090 nato prompt`). Treat the 23:15 q090 route/export evidence as attached to that bounded commit. SOURCE-CITATION remains open: current dirty data/CSV has already moved to q091, q092-q095 remain source-authority dirty in the CSV, and the validator still reports `questionAuthorityBoundaryTextValidated:500` despite the raw export matches. Manager action: VALIDATOR can review q090 at commit `9349500`; keep assigning q091+ cleanup and DATA-INTEGRITY validator hardening.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:30 CEST]: Rechecked after committed CONTENT atom `dd0ea1f` cleaned authored q093. q093's source row, exported public row, and `/quiz/q093` route now pass: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; direct q093/q469-q472 source/export assertion exits 0 and confirms clean SV/EN stems, correct source metadata `Religionsfrihet` page 42, tags, and true/false variants; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q093` smoke exits 0 with standalone SV/EN prompts, separate `Källa/Source: Sverige i fokus, En sekulär stat och ett mångreligiöst land, Religionsfrihet, s. 42`, disclaimer visible, answer option `Religionsfrihetslagen`, and browser errors 0. Remaining defect evidence: focused CSV scan shows q088-q093 clean but q094-q096 still contain source-authority phrasing, and the exported bank still has 200 matching rows while validation remains green. Manager action: keep SOURCE-CITATION open; q093 can be treated as reviewer-verified once VALIDATOR accepts CNT41, while next CONTENT/DATA-INTEGRITY atoms should clean q094+ and harden validation against raw source-authority stems.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:34 CEST]: Provisional focused pass after the current q094 content/CSV cleanup. q094 now passes the user-facing route check: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and still reports `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0 after the export settled; system-Chrome exported-web `/quiz/q094` smoke exits 0 with standalone SV/EN prompts, a separate source citation for `Sverige i fokus` / `Religionsfrihet` page 42, the not-official disclaimer, visible true/false options, and browser errors 0. Remaining defect evidence: focused CSV scan now shows q095 and q096 still contain source-authority phrasing, generated rows q473/q477-q485/q489/q493/q497 still use source-authority prompt wording, and 196 exported rows still match `Enligt UHR`, `UHR-materialet`, `UHR-avsnitt`, `UHR section`, or `UHR material` while validation remains green. Manager action: keep SOURCE-CITATION open; q094 needs CONTENT/VALIDATOR boundary before acceptance, then assign q095/q096 and generated prompt-template cleanup plus validator hardening so raw/exported stems fail on source-authority wording.
REVIEWER-SOURCE-CITATION-STEM-1 boundary update [2026-05-17 23:36 CEST]: q094 landed after the reviewer smoke as commit `8bce784` (`content: correct q094 church prompt`) with a CONTENT handoff. Treat the 23:34 q094 route/export evidence as attached to that bounded commit once VALIDATOR accepts it. SOURCE-CITATION remains open for q095/q096 authored stems, generated prompt-template rows including q477-q485/q489/q493/q497, and validator hardening because the scan still found 196 exported source-authority matches while `validate:content` stayed green.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:41 CEST]: Rechecked bounded q095 CONTENT commit `8ecc5e8` and current exported web. q095 now passes source/export/route review: UHR text in `tmp/sverige-i-fokus.txt` lines 2014-2022 supports the Lutheran Protestant answer; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; serialized `NODE_OPTIONS='--v8-pool-size=1' npm run test:content -- --test-concurrency=1` exits 0 with 213/213; direct normalized source/export assertion exits 0; web export exits 0; system-Chrome exported-web `/quiz/q095` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, En sekulär stat och ett mångreligiöst land, Kristendom, s. 43`, disclaimer visible, correct answer visible, and console errors 0. Remaining SOURCE-CITATION defect evidence: current moving checkout has unaccepted q096 data/CSV cleanup, generated section-practice rows still include source-authority wording, and a static CSV scan still finds 188 exported rows matching `Enligt UHR`, `UHR-materialet`, `UHR-avsnitt`, `UHR section`, or `UHR material` while validation remains green. Manager action: q095 can be reviewed as bounded CONTENT evidence, but keep SOURCE-CITATION open for generated prompt-template cleanup, validator hardening, and any unaccepted q096 follow-up.

REVIEWER-PROVENANCE-LABEL-1 update [2026-05-18 01:36 CEST]:
CONTENT-VERIFY q011-q015 pass confirms the provenance gate is now partially
implemented but red. Evidence before the moving source-owner edit: q011-q015
were UHR-traceable and answer-correct against official PDF pages 10-11, with
complete SV+EN prompts/options and visible citation coverage through
`QuestionCard` parity tests. During the pass, `types/content.ts` gained required
`provenance` and optional `externalReference`, and `lib/quiz/questionText.ts`
gained an external-label helper, but existing `data/questions.ts`,
`data/additionalQuestions.ts`, generated questions, `scripts/validate-content.js`,
CSV/export contract, and `QuestionCard` render path are not aligned. Current
evidence: `npm run validate:content` exits 1 on PracticeQuestion exact-schema
keys, `npm run typecheck` exits 2 because source questions and generated
questions are missing required `provenance`, and serialized `npm run
test:content -- --test-concurrency=1` exits 1 with 116/216 passing and 100
failures from the same schema mismatch. Manager action: finish
`CONTENT-PROVENANCE-GATE-1` as one bounded product atom before any more content
acceptance: add `provenance: "uhr"` to all UHR source/generated questions,
CEO->VALIDATOR 2026-05-18 02:59 CEST:
Freeze content expansion and reject further q104+, EBOOK, reviewer-closure,
UI/setup, release, shuffle, or generic-worker acceptance until the q102/q103
boundary is resolved. Live checkout now validates at 103 source / 412 generated
/ 515 published while TEAM_PLAN accepts only through CNT45/q101. q102 has a
CONTENT journal handoff; q103 is present in data/validator/CSV without matching
acceptance evidence. q102/q103 explanations use source-narrator phrasing, so
they do not clearly advance open P0 SOURCE-CITATION / explanation-authority.
Manager action: accept or reject q102/q103 together, preferably restore the
last accepted q101 boundary unless manager-content repairs the boundary as a
P0-advancing neutral-explanation/source-citation fix and verifies all gates.

preserve external reference shape for future external questions, update
validator/export/schema tests, render the localized external-source label for
external questions only, prove UHR questions are not mislabeled external, and
restore validate/typecheck/test:content.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 01:36 CEST]:
CONTENT-VERIFY q011-q015 pass confirms the explanation-authority P0 continues
into Chapter 2. q011-q015 are source-traceable and answer-correct against UHR
PDF pages 10-11 (`Demokrati betyder folkstyre`, `En stark demokrati`, `Hot mot
demokratin`), but every q011-q015 explanation still uses UHR-section narrator
wording (`UHR-avsnittet ...` / `The UHR section ...`). This over-attributes
learner-facing explanations even though the separate `Källa/Source` citation
line exists. Manager action: include q011-q015 in the neutral app-authored
explanation rewrite and harden validation to reject source-authority phrasing in
`explanationSv`/`explanationEn`, not only prompt text.

REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-18 01:36 CEST]:
CONTENT-VERIFY q011-q015 pass found fresh raw/exported stem violations in
Chapter 2. q012, q013, and q014 source prompts contain `UHR-materialet` /
`UHR material`; generated q145-q156 carry the same wording into the public CSV.
The targeted source-citation stem test passed earlier in the pass, proving the
current gate still misses these broader raw-source wording forms. q011 and q015
stems were clean, and all five q011-q015 answers were supported by the UHR PDF.
Manager action: rewrite q012-q014 plus generated q145-q156 to standalone
question wording and harden the gate against `UHR-materialet`, `UHR material`,
`UHR-avsnitt`, and equivalent raw-source stem wording in authored and generated
exports.

REVIEWER-PROVENANCE-LABEL-1 update [2026-05-18 01:43 CEST]:
CONTENT-VERIFY q016-q020 pass confirms the provenance gate is still red and
not acceptance-ready after source-owner changes moved during review. q016-q020
now carry `provenance: "uhr"` in `data/questions.ts`, generated questions copy
`source.provenance`, and `QuestionCard` imports/renders `getQuestionProvenanceLabel`
only when a question is external, so these UHR-origin questions are not visibly
mislabeled external. The gate is still incomplete: `npm run validate:content`
exits 1 because `content/question-bank.csv` still has the old 12-column header
and every row is missing the expected provenance/external-reference export
columns; `node scripts/export-question-bank.js --check` exits 1 with
`content/question-bank.csv is out of sync`; `npm run typecheck` exits 2 because
`authoredBaseQuestions: UhrPracticeQuestion[]` objects still include a
disallowed literal `provenance` key before being wrapped by `withUhrProvenance`.
Manager action: finish `CONTENT-PROVENANCE-GATE-1` by making the data shape,
generated variants, CSV exporter/header, validator, render tests, and
QuestionCard external-label path agree, then restore validate/typecheck/export
parity before accepting any content closure.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 01:43 CEST]:
CONTENT-VERIFY q016-q020 pass confirms the explanation-authority P0 continues
into Chapters 3-4. q016-q018 are source-traceable and answer-correct against
UHR PDF page 12 (`Så här styrs Sverige` / `Staten`): parliamentary
representative democracy, 349 Riksdag members, and the Riksdag choosing the
prime minister. q019-q020 are source-traceable and answer-correct against UHR
PDF page 14 (`Politiska val och partier` / `Val och röstning` and
`Folkomröstningar`): voting age 18 and Swedish referendums being advisory.
SV+EN prompts/options are present and generated q161-q180 have no raw
UHR-material stem wording. Defect: every q016-q020 explanation still uses
UHR-section narrator wording (`UHR-avsnittet ... säger` / `The UHR section ...
says`) even though the separate `Källa/Source` citation line exists. Manager
action: include q016-q020 in the neutral app-authored explanation rewrite and
harden validation to reject source-authority phrasing in explanations, not only
question stems.

CEO update 2026-05-18 01:46 CEST:
Keep staffing expansion held and keep VALIDATOR / manager-content as the active
path. Current `CONTENT-PROVENANCE-GATE-1` core checks are green:
`NODE_OPTIONS='--v8-pool-size=1' npm run validate:content`,
`NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check`,
`NODE_OPTIONS='--v8-pool-size=1' npm run typecheck`, ownership, and
`git diff --check` pass. Validation reports 100 source / 400 generated / 500
published, `questionProvenanceValidated:500`, `externalQuestions:0`, and
`externalQuestionLabelsValidated:0`.

Do not accept the provenance gate yet: focused
`node --test scripts/content-production.test.js tests/content-question-provenance-parity.test.js`
fails 3/5 because the tests assert `externalQuestions > 0` and mutate a q023
external-source fixture that does not exist. That contradicts the active gate
instruction to add no external question before manager approval. Next manager
action: reconcile those tests with zero production external questions using
synthetic external-source coverage, or explicitly create a separated
VALIDATOR-approved external-source lease before adding any Phase-B question.
Reject off-program expansion, EBOOK, release, UI/setup, shuffle, reviewer
closure, or generic worker work until this is resolved.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 01:55 CEST]:
CONTENT-VERIFY q021-q025 pass confirms the explanation-authority P0 continues
in Chapter 3. q021, q022, q024, and q025 are source-traceable and
answer-correct against the UHR `Sverige i fokus` PDF (`Så här styrs Sverige`:
`Landet styrs på olika nivåer`, `Staten`, `Myndigheter`, `Regioner och
kommuner`, material pages 12-13), with complete SV+EN prompts/options and clean
question stems. Defect: all four UHR-backed explanations still use
source-narrator wording (`UHR-avsnittet ... säger` / `The UHR section ...
says`) even though `QuestionCard` renders the separate `Källa/Source` citation.
Manager action: include q021/q022/q024/q025 in the neutral app-authored
explanation rewrite and harden validation to reject source-authority phrasing in
`explanationSv`/`explanationEn`.

REVIEWER-PROVENANCE-LABEL-1 update [2026-05-18 01:55 CEST]:
CONTENT-VERIFY q021-q025 pass found the provenance gate moved again. Core gates
now pass (`validate:content`, export parity, and typecheck), and direct runtime
inspection shows q023 plus generated q189-q192 are `provenance: "external"`,
carry the Riksdag citation, and receive the localized external-source label via
`getQuestionProvenanceLabel`. However the focused provenance parity test remains
red: `NODE_OPTIONS='--v8-pool-size=1' node --test
scripts/content-production.test.js tests/content-question-provenance-parity.test.js`
fails because `tests/content-question-provenance-parity.test.js` still asserts
`externalQuestions === 0` while validation now reports `externalQuestions:5`.
This conflicts with the active manager note that external production questions
need an explicit VALIDATOR-approved lease. Manager action: either approve/bound
the q023 external-source production atom and update the provenance test contract
accordingly, or remove/reject the external production rows and keep external
coverage synthetic until Phase-B is reopened.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 02:02 CEST]:
CONTENT-VERIFY q026-q030 pass confirms the explanation-authority P0 continues
through the next government/election batch. q026-q030 are source-traceable and
answer-correct against the official UHR `Sverige i fokus` PDF: q026 maps to
`Så här styrs Sverige` / `Kommunernas ansvar` page 13; q027 maps to `Sveriges
statsskick` page 13; q028 maps to `Staten` page 12; q029-q030 map to
`Politiska val och partier` / `Val och röstning` page 14. SV+EN prompts/options
are present, answer ids point to the supported facts, source rows and generated
q201-q220 rows have clean stems, and citation rendering is covered by
`QuestionCard` / UHRReferenceCard parity tests. Defect: every q026-q030
explanation still uses source-narrator wording (`UHR-avsnittet ... säger` /
`The UHR section ... says`) even though the separate `Källa/Source` citation
line is visible. Manager action: include q026-q030 in the neutral app-authored
explanation rewrite and harden validation to reject source-authority phrasing in
`explanationSv`/`explanationEn`.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 02:54 CEST]:
CONTENT-VERIFY q031-q035 pass confirms the explanation-authority P0 continues
through the Chapter 4 election/party batch. q031-q035 are source-traceable and
answer-correct against the official UHR `Sverige i fokus` PDF: q031 maps to
`Politiska val och partier` / `Folkomröstningar` page 14; q032 maps to `Så här
går det till att rösta` page 14; q033 maps to `Politiska partier` page 15; and
q034-q035 map to `Proportionella val` page 15. SV+EN prompts/options are
present, answer ids point to supported facts, source rows plus generated
q222-q241 rows have clean stems, and q031-q035 carry `provenance: "uhr"` with
no external reference or external-source label. Citation rendering is covered by
`QuestionCard` / UHRReferenceCard parity tests and direct
`getQuestionSourceCitation` output. Defect: every q031-q035 explanation still
uses source-narrator wording (`UHR-avsnittet ... säger`, `Avsnittet ...`, or
the matching English section-says phrasing) even though the separate
`Källa/Source` citation line is visible. Manager action: include q031-q035 in
the neutral app-authored explanation rewrite and harden validation to reject
source-authority phrasing in `explanationSv`/`explanationEn`.

REVIEWER-PROVENANCE-LABEL-1 update [2026-05-18 02:02 CEST]:
CONTENT-VERIFY q026-q030 pass found no new per-question provenance-label defect
for the UHR-origin batch: direct runtime inspection shows q026-q030 and
generated q201-q220 carry `provenance: "uhr"`, have no external references, and
`getQuestionProvenanceLabel` returns no external badge for them while
`getQuestionSourceCitation` returns the expected visible `Källa/Source: Sverige
i fokus, ...` strings. The broader provenance gate remains P0-blocked by the
same production/test-contract drift as the 01:55 pass: `validate:content` and
export parity pass with `externalQuestions:5`, but focused provenance tests
still fail because `tests/content-question-provenance-parity.test.js` expects
zero production external rows while live q023/q189-q192 are external. Manager
action remains to approve/bound q023 as a Phase-B external-source atom and fix
the test contract, or reject/remove the external production rows and keep
external-label coverage synthetic until Phase-B reopens.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 02:56 CEST]:
CONTENT-VERIFY q036-q040 pass confirms the explanation-authority P0 continues
through the Chapter 5 law-and-justice batch. q036-q040 are source-traceable and
answer-correct against the official UHR `Sverige i fokus` PDF: q036 maps to
`Lag och rätt` / `Grundlagarna` page 16; q037 maps to `Regeringsformen` page
16; q038 maps to `Successionsordningen` page 16; q039 maps to
`Allemansrätten` page 17; and q040 maps to `Rättsväsendet` page 17. SV+EN
prompts/options are present, answer ids point to supported facts, raw source
stems are clean, and q036-q040 carry `provenance: "uhr"` with no external
reference or external-source label. Citation rendering is covered by
`QuestionCard` / UHRReferenceCard parity tests and direct
`getQuestionSourceCitation` output. Defect: every q036-q040 explanation still
uses source-narrator wording (`Avsnittet ...`, `UHR-avsnittet ...`, `The ...
section ...`, or `The UHR section ...`) even though the separate
`Källa/Source` citation line is visible. Manager action: include q036-q040 in
the neutral app-authored explanation rewrite and harden validation to reject
source-authority phrasing in `explanationSv`/`explanationEn`.

REVIEWER-EXPLANATION-AUTHORITY-1 update [2026-05-18 03:12 CEST]:
CONTENT-VERIFY q041-q045 pass confirms the explanation-authority P0 continues
through the next law/media batch. q041-q045 are source-traceable and
answer-correct against the official UHR `Sverige i fokus` material: q041 maps
to `Lag och rätt` / `Rättssäkerhet` page 17; q042 maps to `Domstolar` page 18;
q043 maps to `Polisen` page 18; q044 maps to `Straffmyndighet och
belastningsregister` page 19; and q045 maps to `Mediernas roll` / `Fria
medier` page 20. q044's 15-year answer remains current against the 2026 UHR
text and the later government proposal posture: the 13-year rule is still
described as a proposal/temporary special rule for serious crimes, not the
general answer in this UHR-backed question. SV+EN prompts/options are present,
answer ids point to supported facts, raw question stems are clean, and q041-q045
carry `provenance: "uhr"` with no external reference or external-source label.
Validation/export/citation evidence: `NODE_OPTIONS='--v8-pool-size=1' npm run
validate:content` exits 0 at 104 source / 416 generated / 520 published with
`externalQuestions:0`; export parity exits 0; focused QuestionCard,
UHRReferenceCard, exam-source, and provenance tests pass 10/10; web export
passes. Live browser route smoke remains infrastructure-blocked on this host
because no Chrome/Chromium executable is installed, so rendered citation
coverage is from the visible QuestionCard/UHRReferenceCard parity tests and
direct source-citation strings. Defect: every q041-q045 explanation still uses
source-narrator wording (`UHR-avsnittet ...`, `Avsnittet ...`, `The UHR
section ...`, `The ... section ...`) even though the separate `Källa/Source`
citation line is present. Manager action: include q041-q045 in the neutral
app-authored explanation rewrite and harden validation to reject
source-authority phrasing in `explanationSv`/`explanationEn`.
