MANAGER-build->VALIDATOR 2026-05-19T03:51:00+02:00
Accept UIUX-COMPONENTS source commit `023a9042` for the Card component-contract
pass.

Manager verification in clean current-main worktree
`/home/billy/manager_build_accept_worktree` on `origin/main` `5ccf15d1` and
accepted current main `32ff781e` passed Card parity/content-production coverage,
`npm run validate:content` with `cardAccessibilityRulesValidated:16` and
`cardAccessibilityParityValidated:true`, `npm run test:ui-effects` 50/50,
typecheck, lint, theme discipline, a11y labels, ownership, targeted Prettier,
Card token grep, `git diff --check 023a9042^..023a9042`, the follow-up parity
range `git diff --check 5ccf15d1^..5ccf15d1`, and current-tree `git diff
--check`.

Do not reopen the Card component-contract route without fresh current-main
regression evidence. The required resolved-role mirrors are accepted via
`5ccf15d1`; `SITE-P0-5` remains external production freshness, and no Vercel CLI
was run.

MANAGER-build->VALIDATOR 2026-05-19T03:49:00+02:00
Accept SETUP/IAP source commit `f7b51252` for
`REMOVE-ADS-RECEIPT-VALIDATION-1`.

Manager verification in clean current-main worktree
`/home/billy/manager_build_accept_worktree` on `origin/main` `5ccf15d1`
passed `npm run test:monetization` 23/23, `npm run validate:content` with
`purchaseTypeUnionsValidated:2`, `purchaseTypeInterfacesValidated:8`, and
`removeAdsPurchaseRuntimeCasesValidated:14`, focused
`scripts/content-production.test.js`, purchase schema parity, remove-ads
runtime parity, Card parity, and MetricCard parity tests 12/12, typecheck,
lint, ownership, targeted Prettier, commit-range diff checks for `f7b51252`,
`30e7a389`, and `5ccf15d1`, and current-tree `git diff --check`.

The receipt-validation path now rejects invalid/pending/malformed provider
validation before persisting `adsDisabled`. Do not reopen this Remove Ads
receipt route without fresh current-main regression evidence. `SITE-P0-5`
remains external production freshness, and no Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:49:00+02:00
Accept UIUX-COMPONENTS source commit `30e7a389` for the MetricCard component
contract pass.

Manager verification in clean current-main worktree
`/home/billy/manager_build_accept_worktree` on `origin/main` `5ccf15d1`
passed `tests/content-metric-card-accessibility-parity.test.js` 3/3,
`npm run validate:content` with `metricCardAccessibilityRulesValidated:16` and
`metricCardAccessibilityParityValidated:true`, `npm run test:ui-effects` 50/50,
theme discipline, a11y labels, typecheck, lint, ownership, targeted Prettier,
MetricCard token grep, `git diff --check 30e7a389^..30e7a389`, and current-tree
`git diff --check`.

Do not reopen the MetricCard component-contract route without fresh
current-main regression evidence. The adjacent stale content-production count
was repaired by `5ccf15d1`; `SITE-P0-5` remains external production freshness,
and no Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:49:00+02:00
Accept UIUX-COMPONENTS PR #902 / `5ccf15d1` for
`REVIEWER-CARD-CONTRACT-PARITY-RED-1`.

Manager verification in clean current-main worktree
`/home/billy/manager_build_accept_worktree` passed focused Card parity and
content-production coverage, `npm run validate:content` with
`cardAccessibilityRulesValidated:16` and `cardAccessibilityParityValidated:true`,
`npm run test:ui-effects` 50/50, typecheck, lint, theme discipline, a11y labels,
ownership, targeted Prettier, Card token grep, `git diff --check
5ccf15d1^..5ccf15d1`, and current-tree `git diff --check`.

Do not reopen the Card resolved-role parity route without fresh current-main
regression evidence. This acceptance also confirms current main is no longer
red on the Card/MetricCard production-summary mirrors. `SITE-P0-5` remains
external production freshness, and no Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:28:00+02:00
Accept SETUP source commit `e96237f` for `REVIEWER-HOME-UI-EFFECTS-RED-1`.

Manager verification in clean current-main worktree
`/tmp/sct-manager-home-ui-effects-W2DbZd/wt` rebased on `origin/main` `f7b5125`
passed `npm run test:ui-effects` 50/50, `npm run validate:content`, `npm run
typecheck -- --pretty false`, `npm run lint`, `npm run test:ownership`,
targeted Prettier for `scripts/ui-effects.test.js` and the SETUP journal,
`git diff --check e96237f^..e96237f`, and current-tree `git diff --check`.

Do not reopen the Home UI-effects header guard route without fresh current-main
regression evidence. `SITE-P0-5` remains external production freshness, and no
Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:27:00+02:00
Accept SETUP source commit `4106058` for `READINESS-MOCK-SCORES-1`.

Manager verification in clean worktree
`/tmp/sct-manager-readiness-mock-rsQzKw/wt` passed focused readiness/progress/
exam/home parity tests 21/21, `npm run validate:content`, `npm run typecheck
-- --pretty false`, `npm run lint`, `npm run test:ownership`, targeted
Prettier for the changed source/test files, `git diff --check 4106058^..4106058`,
current-tree `git diff --check`, and source inspection proving completed mock
exam sessions persist through `recordMockExamSession` and feed the Home
readiness indicator via `mockExamSessions`. The latest `origin/main` `cd8d8e0`
is source-equivalent for this atom after a claim-only queue commit; worker
handoff also includes exported-web mock completion smoke showing Home readiness
changing after a completed mock.

Do not reopen the readiness mock-score route without fresh current-main
regression evidence. `SITE-P0-5` remains external production freshness, and no
Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:24:00+02:00
Accept UIUX-COMPONENTS source commit `761e91f` / PR #892 for
`BADGE-STYLE-OVERRIDE-PARITY-1`.

Manager verification in clean current-main worktree
`/tmp/sct-manager-badge-style-6IF6a2/wt` on `origin/main` `0203801` passed
`npm run validate:content` with `badgeAccessibilityParityValidated:true`,
`node --test tests/content-badge-accessibility-parity.test.js` 3/3, `npm run
typecheck -- --pretty false`, `npm run lint`, `npm run test:ownership`,
targeted Prettier for the validator/test/journal files, `git diff --check
761e91f^..761e91f`, and current-tree `git diff --check`.

Do not reopen the Badge style-override validator parity route without fresh
current-main regression evidence. `SITE-P0-5` remains external production
freshness, and no Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:22:00+02:00
Accept DATA-INTEGRITY source commit `ccae92a` plus handoff `5fcf7a` for
`QUESTION-BANK-CSV-EXPLANATION-PARITY-1`.

Manager verification in clean current-main worktree
`/tmp/sct-manager-csv-explanation-green-dzoDVo/wt` on `origin/main` `761e91f`
passed `node scripts/export-question-bank.js --check`, `npm run
validate:content`, `node --test tests/content-question-bank-csv-contract.test.js
tests/content-authored-source-parity.test.js` 9/9, `npm run typecheck --
--pretty false`, `npm run lint`, `npm run test:ownership`, targeted Prettier
for the JS/test files, `git diff --check ccae92a^..ccae92a`, current-tree `git
diff --check`, and a direct CSV scan showing 720 rows, `explanationSv` /
`explanationEn` columns, q002/q006 rows present, zero blank explanations, and
zero exported answer-judgement boilerplate.

Do not reopen the canonical CSV explanation parity route without fresh
current-main regression evidence. `QUESTION-BANK-CSV-OPTION-PARITY-1` and
`QUESTION-BANK-CSV-CORRECT-ANSWER-PARITY-1` remain separate claimed routes;
`SITE-P0-5` remains external production freshness, and no Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T03:05:00+02:00
Accept SETUP/release-public source commit `a28f235` for
`REVIEWER-PUBLIC-PRIVACY-ADS-IAP-POSTURE-1`.

Manager verification in clean worktree
`/tmp/sct-manager-public-privacy-UWhpcM/wt` passed `npm run test:publishing`
7/7; `npm run test:release-preflight` 46/46; `npm run typecheck -- --pretty
false`; `npm run lint`; `npm run test:ownership`; `node --check` for
`scripts/publishing.test.js` and `scripts/release-preflight.js`; targeted
Prettier; `git diff --check a28f235^..a28f235`; current-tree `git diff
--check`; direct stale-copy scan with zero matches for `no user data`, real
ads disabled, or `Data Not Collected`; and direct current-posture scan showing
Google Mobile Ads/AdMob, Remove Ads, 29 SEK, ATT, UMP consent, and local device
storage copy across the public privacy page, public privacy mirror, and Google
Play listing.

Do not reopen the stale public privacy posture route without fresh current-main
regression evidence. `SITE-P0-5` remains external production freshness, and no
Vercel CLI was run.

MANAGER-build->VALIDATOR 2026-05-19T02:10:00+02:00
Accept SETUP/IAP PR #864 / `9fa1c87` for
`REMOVE-ADS-ENTITLEMENT-INTEGRITY-1`.

Manager verification in isolated worktree `/tmp/sct-manager-pr864-verify` on
PR head `90713d4` passed `npm run test:monetization` 22/22;
`node --test tests/content-remove-ads-purchase-runtime-parity.test.js
scripts/content-production.test.js` 4/4; `npm run validate:content`;
`npm run typecheck -- --pretty false`; `npm run lint`; `npm run
test:ownership`; `npm run format:check`; JS syntax checks for the changed
scripts; and `git diff --check origin/main..HEAD`.

`getPurchaseEntitlements` now rejects the legacy bare `"true"` value,
malformed JSON, missing store-confirmation identity, and invalid dates; buy and
restore persist structured records with schema version, canonical product id,
grant source, grantedAt, and purchase token or transaction id. Do not reopen
the boolean-storage entitlement defect without fresh current-main regression
evidence. Next Remove Ads hardening route is
`REMOVE-ADS-RECEIPT-VALIDATION-1`; `SITE-P0-5` remains external production
freshness, and no Vercel CLI was run.

REVIEWER-PUBLIC-PRIVACY-ADS-IAP-POSTURE-1 [product/release defect][2026-05-19 01:44 CEST][P0 COMPLY][public-privacy]: Focused current-main public privacy URL review on `origin/main` `cef401b` found the hostable privacy policy still advertises the old disabled-ads/no-data posture after the release privacy-label and data-safety docs moved to ad-supported v1.0. `publishing/public-site/privacy/index.html` still says "no user data is collected by the app" and "real ad rendering is disabled for the v1.0 release candidate"; `publishing/public-support-and-privacy.md` carries the same public Privacy Policy URL copy. That contradicts `GOAL.md` and current `publishing/privacy-labels.md` / `publishing/google-play-data-safety.md`, which disclose Google Mobile Ads, ATT/UMP consent, Remove Ads, and 29 SEK IAP. Guard gap: `NODE_OPTIONS='--v8-pool-size=1' timeout 120s npm run test:publishing` exits 0 with 7/7 passing while `scripts/publishing.test.js` explicitly asserts the stale public privacy HTML should contain `no user data is collected`; `NODE_OPTIONS='--v8-pool-size=1' timeout 180s npm run test:release-preflight` exits 0 with 44/44 passing while the stale public privacy copy remains. Manager action: route RELEASE/COMPLY or SETUP/public-site to update `publishing/public-site/privacy/index.html` and `publishing/public-support-and-privacy.md` to the current ad-supported v1.0 posture, including Google Mobile Ads, ATT/UMP consent, Remove Ads, and 29 SEK; update `scripts/publishing.test.js` and any public-URL/release-preflight guard so green release checks reject stale "no user data collected/shared", "real ad rendering disabled", or `REAL_ADS_ENABLED_FOR_V1=false` public privacy copy unless explicitly marked historical.

MANAGER-build->VALIDATOR 2026-05-19T01:16:00+02:00
Accept DATA-INTEGRITY PR #809 / `81c3295` for q270/q271 generated true/false
reason-target wording, and PR #819 / `8782487` for generated true/false
explanation-meta cleanup.

Manager verification on current `origin/main` `69b94c4` passed direct
CSV/static scans with zero stale q270/q271 generic reason stems and zero
generated true/false explanation answer-judgement/meta rows; `validate:content`;
`test:derived-content`; focused published/static/content-production gates
38/38; canonical and static export parity; `node --check site/questions.js`;
typecheck; lint; ownership; and `git diff --check`. No fresh DATA-INTEGRITY
source route is visible after #809, #819, and current-route audit #825 /
`5861846`; require new current-main reviewer/VERIFY evidence before restaffing
DATA-INTEGRITY. SETUP remains routed to
`REVIEWER-SCHEDULED-DEPLOY-VERCEL-CLI-1`; SITE-P0-5 remains external deploy
freshness, with no Vercel CLI from this repo.

MANAGER-build->VALIDATOR 2026-05-19T00:45:00+02:00
Route SETUP next to `REVIEWER-SCHEDULED-DEPLOY-VERCEL-CLI-1`.

Evidence from clean `origin/main` `6e63e8b`: `.github/workflows/scheduled-deploy.yml`
still chooses a Vercel CLI production deploy whenever `VERCEL_TOKEN` is set,
writes `.vercel/project.json`, and only falls back to `VERCEL_DEPLOY_HOOK`
when the token branch is absent. That contradicts the repo Vercel-deploy rule
that production deploys are hook-driven and panes must not run Vercel CLI from
this repo. The current focused build-config gate is a green proxy:
`NODE_OPTIONS='--v8-pool-size=1' npm run test:build-config --
--test-name-pattern 'scheduled Vercel deploy has a site-only main trigger and
live smoke gate'` passed 38/38 while the CLI-first branch remained present.

Acceptance should require SETUP/release-deploy to make the scheduled workflow
deploy-hook-only, remove the Vercel token/CLI path and `.vercel/project.json`
write, preserve post-hook live-smoke verification against `VERCEL_PRODUCTION_URL`,
and add a build-config guard rejecting `vercel deploy`, `npx ... vercel`,
`VERCEL_TOKEN`, and `.vercel/project.json` in this workflow. Do not accept a
docs-only closure. SITE-P0-5 remains external until production serves current
main and the hash-aware live smoke passes.

MANAGER-build->VALIDATOR 2026-05-19T00:32:00+02:00
Accept DATA-INTEGRITY PR #805 / `3fcc83e` for the remaining generated
single-choice cleanup under `REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1`.
Manager recheck passed content validation, derived-content tests, focused
published/static/content-production gates 36/36, canonical/static export
parity, `node --check site/questions.js`, typecheck, lint, ownership, targeted
Prettier, diff whitespace, and direct CSV/static zero-count scans. Current
output has zero ordinary generated single-choice `Vilket svar är korrekt?` /
`Which answer is correct?` meta-stem rows, zero generic filler rows, zero
true/false-shell rows, and zero stale True/False explanation-label rows.

Route DATA-INTEGRITY next to q270/q271 generated true/false standalone wording
from VERIFY evidence (`One reason is...` without a named target), with
generator/validator/static mirror coverage and regression proof that the newly
accepted generated single-choice guards remain green. SITE-P0-5 remains
external; no Vercel CLI from this repo.

REVIEWER-DEVICE-QA-TEMPLATE-1 [product/release defect][2026-05-18 23:49 CEST][P0 COMPLY][device-qa]: Focused GOAL.md device-QA pass on clean current `origin/main` `32105c0` found the required ad/IAP physical-device QA sign-off template is missing. `GOAL.md` says the final manual gate must record EAS preview iOS + Android evidence that ads render with test units, Remove Ads purchase removes ads and persists across relaunch, restore works, ATT + EEA consent prompts appear, and no ad appears on the exam screen, in `reports/release-ads-iap-device-qa.md`; `codex-tasks/open.txt` COMPLY-1 also names that template. Evidence: `test -f reports/release-ads-iap-device-qa.md` exits 1; `rg -n "release-ads-iap-device|device-QA|device QA|EAS preview|manual device" reports publishing codex-tasks/validator.txt codex-tasks/open.txt` finds only COMPLY-1 and older generic device/audio release evidence, not the required file; `reports/device-smoke/android.json` and `reports/device-smoke/ios.json` are blocked audio-smoke placeholders and do not cover the ads/IAP/restore/consent/no-exam-ad matrix. Guard gap: `npm run test:publishing` exits 0 with 7/7 passing and `npm run test:release-preflight` exits 0 with 44/44 passing while this required template is absent. Manager action: route RELEASE/COMPLY to add `reports/release-ads-iap-device-qa.md` as the explicit ad-supported v1.0 device-QA sign-off template, update release/publishing guards so green release checks require that path before store submission, and keep actual device QA blocked until real EAS preview evidence fills the template.
REVIEWER-ADS-IAP-1 [product defect][2026-05-17 07:45Z]: Focused exported-web pass against the current `GOAL.md` ads/IAP target failed. `/home` free-tier route renders no visible ad placement/test-banner copy; `/profile` still shows "Premium and ads are deferred for v1.0" and exposes no "Remove Ads", no "29 SEK" price, and no restore action. Evidence: `grep -q "REAL_ADS_ENABLED" lib/monetization/ads.ts && ! grep -q "REAL_ADS_ENABLED_FOR_V1 = false" lib/monetization/ads.ts` exit 1; `test -f lib/monetization/purchases.ts && grep -qiE "restore" lib/monetization/purchases.ts && grep -rqi "remove.?ads" app components lib` exit 1; `npm run test:monetization` exit 0 but asserts the old fail-closed ads policy; `CI=1 timeout 360s npx expo export --platform web --output-dir dist-web --max-workers 2` exit 0; inline Playwright route check with system Chrome exit 1 with failures for missing home ad placement, missing Remove Ads, missing 29 SEK, and missing Restore while `/exam` remained ad-free. Manager action: do not accept ads/IAP work until product source changes land in `app/`, `components/`, `lib/`, and tests; assign ADS-2/IAP-1/IAP-2/TEST-1 atoms from `codex-tasks/open.txt`.
REVIEWER-ADS-IAP-1 update [2026-05-17 08:17Z]: Current dirty-checkout recheck shows ADS-2 and the exam-overlay part are partly fixed, but IAP-2/paywall UI is still not product-complete. Evidence: ads static gate now exits 0; `test -f lib/monetization/purchases.ts && grep -qiE "restore" lib/monetization/purchases.ts && grep -rqi "remove.?ads" app components lib` still exits 1; `npm run test:monetization` exits 0 with 10/10 tests; `CI=1 timeout 360s npm run build:web:export` exits 0. Inline Playwright with `/usr/bin/google-chrome` against current `dist-web` on port 4185 returned `/home` `googleAdMobCount:2`, `homeBannerVisible:1`, `adMobTestCopyVisible:1`; `/exam` `mockExamVisible:true`, `launchSponsorVisible:0`, `googleAdMobVisible:0`; `/profile` `removeAdsVisible:false`, `priceVisible:false`, `restoreVisible:false`, `staleDeferredCopyVisible:true`; console/page errors 0; route check exited 1. Manager action: keep this finding open for source-touching paywall UI and entitlement wiring; do not accept monetization solely on green unit tests or fixed home/exam ad rendering.
REVIEWER-ADS-IAP-1 current GOAL step-3 gate update [2026-05-19 00:09 CEST][P0 ads-iap][release-gate]: Focused current-main recheck on `origin/main` `fd1c12f` found the product Remove Ads implementation is present, but the exact `GOAL.md` step-3 shell check still exits 1 and cannot be used as a release acceptance gate as written. Evidence: `test -f lib/monetization/purchases.ts` exits 0; `grep -qiE "restore" lib/monetization/purchases.ts` exits 0; `grep -rqi "remove.?ads" app components lib` exits 1. The failing grep is the non-extended literal command from `GOAL.md`, while current source contains `REMOVE_ADS_PRODUCT_ID`, `REMOVE_ADS_PRICE_LABEL = '29 SEK'`, `REMOVE_ADS_STORAGE_KEY`, `buyRemoveAds`, `restoreRemoveAdsPurchase`, `PremiumBanner` buy/restore UI, and localized Remove Ads copy. Supporting gates are green: `npm run test:monetization` exits 0 with 21/21 passing, including `remove-ads IAP wrapper buys, restores, and persists adsDisabled` and `remove-ads paywall is surfaced near an ad placement`; `npm run test:publishing` exits 0 with 7/7 passing; `npm run typecheck -- --pretty false` exits 0; and `git diff --check` exits 0 before queue edits. Manager action: do not reopen the already-cleared paywall/product wiring on this evidence alone, but route VALIDATOR/RELEASE to replace this brittle GOAL step with an executable project guard or request an operator-owned GOAL correction; release acceptance cannot claim all GOAL commands pass while this literal step-3 command remains red.
REVIEWER-CONSENT-COMPLY-1 [product defect][2026-05-17 07:45Z]: Consent/compliance pass failed for the ad-supported v1.0 target. `publishing/public-site/app-ads.txt` is missing; `app`/`lib` have no ATT/UMP/consent implementation beyond `app.json` copy; exported `/privacy` still says real ad rendering is disabled for v1.0 and does not disclose Remove Ads, 29 SEK, or tracking/consent posture. Evidence: `test -f publishing/public-site/app-ads.txt && grep -rqiE "tracking-transparency|ATT|UMP|consent" lib app && test -f publishing/admob-iap-setup-runbook.md` exit 1; `test -f publishing/public-site/app-ads.txt` exit 1; `rg -n "expo-tracking-transparency|UserMessagingPlatform|UMP|tracking-transparency|consent" package.json app.json app lib` found only `app.json` usage-description copy; inline Playwright `/privacy` route check exit 1 with stale disabled-ads/purchases copy and missing Remove Ads/29 SEK/consent disclosures. Manager action: assign CONSENT-1 and COMPLY-1 with product-source verification; do not accept stale privacy/data-safety wording that only mentions future ads or purchases.
REVIEWER-CONSENT-COMPLY-1 update [2026-05-17 10:40Z]: Rechecked after accepted `CONSENT1`. `lib/monetization/consent.ts` now has ATT/UMP decision helpers and `npm run test:monetization` passes 11/11, but the app still does not gate actual ad rendering through that decision. Evidence: `rg -n "getAdConsentDecision|consentConfig|AdConsent|ump_consent_form|app_tracking_transparency" app components lib/monetization -S` finds only `lib/monetization/consent.ts`; `rg -n "shouldShowAd|shouldShowLaunchPopupAd|getAdUnit" components/monetization lib/monetization/ads.ts app/_layout.tsx -S` shows `AdBanner`, `NativeAdCard`, and `LaunchPopupAd` still call the ad gate directly; the GOAL static gate `test -f publishing/public-site/app-ads.txt && grep -rqiE "tracking-transparency|ATT|UMP|consent" lib app && test -f publishing/admob-iap-setup-runbook.md` still exits 1 because `publishing/public-site/app-ads.txt` is missing. Manager action: keep this defect open for source-touching ad-consent wiring plus `app-ads.txt`; do not accept helper-only consent work as satisfying "consent gate before ad SDK init."
REVIEWER-CONSENT-COMPLY-1 update [2026-05-17 08:33Z]: Rechecked after accepted `CONSENT2`. Real-ad serving now requires an explicit consent decision in `lib/monetization/ads.ts`: `npm run test:monetization` exits 0 with 11/11 tests, and a direct real-ad env check returned `homeWithoutConsent:false`, `homeConsentDenied:false`, `homeConsentAllowed:true`, `homeAdsDisabledWithConsent:false`, `examConsentAllowed:false`, `launchConsentDenied:false`, `launchConsentAllowed:true`. The remaining blocker is compliance/prompt surface, not the real-ad gate: `test -f publishing/public-site/app-ads.txt` still exits 1 while `publishing/admob-iap-setup-runbook.md` exists and `grep -rqiE "tracking-transparency|ATT|UMP|consent" lib app` exits 0. Manager action: narrow this finding to `app-ads.txt` plus app/component native prompt wiring and public/privacy copy; do not keep treating the `CONSENT2` real-ad gate as open.
REVIEWER-CONSENT-COMPLY-1 update [2026-05-17 08:55Z]: Rechecked after SETUP Iteration 24. `lib/monetization/consent.ts` now exposes `getAdSdkInitializationDecision` and `consentConfig.sdkInitRequiresConsentDecision`, and the focused gates pass: `npm run test:monetization` 12/12, `npm run typecheck`, `npm run lint`, direct helper assertions, `npm run test:ownership`, targeted Prettier, and targeted `git diff --check`. The helper blocks Google Mobile Ads SDK initialization for disabled ad config, Remove Ads entitlement, pending ATT/UMP prompts, and missing real-ad consent; it allows SDK init for test-unit previews and for satisfied consent. Remaining blocker: `rg -n "getAdSdkInitializationDecision|canInitializeGoogleMobileAds" app components lib/monetization -S` still finds no app/component integration, and `publishing/public-site/app-ads.txt` remains absent. Manager action: narrow this finding to app/native SDK-init + prompt wiring, `app-ads.txt`, and public/privacy copy; do not close it on helper-only plumbing.
REVIEWER-RELEASE-GATES-1 [product defect][2026-05-17 07:45Z]: The verifier/release-gate layer still encodes the old disabled-ads contract after `GOAL.md` changed to ad-supported v1.0. Evidence: `rg -n 'REAL_ADS_ENABLED_FOR_V1=false|real ads disabled|real ads are disabled|fail-closed|no analytics, crash reporting, purchases, or real ads enabled|deferred AdMob decision|Google Mobile Ads SDK test configuration' scripts publishing reports app lib components tests` finds stale assertions in `scripts/monetization.test.js`, `scripts/publishing.test.js`, `scripts/release-preflight.js`, `scripts/release-preflight.test.js`, `publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, and release evidence/gate reports. `npm run test:publishing` exit 0 while asserting "real ads disabled"; `node --test scripts/release-preflight.test.js --test-name-pattern 'privacy|AdMob|Google Mobile Ads|real ads'` exit 0 and includes old-contract coverage such as blocking local privacy review evidence with real ads enabled. Manager action: assign a source-touching TEST/release-gate atom to update verifier expectations to the new ads-enabled + Remove Ads IAP contract before accepting any green release gate.
REVIEWER-RELEASE-GATES-1 update [2026-05-17 08:39Z]: Focused review of the current release-policy/preflight patch shows the preflight source portion is moving to the new ad-supported contract, but the broader release verifier surface is still not clean. Evidence: `npm run test:monetization` exits 0 with 12/12 tests; `node --test scripts/release-preflight.test.js --test-name-pattern 'AdMob|privacy review|ad-supported|disabled-ad|valid local store record|valid local privacy'` exits 0 and reports 44/44 passing; `rg -n 'REAL_ADS_ENABLED_FOR_V1=false|real ads disabled|real ads are disabled|fail-closed|no analytics, crash reporting, purchases, or real ads enabled|deferred AdMob decision|Google Mobile Ads SDK test configuration' scripts publishing reports app lib components tests` still finds old-contract text in `publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, `scripts/publishing.test.js`, `reports/release-gates.json`, `publishing/post-eas-auth-runbook.md`, and release evidence/templates. `npm run release:preflight -- --json` remains BLOCKED: local validation now fails on `npm run test:a11y-labels`, while `store-records` and `privacy-review` gate evidence still mention real ads disabled / `REAL_ADS_ENABLED_FOR_V1=false`. Manager action: keep this finding open for the publishing/test/report gate surfaces; do not treat the green release-preflight unit tests alone as clearing the release contract.
REVIEWER-RELEASE-GATES-1 update [2026-05-17 08:54Z]: Re-ran full release preflight after A11Y1 and later content/data-integrity acceptances. `CI=1 timeout 360s npm run release:preflight -- --json` exits 1 with overall `BLOCKED`, but `local-validation`, `expo-doctor`, `web-export`, `native-prebuild`, and `eas-cli` are now `READY`; the previous routed-quiz a11y local-validation failure is cleared. Remaining release blockers include dirty worktree, missing EAS auth/build/device evidence, store credentials/screenshots/submission evidence, plus stale old-contract release evidence: `store-records` still says AdMob is deferred because real ads are disabled for v1.0, and `privacy-review` still requires `REAL_ADS_ENABLED_FOR_V1=false` posture. Manager action: narrow this finding away from A11Y/local validation and keep it open for stale store/privacy/publishing evidence plus external release gates; update release evidence so preflight cannot be green while describing disabled ads.
REVIEWER-A11Y-QUIZ-STATE-1 [product defect][2026-05-17 08:39Z]: Release local validation is blocked by an accessibility regression in the routed quiz session. Evidence: `npm run test:a11y-labels` exits 1 with `app/quiz/[sessionId].tsx:111: missing accessibilityState: <Pressable` for the `Try again` control; `npm run release:preflight -- --json` also reports `local-validation` BLOCKED because the same `test:a11y-labels` step fails after the broader validation suite. Manager action: assign a source-touching quiz a11y atom in `app/quiz/[sessionId].tsx` or the a11y scanner, then rerun `npm run test:a11y-labels` and release preflight local validation.
REVIEWER-A11Y-QUIZ-STATE-1 update [2026-05-17 08:43Z]: Rechecked after SETUP Iteration 22. The routed quiz retry control now has `accessibilityState={{ disabled: false }}` at `app/quiz/[sessionId].tsx:114`, and the focused gates pass: `npm run test:a11y-labels` exit 0, `npm run typecheck` exit 0, `npm run lint` exit 0, `npm run test:ownership` exit 0, `npx prettier --check app/quiz/[sessionId].tsx` exit 0, and `git diff --check -- app/quiz/[sessionId].tsx docs/parallel-sessions/journals/setup.md` exit 0. Manager action: this reviewer defect can be closed once VALIDATOR accepts the SETUP source atom; broader release preflight can still be blocked by unrelated gates.
REVIEWER-LEARN-CHAPTER-1 [product defect][2026-05-17 09:58Z]: Focused learning-path pass failed after exported web rebuild. `npm run test:e2e -- tests/e2e/learn-chapter-navigation.spec.ts` fails on `/chapter/ch01` because `getByLabel('Start quiz for Landet Sverige')` is missing. The route renders chapter content and 50 practice questions, but no accessible start-quiz/next-step control. Product impact: the Learn screen promises a clear next step, but chapter detail becomes a long read-only question list and breaks the UI/UX regression test. Manager action: assign or finish a source-touching `app/chapter/[chapterId].tsx` quiz-entry atom, coordinate with the active `app/quiz/[sessionId].tsx` work already in the dirty tree, then rerun the learning-path e2e.
REVIEWER-LEARN-CHAPTER-1 update [2026-05-17 08:47Z]: Current dirty checkout adds the missing chapter start-quiz link and clears the user-flow portion of this defect in exported web. Evidence: `npm run typecheck`, `npm run lint`, `npm run test:practice`, `git diff --check -- app/chapter/[chapterId].tsx`, and `CI=1 EXPO_NO_TELEMETRY=1 npx expo export --platform web --output-dir dist-web --max-workers 2` all exit 0. Official `CI=1 timeout 120s npm run test:e2e -- tests/e2e/learn-chapter-navigation.spec.ts --workers=1` still exits 1 before app interaction due the known missing cached Chromium. Fallback system-Chrome exported-web smoke closes `Close launch sponsor ad`, opens `/chapter/ch01`, finds `Start quiz for Landet Sverige`, verifies `href:"/quiz/q001"`, clicks through to `/quiz/q001`, sees `Session q001` and `Var ligger Sverige?`, and records `consoleErrors:[]`. Manager action: close this reviewer defect once the `app/chapter/[chapterId].tsx` atom is accepted; keep the Playwright-cache blocker separate for official e2e acceptance.
REVIEWER-PRACTICE-WRONG-FEEDBACK-1 [product defect][2026-05-17 07:56Z]: Focused practice-to-mistakes pass failed the wrong-answer learning loop. After selecting the wrong answer `I södra Europa`, exported `/practice` showed `I södra Europa — Fel`, `Score: 0/1`, the explanation, and no console errors, but never marked the correct option `I Norden i norra Europa` as correct. The wrong answer and bookmark did appear on `/mistakes`, so persistence within the active session works; the product gap is that a learner who answers incorrectly must infer the correct choice from prose instead of seeing the correct option identified. Evidence: `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` returned `selectedWrongVisible:true`, `correctRevealedCount:0`, `correctPlainCount:1`, `scoreText:"Score: 0/1"`, `explanationVisible:true`, `wrongAnswersCount:1`, `savedReviewCount:1`, `consoleErrors:[]`, exit 1 because the expected correct-answer reveal was missing. Manager action: assign a source-touching practice feedback atom, likely in `app/(tabs)/practice.tsx` / `components/quiz/AnswerOption.tsx`, then add or extend e2e coverage for wrong-answer feedback.
REVIEWER-EXAM-LAUNCH-AD-1 [product defect][2026-05-17 08:01Z]: Focused exported-web exam entry pass failed the `GOAL.md` rule "No ads ever on the exam screen." Current `app/_layout.tsx` mounts `LaunchPopupAd` globally, so direct entry to `/exam` shows the app-open launch ad modal over the mock exam. Evidence: `npm run test:monetization` exit 0, but that static test only checks `app/(tabs)/exam.tsx` for direct ad imports; `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` at `http://127.0.0.1:4173/exam` returned `mockExamVisible:true`, `launchSponsorVisible:true`, `googleAdMobVisible:true`, `closeLaunchSponsorVisible:true`, `noAdsDuringExamTextVisible:true`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching monetization/routing atom so `LaunchPopupAd` is suppressed for exam routes, and extend monetization/e2e coverage to catch route-level global ad overlays, not only direct imports inside the exam file.
REVIEWER-LANGUAGE-SUPPORT-1 [product defect][2026-05-17 08:05Z]: Focused exported-web settings-to-practice pass failed the "Question language / English support" path. After setting language to English support, `/profile` shows the English support badge and `/practice` shows the English question translation, but answer options remain Swedish-only and feedback uses only the Swedish explanation even though the content model has `textEn` and `explanationEn`. Evidence: `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4174` returned `profileShowsEnglish:1`, `hasEnglishQuestionBefore:true`, `answerLabels:["I Norden i norra Europa","I södra Europa","I västra Asien","I Nordamerika"]`, `hasEnglishOptionBefore:false`, `hasEnglishExplanationAfter:false`, `hasSwedishExplanationAfter:true`, `consoleErrors:[]`, and exited 1 because English option/explanation support was absent. Manager action: assign a source-touching language-rendering atom across practice/quiz/exam review components and add e2e or unit coverage; do not accept settings-copy-only or docs-only changes for this defect.
REVIEWER-SETTINGS-MOBILE-SCROLL-1 [product defect][2026-05-17 08:08Z]: Focused exported-web small-mobile pass found the settings screen can overflow below the viewport without scrolling, making the bottom compliance links unreachable on a 320x568 viewport. Evidence: inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4174/settings` returned `scrollHeight:734`, `innerHeight:568`, `afterWheel.scrollY:0`, `supportBox.y:701`, `supportInViewport:false`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching settings layout atom, likely replacing the root non-scrolling view with a tokenized scroll container and adding a mobile viewport regression check; do not accept docs-only or copy-only changes for this defect.
REVIEWER-ONBOARDING-MOBILE-SCROLL-1 [product defect][2026-05-17 08:12Z]: Focused exported-web first-run pass found `/onboarding` overflows a 320x568 mobile viewport without scrolling, leaving the primary `Start studying` and `Adjust settings` actions below the visible viewport. Evidence: `CI=1 timeout 360s npm run build:web:export` exit 0; inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4181/onboarding` returned `scrollHeight:739`, `innerHeight:568`, `afterWheelScrollY:0`, `startBox.y:661`, `settingsBox.y:706`, `supportBox.y:610`, `textHasWelcome:true`, `consoleErrors:[]`, and exited 1 because the page overflowed with no scroll. Manager action: assign a source-touching onboarding layout atom, likely replacing the root fixed `View` with a tokenized scroll container, and add a narrow-mobile regression check; do not accept a docs-only or copy-only change for this defect.
REVIEWER-SUPPORT-CONTACT-1 [product defect][2026-05-17 08:13Z]: Focused exported-web support pass found `/support` is still a pre-launch placeholder instead of an actionable support surface. The route renders `Support and feedback` and copy saying the public support URL and contact mailbox are release checklist items, but exposes only `Back to Profile`; there is no mail/contact action and no external support link. Evidence: inline Playwright with `/usr/bin/google-chrome` against `http://127.0.0.1:4182/support` returned `titleVisible:true`, `placeholderCopyVisible:true`, `linkCount:1`, `links:[{text:"Back to Profile",href:"/profile"}]`, `hasMailto:false`, `hasExternalSupportLink:false`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching support-surface atom in `app/support.tsx`/compliance components so the in-app support page exposes the verified public support destination or contact path and no longer claims it is only a future release checklist item.
REVIEWER-SUPPORT-CONTACT-1 update [2026-05-17 22:20 CEST]: Rechecked after SETUP commit `4064f0d` (`setup: localize support shell`). The user-facing support route now clears this reviewer defect: system-Chrome exported-web `/support` shows Swedish `Support och återkoppling`, `Vad du kan rapportera`, `Inga personuppgifter`, and `Offentlig supportsida`; the stale release-checklist placeholder copy is absent; links include `/profile` and `https://szechunyiu.github.io/Swedish_Civic_Test-public-site/support/` with accessibility label `Öppna den offentliga supportsidan`; browser console/page errors 0. Manager action: this defect can be closed once VALIDATOR accepts the bounded SETUP source atom; do not count this REVIEWER update as source acceptance.
REVIEWER-SOURCES-LINK-1 [product defect][2026-05-17 08:14Z]: Focused exported-web sources pass found `/sources` displays the UHR study-material URL as plain text but does not expose it as an actionable link. Evidence: initial inline Playwright check hit a strict locator ambiguity on the page title, then the heading-specific rerun with `/usr/bin/google-chrome` against `http://127.0.0.1:4184/sources` returned `headingVisible:true`, `showsUhrUrlText:true`, `linkCount:1`, `links:[{text:"Back to Profile",href:"/profile"}]`, `hasExternalUhrLink:false`, `consoleErrors:[]`, and exited 1. Manager action: assign a source-touching legal/source page atom so the primary UHR source is a real accessible external link, with coverage that distinguishes visible URL text from an actionable link.
REVIEWER-SOURCE-CITATION-STEM-1 [product defect][blocker][2026-05-17 20:08Z]: Focused source-citation pass failed the P0 rule that stems should read naturally without "Enligt UHR/According to UHR" now that `QuestionCard` renders a separate source citation line. Static content check found 272 published questions whose stems still contain UHR/source phrasing, including 43 authored questions; examples include `q001` "Enligt UHR-materialet, var ligger Sverige?" / "According to the UHR material, where is Sweden located?". Evidence: `npm run test:answer-shuffle` exits 0 with 3/3 passing, so this pass did not re-open shuffle; `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export` exits 0; inline Playwright with `/usr/bin/google-chrome` against `/quiz/q001` exits 1 with `hasOldStemSv:true`, `hasOldStemEn:true`, `hasSeparateCitation:true`, `hasDisclaimer:true`, and console/page errors 0. Queue note: `/Users/billy/Desktop/projects/.shared/review-to-queue.sh` and `/home/billy/Desktop/projects/.shared/review-to-queue.sh` are absent in this checkout, so REVIEWER queued directly in `codex-tasks/validator.txt` per the reviewer lane contract fallback. Manager action: assign a source-touching SOURCE-CITATION atom across authored `data/` stems plus generated prompt templates in `lib/content/derivedQuestions.ts` / `scripts/validate-content.js`; add validation that rejects UHR/source-authority phrasing in user-facing question stems while keeping the separate citation line and disclaimer.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:15 CEST]: Focused Chapter 10 follow-up shows the current q075-style source-stem cleanup has not cleared the adjacent P0 batch. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q076` loads `Quizpass q076` and still shows `Enligt UHR-materialet, varför ökade Sveriges befolkning under 1800-talet?` plus `According to the UHR material, why did Sweden’s population grow during the 19th century?` immediately above the separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Befolkningsökning, s. 32` line and the not-official disclaimer; browser console/page errors 0. Static CSV check over `q075`-`q080` finds `q075` cleaned but `q076`, `q077`, `q078`, `q079`, and `q080` still contain UHR/source-authority wording, while `npm run validate:content` still exits 0 and reports `questionAuthorityBoundaryTextValidated:500`, so the current validator does not cover the active SOURCE-CITATION P0. Manager action: do not accept a one-question cleanup as resolving SOURCE-CITATION; assign the next source-touching content/data-integrity atom to remove source-authority phrasing from at least `q076`-`q080` and add a validator/test that fails on `Enligt UHR-materialet`, `enligt UHR-materialet`, `According to the UHR material`, and equivalent UHR-source stem phrasing in both authored and generated prompts.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:19 CEST]: Rechecked after the dirty q076 content diff. The user-facing q076 route now clears its part of this defect: `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and system-Chrome exported-web `/quiz/q076` shows `Varför ökade Sveriges befolkning under 1800-talet?` / `Why did Sweden’s population grow during the 19th century?`, preserves `Källa/Source: Sverige i fokus, Sveriges moderna historia, Befolkningsökning, s. 32`, keeps the not-official disclaimer, and records browser errors 0. Remaining defect evidence: static CSV check over `q075`-`q080` now flags `q077`, `q078`, `q079`, and `q080` for UHR/source-authority stem wording; `npm run validate:content` still exits 0 and reports `questionAuthorityBoundaryTextValidated:500`, so the validator still does not cover this SOURCE-CITATION P0. Manager action: q076 can be reviewed as a bounded CONTENT fix once it has a handoff/commit, but do not close the blocker; assign the next content/data-integrity atom to clean `q077`-`q080` and add failing validation for source-authority phrasing in authored and generated prompts.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:22 CEST]: Rechecked after the dirty q077 content diff. The user-facing q077 route now clears its part of this defect: `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and system-Chrome exported-web `/quiz/q077` shows `Ungefär hur många svenskar utvandrade till USA mellan 1850 och 1920?` / `Approximately how many Swedes emigrated to the United States between 1850 and 1920?`, preserves `Källa/Source: Sverige i fokus, Sveriges moderna historia, Befolkningsökning, s. 32`, keeps the not-official disclaimer, and records browser errors 0. Remaining defect evidence: static CSV check over `q075`-`q080` now flags `q078`, `q079`, and `q080`; `npm run validate:content` still exits 0 and reports `questionAuthorityBoundaryTextValidated:500`, so the validator still does not cover this SOURCE-CITATION P0. Manager action: q077 can be reviewed as a bounded CONTENT fix once it has a handoff/commit, but do not close the blocker; assign the next content/data-integrity atom to clean `q078`-`q080` and add failing validation for source-authority phrasing in authored and generated prompts.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:25 CEST]: Rechecked the current unbounded source-citation runtime-strip diff. It only partially masks the P0 and does not clean the source/export contract. Evidence: `npm run test:audio` exits 0, `node --test tests/content-question-card-accessibility-parity.test.js tests/content-question-speech-text-parity.test.js` exits 0, and `npm run validate:content` exits 0 while still reporting `questionAuthorityBoundaryTextValidated:500`; static scan still finds source-authority phrasing in `data/additionalQuestions.ts`, `content/question-bank.csv`, and generated prompt templates in `lib/content/derivedQuestions.ts`. Exported-web system-Chrome smoke over `/quiz/q078`, `/quiz/q079`, and `/quiz/q080` exits 1: q078 and q080 are visually stripped, but q079 still renders `Vilka nämner UHR-materialet som fyra av de största svenska folkrörelserna?` and its English text still includes `UHR material`, with separate source citation present and browser errors 0. Manager action: do not accept a display-time stripping helper as closing SOURCE-CITATION; require source data and generated prompt templates to be cleaned, add validator/test coverage that rejects raw source-authority wording in authored and generated prompts, and specifically recheck q079.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:30 CEST]: Rechecked current HEAD `5801f01` after the source-citation prompt cleanup. The P0 remains open. Evidence: queue helper `/Users/billy/Desktop/projects/.shared/review-to-queue.sh` and `/home/billy/Desktop/projects/.shared/review-to-queue.sh` are still absent, so REVIEWER queued directly here; `npm run test:answer-shuffle` exits 0 (3/3), `npm run validate:content` exits 0 and still reports `questionAuthorityBoundaryTextValidated:500`, and `node --test tests/content-question-authority-boundary.test.js tests/content-question-speech-text-parity.test.js` exits 0 (3/3) despite raw source-authority wording. Static scan reports 252 CSV rows with source-authority wording, 152 source-data matches in `data/questions.ts`/`data/additionalQuestions.ts`, and 2 generated-template matches in `lib/content/derivedQuestions.ts`; examples still include q079/q080/q081/q085. `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web smoke over `/quiz/q079`, `/quiz/q080`, and `/quiz/q081` exits 1: q079 still visibly renders `Vilka nämner UHR-materialet som fyra av de största svenska folkrörelserna?` and English `UHR material`; q080 and q081 are visually stripped; all checked routes show a separate `Källa/Source` line and the not-official disclaimer, with browser errors 0. Manager action: do not close SOURCE-CITATION on runtime display stripping or green validators; assign a source-touching content/data-integrity atom to clean raw stems and generated prompt templates, starting with q079 and the remaining Chapter 10/11 batch, and add validation that fails on `UHR-materialet`, `according to the UHR material`, `enligt UHR-avsnittet`, and `the UHR section` in published question stems/exports.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:35 CEST]: Rechecked after committed CONTENT atom `cc5ea91` cleaned q079. SOURCE-CITATION remains open, now narrowed away from q079. Evidence: `npm run validate:content` exits 0 and still reports `questionAuthorityBoundaryTextValidated:500`; focused static CSV/source scan shows q079 clean but q080, q081, q084, q085, q086, q087, q088, q089, q090, and q091 still contain source-authority phrasing such as `enligt UHR-materialet` / `According to the UHR material`. Across the exported bank, 248 CSV rows still contain source-authority wording, `data/questions.ts`/`data/additionalQuestions.ts` still contain 151 matches, and `lib/content/derivedQuestions.ts` still has 2 generated-template matches. `CI=1 EXPO_NO_TELEMETRY=1 timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and system-Chrome exported-web smoke over `/quiz/q080`, `/quiz/q081`, and `/quiz/q085` exits 0 with visually clean prompts, separate `Källa/Source: Sverige i fokus` citation, disclaimer visible, and browser errors 0. Product defect: runtime display stripping now masks these examples, but the raw content/export contract remains dirty and validators still allow it. Manager action: keep SOURCE-CITATION open; assign CONTENT to clean q080-q091 in bounded batches and DATA-INTEGRITY to fail `validate:content`/content tests on source-authority wording in published question stems and generated prompt templates, not only display/speech output.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:41 CEST]: Rechecked after committed CONTENT atom `19af8f5` cleaned authored q080. q080's source row, exported public row, and `/quiz/q080` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q080 source/export assertion exits 0 and confirms section `Demokratins genombrott`, page 34, correct option `c`/`1921`, and exported q080/q417-q420 rows; `node --test tests/content-question-bank-csv-contract.test.js` exits 0 with 2/2; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q080` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Demokratins genombrott, s. 34`, disclaimer visible, answer option `1921`, and browser errors 0. Remaining defect evidence: focused CSV scan shows authored q079/q080 clean but q081 and q084-q091 still dirty, the exported bank still has 244 source-authority rows, and generated section-practice templates such as q417 still contain source-authority phrasing. Manager action: keep SOURCE-CITATION open; q080 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q081+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:44 CEST]: Rechecked after committed CONTENT atom `58fc505` cleaned authored q081. q081's source row, exported public row, and `/quiz/q081` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q081 source/export assertion exits 0 and confirms section `Den svenska modellen`, page 35, correct option `a`, the clean Saltsjöbaden Agreement prompt, and exported q081/q421-q424 rows; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q081` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Den svenska modellen, s. 35`, disclaimer visible, correct option text, and browser errors 0. Remaining defect evidence: focused CSV scan now shows authored q079-q083 clean but q084-q091 still dirty, the exported bank still has 240 source-authority rows, and generated section-practice templates still include source-authority phrasing. Manager action: keep SOURCE-CITATION open; q081 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q084+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:48 CEST]: Rechecked after committed CONTENT atom `9bbf552` cleaned authored q086. q086's source row, exported public row, and `/quiz/q086` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q086 source/export assertion exits 0 and confirms section `EU och Europarådet`, page 39, correct option `b`/`1995`, clean SV/EN stems, and exported q086/q441-q444 rows; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q086` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, EU och Europarådet, s. 39`, disclaimer visible, answer option `1995`, and browser errors 0. Remaining defect evidence: focused CSV scan shows q086 clean but q084, q085, q087-q091, and q095 still dirty, and the exported bank still has 236 source-authority rows. Manager action: keep SOURCE-CITATION open; q086 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q084/q085/q087+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 22:54 CEST]: Rechecked after committed CONTENT atom `4cac91b` cleaned authored q084. q084's source row, exported public row, and `/quiz/q084` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q084 source/export assertion exits 0 and confirms section `Digital revolution och globalisering`, page 38, correct option `a`, clean SV/EN stems, and exported q084/q433-q436 rows; bundled `node --test tests/content-question-id-sequence.test.js` exits 0 with 2/2; `npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web `/quiz/q084` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sveriges moderna historia, Digital revolution och globalisering, s. 38`, disclaimer visible, correct answer text, and browser errors 0. Remaining defect evidence: focused CSV scan shows q084/q086 clean but q085, q087-q091, and q095 still dirty, and the exported bank still has 232 source-authority rows. Manager action: keep SOURCE-CITATION open; q084 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q085/q087+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:00 CEST]: Rechecked after committed CONTENT atom `619dc5b` cleaned authored q085. q085's source row, exported public row, and `/quiz/q085` route now pass: `npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; direct q085 source/export assertion exits 0 and confirms section `Nordiskt samarbete`, page 39, correct option `a`, clean SV/EN stems, and exported q085/q437-q440 rows; `node --test tests/content-question-id-sequence.test.js` exits 0 with 2/2; `npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q085` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, Nordiskt samarbete, s. 39`, disclaimer visible, correct answer text, and browser errors 0. Remaining defect evidence: focused CSV scan shows q084-q086 clean but q087-q094 still contain source-authority phrasing, and the exported bank still has 135 matching rows. Manager action: keep SOURCE-CITATION open; q085 can be reviewed as a bounded CONTENT fix, while the next CONTENT/DATA-INTEGRITY atoms should clean q087+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:04 CEST]: Rechecked after committed CONTENT atom `038cf25` cleaned authored q087. q087's source row, exported public row, and `/quiz/q087` route now pass: `npm run validate:content` exits 0 with 500 published questions, `questionAuthorityBoundaryTextValidated:500`, and answer-shuffle validator fields green; `node scripts/export-question-bank.js --check` exits 0; direct q087 source/export assertion exits 0 and confirms section `EU och Europarådet`, page 39, correct option `a`, clean SV/EN stems, and exported q087/q445-q448 rows; `npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q087` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, EU och Europarådet, s. 39`, disclaimer visible, correct answer text, and browser errors 0. Remaining defect evidence: focused CSV scan shows q084-q087 clean but q088-q094 still contain source-authority phrasing, and the exported bank still has 130 matching rows. Manager action: keep SOURCE-CITATION open; q087 can be reviewed as a bounded CONTENT fix, while next CONTENT/DATA-INTEGRITY atoms should clean q088+ authored stems and generated prompt templates before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:15 CEST]: Provisional focused pass after current dirty q090 content/CSV cleanup. q090 now passes the source-citation route check: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 and still reports `questionAuthorityBoundaryTextValidated:500`; `node scripts/export-question-bank.js --check` exits 0; focused CSV scan shows q088-q090 clean, q091-q095 dirty, and 212 total exported rows still matching source-authority phrasing; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q090` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, Sverige och omvärlden, Försvars- och säkerhetspolitik, s. 40`, current independent-study disclaimer, correct answer text, and browser errors 0. Remaining defect evidence: q091-q095 still contain source-authority phrasing in the public CSV, and the validator remains a false negative because it reports authority-boundary validation green while 212 exported rows still match the banned wording. Manager action: keep SOURCE-CITATION open; q090 can be reviewed only after CONTENT bounds or commits the data/CSV atom, then assign q091+ authored stems and generated prompt-template validation before closing the P0.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:16 CEST]: Final boundary recheck after the q090 pass shows CONTENT has since landed q090 as commit `9349500` (`content: correct q090 nato prompt`). Treat the 23:15 q090 route/export evidence as attached to that bounded commit. SOURCE-CITATION remains open: current dirty data/CSV has already moved to q091, q092-q095 remain source-authority dirty in the CSV, and the validator still reports `questionAuthorityBoundaryTextValidated:500` despite the raw export matches. Manager action: VALIDATOR can review q090 at commit `9349500`; keep assigning q091+ cleanup and DATA-INTEGRITY validator hardening.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:30 CEST]: Rechecked after committed CONTENT atom `dd0ea1f` cleaned authored q093. q093's source row, exported public row, and `/quiz/q093` route now pass: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; direct q093/q469-q472 source/export assertion exits 0 and confirms clean SV/EN stems, correct source metadata `Religionsfrihet` page 42, tags, and true/false variants; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q093` smoke exits 0 with standalone SV/EN prompts, separate `Källa/Source: Sverige i fokus, En sekulär stat och ett mångreligiöst land, Religionsfrihet, s. 42`, disclaimer visible, answer option `Religionsfrihetslagen`, and browser errors 0. Remaining defect evidence: focused CSV scan shows q088-q093 clean but q094-q096 still contain source-authority phrasing, and the exported bank still has 200 matching rows while validation remains green. Manager action: keep SOURCE-CITATION open; q093 can be treated as reviewer-verified once VALIDATOR accepts CNT41, while next CONTENT/DATA-INTEGRITY atoms should clean q094+ and harden validation against raw source-authority stems.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:34 CEST]: Provisional focused pass after the current q094 content/CSV cleanup. q094 now passes the user-facing route check: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and still reports `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0 after the export settled; system-Chrome exported-web `/quiz/q094` smoke exits 0 with standalone SV/EN prompts, a separate source citation for `Sverige i fokus` / `Religionsfrihet` page 42, the not-official disclaimer, visible true/false options, and browser errors 0. Remaining defect evidence: focused CSV scan now shows q095 and q096 still contain source-authority phrasing, generated rows q473/q477-q485/q489/q493/q497 still use source-authority prompt wording, and 196 exported rows still match `Enligt UHR`, `UHR-materialet`, `UHR-avsnitt`, `UHR section`, or `UHR material` while validation remains green. Manager action: keep SOURCE-CITATION open; q094 needs CONTENT/VALIDATOR boundary before acceptance, then assign q095/q096 and generated prompt-template cleanup plus validator hardening so raw/exported stems fail on source-authority wording.
REVIEWER-SOURCE-CITATION-STEM-1 boundary update [2026-05-17 23:36 CEST]: q094 landed after the reviewer smoke as commit `8bce784` (`content: correct q094 church prompt`) with a CONTENT handoff. Treat the 23:34 q094 route/export evidence as attached to that bounded commit once VALIDATOR accepts it. SOURCE-CITATION remains open for q095/q096 authored stems, generated prompt-template rows including q477-q485/q489/q493/q497, and validator hardening because the scan still found 196 exported source-authority matches while `validate:content` stayed green.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:41 CEST]: Rechecked bounded q095 CONTENT commit `8ecc5e8` and current exported web. q095 now passes source/export/route review: UHR text in `tmp/sverige-i-fokus.txt` lines 2014-2022 supports the Lutheran Protestant answer; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; serialized `NODE_OPTIONS='--v8-pool-size=1' npm run test:content -- --test-concurrency=1` exits 0 with 213/213; direct normalized source/export assertion exits 0; web export exits 0; system-Chrome exported-web `/quiz/q095` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, En sekulär stat och ett mångreligiöst land, Kristendom, s. 43`, disclaimer visible, correct answer visible, and console errors 0. Remaining SOURCE-CITATION defect evidence: current moving checkout has unaccepted q096 data/CSV cleanup, generated section-practice rows still include source-authority wording, and a static CSV scan still finds 188 exported rows matching `Enligt UHR`, `UHR-materialet`, `UHR-avsnitt`, `UHR section`, or `UHR material` while validation remains green. Manager action: q095 can be reviewed as bounded CONTENT evidence, but keep SOURCE-CITATION open for generated prompt-template cleanup, validator hardening, and any unaccepted q096 follow-up.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:50 CEST]: Rechecked q096/CNT44 before the checkout moved again. q096 now passes source/export/route review: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 published questions and `questionAuthorityBoundaryTextValidated:500`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; direct normalized q096/q481-q484 assertion exits 0 and confirms clean SV/EN stems, source metadata `Islam` page 44, tags, generated variants, and UHR text support for Islam as the second-largest religion; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` exits 0; `node scripts/prepare-web-export.js --check dist-web` exits 0; system-Chrome exported-web `/quiz/q096` smoke exits 0 with clean SV/EN prompts, separate `Källa/Source: Sverige i fokus, En sekulär stat och ett mångreligiöst land, Islam, s. 44`, disclaimer visible, `Islam` correct, launch ad dismissible, and console/page errors 0. Remaining SOURCE-CITATION defect evidence: static CSV scan still finds 35 exported prompt rows with source-authority wording while validation stays green: authored rows `q010`, `q012`, `q013`, `q014`, `q086`, `q088`, and `q089`, plus 28 generated rows including `q137`-`q140` and `q145`-`q156`. Manager action: q096 can stay accepted, but do not close SOURCE-CITATION until authored raw stems and generated prompt templates fail validation when they contain source-authority wording.
REVIEWER-SOURCE-CITATION-STEM-1 update [2026-05-17 23:54 CEST]: Focused source-citation gate pass shows the accepted DI39 gate is still a false negative in the live checkout. Evidence: `node --test tests/content-uhr-source-citation-stem.test.js` exits 0 with 2/2, and `npm run validate:content` exits 0 with `questionAuthorityBoundaryTextValidated:500`, but an exported `content/question-bank.csv` stem scan finds 55 `questionSv`/`questionEn` matches for source-authority wording (`UHR-materialet`, `UHR material`, `UHR-avsnitt`, or `UHR section`). Examples include q010, q012, q013, q014, q086, q088, q089, and generated q137. The test currently bans only narrower connective forms such as `enligt UHR...`, `according to UHR...`, and `best matches the UHR section`, so stems like `Vad beskriver UHR-materialet...` still pass. Manager action: keep SOURCE-CITATION open despite DI39; assign CONTENT/DATA-INTEGRITY to clean the remaining authored/generated exported stems and widen the stem gate so any UHR source-authority reference in `questionSv`/`questionEn` fails. Do not accept this on green `validate:content` or the current focused stem test alone.
REVIEWER-SOURCE-CITATION-STEM-1 resolution check [2026-05-18 00:10 CEST]: Focused REVIEWER pass against the current CONTENT q100 and DATA-INTEGRITY broad source-citation handoffs finds the previously queued raw/export stem defect cleared in this checkout. Evidence: `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-question-authority-boundary.test.js` passed 5/5; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` passed with `questionAuthorityBoundaryTextValidated:500`, `questionBankCsvRowsValidated:500`, and `generatedPromptTemplateParityValidated:400`; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` passed; direct CSV scan over 500 `questionSv`/`questionEn` rows found `offenders:0`; web export passed; system-Chrome exported-web smoke on `/quiz/q010`, `/quiz/q088`, `/quiz/q100`, and `/quiz/q497` found SV+EN standalone stems, `Källa/Source: Sverige i fokus`, the independent-study disclaimer, no banned UHR source-authority phrases, and no console/page errors. Manager action: VALIDATOR can review the bounded q100 CONTENT atom and broad source-citation DATA-INTEGRITY atom for acceptance; do not count this REVIEWER queue update as product acceptance.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:18 CEST][P0 TRANSLATE-COMPLETE]: Rechecked the current exported-web English-support path. Options and explanations now switch to English, but the primary question heading and answer-result labels still remain Swedish in English mode on both `/practice` and `/quiz/q001`. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass `/settings` -> English support -> `/practice` -> wrong answer -> `/quiz/q001` -> wrong answer exited 1 with `settingsEnglishSelected:true`, practice `headingSvCount:1`, `headingEnCount:0`, `bodyHasSvStem:true`, `bodyHasEnStem:true`, `hasEnglishWrongOption:true`, `hasSwedishWrongOption:false`, `afterHasEnglishExplanation:true`, `afterHasSwedishExplanation:false`, `afterHasSwedishFeedbackLabels:true`, `afterHasEnglishFeedbackLabels:false`; routed quiz showed the same heading/feedback-label gap; console/page errors 0. The focused static coverage `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/ui-effects.test.js --test-name-pattern 'English support reaches quiz options, explanations, and exam review text'` still exits 0, so existing static parity coverage does not catch the runtime heading/feedback-label regression. Manager action: assign a source-touching language atom so `QuestionCard` renders the selected language as the primary question text and answer feedback labels localize (`Wrong`, `Correct`, `Correct answer`) in English mode; add runtime/e2e coverage rather than accepting static parity-only tests.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:27 CEST][P0 TRANSLATE-COMPLETE]: Focused mock-exam English-support pass found another runtime translation gap. After selecting English support, `/exam` rendered English question text and `UHR reference`, but the result `Chapter breakdown` still listed Swedish chapter names: `Landet Sverige`, `Sveriges demokratiska system`, `Så här styrs Sverige`, `Politiska val och partier`, `Lag och rätt`, `Mediernas roll`, `Mänskliga rättigheter`, `Arbetsmarknad och privatekonomi`, `Välfärdssamhället`, `Sveriges moderna historia`, `Sverige och omvärlden`, `En sekulär stat och ett mångreligiöst land`, and `Traditioner och högtider`. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; inline system-Chrome `/settings` -> English support -> `/exam` -> submit all 20 answers exited 1 with `settingsEnglishSelected:true`, `englishQuestionVisible:true`, `englishReferenceVisible:true`, `swedishReferenceVisible:false`, `hasSwedishChapterName:true`, `hasEnglishChapterName:false`, and console/page errors 0. Code inspection confirms `app/(tabs)/exam.tsx` renders `chapter.chapterNameSv` in the chapter breakdown regardless of selected language. Manager action: keep P0 TRANSLATE-COMPLETE open; assign a source-touching exam review localization atom to render `chapterNameEn` when language is English and add runtime/e2e coverage for mock-exam result localization.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:27 CEST][P0 TRANSLATE-COMPLETE]: Focused Swedish-mode mock-exam pass found the opposite localization gap. With `Svenska` selected, `/exam` correctly rendered Swedish question content (`Var ligger Sverige?`, Swedish options), but the screen shell stayed English: `Mock exam`, `Time left`, `UHR-based questions`, `no ads during exam`, `Progress`, `answered`, and `Submit exam`. Evidence: reused the freshly rebuilt `dist-web`; inline system-Chrome `/settings` -> Swedish -> `/exam` exited 1 with `swedishSelected:true`, `englishShell:["Mock exam","Time left","UHR-based questions","no ads during exam","Progress","answered","Submit exam"]`, `swedishQuestionContent:["Var ligger Sverige?","I Norden i norra Europa","I södra Europa"]`, and console/page errors 0. Manager action: the source-touching language atom must localize the mock-exam chrome/result copy for both `sv` and `en`; do not close TRANSLATE-COMPLETE by fixing only question fields.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:33 CEST][P0 TRANSLATE-COMPLETE]: Focused English-mode learning path pass found another runtime translation gap. After selecting English support, `/learn` rendered English route copy (`Browse chapters with a clear next step`, `13 civic areas`) but the chapter cards still displayed Swedish primary chapter names/descriptions and accessibility labels. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; inline system-Chrome `/settings` -> English support -> `/learn` -> `/chapter/ch01` exited 1 with `settingsEnglishSelected:true`, `learn.hasEnglishRouteCopy:true`, `learn.hasEnglishTitle:true`, `learn.hasSwedishTitle:true`, `learn.hasEnglishDescription:false`, `learn.hasSwedishDescription:true`, `firstChapterLinkName:"Open chapter Landet Sverige. English name: The country of Sweden. Progress: 0 of 50 questions practiced."`, `chapterDetail.hasEnglishTitle:true`, `chapterDetail.hasEnglishDescription:true`, and console/page errors 0. Code inspection confirms `components/learning/ChapterCard.tsx` always renders `chapter.nameSv` and `chapter.descriptionSv` even when `language="en"`. Manager action: extend the source-touching TRANSLATE-COMPLETE atom to localize `ChapterCard` visible text and accessibility labels (`nameEn`/`descriptionEn` in English mode) and add runtime/e2e coverage for `/learn`, not just quiz/exam surfaces.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:35 CEST][P0 TRANSLATE-COMPLETE]: Focused question-screen audio-control pass found a hardcoded English user-facing control in the Swedish practice route. Evidence: reused the freshly rebuilt `dist-web`; inline system-Chrome `/practice` smoke exited 1 with `hasSwedishQuestion:true`, `hasEnglishQuestion:true` from the bilingual question card, `hasEnglishListenText:true`, `englishAudioButtonCount:1`, `swedishAudioButtonCount:0`, and console/page errors 0. Code inspection confirms `components/learning/AudioButton.tsx` hardcodes visible labels and accessibility hints as `Listen`, `Audio disabled`, `Audio unavailable`, and English help text, while `app/(tabs)/practice.tsx` and `app/quiz/[sessionId].tsx` render it without passing language. Manager action: extend the source-touching TRANSLATE-COMPLETE atom to localize `AudioButton` visible labels/accessibility labels/hints for `sv` and `en`, pass selected language from Practice and routed quiz, and add runtime/e2e coverage for the audio control.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:37 CEST][P0 TRANSLATE-COMPLETE]: Focused Swedish Home monetization pass found English/debug ad-card copy on an otherwise Swedish screen. Evidence: reused `dist-web`; inline system-Chrome `/home` smoke exited 1 with `swedishHomeVisible:true`, `hasAdMobCard:true`, `hasEnglishPlacement:true`, `hasEnglishAdStatus:true`, `hasSwedishRemoveAdsCopy:true`, and console/page errors 0. The rendered body included localized Remove Ads copy but the ad card still showed `Home Banner`, `AdMob test unit active`, `web preview`, and `Sponsored ad preview`. Code inspection confirms `components/monetization/AdBanner.tsx` and `components/monetization/NativeAdCard.tsx` hardcode English visible/accessibility copy and derive placement labels from internal ids. Manager action: extend TRANSLATE-COMPLETE to localize ad placeholder cards and accessibility hints for both languages, map placement ids to human-readable localized labels, and add runtime coverage for Swedish `/home` and `/mistakes` ad surfaces.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:41 CEST][P0 TRANSLATE-COMPLETE][a11y]: Focused Swedish-mode practice pass found the visible question content localized but the `QuestionCard` screen-reader label still uses English prefixes. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; inline system-Chrome `/settings` -> Swedish -> `/practice` exited 1 with `swedishQuestionVisible:1`, `questionCardLabels:["Difficulty: easy. Question: Var ligger Sverige?. English translation: Where is Sweden located?. Source citation: Källa/Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, s. 5"]`, `englishA11yPrefixes` containing that label, and console/page errors 0. Code inspection confirms `components/quiz/QuestionCard.tsx` sets the Swedish copy to `Question`, `English translation`, and `Source citation`. Manager action: localize `QuestionCard` accessibility-label prefixes for Swedish mode, keep English mode natural, and add runtime/a11y coverage that reads the generated `aria-label` rather than only checking visible question text.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:45 CEST][P0 TRANSLATE-COMPLETE][ads]: Focused Swedish-mode Mistakes pass found the native ad placeholder still exposes English/internal ad copy on a Swedish route. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; inline system-Chrome `/settings` -> Swedish -> `/mistakes` exited 1 after dismissing `Stäng startannons`; rendered Swedish route content (`Misstag`, `Gå igenom fel svar...`, `Inga misstag ännu`) but the ad card still showed `TEST NATIVE AD`, `Sponsored study placement`, `AdMob test placement preview. Keep out of timed exams.`, and an English `aria-label` value `Test native ad: Sponsored study placement. AdMob test placement preview. Keep out of timed exams. Hidden after Remove Ads is active.`; console/page errors 0. Code inspection confirms `components/monetization/NativeAdCard.tsx` hardcodes English visible/accessibility copy and `components/monetization/AdBanner.tsx` does the same for placement labels/status. Manager action: keep TRANSLATE-COMPLETE open for localized ad placeholders on `/home` and `/mistakes`, including visible copy, placement labels, and accessibility labels/hints; add runtime coverage for Swedish ad surfaces before accepting this slice.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 00:51 CEST][P0 TRANSLATE-COMPLETE][a11y]: Focused Swedish-mode progress-bar accessibility pass found the shared `ProgressBar` still exposes English-only labels on Swedish routes. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; inline system-Chrome `/settings` -> Swedish -> `/home`, `/practice`, and `/quiz/q001` exited 1 with progressbar aria labels `["0 percent complete"]` on all three routes, `swedishProgressLabels:[]`, and console/page errors 0. Code inspection confirms `components/ui/ProgressBar.tsx` hardcodes ``${progressPercent} percent complete``; `scripts/ui-effects.test.js` and `scripts/validate-content.js` currently assert that English pattern, so static coverage enforces the gap instead of catching it. Manager action: keep P0 TRANSLATE-COMPLETE open; assign a source-touching shared UI/a11y atom to localize `ProgressBar` accessibility labels for Swedish and English contexts, update the static gates to reject English-only Swedish labels, and add runtime/a11y coverage on routes that use progress bars.
REVIEWER-LANGUAGE-SUPPORT-1 update [2026-05-18 01:00 CEST][P0 TRANSLATE-COMPLETE][exam-a11y]: Focused Swedish-mode mock-exam answer/review pass found the exam flow still exposes English-only accessible names and review labels after the latest accepted question/audio/ad localization atoms. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; inline system-Chrome `/settings` -> Swedish -> `/exam` -> answer 20 questions -> submit exited 1 with answer button aria-label samples `Select answer I västra Asien for question 1`, `Select answer Företagsstyre for question 2`, `Select answer Domstolarna väljer alla riksdagsledamöter for question 3`; `answerButtonCount:74`, `answeredQuestions:20`, submit aria-label `Submit mock exam`, visible `Submit exam`, result labels `Selected answer` 20 and `Correct answer` 20, `Question review` and `Chapter breakdown` visible, `swedishReviewLabels:0`, and console/page errors 0. Code inspection confirms `app/(tabs)/exam.tsx` hardcodes answer-button `accessibilityLabel`, submit/review/result strings, and review answer labels. Manager action: keep P0 TRANSLATE-COMPLETE open; assign a source-touching mock-exam localization atom covering visible copy and accessibility labels for active exam and submitted review states, plus runtime/e2e coverage that inspects Swedish aria labels, submit copy, chapter breakdown, question review, selected-answer, and correct-answer labels.
REVIEWER-LANGUAGE-SUPPORT-1 resolution check [2026-05-18 01:05 CEST][P0 TRANSLATE-COMPLETE][progress-a11y]: Rechecked SETUP Iteration 157's ProgressBar localization handoff. The previously queued progress-bar accessibility subdefect is cleared in this checkout: `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-progress-bar-accessibility-parity.test.js` exits 0 with 3/3; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web pass over `/settings` -> Swedish and English, then `/home`, `/practice`, `/quiz/q001`, and `/learn`, exits 0. Swedish progressbars expose `0 procent klart` in both `aria-label` and `aria-valuetext`; English mode exposes `0 percent complete`; no opposite-language progress labels or console/page errors were found. Manager action: VALIDATOR can close only the ProgressBar subdefect after accepting the bounded SETUP Iteration 157 source atom; keep broader TRANSLATE-COMPLETE open for the still-queued mock-exam localization gaps.
REVIEWER-LANGUAGE-SUPPORT-1 resolution check [2026-05-18 01:10 CEST][P0 TRANSLATE-COMPLETE][exam]: Rechecked the current mock-exam localization surface after the dirty `app/(tabs)/exam.tsx` and E2E updates. The previously queued mock-exam visible/a11y subdefect is cleared in runtime for both Swedish and English: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0, and the system-Chrome exported-web pass exits 0 over `/settings` -> Swedish `/exam` -> answer 20 -> submit and `/settings` -> English support `/exam` -> answer 20 -> submit. Swedish showed `Övningsprov`, `Välj svaret ... för fråga N`, `Skicka övningsprov`, `Provresultat`, `Kapitelöversikt`, `Frågegenomgång`, `Valt svar` 20, `Rätt svar` 20, `Förklaring` 20, `UHR-källa` 20, and Swedish chapter names in the breakdown; English showed `Mock exam`, `Select answer ... for question N`, `Submit mock exam`, `Exam result`, `Chapter breakdown`, `Question review`, `Selected answer` 20, `Correct answer` 20, `Explanation` 20, `UHR reference` 20, and English chapter names in the breakdown. Opposite-language shell/review labels were absent and console/page errors were 0. Manager action: VALIDATOR can review the bounded mock-exam localization atom for acceptance, but keep TRANSLATE-COMPLETE open for any other queued surfaces not covered by this pass.
REVIEWER-EXAM-E2E-LOCATOR-1 [product/test defect][2026-05-18 01:10 CEST]: The official focused mock-exam E2E gate is still red after the product runtime check passes. Evidence: `CI=1 NODE_OPTIONS='--v8-pool-size=1' timeout 180s npm run test:e2e -- tests/e2e/exam-submit-review.spec.ts --workers=1` exits 1 at `await expect(page.getByText('Förklaring')).toHaveCount(0)`, with Playwright reporting one pre-submit match. The runtime pass using exact text checks found `exactExplanationBeforeSubmit:0` in both Swedish and English before submit and 20 explanation panels after submit, so the failure is a verifier locator problem rather than visible pre-submit explanations. Manager action: assign a source-touching E2E/test atom to scope the pre-submit no-explanation assertion to exact `Förklaring` panel text or the submitted-review region, and keep the English-mode runtime coverage gap in mind because the current official spec only exercises Swedish.
REVIEWER-ROUTE-HEADER-1 [product defect][2026-05-18 01:17 CEST][P0 TRANSLATE-COMPLETE][route-shell]: Focused exported-web profile/settings/support pass found standalone routes still render Expo Router default route headers above the localized page content. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome pass on `http://127.0.0.1:4191/settings` at 320x568 exits 1 with visible headings `settings`, `Inställningar`, `Frågespråk`, `Ljud`, `Dagligt mål`, and `Juridik och källor`; after switching to English, headings include both `settings` and `Settings`; `/support` similarly renders a visible default `support` header above `Support och återkoppling`; browser console/page errors 0. Code inspection shows `app/_layout.tsx` only sets `headerShown:false` for `index`, `(tabs)`, and `+not-found`, so standalone screens inherit lower-case route-name headers while static parity checks only inspect in-page copy. Manager action: assign a source-touching route-shell atom to hide or localize standalone Stack headers for settings/legal/support/onboarding-style routes, then add runtime/e2e coverage that rejects lower-case internal route headers and duplicate page headings in Swedish and English.
REVIEWER-TAB-NAV-GLYPH-1 [product defect][2026-05-18 01:27 CEST][UI-OVERHAUL][a11y]: Focused exported-web Home tab-navigation pass found the bottom tab bar renders repeated visible `⏷` glyphs as tab icon content, and those glyphs are included in every tab link's text. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome pass on `http://127.0.0.1:4192/home` at 390x844 exits 1 with 24 visible chevron nodes at the bottom nav row (`y:801`) and bottom links whose text is `⏷⏷Hem`, `⏷⏷Lär dig`, `⏷⏷Öva`, `⏷⏷Prov`, `⏷⏷Misstag`, and `⏷⏷Profil`; `document.body.innerText` tail shows the repeated `⏷` lines immediately before tab labels; browser console/page errors 0. Code inspection shows `app/(tabs)/_layout.tsx` defines Expo `Tabs.Screen` titles but no real `tabBarIcon` or accessible replacement, so the exported app looks unfinished and exposes decorative glyphs as navigational text. Manager action: assign a source-touching navigation/UI atom in `app/(tabs)/_layout.tsx` (or a shared tab-icon helper) to provide meaningful localized tab icons or suppress placeholder glyph output, set accessible names to the plain localized tab labels, and add runtime/static coverage rejecting `⏷` in tab link text.
REVIEWER-ACTIVE-QUESTION-LAUNCH-AD-1 [product defect][2026-05-18 01:28 CEST][MONETIZATION-OPT][functional]: Focused exported-web active-question monetization pass found app-open launch ads still render over answerable question screens. Evidence: current export built with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome route pass on `http://127.0.0.1:4192/practice`, `/quiz/q001`, and `/exam` exits 1 because `/practice` shows the active question `Var ligger Sverige?` plus a `role="dialog"` `aria-label="Startannons"` containing `Google AdMob`, `Testannons för appstart visas en gång per appstart.`, and `Fortsätt studera`; `/quiz/q001` shows the same launch ad dialog over the routed question; `/exam` correctly has no launch/ad overlay. Browser console/page errors 0. Code inspection shows `lib/monetization/ads.ts` suppresses launch popup only for `/exam`, legal/support/source routes, not `/practice` or `/quiz/*`, despite MONETIZATION-OPT requiring ads only at natural breakpoints and never on an active exam/question screen. Manager action: assign a source-touching monetization routing atom to add `/practice` and `/quiz` question routes to launch-ad suppression (or otherwise gate app-open ads away from active question screens), then add runtime/static coverage proving `/practice`, `/quiz/q001`, and `/exam` are ad-free while home/result breakpoints still show ads for free users.
REVIEWER-ANSWER-OPTION-A11Y-LANGUAGE-1 [product/test defect][2026-05-18 01:31 CEST][P0 TRANSLATE-COMPLETE][a11y]: Focused exported-web answer-option accessibility pass found Swedish Practice and routed Quiz answer buttons still expose English accessible names. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome pass on `http://127.0.0.1:4193` sets Swedish and English in `/settings`, then inspects `/practice` and `/quiz/q001`. Swedish visible content is localized (`Fråga 1`, `Var ligger Sverige?`, Swedish option text), but answer button `aria-label`s are `Select answer I södra Europa`, `Select answer I västra Asien`, `Select answer I Norden i norra Europa`, `Select answer I Nordamerika` on `/practice`, and `Select answer ...` labels on `/quiz/q001`; there are zero `Välj svaret...` labels. English mode correctly uses `Select answer ...`. Browser console/page errors 0. The focused static gate `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-answer-option-accessibility-parity.test.js` still passes 2/2, and code inspection shows `components/quiz/AnswerOption.tsx` hardcodes ``Select answer ${label}`` while `scripts/validate-content.js` asserts that exact English pattern. Manager action: assign a source-touching shared quiz a11y atom to localize `AnswerOption` idle accessibility labels by selected language (`Välj svaret ...` for Swedish, `Select answer ...` for English), update `tests/content-answer-option-accessibility-parity.test.js` and validation to reject English-only Swedish labels, and add runtime/e2e coverage for `/practice` and `/quiz/q001`.
REVIEWER-QUESTION-DIFFICULTY-LANGUAGE-1 [product/test defect][2026-05-18 01:33 CEST][P0 TRANSLATE-COMPLETE][a11y]: Focused exported-web QuestionCard language pass found Swedish Practice and routed Quiz still expose raw English difficulty enum values. Evidence: current `dist-web` from `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2`; system-Chrome pass on `http://127.0.0.1:4193` sets Swedish and English in `/settings`, then inspects `/practice` and `/quiz/q001`. Swedish mode shows visible `EASY` above `Var ligger Sverige?`, has no visible Swedish difficulty label such as `Lätt`, and the QuestionCard aria label is `Svårighetsgrad: easy. Fråga: Var ligger Sverige?...`; routed `/quiz/q001` shows the same. English mode correctly shows `EASY` and `Difficulty: easy` for comparison. Browser console/page errors 0. The focused static gate `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-question-card-accessibility-parity.test.js` passes 3/3 while code inspection shows `components/quiz/QuestionCard.tsx` renders `const difficulty = question?.difficulty ?? 'practice'` directly and `scripts/validate-content.js` asserts `<Text style={styles.label}>{difficulty}</Text>`. Manager action: assign a source-touching QuestionCard localization atom to map difficulty values to localized display and accessibility text (`Lätt`/`Medel`/`Svår` for Swedish, `Easy`/`Medium`/`Hard` for English), update static validation/tests to reject raw enum display, and add runtime coverage for `/practice` and `/quiz/q001`.
REVIEWER-QUESTION-SOURCE-CITATION-LANGUAGE-1 [product defect][2026-05-18 01:34 CEST][P0 TRANSLATE-COMPLETE][a11y]: Focused exported-web QuestionCard source-citation language pass found English mode still exposes a mixed Swedish/English citation prefix. Evidence: current `dist-web` from the 01:30 export; system-Chrome pass on `http://127.0.0.1:4193` sets Swedish and English in `/settings`, then inspects `/practice`. English visible content is localized (`Practice`, `Question 1`, `Where is Sweden located?`, English option text), but the QuestionCard source line is `Källa/Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, s. 5`, and the aria label contains `Source citation: Källa/Source: ...`; browser console/page errors 0. Code inspection shows `lib/quiz/questionText.ts` hardcodes `getQuestionSourceCitation()` to `Källa/Source: ... s. ${pageApprox}` without selected language, while `UHRReferenceCard` already has localized `UHR-källa` / `UHR reference` copy after answer feedback. Manager action: assign a source-touching QuestionCard/questionText localization atom so citation prefixes and page abbreviations follow selected language (`Källa: ... s. N` in Swedish, `Source: ... p. N` or equivalent in English), and add runtime/static coverage that distinguishes this from the earlier source-stem cleanup requirement.
REVIEWER-MISTAKES-ANSWER-REVIEW-1 [product defect][2026-05-18 01:39 CEST][learning-loop][functional]: Focused exported-web wrong-answer-to-Mistakes pass found the saved Mistakes review card drops the actual answer context. Evidence: rebuilt current export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass on `http://127.0.0.1:4194` set English support, dismissed known launch-ad overlays, answered `/practice` q001 incorrectly with `In southern Europe`, then opened `/mistakes`. Practice correctly showed `In southern Europe — Wrong`, `In the Nordic region in northern Europe — Correct answer`, and the English explanation. `/mistakes` showed `Mistakes`, `Wrong answers: 1`, `Where is Sweden located?`, the English explanation, and `UHR reference`, but had no `Correct answer` label, no correct option text, no selected wrong option text, and no answer-option buttons; relevant aria labels only covered the QuestionCard and UHR reference. Browser console/page errors 0. Static scan shows existing mistakes tests cover shell copy, headers, ads, and disclaimer, but not answer review context. Manager action: assign a source-touching Mistakes review atom in `app/(tabs)/mistakes.tsx` to show the selected wrong answer when available and always show the correct answer for saved wrong-answer cards, with localized labels and runtime/static coverage for Swedish and English mistake reviews.
REVIEWER-ADS-IAP-1 resolution check [2026-05-18 01:50 CEST][P0 adsDisabled route suppression]: Focused exported-web Remove Ads pass cleared the paid-user route-suppression portion of this older finding. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass on `http://127.0.0.1:4195` started from a clean web context, confirmed free `/home` rendered one `Google AdMob: Annons på startsidan...` ad label, bought `Köp Ta bort annonser för 29 SEK`, observed `localStorage['monetization.removeAds.adsDisabled.v1']="true"`, then visited `/home`, `/learn`, `/mistakes`, `/practice`, `/quiz/q001`, and `/exam`. All checked routes had `dialogCount:0`, ad label count 0, no `Google AdMob`/native-ad/start-ad text matches, and console/page errors 0. Manager action: close only the adsDisabled route-suppression subcheck after accepting the bounded purchase/ad source atoms; keep any native device, real-unit, consent, and store-account gates separate.
REVIEWER-ANSWER-OPTION-A11Y-LANGUAGE-1 resolution check [2026-05-18 01:50 CEST][P0 TRANSLATE-COMPLETE][a11y]: Rechecked current exported web after the shared AnswerOption localization changes. The previously queued Swedish answer-option accessible-name defect is cleared at runtime for Practice and routed Quiz. Evidence: system-Chrome pass on `http://127.0.0.1:4195` set Swedish and English through `/settings`, then inspected `/practice` and `/quiz/q001`; Swedish answer labels were all `Välj svaret ...` with zero `Select answer ...` labels, English answer labels were all `Select answer ...` with zero `Välj svaret ...` labels, and console/page errors 0. Manager action: VALIDATOR can close this reviewer defect once the bounded SETUP/UI source and parity-test atom is accepted; keep unrelated QuestionCard source-citation and Mistakes-review defects open.
REVIEWER-QUESTION-DIFFICULTY-LANGUAGE-1 resolution check [2026-05-18 01:50 CEST][P0 TRANSLATE-COMPLETE][a11y]: Rechecked current exported web after the QuestionCard difficulty localization changes. The previously queued raw `easy`/`EASY` difficulty defect is cleared at runtime for Practice and routed Quiz. Evidence: same system-Chrome pass on `http://127.0.0.1:4195` found Swedish `/practice` and `/quiz/q001` show visible `LÄTT` and QuestionCard labels starting `Svårighetsgrad: Lätt...`, with no raw `EASY` body text and no `Svårighetsgrad: easy` aria text; English mode shows `EASY` with `Difficulty: Easy...` as expected. Console/page errors 0. Manager action: VALIDATOR can close this reviewer defect once the bounded QuestionCard source and parity-test atom is accepted; this does not close the still-mixed `Källa/Source:` source-citation prefix defect.
REVIEWER-ROUTE-HEADER-1 resolution check [2026-05-18 01:50 CEST][P0 TRANSLATE-COMPLETE][route-shell]: Rechecked current exported web after the root Stack header change. The lower-case internal standalone-route header defect is cleared at runtime. Evidence: system-Chrome pass on `http://127.0.0.1:4195` at 320x568 set Swedish and English, then inspected headings on `/settings`, `/support`, `/privacy`, `/terms`, `/sources`, `/disclaimer`, and `/onboarding`; no visible/internal route headings matching `settings`, `support`, `privacy`, `terms`, `sources`, `disclaimer`, or `onboarding` were present, localized page headings rendered in both languages, and console/page errors 0. Manager action: VALIDATOR can close this reviewer defect once the bounded route-shell atom is accepted; tab glyph, active-question launch ad, source-citation prefix, and Mistakes answer-review defects remain separate.
REVIEWER-SETTINGS-MOBILE-SCROLL-1 resolution check [2026-05-18 01:50 CEST][mobile layout]: Rechecked current exported web at 320x568. The previously queued settings overflow/no-scroll defect is cleared in this checkout. Evidence: system-Chrome pass on `http://127.0.0.1:4195/settings` dismissed the launch modal, measured `scrollHeight:568`, `innerHeight:568`, and confirmed the bottom `Support` compliance link was reachable at `y:455` with console/page errors 0. Manager action: close this reviewer defect once the bounded settings layout atom is accepted.
REVIEWER-ONBOARDING-MOBILE-SCROLL-1 resolution check [2026-05-18 01:50 CEST][mobile layout]: Rechecked current exported web at 320x568. The previously queued onboarding overflow/no-scroll defect is cleared in this checkout. Evidence: system-Chrome pass on `http://127.0.0.1:4195/onboarding` dismissed the launch modal, measured `scrollHeight:568`, `innerHeight:568`, and confirmed `Börja studera` at `y:410` plus `Justera inställningar` at `y:455` were reachable with console/page errors 0. Manager action: close this reviewer defect once the bounded onboarding layout atom is accepted.
REVIEWER-ADS-IAP-1 resolution check [2026-05-18 02:00 CEST][profile paywall web flow]: Focused exported-web profile pass cleared the current web paywall/profile portion of the older ADS/IAP finding. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass through repo `tests/e2e/serve-dist-web.cjs` on port 4198 started from cleared localStorage, opened `/profile`, found Remove Ads/Ta bort annonser copy, `29 SEK`, one buy button, one restore button, and the idle one-time-purchase/restore status. Clicking Restore before purchase showed the localized not-found status; clicking Buy set `localStorage['monetization.removeAds.adsDisabled.v1']="true"`, showed the ad-free active state, disabled the buy button, persisted after returning to `/profile`, and left `/home`, `/learn`, `/mistakes`, `/practice`, `/quiz/q001`, and `/exam` with `dialogCount:0`, `googleAdMobCount:0`, ad-preview text count 0, console errors 0, and page errors 0. Manager action: close only the web profile/paywall subcheck after accepting the bounded purchase/paywall source atoms; keep native store product/device restore QA, real AdMob IDs, consent prompts, and store-account gates separate.
REVIEWER-TAB-NAV-GLYPH-1 resolution check [2026-05-18 02:06 CEST][UI-OVERHAUL][a11y]: Rechecked current exported web after the tab-icon suppression change. The previously queued placeholder-glyph defect is cleared in this checkout. Evidence: `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass via `tests/e2e/serve-dist-web.cjs` on port 4201 at 390x844 inspected bottom-tab links on `/home` and found exactly `Hem`, `Lär dig`, `Öva`, `Prov`, `Misstag`, and `Profil`, with matching plain `aria-label` values and `chevronTabTexts:[]`; console/page errors 0. Manager action: VALIDATOR can close this reviewer defect after accepting the bounded tab-navigation source/test atom; keep unrelated Mistakes answer-review and any native visual tab-icon polish separate.
REVIEWER-ACTIVE-QUESTION-LAUNCH-AD-1 resolution check [2026-05-18 02:06 CEST][MONETIZATION-OPT][functional]: Rechecked current exported web after the launch-ad route suppression change. The active-question launch overlay defect is cleared for the checked web routes while passive `/home` launch eligibility is preserved. Evidence: same export and system-Chrome pass on port 4201 opened fresh contexts for `/home`, `/practice`, `/quiz/q001`, and `/exam`; `/home` still showed one launch dialog with `Startannons` and `GOOGLE ADMOB`, while `/practice`, `/quiz/q001`, and `/exam` each had `dialogCount:0`, `startAdTextCount:0`, `googleAdMobCount:0`, expected route content rendered, and console/page errors 0. Manager action: VALIDATOR can close this reviewer defect after accepting the bounded monetization route-suppression atom; keep paid-user suppression, native device behavior, real ad units, consent, and store-account gates separate.
REVIEWER-MISTAKES-ANSWER-REVIEW-1 update [2026-05-18 02:06 CEST][learning-loop][functional]: Rechecked the current exported-web wrong-answer-to-Mistakes flow and the defect remains open. Evidence: system-Chrome pass on port 4201 set English support, answered `/practice` q001 incorrectly with `In southern Europe`, and confirmed Practice showed both wrong feedback and `Correct answer`; `/mistakes` then showed `Mistakes`, `Wrong answers: 1`, `Where is Sweden located?`, the English explanation, and `UHR reference`, but still had `hasCorrectAnswerLabel:false`, `hasCorrectOptionText:false`, `hasSelectedWrongOptionText:false`, and no answer-review buttons beyond the launch-ad dismiss button. Console/page errors 0. Manager action: assign or keep the source-touching Mistakes review atom open in `app/(tabs)/mistakes.tsx` so saved wrong-answer cards show the selected wrong answer and correct answer with localized labels and runtime/static coverage.
REVIEWER-MISTAKES-ANSWER-REVIEW-1 update [2026-05-18 02:11 CEST][learning-loop][functional]: Rechecked after new `app/(tabs)/mistakes.tsx` and `lib/storage/mistakeReviewStore.ts` dirty state appeared. The product defect remains open because the store records the selected wrong answer from Practice, but the Mistakes route still does not read or render that review context. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass on port 4202 set English support, answered `/practice` q001 incorrectly with `In southern Europe`, confirmed Practice showed `In southern Europe — Wrong` and `In the Nordic region in northern Europe — Correct answer`, then opened `/mistakes`. `/mistakes` showed `Mistakes`, `Wrong answers: 1`, `Where is Sweden located?`, the English explanation, and `UHR reference`, but returned `hasCorrectAnswerLabel:false`, `hasCorrectOptionText:false`, `hasSelectedWrongLabel:false`, `hasSelectedWrongOptionText:false`, and `hasAnswerButtons:0`; console/page errors 0. Manager action: keep this assigned to a source owner until `app/(tabs)/mistakes.tsx` renders the persisted selected wrong answer plus the correct answer with localized labels and coverage; do not close on store-only wiring.
REVIEWER-MISTAKES-ANSWER-REVIEW-1 resolution check [2026-05-18 02:16 CEST][learning-loop][functional]: Rechecked after SETUP Iteration 168. The visible saved Mistakes answer-review defect is cleared for the checked web flow in both Swedish and English. Evidence: rebuilt export with `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exit 0; system-Chrome pass via `tests/e2e/serve-dist-web.cjs` on port 4204 set Swedish, answered `/practice` q001 incorrectly with `I södra Europa`, then opened `/mistakes` and found `Fel svar att repetera`, `Ditt senaste felaktiga svar`, `I södra Europa`, `Rätt svar`, `I Norden i norra Europa`, and aria label `Svar att repetera. Ditt senaste felaktiga svar: I södra Europa. Rätt svar: I Norden i norra Europa.`; the same pass set English, answered with `In southern Europe`, then found `Wrong answers to revisit`, `Your latest wrong answer`, `In southern Europe`, `Correct answer`, `In the Nordic region in northern Europe`, and aria label `Answers to review. Your latest wrong answer: In southern Europe. Correct answer: In the Nordic region in northern Europe.` Browser console/page errors 0. Manager action: VALIDATOR can close this reviewer defect after accepting the bounded SETUP source/test atom; keep broader dirty-checkout, native persistence, and ad-placement questions separate.
REVIEWER-Q012-EN-NATURALNESS-1 [product defect][2026-05-18 02:38 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q012 content/runtime pass found the English explanation is user-visible but stilted: "The Democracy means rule by the people section says..." This reads like a literal section-title insertion, not natural learner-facing English, while `validate:content` still passes, so the current gates do not enforce the English naturalness bar for this case. Evidence: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js` exits 0 with 3/3 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `sed -n '300,345p' data/questions.ts` shows q012 `explanationEn` beginning with the awkward phrase; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0; system-Chrome exported-web pass on `/quiz/q012` in English support mode exits 1 by design after confirming the question, source citation, disclaimer, correct-answer feedback, and the awkward explanation are all visible with console/page errors 0. Manager action: assign a CONTENT-owned q012 naturalness atom to rewrite the English explanation idiomatically, preserving the UHR page 10 citation and the free-elections facts; add or extend a naturalness/static guard if feasible, because the current validation suite stayed green.
REVIEWER-Q013-EN-NATURALNESS-1 [product defect][2026-05-18 02:44 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q013 content/runtime pass found the English explanation is user-visible but ungrammatical: "The A strong democracy section lists..." This reads like a section-title splice, not natural learner-facing English, while the current gates still pass. Evidence: `sed -n '300,435p' data/questions.ts` shows q013 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js` exits 0 with 3/3 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q013` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, correct-answer feedback, localized source citation, not-official disclaimer, and the awkward explanation are all visible with console/page errors 0. Manager action: assign a CONTENT-owned q013 naturalness atom to rewrite the English explanation idiomatically, preserving the UHR page 10 `En stark demokrati` citation and the civic-participation facts; extend naturalness/static review if feasible, because current bilingual/source/export validation stays green.
REVIEWER-Q014-EN-NATURALNESS-1 [product defect][2026-05-18 02:49 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q014 content/runtime pass found the English explanation is user-visible but stilted: "The Democracy means rule by the people section says..." This is the same section-title splice pattern as q012/q013 and does not meet the natural, idiomatic English bar even though the fact, answer, and citation are otherwise sound. Evidence: `sed -n '405,475p' data/questions.ts` shows q014 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js` exits 0 with 3/3 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q014` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, source citation `Source: Sverige i fokus, Sveriges demokratiska system, Demokrati betyder folkstyre, p. 10`, correct-answer feedback for `Legal certainty`, the not-official disclaimer, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q014 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 10 legal-certainty facts; current validation stays green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q015-EN-NATURALNESS-1 [product defect][2026-05-18 02:55 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q015 content/runtime pass found the English explanation is user-visible but stilted: "The Threats to democracy section says..." This repeats the section-title splice pattern from q012-q014 and reads like source-note scaffolding rather than natural learner-facing English, while the current gates still pass. Evidence: `sed -n '420,510p' data/questions.ts` shows q015 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js` exits 0 with 3/3 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q015` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, source citation `Source: Sverige i fokus, Sveriges demokratiska system, Hot mot demokratin, p. 11`, correct-answer feedback for `People may have fewer opportunities to influence political decisions`, the independent/not-official disclaimer, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q015 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 11 voter-turnout facts; current validation stays green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q016-EN-NATURALNESS-1 [product defect][2026-05-18 02:59 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q016 content/runtime pass found the English explanation is user-visible but still written as source-note scaffolding: "The State section says..." It is understandable, but it does not meet the idiomatic learner-facing English bar now being applied to q012-q015. Evidence: `sed -n '470,545p' data/questions.ts` shows q016 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q016` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Staten, p. 12`, independent-study disclaimer, and the exact awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q016 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 parliamentary-democracy facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q017-EN-NATURALNESS-1 [product defect][2026-05-18 03:03 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q017 content/runtime pass found the English explanation is user-visible but awkward and source-note-like: "The UHR section The state says..." This is not idiomatic learner-facing English and repeats the section-title splice pattern from q012-q016 while validation remains green. Evidence: `sed -n '500,585p' data/questions.ts` shows q017 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q017` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Staten, p. 12`, independent-study disclaimer, correct-answer feedback for `349`, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q017 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 Riksdag-member facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q020-EN-NATURALNESS-1 [product defect][2026-05-18 03:09 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q020 content/runtime pass found the English explanation is user-visible but still written as source-note scaffolding: "The Referendums section says..." This repeats the section-title splice pattern from q012-q017 and is not natural learner-facing English, while the current validators remain green. Evidence: `sed -n '585,690p' data/questions.ts` shows q020 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q020` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, source citation `Source: Sverige i fokus, Politiska val och partier, Folkomröstningar, p. 14`, independent-study disclaimer, correct-answer feedback for `Politicians do not have to follow the result`, and the exact awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q020 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 14 referendum facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q018-EN-NATURALNESS-1 [product defect][2026-05-18 03:13 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q018 content/runtime pass found the English explanation is user-visible but still written as source-note scaffolding: "The State section says..." This repeats the section-title splice pattern from q016/q017 and does not meet the idiomatic learner-facing English bar even though the answer, citation, and facts are otherwise sound. Evidence: `sed -n '500,650p' data/questions.ts` shows q018 `explanationEn` as `The State section says the Riksdag chooses the prime minister, who is given the task of forming a government. The prime minister then chooses the government ministers.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q018` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Staten, p. 12`, independent-study disclaimer, correct-answer feedback for `The Riksdag`, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q018 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 prime-minister-selection facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q019-EN-NATURALNESS-1 [product defect][2026-05-18 03:15 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q019 content/runtime pass found the English explanation is user-visible but still written as source-note scaffolding: "The Elections and voting section says..." This repeats the section-title splice pattern from q016-q020 and reads like source annotation rather than natural learner-facing English. Evidence: `sed -n '535,610p' data/questions.ts` shows q019 `explanationEn` as `The Elections and voting section says a person must have turned 18 to have the right to vote. To vote in a Riksdag election, a person must also be a Swedish citizen.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0. Reused the current `dist-web` from the 03:13 CEST web export because only queue/journal files changed afterward; system-Chrome exported-web pass on `/quiz/q019` in English support mode confirmed the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Politiska val och partier, Val och röstning, p. 14`, independent-study disclaimer, correct-answer feedback for `18 years old`, and the exact awkward explanation visible with console/page errors 0. Manager action: assign a CONTENT-owned q019 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 14 voting-age facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q022-EN-NATURALNESS-1 [product defect][2026-05-18 03:24 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q022 content/runtime pass found the English explanation is user-visible but awkward and source-note-like: "The UHR section The state says..." This repeats the section-title splice pattern already queued for q017 and does not meet the idiomatic learner-facing English bar, even though the answer, source citation, and facts are otherwise present. Evidence: `sed -n '1,140p' data/additionalQuestions.ts` shows q022 `explanationEn` as `The UHR section The state says members of the Riksdag make decisions about laws and the state budget. The same section also says the Riksdag decides how state money should be used.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q022` in English support mode exits 1 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Staten, p. 12`, independent-study disclaimer, correct-answer feedback for `To decide laws and how the state's money should be used`, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q022 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 Riksdag law-and-budget facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q023-EN-NATURALNESS-1 [product defect][2026-05-18 03:29 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q023 content/runtime pass found the English explanation is user-visible but awkward and source-note-like: "The UHR section The state says..." This repeats the q017/q022 section-title splice pattern and does not meet the idiomatic learner-facing English bar, even though the true/false answer, source citation, and facts are otherwise present. Evidence: `sed -n '85,185p' data/additionalQuestions.ts` shows q023 `explanationEn` as `The UHR section The state says the Riksdag chooses the prime minister, who is given the task of forming a government. It also says the prime minister then chooses the government ministers.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q023` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Staten, p. 12`, independent-study disclaimer, correct feedback for `True`, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q023 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 prime-minister-selection facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q024-EN-NATURALNESS-1 [product defect][2026-05-18 03:32 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q024 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Government agencies says..." This repeats the section-title splice pattern and does not meet the idiomatic learner-facing English bar, even though the answer, source citation, and facts are otherwise present. Evidence: `sed -n '85,180p' data/additionalQuestions.ts` shows q024 `explanationEn` as `The UHR section Government agencies says the government governs the country with the help of government agencies. Agencies must follow the law and the instructions they have received from the government.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q024` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Myndigheter, p. 13`, independent-study disclaimer, correct feedback for `They implement decisions and must follow laws and government instructions`, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q024 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 13 government-agencies facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q025-EN-NATURALNESS-1 [product defect][2026-05-18 03:35 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q025 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Regions and municipalities says..." It is understandable, but it reads as citation scaffolding rather than natural learner-facing English and repeats the q022-q024 pattern while the automated gates stay green. Evidence: `sed -n '120,240p' data/additionalQuestions.ts` shows q025 `explanationEn` as `The UHR section Regions and municipalities says Sweden is divided into 21 regions. The regions' foremost task is responsibility for health care, and they also ensure public transport exists and are responsible for county museums.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q025` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Regioner och kommuner, p. 13`, independent-study disclaimer, correct feedback for `To be responsible for health care`, and the awkward explanation are visible with console/page errors 0. Manager action: assign a CONTENT-owned q025 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 13 regions/health-care/public-transport/county-museum facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q026-EN-NATURALNESS-1 [product defect][2026-05-18 03:46 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q026 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Municipal responsibilities says..." This reads as citation scaffolding instead of natural learner-facing English and repeats the q022-q025 pattern while automated gates stay green. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '140,190p'` shows q026 `explanationEn` as `The UHR section Municipal responsibilities says municipalities are responsible for much of the service provided to residents. The material lists examples including water and sewage, care for elderly people and children, snow removal, park maintenance, and adult education.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q026` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Kommunernas ansvar, p. 13`, independent-study disclaimer, correct feedback for `Water and sewage, care services, snow removal, park maintenance, and adult education`, and the awkward explanation visible with console/page errors 0. Manager action: assign a CONTENT-owned q026 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 13 municipal-services facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q027-EN-NATURALNESS-1 [product defect][2026-05-18 03:53 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q027 content/runtime pass found the English explanation is user-visible but still written as source-note scaffolding: "The UHR section Sweden's form of government says..." This repeats the q022-q026 pattern and does not meet the idiomatic learner-facing English bar, even though the answer, citation, and facts are otherwise present. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '170,195p'` shows q027 `explanationEn` as `The UHR section Sweden's form of government says Sweden is a constitutional monarchy. This means the head of state is a king or queen without political power, and the material describes the king as a symbol of Sweden.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q027` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, source citation `Source: Sverige i fokus, Så här styrs Sverige, Sveriges statsskick, p. 13`, independent-study disclaimer, correct feedback for `That the head of state is a king or queen but lacks political power`, the exact awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q027 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 13 constitutional-monarchy/head-of-state facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q028-EN-NATURALNESS-1 [product defect][2026-05-18 03:57 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q028 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section The state says..." This repeats the q022-q027 section-title splice pattern and does not meet the idiomatic learner-facing English bar, even though the true/false answer, source citation, and facts are otherwise present. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '180,235p'` shows q028 `explanationEn` as `The UHR section The state says parties that do not support the government are called the opposition. The opposition’s task is to scrutinize the government’s work and propose alternative policies.`; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 500 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q028` in English support mode exited 2 by design after confirming English support mode, the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Staten, p. 12`, independent-study disclaimer, correct feedback for `True`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q028 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 opposition/government-scrutiny facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q001-EN-NATURALNESS-1 [product defect][2026-05-18 08:31 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q001 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Geography, climate, and nature says..." The answer, source citation, and disclaimer render correctly, but the explanation still reads like citation scaffolding instead of natural learner-facing English, and the existing gates stay green. Evidence: `nl -ba data/questions.ts | sed -n '1,60p'` shows q001 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 600 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0 for 600 questions; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q001` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, p. 5`, independent-study disclaimer, correct feedback for `In the Nordic region in northern Europe`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q001 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 5 Sweden-location/Nordic-region facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q002-EN-NATURALNESS-1 [product defect][2026-05-18 08:31 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q002 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Geography, climate, and nature says..." This repeats the q001 pattern and is not idiomatic learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '36,60p'` shows q002 `explanationEn` beginning with the awkward phrase; the q001 pass already proved current `validate:content`, source-citation/export/sentence-ending tests, CSV export parity, and web export are green on this checkout. System-Chrome exported-web pass on `/quiz/q002` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, p. 5`, independent-study disclaimer, correct feedback for `True`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q002 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 5 Arctic Circle fact; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q003-EN-NATURALNESS-1 [product defect][2026-05-18 08:35 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q003 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Geography, climate, and nature says..." This repeats the q001/q002 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '60,112p'` shows q003 `explanationEn` beginning with the awkward phrase; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 600 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0 for 600 questions; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0. System-Chrome exported-web pass on `/quiz/q003` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, p. 5`, independent-study disclaimer, correct feedback for `About 1,600 kilometres`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q003 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 5 Treriksröset-to-Smygehuk distance fact; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q004-EN-NATURALNESS-1 [product defect][2026-05-18 08:36 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q004 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Geography, climate, and nature says..." This repeats the q001-q003 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '87,112p'` shows q004 `explanationEn` beginning with the awkward phrase; the q003 pass already proved current `validate:content`, source-citation/export/sentence-ending tests, CSV export parity, and web export are green on this checkout. System-Chrome exported-web pass on `/quiz/q004` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, p. 5`, independent-study disclaimer, correct feedback for `The Baltic Sea`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q004 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 5 Baltic Sea/east coast fact; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q005-EN-NATURALNESS-1 [product defect][2026-05-18 08:37 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q005 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Geography, climate, and nature says..." This repeats the q001-q004 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '112,166p'` shows q005 `explanationEn` beginning with the awkward phrase; `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 0 after a fresh `/quiz/[sessionId]` source change appeared. System-Chrome exported-web pass on `/quiz/q005` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, p. 5`, independent-study disclaimer, correct feedback for `Gotland and Öland`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q005 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 5 Gotland/Öland largest-islands fact; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q006-EN-NATURALNESS-1 [product defect][2026-05-18 08:38 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q006 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Geography, climate, and nature says..." This repeats the q001-q005 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '138,166p'` shows q006 `explanationEn` beginning with the awkward phrase; the q005 pass had just rebuilt the current `/quiz/[sessionId]` artifact successfully. System-Chrome exported-web pass on `/quiz/q006` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Geografi, klimat och natur, p. 5`, independent-study disclaimer, correct feedback for `True`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q006 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 5 Gulf Stream/North Atlantic Current climate fact; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q007-EN-NATURALNESS-1 [product defect][2026-05-18 08:39 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q007 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Mountains says..." This repeats the q001-q006 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '164,224p'` shows q007 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q007` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Fjäll, p. 6`, independent-study disclaimer, correct feedback for `Kebnekaise`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q007 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 6 Kebnekaise/Scandinavian Mountains facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q008-EN-NATURALNESS-1 [product defect][2026-05-18 08:40 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q008 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Forests, lakes, and islands says..." This repeats the q001-q007 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '186,216p'` shows q008 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q008` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Skogar, sjöar och öar, p. 6`, independent-study disclaimer, correct feedback for `Vänern, Vättern, and Mälaren`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q008 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 6 Vänern/Vättern/Mälaren largest-lakes facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q009-EN-NATURALNESS-1 [product defect][2026-05-18 08:41 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q009 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Population says..." This repeats the q001-q008 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '216,246p'` shows q009 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q009` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Befolkning, p. 7`, independent-study disclaimer, correct feedback for `Almost 11 million`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q009 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 7 population/distribution facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q010-EN-NATURALNESS-1 [product defect][2026-05-18 08:42 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q010 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Natural resources says..." This repeats the q001-q009 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '232,286p'` shows q010 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q010` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Landet Sverige, Naturresurser, p. 7`, independent-study disclaimer, correct feedback for `Iron ore and other minerals, forest, agricultural land, and water`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q010 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 7 natural-resources facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q011-EN-NATURALNESS-1 [product defect][2026-05-18 08:42 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q011 content/runtime pass found the English explanation is user-visible but source-note-like: "The UHR section Democracy means rule by the people says..." This repeats the q001-q010 pattern and reads like citation scaffolding rather than natural learner-facing English, even though the answer, source citation, disclaimer, and facts are otherwise present. Evidence: `nl -ba data/questions.ts | sed -n '280,326p'` shows q011 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q011` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Sveriges demokratiska system, Demokrati betyder folkstyre, p. 10`, independent-study disclaimer, correct feedback for `Rule by the people`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q011 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 10 democracy-definition facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q021-EN-NATURALNESS-1 [product defect][2026-05-18 08:44 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q021 content/runtime pass found the English explanation is user-visible but grammatically awkward: "Therefore the state, regions, and municipalities is correct..." The facts and citation are present, but this reads like a raw answer-label insertion rather than natural learner-facing English. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '68,86p'` shows q021 `explanationEn` with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q021` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Så här styrs Sverige, Landet styrs på olika nivåer, p. 12`, independent-study disclaimer, correct feedback for `The state, regions, and municipalities`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q021 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 12 state/region/municipality responsibility-level facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q071-EN-NATURALNESS-1 [product defect][2026-05-18 08:45 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q071 content/runtime pass found the English explanation is user-visible but source-note-like: "The State-financed welfare section says..." The facts and citation are present, but the explanation reads like citation scaffolding rather than natural learner-facing English. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '1180,1210p'` shows q071 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q071` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Välfärdssamhället, Statligt finansierad välfärd, p. 30`, independent-study disclaimer, correct feedback for `Pensions, sickness insurance, parental insurance, unemployment insurance, study support, and child allowance`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q071 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 30 state-financed welfare facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-Q072-EN-NATURALNESS-1 [product defect][2026-05-18 08:46 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q072 content/runtime pass found the English explanation is user-visible but source-note-like and ungrammatical: "The Regions are responsible for health care section says..." The facts and citation are present, but the explanation reads like citation scaffolding rather than natural learner-facing English. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '1211,1233p'` shows q072 `explanationEn` beginning with the awkward phrase; current checkout is `main...origin/main [behind 1]` with active dirty work from other lanes, so this reviewer pass did not pull over shared work and reused the current exported artifact. System-Chrome exported-web pass on `/quiz/q072` in English support mode exited 2 by design after confirming the English question, Swedish secondary question, localized source citation `Source: Sverige i fokus, Välfärdssamhället, Regionerna ansvarar för sjukvården, p. 30`, independent-study disclaimer, correct feedback for `To provide health and medical care for everyone`, the awkward explanation, and console/page errors 0. Manager action: assign a CONTENT-owned q072 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 30 regions/health-care facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
REVIEWER-REMOVE-ADS-PRICE-COPY-1 [product defect][2026-05-18 10:43 CEST][P0 ads-iap][ux]: Mobile free-learner persona pass found inconsistent Remove Ads price copy before purchase. The Home pricing wedge tells learners "29 kr" while the actual IAP constant, profile paywall, privacy copy, release policy, and runbook use "29 SEK". For a paid Remove Ads flow this creates trust and store-review risk because the learner sees two price formats for the same purchase before tapping Buy. Evidence: `rg -n "29 kr|29 SEK|REMOVE_ADS_PRICE_LABEL" app components lib scripts tests publishing -S` shows `components/monetization/PricingWedge.tsx` has `29 kr` in both Swedish and English pitch copy, while `lib/monetization/purchases.ts` exports `REMOVE_ADS_PRICE_LABEL = '29 SEK'`, `components/monetization/PremiumBanner.tsx` renders `REMOVE_ADS_PRICE_LABEL`, `app/privacy.tsx` says `29 SEK`, and release tests/policy assert `29 SEK`. Checks run in this pass: `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/accessibility-labels.test.js` exit 0; `NODE_OPTIONS='--v8-pool-size=1' npm run test:monetization` exit 1 because this checkout cannot resolve module `typescript`; fresh Expo export was blocked because local `expo` is unavailable and `npx expo export` timed out during CLI resolution, leaving no `dist-web` artifact. Manager action: assign a source-touching UI/monetization atom in `components/monetization/PricingWedge.tsx` to use the canonical `REMOVE_ADS_PRICE_LABEL` or exact `29 SEK` copy, add a parity assertion so Home and PremiumBanner cannot drift, and rerun monetization/UI checks in a dependency-complete environment; do not close this with docs-only acceptance.
REVIEWER-Q037-EN-NATURALNESS-1 [product defect][2026-05-18 12:08 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q037 content review found the English explanation is user-visible but source-note-like: "The section on the Instrument of Government says..." and "It also describes that..." The facts, answer, and UHR citation are otherwise aligned, but this reads like citation scaffolding instead of natural learner-facing English. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '368,390p'` shows q037 `explanationEn` with the awkward wording; `rg -n "explanationEn|ExplanationPanel|language" app/quiz components/quiz app/'(tabs)'/practice.tsx -S` shows routed quiz and practice pass `question.explanationEn` to `ExplanationPanel` when English support is selected; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 705 `questionBilingualTextPairsValidated`; `NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-uhr-source-citation-stem.test.js tests/content-export-parity.test.js tests/content-question-sentence-endings.test.js` exits 0 with 6/6 passing; `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0 for 705 questions. Route export evidence is limited because `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' timeout 360s npm run build:web:export -- --max-workers 2` exits 1 when Expo cannot resolve the configured `expo-web-browser` plugin from local `node_modules`. Manager action: assign a CONTENT-owned q037 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 16 Instrument of Government facts; current validators stay green, so do not close TRANSLATE-COMPLETE on gate status alone.
CEO->VALIDATOR 2026-05-18T12:27:59+02:00
Executive decision: keep HOLD/add-none and accept only source-changing,
evidence-backed P0 work while `codex-tasks/P0.md` remains unchecked.
Immediate actions:
1. q008 is on `origin/main` via PR #172; accept or reject it only from PR/merge
   evidence, journal handoff, preserved UHR locator, export parity,
   `validate:content`, and focused content tests.
2. Treat q009 and q010 as active content-manager assignments. Keep
   TRANSLATE-COMPLETE open after q008/q009/q010 unless the validator audit shows
   the source-note explanation pattern is exhausted. Current visible next
   content defects include q011, q037, q071, and q072.
3. Do not add remote/LUNARC requirements to acceptance this cycle. The current
   `csup staff --scenario=resume --dry-run` evidence is STAFF-DOWN/no queued
   remote work, with local `civic-content` as the only active staffed team.
4. Continue rejecting docs-only closure for reviewer product defects; each
   acceptance needs source change plus verification evidence.

REVIEWER-GENERATED-JUDGEMENT-PROMPT-1 [product defect][2026-05-18 12:32 CEST][P0 TRANSLATE-COMPLETE][language]: After PR #170 cleared nested true/false source stems, generated judgement variants still turn ordinary questions into awkward statement-judgement prompts. Current `origin/main` renders/export-publishes examples such as q145 `Vilket alternativ motsvarar rätt bedömning av påståendet? Var ligger Sverige?` and `Which option gives the correct judgment of the statement? Where is Sweden located?`, then offers answer labels as options. This asks the learner to judge a "statement" that is actually a question. Evidence: `rg -n "correct judgment of the statement|rätt bedömning|Vilket alternativ motsvarar rätt" lib/content/derivedQuestions.ts scripts/validate-content.js content/question-bank.csv -S` finds the generator, validator parity mirror, and exported CSV; `sed -n '142,158p' content/question-bank.csv` shows q145/q153/q157-style interrogative stems still published. Manager action: DATA-INTEGRITY should replace `buildAnswerJudgementVariant` with natural templates for interrogative and true/false sources, update the validator/parity mirror, regenerate `content/question-bank.csv`, and add a negative guard for the current judgement-of-interrogative scaffolding. Do not close with docs-only or test-only work.

CEO->VALIDATOR 2026-05-18T12:43:40+02:00
Executive decision: keep HOLD/add-none and P0-first acceptance. q010 is now on
origin/main via PR #182 / `8d6cead`, q011 is on origin/main via PR #180 /
`793ec73`, q037 is the active/next visible source-note naturalness defect, and
q071/q072/q021 remain queued after that. A clean `origin/main` staffing rerun
sees queued expansion work from #186 but holds every LUNARC line with
`reason=lunarc_requires_slurm_station`; this is not approval to start expansion
teams.
Immediate actions:
1. Accept/reject q010 and q011 from PR #182/`8d6cead` and PR #180/`793ec73`,
   preserved UHR locators, export parity, `validate:content`, focused content
   tests, and journal evidence.
2. Keep content capacity on q037, then q071/q072/q021. Reject duplicate q009,
   q010, or q011 branches.
3. Reconcile the #186 research/verify/delight/mascot queues into
   `TEAM_PLAN.md` acceptance rows and leases or explicitly defer them. Do not
   staff those teams while P0 is open and no accepted manager row exists.
4. Continue rejecting docs-only or report-only closures. Each accepted item
   needs a source change or validator ledger artifact that maps to a P0/reviewer
   defect and includes command evidence.

CEO->VALIDATOR 2026-05-18T12:53:42+02:00
Executive decision: keep HOLD/add-none and P0-first acceptance. The CEO reran
`/home/billy/bin/csup staff --scenario=resume --dry-run`; all configured remote
hosts returned STAFF-DOWN/no usable queued work, and local tmux still shows only
`civic-content` active. No `--apply` was run.
Immediate actions:
1. Accept/reject q037 from PR #189 / `cd89504` and q071 from PR #192 /
   `4e1faf9` using preserved UHR locators, content journal evidence, export
   parity, `validate:content`, focused content tests, typecheck/ownership, and
   clean diff evidence.
2. Treat q072, q021, and the visible q073 work as active or in-flight content
   atoms. Reject duplicate q072/q021/q073 work; after those, route q012, q013,
   q014, q015, q016, q017, then q020 unless newer PR evidence already closed
   them.
3. Keep TRANSLATE-COMPLETE and SOURCE-CITATION open until the naturalness and
   generated/source-stem evidence is exhausted. Green validators alone are not
   enough while reviewer rows still show source-note wording or generated
   judgement prompt defects.
4. Keep the DATA-INTEGRITY generated-judgement prompt fix queued as P0 for a
   build-capable staffing window, but do not start expansion teams or remote
   lanes without TEAM_PLAN acceptance rows, leases, and usable queue/headroom
   evidence.

CEO->VALIDATOR 2026-05-18T13:22:00+02:00
Executive decision: keep HOLD/add-none and P0-first acceptance. The CEO reran
`/home/billy/bin/csup staff --scenario=resume --dry-run`; it saw queued work
across configured hosts, but every LUNARC/remote line held with
`reason=lunarc_requires_slurm_station`. No `--apply` was run.
Immediate actions:
1. Accept/reject q072 from PR #194 / `6fc47b4` and q073 from `ee4ddb4` using
   preserved UHR locator, content journal evidence, export parity,
   `validate:content`, focused content tests, typecheck/ownership, and clean
   diff evidence.
2. Accept/reject the DATA-INTEGRITY generated-stem fix from PR #198 /
   `02193fb`; do not keep a duplicate generated-judgement worker queued unless
   that executable evidence fails the P0 bar.
3. Treat q021 as active/in-flight. q021 has PR #203 open at observed pane
   state, and q012 status is superseded by the correction below. Reject
   duplicate q021 work unless the manager explicitly abandons the active atom.
4. Keep TRANSLATE-COMPLETE and SOURCE-CITATION open while reviewer rows still
   show naturalness/source-scaffolding defects. After active q021/q012, route
   q013, q014, q015, q016, q017, q020, then q027/q028 unless newer accepted
   evidence changes the order.
5. Do not staff expansion teams or remote lanes until TEAM_PLAN rows, leases,
   and usable headroom evidence exist.

CEO->VALIDATOR correction 2026-05-18T13:27:00+02:00
q012 landed after the 13:22 note via PR #204 / `2c7c02f`; accept/reject it from
that merge evidence and treat stale q012 routing as superseded. PR #206 is an
open MANAGER-content queue update that still assigns q012 and should not be
accepted as-is unless rebased/reconciled. q021 remains open in PR #203 and not
mergeable at connector check time; keep q021 duplicate-guarded and require
updated merge evidence before accepting.

MANAGER-build acceptance 2026-05-18T13:10+02:00
- Accepted DATA-INTEGRITY generated-stem P0 atom from PR #198 / `02193fb` for
  `REVIEWER-GENERATED-TF-META-STEM-1` and
  `REVIEWER-GENERATED-JUDGEMENT-PROMPT-1`. GitHub reports PR #198 merged, and
  clean current-main verification passed export parity, `validate:content`,
  `test:derived-content`, published-question guards, content-production,
  typecheck, ownership, and `git diff --check`.
- Do not keep the old generated-judgement task queued. `codex-tasks/data-integrity.txt`
  now routes `REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1`; broader generated
  true/false grammar-splice naturalness remains a follow-up unless DATA-INTEGRITY
  can cover both in one bounded generator/validator/CSV/test atom.

MANAGER-build acceptance 2026-05-18T13:17+02:00
- Accepted DATA-INTEGRITY nested single-choice true/false prompt atom from PR
  #212 / `22d0d42` for `REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1`.
  Clean `origin/main` verification passed: CSV scan found no rejected
  single-choice/nested true-false/judgement scaffolding; `node scripts/export-question-bank.js --check`;
  `npm run validate:content`; `npm run test:derived-content`;
  `node --test tests/content-published-question-types.test.js`; `npm run typecheck`;
  `npm run test:ownership`; and `git diff --check`.
- `codex-tasks/data-integrity.txt` now routes `REVIEWER-GENERATED-TF-NATURALNESS-1`
  as the next bounded generator-naturalness atom. Keep broader content
  naturalness defects in CONTENT unless they require generated-template changes.
REVIEWER-Q074-EN-NATURALNESS-1 [product defect][2026-05-18 13:26 CEST][P0 TRANSLATE-COMPLETE][language]: Focused English-support quiz pass found q074 still has unnatural user-visible English explanation prose on both the current checkout and `origin/main`. Static source evidence from `origin/main:data/additionalQuestions.ts:1267`-`:1268`: q074 `explanationEn` begins `The UHR section Municipalities have a large responsibility says...`, a source-note splice rather than idiomatic learner-facing English. Runtime evidence: after selecting English support, exported web `/quiz/q074` rendered the English question, Swedish secondary prompt, localized source citation, independent-study disclaimer, correct feedback for `True`, and the same awkward explanation; browser console/page errors were empty. Gate evidence: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 for 705 questions, `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0, and no existing `REVIEWER-Q074` row was found, so current validators do not enforce this naturalness bar. Manager action: assign a CONTENT-owned q074 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 31 elderly-care municipal responsibility facts and citation; do not close TRANSLATE-COMPLETE on green validation alone.
REVIEWER-Q078-EN-NATURALNESS-1 [product defect][2026-05-18 13:39 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q078 content review found the English explanation is user-visible but still written as source-note scaffolding: `The UHR section Sweden’s path to democracy says...`. The question stem, answer, UHR citation, and source-citation cleanup are otherwise present, but the explanation reads like a source note instead of idiomatic learner-facing English. Evidence from clean `origin/main`: `nl -ba data/additionalQuestions.ts | sed -n '1332,1352p'` shows q078 `explanationEn` starts with the awkward phrase and the Swedish explanation starts `UHR-avsnittet Sveriges väg till demokrati säger...`; duplicate scan found no existing q078 English-naturalness row, only older source-citation prompt cleanup entries. Gates remain green: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 for 705 questions, `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0, and focused source-citation/export/sentence tests pass 6/6. Runtime export evidence was limited because the clean temporary worktree could not resolve the configured `expo-web-browser` plugin from the shared `node_modules`, but routed quiz/practice source passes `question.explanationEn` into `ExplanationPanel` for English support. Manager action: assign a CONTENT-owned q078 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 33 1809 constitution / limited royal power / restricted suffrage facts; do not close TRANSLATE-COMPLETE on green validation alone.

MANAGER-content -> VALIDATOR 2026-05-18T13:39:36+02:00
q016 landed via PR #225 / `31d9828`, q017 landed via PR #222 / `fc19b58`, and
duplicate q016 draft PR #230 was closed unmerged with its remote branch deleted.
Per CEO 13:32, do not route q020/q027/q028 or other non-site content
automatically while SITE-P0 deployed-site blockers are active in
`codex-tasks/blockers.txt`. Please decide SITE lease/staffing for fresh
capacity, or explicitly defer SITE before content naturalness continuation.
q074 and q078 remain reviewer-filed content defects, but they should not
preempt SITE-P0 without VALIDATOR/CEO direction. Local non-site worktrees for
q018, q020, and q074 were observed after the CEO 13:32 update; treat them as
unaccepted unless VALIDATOR explicitly leases content despite SITE-P0.

MANAGER-build acceptance 2026-05-18T13:57+02:00
- Accepted `SITE-P0-1` from PR #242 / `c4404d8`. Clean `origin/main`
  verification passed: `node --check site/app.js`; `node --check
  site/practice.js`; `node --check site/questions.js`; route/static-link
  assertion for `#/practice`, `questions.js`, `practice.js`, and `.hub__grid`;
  `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`;
  `npm run test:ownership`; targeted `git diff --check`; and local static-site
  smoke with system Chrome from `/` to `#/practice`, first chapter drill, and
  `#/ebook` showing 12 hub cards, 4 quiz options, Ebook rendered, and no browser
  console/page errors.
- Keep SITE-P0 stop-the-line active, but remove `SITE-P0-1` from active work.
  Next SETUP/build atom is `SITE-P0-2`: static Mock exam in `site/` with timed
  flow, answer submission, result/review UX, no console errors, `node --check`
  for changed site JS, and clean `git diff --check`.

REVIEWER-Q079-EN-NATURALNESS-1 [product defect][2026-05-18 13:44 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q079 content review found the English explanation is user-visible but still written as source-note scaffolding: `The UHR section Popular movements describes...` and `the material mentions...`. The question stem, answer, UHR citation, and source-citation cleanup are otherwise present, but the explanation reads like a source note instead of idiomatic learner-facing English. Evidence from clean `origin/main`: `nl -ba data/additionalQuestions.ts | sed -n '1353,1375p'` shows q079 `explanationEn` starts with the awkward phrase and the Swedish explanation starts `UHR-avsnittet Folkrörelserna beskriver...`; duplicate scan found no existing q079 English-naturalness row, only older source-citation prompt cleanup entries. Gates remain green when run from the clean worktree with the shared dependency path: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 for 705 questions, `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0, and focused source-citation/export/sentence tests pass 6/6. Runtime export evidence was not rerun for this atom because the clean worktree has no local `node_modules`, but routed quiz/practice source passes `question.explanationEn` into `ExplanationPanel` for English support. Manager action: assign a CONTENT-owned q079 naturalness atom to rewrite the English explanation idiomatically while preserving the UHR page 33 popular-movements facts about labour, free church, women, and temperance movements; do not close TRANSLATE-COMPLETE on green validation alone.

REVIEWER-SITE-STATIC-WIRING-1 [product defect][2026-05-18 14:00 CEST][P0 SITE][functional]: Focused static-site audit after rebasing onto `origin/main` `c4404d8` found SITE-P0-1 is partly fixed, but SITE-P0-2 and SITE-P0-4 are still not wired into the deployed page shell. `site/index.html` now loads `site/questions.js` and `site/practice.js`, and `/practice` renders the new hub; however `site/app.js` still only allow-lists `/`, `/practice`, `/ebook`, `/privacy`, `/support`, `/terms`, and `/sources`, and there is still no `data-page="/mock"` / `#mock-stage` shell. `site/index.html` also still does not load `site/ebook-tools.js` or `site/i18n-extras.js`, so the richer ebook helper surface is bypassed. Runtime evidence from the rebased clean worktree served at `http://127.0.0.1:8197`: Home script list includes `questions.js` and `practice.js`; `window.smtRecordAnswer` is present; `window.SMT_QUESTIONS` has 57 questions; `/practice` shows `hubCards:12` and one `#/mock` link; but `#/mock` falls back to active page `/` with `mockStageExists:false` and no `Build your exam` copy; `/ebook` still renders with 13 chapter links while `ebookToolsLoaded:false`. Gate evidence: `node --check` passes for `site/app.js`, `site/practice.js`, `site/questions.js`, `site/ebook.js`, `site/ebook-tools.js`, `site/settings.js`, `site/signin.js`, `site/extras.js`, `site/buddies.js`, and `site/fx.js`, so syntax is not the blocker. Duplicate scan found no existing SITE static-wiring reviewer row. Manager action: assign SETUP to add the static `/mock` route shell/stage, allow-list `/mock`, ensure the Practice mock link opens the live mock exam flow, load/preserve the ebook helper tooling, and add a static smoke/assertion that fails when present SITE P0 files are not reachable through the deployed page shell. Do not accept SITE-P0-2, SITE-P0-4, or SITE-P0-5 while `#/mock` falls back to Home and ebook helpers are bypassed.

MANAGER-build acceptance 2026-05-18T14:08+02:00
- Accepted `SITE-P0-2` from PR #252 / `5e834b9`, with SETUP handoff PR #254 /
  `bfd6c7e`. This closes the `/mock` route-shell and mock-exam portion of
  `REVIEWER-SITE-STATIC-WIRING-1`; keep its ebook-helper/SITE-P0-4 portion open.
- Clean manager verification on current `origin/main` passed after a fresh
  `npm ci`: `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`;
  `node --check` for `site/app.js`, `site/practice.js`, `site/questions.js`,
  `site/i18n-extras.js`, and `site/ebook-tools.js`; targeted `git diff --check`;
  and static-site Chrome smoke from `/` to `#/mock`, 5-question exam completion
  and result review, `#/practice`, and `#/ebook` with no browser console/page
  errors.
- Next SETUP/build atom is `SITE-P0-3`: sync `site/questions.js` from canonical
  app data and add/keep a parity guard before the SITE-P0-4 ebook quality pass.

MANAGER-build acceptance 2026-05-18T14:29+02:00
- Accepted `SITE-P0-3` from PR #264 / `19a420e`, with SETUP handoff PR #267 /
  `4f2f9c5`. This closes the static question-bank drift portion of SITE-P0.
- Clean manager verification on current `origin/main` passed: `node
  scripts/export-site-question-bank.js --check`; `node --test
  tests/content-static-site-question-bank-parity.test.js`; `node
  scripts/export-question-bank.js --check`; `node --check site/questions.js`;
  `npm run validate:content`; `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`;
  `npm run test:ownership`; targeted `git diff --check`; and static-site Chrome
  smoke showing 13 Practice cards, 705-question total, q001 feedback, 5-row Mock
  review, and no browser console/page errors.
- Next SETUP/build atom is `SITE-P0-4`: preserve the richer `site/ebook.js` and
  `site/ebook-tools.js` surface while improving visible writing/navigation
  quality. Keep `SITE-P0-5` as reviewer audit only after SITE-P0-4 lands.

REVIEWER-SITE-EBOOK-SV-PARITY-1 [product defect][2026-05-18 14:34 CEST][P0 SITE][language]: Focused SITE-P0-4 ebook pass found the deployed static ebook still violates the active SV/EN parity bar. Static evidence from clean `origin/main` `7f3c84b`: `rg -n "Svenska översättningen kommer|friendly stubs|We're writing this chapter now|kommer i v1.1|v1.1" site/ebook.js site/index.html -S` finds the file header `12 chapters; first 3 written in full, rest are friendly stubs`, twelve Swedish chapter-body placeholders saying `Svenska översättningen kommer i v1.1`, and the generic unwritten-chapter fallback. A focused Node scan reports `svStubCount:12` while the English ebook has 12 `Facts you'll see on the test` chapter bodies. Runtime evidence from a local static serve of `site/` at `http://127.0.0.1:8204`: system Chrome with `localStorage.smt_lang="sv"` on `#/ebook?c=1` renders the Swedish heading and lede, but the body is only `Svenska översättningen kommer i v1.1 - på engelska tills vidare. Klicka Öva för svenska frågor.`; ebook nav has 13 links, ebook tools are loaded, and browser console/page errors are 0. Manager action: assign SETUP/content-supported SITE-P0-4 work to replace all Swedish ebook placeholders with natural Swedish chapter prose matching the English coverage, remove v1.1/placeholder copy from the deployed site, and add a static ebook parity guard that fails on untranslated chapter bodies before accepting SITE-P0-4 or starting SITE-P0-5.

REVIEWER-ACCOUNT-SCOPE-1 site update [2026-05-18 14:40 CEST][P0 SITE][scope/privacy]: Rechecked the deployed static `site/` surface against the same no-account MVP contract. The previous app-shell account cleanup does not cover the static site: `site/index.html` still exposes a global `Sign in` button and `#signin-modal`, `site/signin.js` opens the modal, and `site/ebook-tools.js` gates highlights/notes on `localStorage["smt_signed_in"] === "1"` with sign-in nudges. Runtime evidence from clean `origin/main` `c37bf5a` served at `http://127.0.0.1:8206`: system Chrome on `#/` found one Sign in button; clicking it opened a modal with `Sync your progress.`, `Continue with Google`, `Continue with Apple`, `Email`, `Send magic link`, and `Beta: accounts are rolling out gradually`; console/page errors were 0. Static syntax checks for `site/app.js`, `site/signin.js`, and `site/ebook-tools.js` pass, so syntax gates do not cover this scope drift. Manager action: include `site/` in `REVIEWER-ACCOUNT-SCOPE-1`: remove or hide static Sign in/OAuth/magic-link/account-sync UI and make ebook highlights/notes purely local or defer them without account prompts; add a static-site guard rejecting reachable account/auth copy before accepting the no-account MVP scope.

MANAGER-build acceptance 2026-05-18T14:43+02:00
- Accepted `SITE-P0-4` from PR #279 / `9def23a`, with SETUP handoff PR #280 /
  `41a6bcb`. This closes `REVIEWER-SITE-EBOOK-SV-PARITY-1` and the remaining
  ebook-helper/SITE-P0-4 portion of `REVIEWER-SITE-STATIC-WIRING-1`.
- Clean current-main manager verification passed: `node --check site/ebook.js`;
  `node --check site/ebook-tools.js`; focused ebook copy/navigation source
  assertion for no stale Swedish placeholders, exposed `window.smtEbookRender`,
  `smtApplyEbookHighlights`, chapter practice links, notes host, Swedish study
  briefs, and source caveat; `NODE_OPTIONS='--v8-pool-size=1' npm run
  typecheck -- --pretty false`; `NODE_OPTIONS='--v8-pool-size=1' npm run
  validate:content`; `NODE_OPTIONS='--v8-pool-size=1' npm run test:ownership`;
  `git diff --check HEAD~2..HEAD`; and static-site Chrome smoke rooted at
  `site/` for English and Swedish Ebook, `11 / 12` progress, `Öva kapitlet`
  navigation to `#/practice?c=11`, active Practice question render, and browser
  console/page errors 0.
- SITE-P0 stop-the-line remains active only for `SITE-P0-5`: REVIEWER must
  audit the live deployed static site against the draft mockups and queue
  concrete parity defects. Do not close `SITE-P0-5` with a report-only audit.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 [product/deploy defect][2026-05-18 14:55 CEST][P0 SITE][functional]: SITE-P0-5 live audit found production is not serving the accepted SITE-P0-1 through SITE-P0-4 state. GitHub workflow API shows `Scheduled Vercel deploy` is active, but `actions/workflows/278713304/runs` reports `total 0`; GitHub deployments show the latest Production success is deployment `4726822421` from `2026-05-18T10:26:13Z` at `https://dist-oyquhbnhz-billy10384-5430s-projects.vercel.app`, while the latest Preview deployment `4728409781` failed with `Deployment was blocked`. Runtime evidence against the latest Production URL: `/practice` has `qCount:0`, `hubCards:0`, and no mock nav; `#/mock` falls back to active page `/` with no `#mock-stage`; Swedish `#/ebook?c=1` still renders the old `Svenska översättningen kommer i v1.1` placeholder. Runtime evidence against the latest successful Preview URL `https://dist-g1pghzqa5-billy10384-5430s-projects.vercel.app`: only 57 questions load and the Swedish ebook still has the old placeholder. Clean local `origin/main` `04886a5` served from `site/` loads 705 questions, 13 Practice cards, `#/mock`, and Swedish ebook chapter 1 with no placeholder. Manager action: fix the production deploy path/cap/blocking state so a scheduled or manual production deploy actually runs from current `origin/main`, then add a live smoke gate that fails unless the production URL shows `window.SMT_QUESTIONS.length === 705`, `#/mock` renders `#mock-stage`, `/practice` has the current hub, and Swedish ebook chapters contain no v1.1 placeholders. Do not close SITE-P0-5 on local static evidence while production remains stale.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 15:22 CEST][P0 SITE][functional]: Rechecked the live deployment after the AnswerOption stop-line was cleared and current `origin/main` advanced to `4636321e1287`. GitHub still reports `Scheduled Vercel deploy` active with `runCount:0`. Latest Production remains deployment `4726822421` from `2026-05-18T10:26:13Z`, SHA `3d921720f8c4`, URL `https://dist-oyquhbnhz-billy10384-5430s-projects.vercel.app`; latest Preview remains blocked (`4728409781`, `Deployment was blocked`), and latest successful Preview is still `https://dist-g1pghzqa5-billy10384-5430s-projects.vercel.app`. Runtime recheck with System Chrome: clean local `site/` from `4636321e1287` has `qCount:705`, `hubCards:13`, `mockLinks:2`, `mockStageExists:true`, and Swedish ebook chapter 1 has no placeholder; latest Production still has `qCount:0`, `hubCards:0`, `mockLinks:0`, `mockActivePage:"/"`, `mockStageExists:false`, and Swedish ebook placeholder text; latest successful Preview still has only `qCount:57`, `hubCards:12`, and the old Swedish ebook placeholder. Manager action remains unchanged: route the deploy path/cap/blocking fix before treating local SITE work as live; add a live smoke gate tied to current production so this stale-site state cannot pass again.

REVIEWER-SITE-PRACTICE-WIDTH-PARITY-1 [product defect][2026-05-18 14:55 CEST][P0 SITE][ux-parity]: SITE-P0-5 design parity audit found the current Practice hub still uses the old narrow shell instead of the draft's wide chapter-hub layout. The reference draft has `docs/design/drafts/practice-mockexam-2026-05-18/site/index.html:204` using `class="practice__inner practice__inner--wide"` and `docs/design/drafts/practice-mockexam-2026-05-18/site/styles.css:1515` setting `.practice__inner--wide { max-width: 1080px; }`. Current `origin/main` has only `site/index.html:189` `class="practice__inner"` and no production `practice__inner--wide` rule. Runtime evidence from clean local `origin/main` at 1440px: `hasWideClass:false`, `innerClientWidth:720`, `innerMaxWidth:"720px"`, grid columns `353px 353px`, and hub card widths `353`; latest successful Preview shows the same 720px container. This compresses the chapter hub/stats against the visual target even after the route is functionally wired. Manager action: assign SETUP/UI to port the draft wide Practice container and responsive guard into `site/index.html`/`site/styles.css`, preserving current question data and mock behavior; add a static or browser assertion that the desktop Practice hub uses the intended wide layout.

MANAGER-build blocker 2026-05-18T15:07+02:00
- Current `origin/main` `d42cb51` fails
  `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` with four
  AnswerOption accessibility parity errors: missing shared `Button` import,
  explicit button role, feedback-aware visible label, and tone-to-variant
  mapping.
- Root cause is a validator/test mirror mismatch after the accepted
  `OptionCard` component refactor. The DATA-INTEGRITY queue now routes
  `ANSWER-OPTION-OPTIONCARD-PARITY-1` as the stop-the-line atom.
- Do not accept additional manager/build/content/site work until
  `validate:content` is green on current `origin/main`.

MANAGER-build acceptance 2026-05-18T15:15+02:00
- Accepted `ANSWER-OPTION-OPTIONCARD-PARITY-1` from PR #303 / `e232bf7`.
  GitHub reports PR #303 closed and merged; current `origin/main` is `ff117b1`
  after later docs/UIUX/reviewer merges and contains the repair.
- Clean manager verification on current `origin/main` passed:
  `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` with
  `answerOptionAccessibilityParityValidated:true`; focused
  `node --test tests/content-answer-option-accessibility-parity.test.js` 4/4;
  `npm run typecheck -- --pretty false`; `npm run test:ownership`; and
  `git diff --check`.
- The AnswerOption stop-line blocker is resolved. Resume site-first P0 routing:
  `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`, then
  `REVIEWER-SITE-PRACTICE-WIDTH-PARITY-1`, with static
  `REVIEWER-ACCOUNT-SCOPE-1` still ahead of release-config, visual-smoke, and
  search-placeholder work.

REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1 [product defect][2026-05-18 15:15 CEST][P0 SITE][privacy/monetization]: Focused static-site privacy audit on clean `origin/main` `9217f48` found the deployed `site/` JavaScript translations still overwrite the updated static HTML with the old disabled-ads/no-sales privacy contract. Source evidence: `site/index.html` now says the website uses Google AdSense and the mobile app uses Google Mobile Ads/AdMob, but `site/app.js` English `privacy.lede` still says "we don't sell anything", English `privacy.s5.p` still says "real ad rendering is disabled for the v1.0 release candidate", Swedish `privacy.lede` still says `säljer ingenting`, and Swedish `privacy.s5.p` still says `riktig annonsvisning är avstängd för v1.0-kandidaten`. Runtime evidence from locally served `site/` at `http://127.0.0.1:4317/#/privacy`: System Chrome with `localStorage.smt_lang="en"` reported `disabledAdsEn:true`, `dontSellEn:true`, and `removeAds:false`; the same route with `localStorage.smt_lang="sv"` reported `disabledAdsSv:true`, `dontSellSv:true`, and `removeAds:false`; browser console/page errors were 0. Duplicate scan found the older exported-app privacy finding and the live-deploy stale-site blocker, but no static `site/app.js` i18n privacy/monetization row. Manager action: assign SETUP/site copy work to update `site/app.js` EN/SV privacy and FAQ monetization strings so the current static site accurately states web ads use AdSense, the app uses AdMob/Google Mobile Ads with consent, Remove Ads is a one-time 29 SEK IAP that removes ads, and ads never collect study answers/progress; add a static or browser guard rejecting stale strings such as `real ad rendering is disabled`, `avstängd för v1.0`, `If we ever add premium`, and `Om vi någonsin lägger till premium`.

REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1 [product defect][2026-05-18 15:29 CEST][P0 SITE][content-sync]: Focused static-site question-bank parity check on clean `origin/main` `056c4d4` found `site/questions.js` is stale after the q142 source-expansion merge. Canonical content gates are green at 710 published questions: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with `questions:710`, `sourceQuestions:142`, and `generatedPublishedQuestions:568`; `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0 at 710 questions. Static-site gates are red: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 1 with `site/questions.js is out of sync`; focused `node --test tests/content-static-site-question-bank-parity.test.js` exits 1 with 0/2 passing and reports canonical 710 vs site 705. Direct `site/questions.js` VM inspection reports `siteCount:705`, `hasQ142:true`, `hasQ707:false`, `hasQ710:false`, and last IDs `q694`-`q705`, so the deployed static bank is missing the generated q142 variants and any live smoke target still expecting 705 is now stale. Duplicate scan found the accepted SITE-P0-3 static-bank sync and content q142 handoff noting `site/questions.js` was intentionally not touched by CONTENT, but no reviewer row for the new q142 drift. Manager action: route SETUP/DATA-INTEGRITY to regenerate `site/questions.js` from current canonical content, update live/static smoke expectations from 705 to 710 or derive the count dynamically, and keep `scripts/export-site-question-bank.js --check` plus `tests/content-static-site-question-bank-parity.test.js` green before accepting SITE content sync.

MANAGER-build routing 2026-05-18T15:30+02:00
- `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1` is now first in the SETUP/site
  queue because live deployment of current `origin/main` would still ship a
  stale 705-question static bank after source expansions moved canonical
  content beyond it.
- Live deploy stale remains P0 but moves behind this sync repair. Its smoke
  must use the current canonical/static count or derive it dynamically; do not
  accept a hard-coded 705/710-question gate.
- SETUP pane 2 received a direct tmux redirect to fetch/rebase before
  continuing deploy work.

MANAGER-build correction 2026-05-18T15:36+02:00
- `origin/main` advanced through q143 and validates at 715 questions. Treat the
  q142-named static-bank defect as the current static-bank drift atom, not a
  frozen 710-question task. DATA-INTEGRITY/SETUP must fetch/rebase, regenerate
  `site/questions.js` from the latest canonical content, and keep count checks
  derived from canonical data.

MANAGER-build review 2026-05-18T15:39+02:00
- PR #321 / `3a323ff` landed a useful deploy workflow and
  `scripts/check-live-site.js` harness, but do not close
  `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` from this evidence. The handoff only
  tested local assets at 705 questions, `check-live-site` defaults to 705, and
  no production deploy run/live URL evidence is recorded.
- Keep `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1` first. Its repair must
  regenerate `site/questions.js` from current canonical content and fix the
  live-smoke count expectation so it cannot pass or require stale 705 output.

REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1 resolution recheck [2026-05-18 15:40 CEST][P0 SITE][content-sync]: Rechecked current `origin/main` `d4ffd91` after `data-integrity: sync static site question bank`. The static-bank drift is now green at the current 715-question canonical count: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with `questions:715`, `sourceQuestions:143`, and `generatedPublishedQuestions:572`; `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check` exits 0 with `Question bank export parity OK (715 questions)`; `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with `Static site question-bank parity OK (715 questions, 13 chapters)`; focused `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-static-site-question-bank-parity.test.js` exits 0 with 2/2 passing. Direct VM inspection of `site/questions.js` reports `siteCount:715`, `hasQ142:true`, `hasQ143:true`, `hasQ707:true`, `hasQ710:true`, `hasQ711:true`, `hasQ715:true`, and last IDs `q704`-`q715`. Manager/VALIDATOR action: accept the static-bank drift atom only after reviewing the DATA-INTEGRITY handoff/PR evidence, then move back to `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`; any live smoke gate must now derive the count or expect 715, not stale 705.

MANAGER-build acceptance 2026-05-18T15:41+02:00
- Accepted the static-bank portion of `REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1`
  from PR #324 / `d4ffd91`. Clean manager verification passed `node
  scripts/export-site-question-bank.js --check` with 715 questions and 13
  chapters; `node --test tests/content-static-site-question-bank-parity.test.js`
  2/2; `node scripts/export-question-bank.js --check`; `npm run
  validate:content` at 715 questions; `node --check site/questions.js`; direct
  VM inspection for q142/q143/q715; `npm run typecheck -- --pretty false`;
  `npm run test:ownership`; and `git diff --check`.
- Split the remaining live-smoke default into `REVIEWER-SITE-LIVE-SMOKE-COUNT-1`.
  Current local `site/` fails `npm run test:site-live` by default with
  `expected 705, found 715`, but passes only with
  `SITE_LIVE_REQUIRED_QUESTION_COUNT=715`. Fix that before production deploy
  closure.
REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 15:47 CEST][P0 SITE][functional]: Rechecked current `origin/main` `11b2b96` after `setup: derive live site question count`. The local live-smoke count gate is now green: `node --test scripts/check-live-site.test.js` exits 0 with 5/5 passing, `NODE_OPTIONS='--v8-pool-size=1' node --test --test-name-pattern 'scheduled Vercel deploy has a site-only main trigger and live smoke gate' scripts/build-config.test.js` exits 0, and default `node scripts/check-live-site.js http://127.0.0.1:4331` against locally served current `site/` exits 0 with 715 questions, Practice, Mock, Ebook, and no placeholder copy. The production deploy blocker is still open: GitHub Actions scheduled deploy run `26037443794` for `11b2b96` completed `failure` at `2026-05-18T13:45:19Z`; earlier runs for `d4ffd91` and `3a323ff` also failed. GitHub deployments still show latest Production success as deployment `4726822421`, SHA `3d921720f8c4`, created `2026-05-18T10:26:13Z`, URL `https://dist-oyquhbnhz-billy10384-5430s-projects.vercel.app`; default `node scripts/check-live-site.js` against that URL exits 1 because `/practice.js` returns HTTP 404. Latest successful Preview remains `https://dist-g1pghzqa5-billy10384-5430s-projects.vercel.app`; the default live check exits 1 with 57 questions, missing current Ebook helper wiring, and stale v1.1 placeholder copy. Manager action: accept the live-smoke count fix only after reviewing SETUP PR evidence, but keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open until a scheduled/manual production deployment from current main succeeds and the live URL passes `npm run test:site-live` without count overrides.

MANAGER-build acceptance 2026-05-18T15:47+02:00
- Accepted `REVIEWER-SITE-LIVE-SMOKE-COUNT-1` from PR #328 / `11b2b96`.
  Clean current-main verification passed: `node --test
  scripts/check-live-site.test.js` 5/5; local default `checkLiveSite` smoke
  against current `site/` derived 715 and passed without
  `SITE_LIVE_REQUIRED_QUESTION_COUNT`; synthetic stale-705 `questions.js`
  failed with `expected 715, found 705`; `node
  scripts/export-site-question-bank.js --check`; `node --test
  tests/content-static-site-question-bank-parity.test.js` 2/2; `npm run
  validate:content` at 715 questions; `npm run typecheck -- --pretty false`;
  `npm run test:ownership`; and `git diff --check HEAD^..HEAD`.
- Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open. The smoke harness now uses
  current-count behavior, but there is still no production deploy evidence for
  current `origin/main`.

CEO->VALIDATOR 2026-05-18T15:52:00+02:00
Treat PR #324 / `d4ffd91` static-bank sync and PR #328 / `11b2b96` live-smoke
count derivation as accepted unless newer evidence contradicts them. Keep
`REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open until a scheduled/manual production
deploy from current `origin/main` succeeds and the production URL passes
`npm run test:site-live` without count overrides. Surface
`REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1` in BLOCKERS/SETUP as active SITE-P0
behind deploy, Practice width, and account-scope; the validator evidence is
already concrete enough for manager routing. Duplicate-guard q108 from
`d86717e`, QuestionNavigator from `dd126cd`, and MockExamConfigPanel from
`84f787b`.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 15:53 CEST][P0 SITE][functional]: Rechecked current `origin/main` `9023506` after the latest UIUX/content merges. No newer scheduled deploy run exists for `9023506`; the newest `Scheduled Vercel deploy` run remains `26037443794` for `11b2b96`, completed `failure` at `2026-05-18T13:45:19Z`. The run log archive contains only `Deploy static site to Vercel production/system.txt` and stops while waiting for a hosted runner before any checkout, Vercel CLI, or live-smoke step runs, so the open issue is deployment infrastructure/evidence, not current `site/` source readiness. Current local gates are green from a clean worktree: `node --test scripts/check-live-site.test.js` exits 0 with 5/5 passing, the focused scheduled-deploy build-config test exits 0 with 1/1 passing, and `SITE_LIVE_TIMEOUT_MS=30000 node scripts/check-live-site.js http://127.0.0.1:4339` exits 0 with 715 questions plus Practice, Mock, Ebook, and no placeholder copy. Live evidence remains red: latest Production success is still deployment `4726822421`, SHA `3d921720`, created `2026-05-18T10:26:13Z`, URL `https://dist-oyquhbnhz-billy10384-5430s-projects.vercel.app`; `SITE_LIVE_TIMEOUT_MS=30000 node scripts/check-live-site.js` against it exits 1 because `/questions.js` returns HTTP 404. Latest successful Preview `https://dist-g1pghzqa5-billy10384-5430s-projects.vercel.app` still exits 1 with `expected 715, found 57`, missing current Ebook helper wiring, and stale Swedish placeholder copy. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` first for SETUP/release infrastructure; rerun or repair the scheduled/manual production deployment path until a Production deployment from current `origin/main` exists and `npm run test:site-live` passes against that production URL without overrides.

MANAGER-build acceptance 2026-05-18T15:58+02:00
- Accepted current-main static-bank sync from PR #345 / `7876dd2` after
  rebasing the worker branch onto current `origin/main`. Clean manager
  verification passed: `node scripts/export-site-question-bank.js --check`
  with 715 questions and 13 chapters; focused static-site bank parity 2/2;
  `npm run validate:content` at 715 questions; `node --check
  site/questions.js`; `npm run test:ownership`; and `git diff --check`.
- Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` first. The deployed production URL
  still lacks current `site/` assets, and production deploy evidence remains
  required before moving to Practice width, account scope, or privacy copy.

REVIEWER-SITE-Q018-QUESTION-BANK-DRIFT-1 [product defect][2026-05-18 16:08 CEST][P0 SITE][content-sync]: Rechecked after rebasing onto current `origin/main` `9d6b55f`. The latest Production deployment `4729276163`, SHA `767c87d`, created `2026-05-18T14:00:23Z`, is now successful at `https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app`, and `SITE_LIVE_TIMEOUT_MS=30000 node scripts/check-live-site.js` against that URL exits 0 with 715 questions, Practice, wide Practice layout, Mock, Ebook, and no placeholder copy. However current `origin/main` moved after that deploy through `76b3bde` (`content: improve q018 prime minister explanation`), which touches `data/questions.ts`; `git log 767c87d..HEAD -- site scripts/check-live-site.js scripts/check-live-site.test.js tests/content-static-site-question-bank-parity.test.js data content package.json` shows that q018 content delta. Current content validates (`NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with `questions:715`), but the deployed static bank is stale again: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 1 with `site/questions.js is out of sync`, and focused static-site parity exits 1 with 1/2 passing. `node --test scripts/check-live-site.test.js` still exits 0, so the live smoke count/asset harness does not cover same-count content drift. Manager action: route DATA-INTEGRITY/SETUP to regenerate `site/questions.js` from current canonical content after q018, then rerun export-site parity and production live smoke before closing `REVIEWER-SITE-LIVE-DEPLOY-STALE-1`; do not close live deploy solely on the green Production URL while current static-bank parity is red.

MANAGER-build acceptance 2026-05-18T16:16+02:00
- Accepted UIUX `QuestionCard` source-citation composition from PR #349 /
  `529480f`. Current-main verification in a clean manager worktree passed
  `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false`,
  focused QuestionCard/source-citation/disclaimer tests 12/12,
  `npm run test:theme-discipline`, `npm run test:a11y-labels`,
  `npm run validate:content`, `npm run test:ownership`, and `git diff --check`.
- Keep SITE-P0 first. Current `origin/main` still has
  `REVIEWER-SITE-Q018-QUESTION-BANK-DRIFT-1`: clean manager evidence shows
  `validate:content` passes at 715 questions while
  `node scripts/export-site-question-bank.js --check` and focused static-site
  parity fail. DATA-INTEGRITY already owns the active repair; do not duplicate
  the source-sync work from UIUX or CONTENT.

REVIEWER-SITE-Q018-QUESTION-BANK-DRIFT-1 update [2026-05-18 16:18 CEST][P0 SITE][content-sync]: Rechecked clean current `origin/main` `2c4dce6` after the DATA-INTEGRITY guard/sync movement. Local static-site parity is now green again: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with 715 questions and 13 chapters, focused `tests/content-static-site-question-bank-parity.test.js` exits 0 with 2/2 passing, and `node --test scripts/check-live-site.test.js` exits 0 with 5/5 passing. Production still serves stale same-count content: `SITE_LIVE_TIMEOUT_MS=30000 node scripts/check-live-site.js https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` exits 0, but `sha256sum` differs for current `origin/main:site/questions.js` (`159954bb...`) vs live `/questions.js` (`5d2710be...`), and a VM comparison of q018 shows current local `why.en` begins `The Riksdag chooses the prime minister...` while live still begins `The State section says...`; full static question arrays are not equal despite both containing 715 questions. GitHub evidence: latest scheduled deploy run `26038950515` for `1376794` completed `failure`, while latest Production success remains deployment `4729276163`, SHA `767c87d`, URL `https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app`. Manager action: treat the local q018/static-bank regenerate as fixed unless parity goes red again; keep live deploy/content-drift open until Production serves current `site/questions.js`, and extend the live gate to compare a content hash or sentinel question such as q018 instead of only count/assets.

MANAGER-content -> VALIDATOR 2026-05-18T16:22:49+02:00
q114 PR #352 is still the canonical CONTENT-VERIFY path; do not duplicate it.
The existing q114 owner rebased locally in `/tmp/sct-content-verify-pane3` and
found a gate/lease conflict, not a content-quality failure: q114's
`data/additionalQuestions.ts` explanation cleanup makes generated
`site/questions.js` stale, so `validate:content` fails, while CONTENT remains
barred from `site/**`. Please decide whether SETUP/build or DATA-INTEGRITY owns
the companion static mirror sync, or explicitly authorize a bundled cross-lane
fix for q114. Keep q144 in PR #331 held unless non-site content is explicitly
leased.

MANAGER-build acceptance 2026-05-18T16:24+02:00
- Accepted `REVIEWER-SITE-PRACTICE-WIDTH-PARITY-1` from PR #338 / `c7227b6`
  and the local q018 static-bank parity/validator gate from PR #355 / `1376794`.
- Clean current-main verification passed `npm run validate:content` with
  `staticSiteQuestionBankParityValidated:true`, `node
  scripts/export-site-question-bank.js --check`, focused static-bank parity
  2/2, `node scripts/export-question-bank.js --check`, `node --check
  site/questions.js`, `node --test scripts/check-live-site.test.js` 5/5, local
  `npm run test:site-live` including `practice wide layout`, typecheck,
  ownership, and `git diff --check` over both accepted PR commits.
- Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open and first. Reviewer evidence
  now shows production can pass count/assets while still serving stale q018
  content, so the next SETUP action is current production deploy evidence plus
  production live smoke, then static privacy monetization copy.

MANAGER-build acceptance 2026-05-18T16:28+02:00
- Accepted the static-site portion of `REVIEWER-ACCOUNT-SCOPE-1` from PR #359 /
  `392b84e`. Clean current-main verification passed `npm run
  test:static-site-account-scope` 2/2, syntax checks for changed site JS and the
  guard script, typecheck, ownership, and `git diff --check 392b84e^..392b84e`.
- Do not duplicate static `site/` account cleanup. If the broader native
  account route/dependency question remains in P0 scope, route it separately;
  current SITE-P0 order is production deploy/hash evidence, then static privacy
  monetization copy.

MANAGER-build acceptance 2026-05-18T16:32+02:00
- Accepted the live question-bank hash guard from PR #361 / `701a181`.
  Verification to keep before deploy closure: `node --test
  scripts/check-live-site.test.js`, local `npm run test:site-live`, typecheck,
  ownership, and `git diff --check 701a181^..701a181`.
- Current SITE-P0 order is production deploy/current live smoke evidence, then
  static privacy monetization copy. Keep q114 as a separate VALIDATOR routing
  decision because CONTENT cannot write the required static mirror.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 16:30 CEST][P0 SITE][external/live]: Rechecked current `origin/main` `9df0988` after the live hash-guard acceptance. Production is still stale: latest Production deployment remains `4729276163`, SHA `767c87d`, created `2026-05-18T14:00:23Z`, URL `https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app`, while current main is `9df0988`. The current hash-aware live check exits 1 against that URL: question count and Practice/Mock/Ebook assets pass, but static question-bank content fails with expected hash prefix `159954bb98a3` and live hash prefix `5d2710bebf7e`. The controlled deploy path is externally blocked: Scheduled Vercel deploy runs `26039712170`, `26039467016`, `26038950515`, `26038147705`, `26037443794`, `26037143042`, and `26036978641` all completed `failure`; the latest check-run annotation for job `76547582762` reports account payment/spending-limit failure before the job starts. Manager action: keep this first in SITE-P0; restore GitHub Actions billing/spending capacity or provide an operator-verified production deployment, then rerun `SITE_LIVE_TIMEOUT_MS=30000 node scripts/check-live-site.js <production-url>` and a reviewer live parity pass before closing SITE-P0-5.

MANAGER-build acceptance 2026-05-18T16:36+02:00
- Accepted `REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1` from PR #368 /
  `c118147`. Clean current-main verification passed `npm run
  test:static-site-privacy-copy` 2/2, `node --check site/app.js`, `node --check
  scripts/static-site-privacy-copy.test.js`, source spot-checks for current
  AdSense/AdMob/consent/local-progress/29 SEK Remove Ads copy in English and
  Swedish, `npm run typecheck -- --pretty false`, `npm run test:ownership`,
  targeted Prettier, and `git diff --check`.
- Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open and first. Production deploy
  evidence from current `origin/main` plus hash-aware live smoke is now the only
  remaining SITE-P0-5 blocker in the build queue.

REVIEWER-GENERATED-TF-NATURALNESS-2 [product defect][2026-05-18 16:36 CEST][P0 TRANSLATE-COMPLETE][language]: Manager-content audit on current `origin/main` `97f3096` found generated true/false rows still publish unnatural SV/EN grammar even though the earlier generated naturalness atom was accepted. Evidence: a focused CSV scan over `content/question-bank.csv` found `badRows=162` for patterns including `Det att`, `describes that`, `It is correct that the answer is`, and `Sant eller falskt: Det är korrekt att Svaret är`. Examples include q221/q222 (`Det att folkomröstningar...` / `Politicians ... describes that referendums...`), q241/q242 (`Sweden's regions's foremost task is be/judge...`), q245/q246 (`... is an example of municipal responsibilities`), and q249/q250 (`That the head of state... describes that Sweden is a constitutional monarchy`). Static source evidence shows the generator and validator mirror still contain the problematic template roots in `lib/content/derivedQuestions.ts` and `scripts/validate-content.js` (`truthStatementSv`, `truthStatementEn`, and the `What does it mean that` civic-statement branch). Manager action: route a fresh DATA-INTEGRITY atom, not a CONTENT hand edit, to replace the generated true/false prompt templates and validator mirror, add negative guards for these patterns, regenerate `content/question-bank.csv` and any required static mirror, and verify with `npm run validate:content`, `npm run test:derived-content`, focused published-question/naturalness tests, export parity, typecheck, ownership, Prettier, and diff checks. Keep q114 duplicate-guarded separately; do not close TRANSLATE-COMPLETE while these generated rows remain published.

CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1 [product defect][2026-05-18 16:56 CEST][P0 TRANSLATE-COMPLETE][SOURCE-CITATION][content-sync]: Manager-content follow-up on PR #386 / `2010bc8` found q123 still uses learner-visible source-section phrasing in canonical content and the static mirror. Evidence on current `origin/main`: `rg -n "q123|this section|avsnittet" data/additionalQuestions.ts site/questions.js content/question-bank.csv` reports `data/additionalQuestions.ts` q123 explanations with Swedish `blandar ihop avsnittet...` and English `confuse this section...`, plus matching `site/questions.js` mirror rows. The UHR locator is still specific: `Sverige i fokus`, `En sekulär stat och ett mångreligiöst land` / `Hinduism och buddhism`, printed page 43. Manager action: VALIDATOR should explicitly lease one DATA-INTEGRITY/static-mirror atom, or another approved cross-lane path, to clean q123 explanations in canonical data and regenerate all mirrors together. Acceptance requires export parity, `node scripts/export-site-question-bank.js --check`, `npm run validate:content`, focused content/source tests, typecheck, ownership, Prettier, diff checks, and PR/merge evidence. CONTENT must not hand-edit `site/**` or publish a data-only q123 branch that leaves the static site stale.

CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1 [product defect][2026-05-18 17:07 CEST][P0 TRANSLATE-COMPLETE][content-sync]: CONTENT Pane 1 audit PR #398 / `ec0f0b3` found q124 has a learner-visible wrong-option naturalness defect in canonical content and the static mirror. Evidence on current `origin/main`: `data/additionalQuestions.ts` and `site/questions.js` contain `Midsommarafton började alltid firas på en fredag` / `Midsummer Eve began always being celebrated on a Friday`; `codex-tasks/content.txt` and `codex-tasks/validator.txt` had no prior q124 route at audit time. The UHR locator remains `Sverige i fokus`, `Traditioner och högtider` / `Sveriges nationaldag`, printed page 46. Manager action: lease one split data/static-mirror cleanup, with CONTENT owning authored q124 option wording and DATA-INTEGRITY owning regenerated `site/questions.js`, static parity, validator/export evidence, and handoff. Acceptance requires export parity, `node scripts/export-site-question-bank.js --check`, `npm run validate:content`, focused content tests, typecheck, ownership, Prettier, diff checks, and PR/merge evidence. CONTENT must not publish a data-only q124 branch that leaves static-site parity red.

REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1 reviewer confirmation [2026-05-18 16:39 CEST][P0 SITE][privacy/monetization]: Independent reviewer recheck of current `origin/main` `c118147` agrees with the manager acceptance. `npm run test:static-site-privacy-copy` exits 0 with 2/2 passing; `node --test scripts/check-live-site.test.js` exits 0 with 7/7 passing; system-Chrome local `/privacy` smoke in English and Swedish found AdSense, AdMob, consent, and Remove Ads 29 SEK copy present, stale disabled-ad/no-sales copy absent, and no console/page errors. No further static-copy action is needed; keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open for production deployment freshness/current-main evidence.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 17:17 CEST][P0 SITE][external/live]: Rechecked clean current `origin/main` `998185a` after the release-validation workflow PR-trigger and queue handoff changes. Local readiness remains green: `node --test scripts/check-live-site.test.js` exits 0 with 7/7 passing, `npm run validate:content` exits 0 with 715 questions and `staticSiteQuestionBankParityValidated:true`, and `node scripts/export-site-question-bank.js --check` exits 0 with 715 questions and 13 chapters. `git diff --name-only 034fe61..HEAD -- site scripts/check-live-site.js scripts/check-live-site.test.js tests/content-static-site-question-bank-parity.test.js data content package.json .github/workflows vercel.json` shows only `.github/workflows/release-validation.yml`; `git diff --name-only 11e60ac..ae25584` shows only `docs/parallel-sessions/journals/setup.md`; and `git diff --name-only ae25584..origin/main` shows only queue/journal files, so no current-main site/content fix supersedes the previous live-deploy blocker. Production is still stale: `SITE_LIVE_TIMEOUT_MS=30000 node scripts/check-live-site.js https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` exits 1 because static question-bank content has expected hash prefix `3c425f0ad2c7` but live hash prefix `5d2710bebf7e`, while Practice, Mock, Ebook, wide layout, and placeholder-copy asset checks pass. Manager action: keep this as the only active SITE-P0 blocker; restore deployment capacity or provide an operator-verified production deploy from current `origin/main`, then rerun the hash-aware live smoke before accepting SITE-P0-5.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 17:08 CEST][P0 SITE][external/live]: Rechecked clean current `origin/main` `6d6398f` after the UIUX top-bar a11y label merge. Local readiness is still green: `node --test scripts/check-live-site.test.js` exits 0 with 7/7 passing; `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with 715 questions and `staticSiteQuestionBankParityValidated:true`; and `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with 715 questions and 13 chapters. `git diff --name-only 998185a..HEAD -- site scripts/check-live-site.js scripts/check-live-site.test.js tests/content-static-site-question-bank-parity.test.js data content package.json .github/workflows vercel.json` is empty, so no current-main site/content/live-check source change supersedes the existing blocker. Production remains stale: `SITE_LIVE_TIMEOUT_MS=30000 NODE_OPTIONS='--v8-pool-size=1' node scripts/check-live-site.js https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` exits 1 because static question-bank content has expected hash prefix `3c425f0ad2c7` but live hash prefix `5d2710bebf7e`; Practice, wide layout, Mock, Ebook, and placeholder-copy asset checks pass. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` as the only active SITE-P0 blocker; restore deployment capacity or provide an operator-verified production deploy from current `origin/main`, then rerun the hash-aware live smoke before accepting SITE-P0-5.

CEO->VALIDATOR 2026-05-18T17:19:08+02:00
Executive decision: HOLD/add-none and P0-first acceptance. Clean
`origin/main` is now `de64960`; local readiness remains green for
`node --test scripts/check-live-site.test.js` 7/7, `npm run validate:content`
with 715 questions and static parity true, and
`node scripts/export-site-question-bank.js --check`. Production still fails the
hash-aware live check against
`https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` with expected
hash `cbb24a108686` and live hash `5d2710bebf7e`.
Immediate actions:
1. Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open. Do not accept SITE-P0-5 from
   local smoke, workflow edits, or source readiness alone; acceptance requires a
   production deploy from current `origin/main` plus passing hash-aware live
   smoke.
2. Escalate the external deploy-capacity blocker to the operator: GitHub
   Actions payment/spending capacity must be restored, or an operator-verified
   production deployment must be provided. No pane should run local `vercel`
   commands from this checkout.
3. While deploy capacity is externally blocked, accept only P0 source atoms:
   SETUP visual-smoke launch-overlay evidence, DATA-INTEGRITY generated
   true/false naturalness, and q124 data/static-mirror cleanup with CONTENT
   limited to authored wording. Production submit guard is already accepted via
   PR #407 / `0572be8`; q123 cleanup is on current main via `f15404a` and needs
   normal validator acceptance/duplicate-guarding.
4. Reject data-only q124 or generated-content branches that leave
   `site/questions.js`, `content/question-bank.csv`, validator mirrors, or
   static-site parity stale.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 17:24 CEST][P0 SITE][external/live]: Rechecked clean current `origin/main` `de64960` after #409 `feat(web): activate AdSense + fix CLI deploy path`, #410 submit-guard work, q123 static-mirror sync, UIUX account/language copy, and generated-naturalness audit updates. Local readiness remains green: `node --test scripts/check-live-site.test.js` exits 0 with 7/7 passing; `npm run validate:content` exits 0 with 715 questions and `staticSiteQuestionBankParityValidated:true`; `node scripts/export-site-question-bank.js --check` exits 0 with 715 questions and 13 chapters; and `npm run typecheck -- --pretty false` exits 0 after linking this temporary worktree to the shared dependency install. Prior focused checks in this reviewer pass also passed for the same deploy path: `npm run test:ownership`, `npm run test:static-site-privacy-copy`, `node --check site/app.js`, `node --check site/practice.js`, and `npm run test:build-config -- --test-name-pattern 'vercel|Vercel|deployment|live site|GitHub release validation workflow'` with 38/38 passing. `git diff --name-only 6d4a5fe..origin/main -- site scripts/check-live-site.js scripts/check-live-site.test.js tests/content-static-site-question-bank-parity.test.js data content package.json .github/workflows vercel.json components app lib types codex-tasks/setup.txt docs/parallel-sessions/journals/content.md docs/parallel-sessions/journals/uiux-components.md docs/parallel-sessions/journals/data-integrity.md docs/parallel-sessions/meeting_sheet.md` shows real current-main changes since the last reviewer merge (`codex-tasks/setup.txt`, `components/auth/AccountHeader.tsx`, `components/auth/AccountSection.tsx`, `components/auth/WelcomeBanner.tsx`, `components/ui/LanguagePicker.tsx`, `components/ui/LanguageToggle.tsx`, `data/additionalQuestions.ts`, `docs/parallel-sessions/journals/content.md`, `docs/parallel-sessions/journals/data-integrity.md`, `docs/parallel-sessions/journals/uiux-components.md`, `docs/parallel-sessions/meeting_sheet.md`, `package.json`, `site/ads.txt`, `site/app.js`, `site/index.html`, `site/practice.js`, `site/questions.js`, `site/styles.css`, and `vercel.json`), so this is not a queue-only recheck. Production remains stale: `SITE_LIVE_TIMEOUT_MS=30000 NODE_OPTIONS='--v8-pool-size=1' npm run test:site-live -- https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` exits 1 because static question-bank content has expected hash prefix `cbb24a108686` but live hash prefix `5d2710bebf7e`; count, Practice, wide layout, Mock, Ebook, and placeholder-copy asset checks pass. Local `gh` is not GitHub CLI (`gh --version` fails with `No such option: --version`), so this pass did not refresh Actions run metadata. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` as the only active SITE-P0 blocker; restore deployment capacity or provide an operator-verified production deploy from current `origin/main`, then rerun the hash-aware live smoke before accepting SITE-P0-5.

MANAGER-build acceptance 2026-05-18T17:36+02:00
- Accepted `REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1` from PR #425 /
  `eb3b3ac` plus formatting follow-up PR #436 / `6c26380`.
- Clean current-main verification on `origin/main` `7dd0828` passed
  `NODE_OPTIONS='--v8-pool-size=1' npm run test:screenshot-manifest` 2/2,
  targeted `prettier --check package.json scripts/visual-smoke-report.test.js
  tests/e2e/visual-smoke.spec.ts
  reports/2026-05-15-uiux-screenshots/manifest.json`, `npm run typecheck --
  --pretty false`, `npm run lint`, `npm run test:ownership`, and
  `git diff --check` for both landed visual-smoke commits.
- Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open and first. Local visual-smoke
  evidence is now accepted; SITE-P0-5 still requires production deploy capacity
  or operator-verified production deployment plus hash-aware live smoke.

REVIEWER-COMPONENT-FALLBACK-COPY-1 [product defect][2026-05-18 17:34 CEST][P0 TRANSLATE-COMPLETE][a11y/i18n]: Current `origin/main` `e5504a0` has localized Button, ProgressBar, ChapterProgressCard, and QuestionNavigator fallbacks, but `ResultSummary` defaults still speak English when screens do not pass overrides. Evidence from the clean reviewer worktree after rebasing through the QuestionNavigator localization and confirming later main changes only touched setup/content/validator docs: `components/ResultSummary.tsx` defaults `Passing line`, `Score`, `Passed`, `Needs review`, `${n}/${total} correct`, `${percent} percent correct`, and a summary `accessibilityLabel` containing `${percent} percent`. These defaults feed `accessibilityLabel` / `accessibilityValue` and visible metric labels, so a Swedish user can still hear or see English fallback copy in component-library use. Current gates do not catch the gap: `npm run test:a11y-labels` and the focused Button/ProgressBar parity tests pass after the recent fallback fixes. Manager action: lease UIUX/COMPONENTS to add settings-aware SV/EN fallback copy or required localized props for `ResultSummary`, plus focused parity tests that fail on hardcoded English defaults. Keep this separate from `ProgressBar`, `Button`, `ChapterProgressCard`, and `QuestionNavigator`, which are already localized on current main.

MANAGER-build acceptance 2026-05-18T17:52+02:00
- Accepted `REVIEWER-GENERATED-TF-NATURALNESS-2` from PR #438 / `d4a2146`;
  handoff-only evidence is updated on current `origin/main` by `f293f0e`.
- Clean manager verification in `/tmp/sct-manager-build-tf2` passed
  `npm run validate:content`, `npm run test:derived-content`, focused
  content-production/published-question/test-gate tests 13/13,
  `node scripts/export-question-bank.js --check`,
  `node scripts/export-site-question-bank.js --check`, `npm run typecheck --
  --pretty false`, `npm run test:ownership`, targeted Prettier, `git diff
  --check d4a2146^..d4a2146`, and serialized `npm run test:content --
  --test-concurrency=1` 295/295.
- Do not keep the generated true/false naturalness atom queued. Keep
  `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open because production still needs a
  current-main deploy plus hash-aware live smoke; q123/q124 authored
  content/static-mirror work remains separate from generated-template repair.
REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1 [product defect][2026-05-18 17:56 CEST][P0 TRANSLATE-COMPLETE][language][content-sync]: Post-#438 current main still publishes residual generated true/false naturalness defects in both `content/question-bank.csv` and `site/questions.js`. Evidence: VERIFY q151-q200 on `65f6452` found `q174` English `are The Baltic Sea...`, `q189`/`q190` option-fragment splices (`...har en röst var ingår...` / `...may stand for election is part...`), and `q193`/`q194` imperative/infinitive fragment subjects (`Kontakta... är...` / `Contact... is...`, `Förbjuda... är...` / `Ban... is...`). CONTENT Iteration 361 / PR #451 (`ae98060`) found broader residual generated splice defects in `q261`, `q262`, `q277`, `q278`, `q309`, `q310`, `q357`, `q358`, `q361`, `q362`, `q369`, `q381`, and `q382`, including `gäller för` / `applies to` prompt splices and fragment true/false subjects. Facts, citations, answer keys, and distractors remain source-supported; the product defect is generated learner-visible language. Manager action: route one fresh DATA-INTEGRITY atom for generator templates, validator mirror coverage, focused negative guards, regenerated CSV and static mirror, export/static parity, `validate:content`, `test:derived-content`, focused published-question/content gates, typecheck, ownership, Prettier/diff checks, PR/merge evidence, and VERIFY ledger recheck for `q174`, `q189`, `q190`, `q193`, and `q194`. Do not reopen accepted `REVIEWER-GENERATED-TF-NATURALNESS-2`; this is the residual follow-up.

REVIEWER-NATIVE-ACCOUNT-SCOPE-1 [product defect][2026-05-18 18:02 CEST][P0 scope/privacy][account]: Focused native app account-scope audit found the Expo app still ships a Supabase-backed account and sign-in surface even though `GOAL.md` lists user accounts/backend services as out of scope and `docs/architecture.md` says MVP privacy is "No user accounts" and "All data local only." Evidence from the audit worktree at `479f5bc`, rechecked after rebasing to `cb7df5a`: `app/_layout.tsx` imports `AuthProvider`, registers `(auth)` and `account` stack screens, and wraps the app in auth state; `app/(auth)/sign-in.tsx` exposes Google/Facebook/Apple sign-in plus "Sync your progress across devices and tie Remove-Ads to your account"; `app/account.tsx` writes `profiles`, reads remote entitlements, and says profile/progress/entitlement are stored in Supabase; `components/auth/AccountHeader.tsx`, `AccountSection.tsx`, and `WelcomeBanner.tsx` surface sign-in/sync/account copy on Home, Profile, and Settings; `lib/supabase.ts` creates a Supabase client and `package.json` includes `@supabase/supabase-js` plus URL polyfill dependencies. Guard gap: `npm run test:static-site-account-scope` exits 0 because it only covers `site/`; `npm run test:compliance` and focused profile/legal tests exit 0 while allowing native account copy, so current green gates do not enforce the no-account MVP scope. Other gates: `npm run typecheck -- --pretty false`, `npm run validate:content`, and `npm run test:ownership` exit 0 after linking the clean worktree to the shared dependency install. `npm run test:router-shell` still exits 1 on the already-queued `REVIEWER-ROUTE-HEADER-1` header contract, so do not treat that as this account-scope defect. Manager action: route SETUP/build to either remove the native account/auth/Supabase surface for v1.0 or get an explicit operator scope change for accounts; if removal is chosen, delete/hide `(auth)`, `account`, `AuthProvider`, AccountHeader/AccountSection/WelcomeBanner sign-in surfaces, remote entitlement reads, and Supabase runtime dependency/copy, then add a native account-scope guard that rejects account routes, OAuth provider copy, remote progress/entitlement storage copy, and Supabase imports outside release tooling. Keep the existing static-site account cleanup accepted; this is the remaining native app scope gap.

MANAGER-build acceptance 2026-05-18T18:03+02:00
- Accepted `CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1` from PR #456 /
  `67847d4`.
- Clean current-main verification in `/tmp/sct-manager-build-q124` passed:
  direct old/new q124 wording scans across `data/additionalQuestions.ts`,
  `site/questions.js`, and `content/question-bank.csv`; `npm run
  validate:content`; `node scripts/export-question-bank.js --check`; `node
  scripts/export-site-question-bank.js --check`; focused
  content/static/source tests 8/8; `node --check site/questions.js`; `npm run
  typecheck -- --pretty false`; `npm run test:ownership`; targeted Prettier;
  `git diff --check 67847d4^..67847d4`; and serialized `npm run test:content
  -- --test-concurrency=1` 295/295.
- Do not duplicate q124. Keep `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1` as
  the active DATA-INTEGRITY generated-template route; q123 remains separate
  pending normal manager acceptance.

MANAGER-build acceptance 2026-05-18T18:10+02:00
- Accepted `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1` from PR #418 /
  `f15404a`.
- Clean current-main verification in `/tmp/sct-manager-build-q123-1779120511`
  passed: direct old-wording scan for `avsnittet` / `this section` across
  `data/additionalQuestions.ts`, `site/questions.js`, and
  `content/question-bank.csv`; q123 source spot-checks preserving UHR section
  `Hinduism och buddhism` and page 43; `node scripts/export-question-bank.js
  --check`; `node scripts/export-site-question-bank.js --check`; `npm run
  validate:content`; focused content/source/static tests 23/23; `node --check
  site/questions.js`; `npm run typecheck -- --pretty false`; `npm run
  test:ownership`; targeted Prettier; `git diff --check f15404a^..f15404a`;
  and serialized `npm run test:content -- --test-concurrency=1` 298/298.
- Do not duplicate q123. Keep `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1` as
  the active DATA-INTEGRITY generated-template route.

REVIEWER-SITE-QUESTION-CITATION-DISCLAIMER-1 [product defect][2026-05-18 18:10 CEST][P0 SITE][source-citation]: The deployable static `site/` question flows carry source metadata but do not render the required source citation or not-a-real-exam disclaimer in Practice or Mock review. Static evidence: `site/questions.js` q001 includes `source: { title: "Sverige i fokus", chapter: "Landet Sverige", section: "Geografi, klimat och natur", page: 5 }`. Runtime evidence from `python3 -m http.server 8214 --bind 127.0.0.1 --directory site` plus system-Chrome Playwright: `#/practice?c=1` rendered q001 `Where is Sweden located?` and, after answering, showed feedback but `practiceAfterHasCitation:false` and `practiceAfterHasDisclaimer:false`; `#/mock?run=1` completed a 25-question run and rendered result review rows, but `mockReviewHasCitation:false` and `mockReviewHasDisclaimer:false`. Browser console/page errors were 0. Code inspection matches the runtime: `site/app.js` `smtQuizRender()` renders `q.chapter`, `q.q`, options, and `q.why` only; `site/practice.js` `renderMockResult()` renders question, selected/correct answers, and `q.why` only. Manager action: assign SETUP/site to render localized `Kalla/Source` citations from `q.source` and the independent/not-official-exam disclaimer on static Practice feedback and Mock review, then add a static browser guard proving citations/disclaimers are visible for both flows.
REVIEWER-SITE-ANSWER-SHUFFLE-1 [product defect][2026-05-18 18:11 CEST][P0 SITE][answer-order]: The deployed static site bypasses the accepted app answer-shuffle pipeline and exposes a highly concentrated correct-answer position distribution. Evidence: a direct `site/questions.js` VM load counted 705 static questions with answer indexes `[533,160,12,0]`, so slot A is correct for 75.6% of the deployed static bank, far above the 35% P0 shuffle bar. Runtime/source evidence: `site/app.js` `smtQuizRender()` renders `q.opts.map(...)` in stored order and scores against `q.answer`; `site/practice.js` `renderMockExam()` also renders `q.opts.map(...)` in stored order after `pickMockQuestions()` only shuffles question order, not answer options. Duplicate scan found the app-side `SHUFFLE-FIX` evidence and tests, but no static-site shuffle guard or reviewer defect. Manager action: assign SETUP/site or DATA-INTEGRITY/site to add deterministic per-session answer-option shuffling for static Practice and Mock flows, remap scoring/review to the shuffled display order, keep true/false order stable if required, and add a static-site guard rejecting correct-position concentration above 35%.
MANAGER-build QA 2026-05-18T18:20+02:00
- Do not accept PR #470 / `204e522` for
  `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`; it is merged but partial.
- Clean current-main spot-check in
  `/tmp/sct-manager-build-accept-tfsplice-1779121093` confirms remaining
  generated true/false residual rows in both `content/question-bank.csv` and
  `site/questions.js`: `q206`, `q237`, `q238`, `q253`, `q254`, `q265`, `q266`,
  `q270`, `q273`, `q274`, `q285`, `q286`, `q289`, `q290`, `q297`, and `q298`.
  The same recheck confirms `q174`, `q189`, `q190`, `q193`, `q194`, `q261`,
  and `q262` are fixed.
- Keep the DATA-INTEGRITY route open for a follow-up generator/template,
  validator-mirror, CSV/static mirror, focused-guard, export/static parity,
  `validate:content`, `test:derived-content`, typecheck, ownership,
Prettier/diff, PR/merge, and current-main residual-row recheck pass.
REVIEWER-SITE-SETTINGS-LANGUAGE-RERENDER-1 [product defect][2026-05-18 18:18 CEST][P0 SITE][language]: The deployable static site Settings language control changes the saved language but does not re-render active Practice or Mock exam task surfaces. Evidence from clean `origin/main` served with `python3 -m http.server 8224 --bind 127.0.0.1 --directory site` plus system-Chrome Playwright: on `#/practice?c=1`, clicking Settings -> Language -> Svenska set `{doc:"sv", saved:"sv"}` but `#quiz-stage` still showed `Where is Sweden located?`, `Source: ... p. 5`, and English answer options; `afterSettingsStillEnglish:true`, `afterSettingsHasSwedish:false`. On `#/mock?run=1`, the same Settings path set `{doc:"sv", saved:"sv"}` but `#mock-stage` still showed `MOCK EXAM`, `TIME LEFT`, `Submit`, and English question-navigation labels; `afterSettingsStillEnglishUi:true`, `afterSettingsHasSwedishUi:false`. Browser console/page errors were 0. Code inspection matches the runtime: `site/settings.js` handles `[data-set="language"]` with `applyLang(v)` only, while `site/app.js` re-renders the Practice quiz only for `.lang button[data-lang]` clicks and `site/practice.js` re-renders only the Practice hub, not active chapter quizzes or Mock exam. Manager action: assign SETUP/site to centralize language-change events so Settings language changes immediately re-render active Practice quiz and Mock landing/exam/result surfaces, then add a static browser guard for the Settings EN/SV path on `#/practice?c=1` and `#/mock?run=1`.

MANAGER-build acceptance 2026-05-18T18:25+02:00
- Accepted `REVIEWER-NATIVE-ACCOUNT-SCOPE-1` from PR #476 / `dc8fecf`.
- Clean current-main verification in
  `/tmp/sct-manager-build-route-static-site-p0-1779121359` passed `npm run
  test:native-account-scope` 4/4; `npm run typecheck -- --pretty false` after
  linking the shared dependency tree into the temp worktree; `npm run
  test:compliance`; `npm run test:router-shell` 5/5; `npm run
  test:architecture` 22/22; `npm run validate:content`; focused profile/legal
  route parity tests 8/8; `npm run lint`; `npm run test:ownership`; `npm run
  test:monetization` 21/21; `npm run test:static-site-account-scope` 2/2;
  `npm run test:static-site-privacy-copy` 2/2; export/static parity; and `git
  diff --check dc8fecf^..dc8fecf`.
- Environment notes: the first temp-worktree typecheck failed because
  `node_modules` was absent, then passed after dependency linking. Broad
  `npm run test:build-config` hung in this temp environment and was terminated,
  so it is not counted as acceptance evidence.
- Do not duplicate native account-scope work. SETUP now owns the static-site
  citation/disclaimer defect first, then static answer-option shuffling, then
  Settings language rerender, while live deploy freshness remains externally
  blocked.
REVIEWER-SITE-PRACTICE-RESULT-I18N-1 [product defect][2026-05-18 18:28 CEST][P0 SITE][language]: The static Practice completion screen leaves an English hardcoded result label in Swedish mode and renders it without spacing. Evidence from `python3 -m http.server 8215 --bind 127.0.0.1 --directory site` plus system-Chrome Playwright: after setting `smt_lang=sv`, running `#/practice?c=mix`, answering 10 questions, and reaching `.quiz__result`, the result text included Swedish copy (`ÖVNINGSRUNDA KLAR`, `Hyfsat...`, `Rätt`, `Fel`) but the third breakdown row rendered `70%score`; `document.documentElement.lang` was `sv`, `hasEnglishScore:true`, `hasSwedishScoreWord:false`, and browser console/page errors were empty. Source inspection matches the runtime: `site/app.js` `smtQuizRender()` hardcodes `<li><b>${pct}%</b>score</li>` in the result breakdown instead of a localized label. Manager action: assign SETUP/site to localize and space the Practice result percentage label (`Poäng`/`Score` or equivalent), add a static browser/i18n guard for Swedish and English completion results, and include this in the broader static question-flow language pass.

MANAGER-content->VALIDATOR 2026-05-18T18:22:23+02:00
Current `origin/main` is `673e87a`. CONTENT has no safe standalone
authored-content atom: q123 and q124 are accepted/duplicate-guarded, q144 is
still held without an explicit non-site lease, and remaining content-facing
defects are generated/static output issues. Keep
`REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1` with DATA-INTEGRITY after `204e522`;
VERIFY q251-q300 (`961607f`), q301-q350 (`5364104` / `6fda9b8`), q351-q400
(`73ef69b`), q451-q500 (`673e87a`), and current-main scans still show
generated residuals such as q206, q237, q238, q270, q297, and q298 in both
canonical and static output. I also filed
`REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1` to DATA-INTEGRITY: generated
single-choice fallback options use `Det går inte att avgöra av materialet` /
`It cannot be determined from the material` from `UNKNOWN_OPTION` in
`lib/content/derivedQuestions.ts`, mirrored by `scripts/validate-content.js`,
with 154 copies in `site/questions.js`. Validate this as DATA-INTEGRITY-owned
generator/validator/static-mirror work, not a CONTENT hand edit.
REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1 [product defect][2026-05-18 18:39 CEST][P0 SITE][mobile-nav]: The current `origin/main` static site still hides required top-level destinations on a narrow mobile viewport, so Mock exam, Ebook, and Support are not consistently reachable from the header. Evidence from clean `origin/main` commit `711b20f`: `python3 -m http.server 8217 --bind 127.0.0.1 --directory /tmp/sct-review-main-lWKrEL/wt/site` plus system-Chrome Playwright at 390x844 exited 2 with `documentScrollWidth:448`, `bodyScrollWidth:448`, `innerWidth:390`, visible nav only `Home` and `Practice`, hidden nav `Mock exam`, `Ebook`, and `Support`, and `menuButtonCount:0`; browser console/page errors were empty. Screenshot: `/tmp/sct-origin-main-mobile-nav-390.png`. This conflicts with the SITE mandate that Practice, Mock exam, Ebook, and Support are reachable in the deployed product. Manager action: assign SETUP/site to add a responsive accessible nav/menu or equivalent mobile route list, remove horizontal overflow, and add a static mobile viewport guard that proves `#/practice`, `#/mock`, `#/ebook`, and `#/support` remain reachable at <=390px width.

MANAGER-build QA 2026-05-18T18:48+02:00
- Do not accept merged commit `cda854f` for
  `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`; it remains partial.
- Evidence: clean current-main worktree `/tmp/sct-manager-di-current` passed
  `validate:content`, `test:derived-content`, export/static parity, typecheck,
  ownership, and `git diff --check cda854f^..cda854f`, but a direct scan still
  found learner-visible generated residual rows including `q317`/`q318`,
  `q325`/`q326`, `q329`/`q330`, `q333`/`q334`, `q345`/`q346`,
  `q365`/`q366`, `q373`/`q374`, `q377`/`q378`, `q389`/`q390`,
  `q393`/`q394`, and q501-q550 samples.
- Keep DATA-INTEGRITY on a follow-up that closes the full verifier row set,
  regenerates CSV/static mirrors, and reruns the required content/static/type/
  ownership/diff gates. Do not start the unknown-material option cleanup until
  this true/false residual route is accepted.
REVIEWER-SITE-QUESTION-CITATION-DISCLAIMER-1 update [2026-05-18 18:54 CEST][P0 SITE][source-citation]: Rechecked current `origin/main` `4998697` after `setup: add static question disclaimers`. The original Practice-feedback and Mock-review parts of this defect are now green, but active Mock exam questions still lack the independent/not-official-exam disclaimer. Evidence from `python3 -m http.server 8231 --bind 127.0.0.1 --directory site` plus system Chrome: after answering `#/practice?c=1`, `#quiz-stage` had `hasSource:true` and `hasDisclaimer:true` with `Independent study practice, not a real exam or an official UHR question.`; after submitting `#/mock?run=1` and expanding the first review item, the review detail also had `hasSource:true` and `hasDisclaimer:true`. However the active Mock exam question surface before submission had `hasSource:true` and `hasDisclaimer:false` while showing the current question, options, timer, and navigation. `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with 715 questions and 13 chapters. Manager action: close the Practice feedback and Mock review parts once the SETUP PR is accepted, but keep a focused SITE follow-up for active Mock exam question disclaimers if the product bar remains "disclaimer present on every question screen."
REVIEWER-SITE-ANSWER-SHUFFLE-1 update [2026-05-18 18:54 CEST][P0 SITE][answer-order]: Rechecked current `origin/main` `4998697` after the static mirror and disclaimer changes; the answer-order defect remains and the current evidence should replace the stale 705-question count. Direct VM load of `site/questions.js` reports `total:715`, `single:418`, correct-answer slot counts `[388,18,12,0]`, and max correct-position share `0.928`, still far above the accepted 0.35 shuffle bar. Source inspection still shows static Practice and Mock render options via stored `q.opts.map(...)` and score against stored `q.answer`; no static answer-order shuffle helper is present in `site/app.js` or `site/practice.js`. Static-site parity is otherwise green: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with 715 questions and 13 chapters. Manager action remains SETUP/site or DATA-INTEGRITY/site: add deterministic per-session static answer-option shuffling for Practice and Mock, remap scoring/review to displayed order, keep true/false ordering stable if documented, and add a current-count guard rejecting concentration above 35%.

MANAGER-build routing guard 2026-05-18T18:55+02:00: `task/data-integrity/unknown-material-option-1779123114` was pushed after the queue had already placed `REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1` behind `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`. Connector PR lookup found no open PR for that branch at the time of this guard. Do not accept or merge that branch unless it is explicitly re-authorized after the true/false residual row set is accepted on current `origin/main`; DATA-INTEGRITY should retask to the residual follow-up first.
REVIEWER-SITE-QUESTION-COUNT-COPY-1 [product defect][2026-05-18 19:03 CEST][P0 SITE][copy/content-sync]: The current deployable static site exports 720 questions but still tells users the product is "Free for the core 500 questions, always." Evidence from the clean reviewer branch rebased onto current `origin/main` `c9a3074`: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with `Static site question-bank parity OK (720 questions, 13 chapters)`. Runtime evidence from `python3 -m http.server 8233 --bind 127.0.0.1 --directory site` plus system-Chrome Playwright on `#/`: `window.SMT_QUESTIONS.length` is 720, `hasCore500:true`, footer copy is `An independent study tool, built by people who've taken the test themselves. Free for the core 500 questions, always.`, and browser console/page errors are empty. Static source evidence: `site/index.html` hardcodes the English footer copy, while `site/i18n-extras.js` repeats `500` in extra-language footer strings; the Swedish hero says `500+ frågor`, which is less wrong but still stale against the exact current bank. Duplicate scan found no existing static-site question-count copy defect; live-smoke count defects cover deploy/test expected counts, not user-visible product copy. Manager action: assign SETUP/site or LANGUAGE/site to make visible question-count copy derive from the static bank count or use non-numeric wording, update all localized footer/about variants consistently, and add a static copy guard that fails when visible count claims drift from `window.SMT_QUESTIONS.length`.

MANAGER-content->VALIDATOR 2026-05-18T19:05:00+02:00
Current `origin/main` is `332bfc9`. q551-q600 residual evidence is on main via
PR #505 / `7d77249`, q601-q650 residual evidence is on main via PR #503 /
`95aa330`, q651-q700 evidence is on main via PR #506 / `eb10dc5`, and
q701-q720 evidence is on main via PR #512 / `2d41d8f`. Add them to the
acceptance target for `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1` alongside
q401-q450 PR #495 and q501-q550 `f6bbeb0` evidence. This remains
DATA-INTEGRITY-owned: generator templates, validator mirrors, regenerated
canonical/static exports, focused negative guards, parity checks, typecheck,
ownership, and PR/merge evidence. CONTENT should not open generated-output
implementation branches; no remaining generated residual CONTENT audit slice
exists. Keep q123/q124 duplicate-guarded as accepted, treat q144 as landed via
PR #513 / `38e4a45`, and keep the unknown-material option defect with
DATA-INTEGRITY rather than authored content.

MANAGER-build acceptance 2026-05-18T19:05+02:00:
- Accepted `REVIEWER-SITE-QUESTION-CITATION-DISCLAIMER-1` for the original
  Practice-feedback and Mock-review scope via commit `1db08b3`. Clean
  current-main verification in `/tmp/sct-manager-build-accept-shuffle-qThIhk/wt`
  passed `npm run test:static-site-question-feedback` 2/2,
  `node --test tests/content-static-site-source-citation-parity.test.js` 3/3,
  `npm run validate:content`, `npm run typecheck -- --pretty false`, `npm run
  lint`, `npm run test:ownership`, `node --check` for `site/app.js`,
  `site/practice.js`, and `scripts/static-site-question-feedback.test.js`, plus
  diff whitespace checks. Keep the active Mock question disclaimer follow-up
  open because current reviewer evidence still shows `hasSource:true` and
  `hasDisclaimer:false` before Mock submission.
- Accepted `REVIEWER-SITE-ANSWER-SHUFFLE-1` via PR #515 / `cac0c85` plus
  handoff PR #517 / `c9a3074`. Clean current-main verification passed
  `npm run test:static-site-answer-shuffle` 4/4, `npm run test:answer-shuffle`
  7/7, `npm run validate:content` at 720 questions with
  `answerShuffleDistributionParityValidated:true` and
  `staticSiteQuestionBankParityValidated:true`, `npm run typecheck -- --pretty
  false`, `npm run lint`, `npm run test:ownership`, `node --check` for
  `site/app.js`, `site/practice.js`, and
  `scripts/static-site-answer-shuffle.test.js`, and `git diff --check
  cac0c85^..cac0c85`.
- SETUP routing: do not duplicate citation/disclaimer review-scope or static
  answer-shuffle work. Next active source atoms are
  `REVIEWER-SITE-ACTIVE-MOCK-DISCLAIMER-1`,
  `REVIEWER-SITE-SETTINGS-LANGUAGE-RERENDER-1`,
  `REVIEWER-SITE-PRACTICE-RESULT-I18N-1`, then
  `REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1`; queue
  `REVIEWER-SITE-QUESTION-COUNT-COPY-1` behind those P0 surface defects. Live
  deploy freshness remains externally blocked on production deploy
  capacity/operator evidence.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 19:13 CEST][P0 SITE][external/live]: Rechecked after the latest GitHub deployment list changed. Latest Production is now deployment `4731442202`, SHA `3be70d4`, created `2026-05-18T16:56:19Z`, target `https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app`; current `origin/main` is `916ae1b`. Clean current-main worktree verification passed `node --test scripts/check-live-site.test.js` 7/7, `node scripts/export-site-question-bank.js --check` with 720 questions and 13 chapters, and `npm run validate:content` with 720 questions and `staticSiteQuestionBankParityValidated:true`. The latest production URL is still stale: `SITE_LIVE_TIMEOUT_MS=30000 NODE_OPTIONS='--v8-pool-size=1' npm run test:site-live -- https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app` exits 1 because live serves 715 questions with hash prefix `afb9eec56629`, while current main expects 720 questions with hash prefix `57e05be047f9`; Practice hub, wide layout, Mock route, Ebook renderer, and placeholder-copy asset checks pass. Source delta from the deployed SHA to current main includes `content/question-bank.csv`, `data/additionalQuestions.ts`, `data/chapters.ts`, `package.json`, `site/app.js`, `site/practice.js`, and `site/questions.js`, so this is real product drift, not a queue-only update. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open; the production deploy moved forward but still does not serve current `origin/main`. Restore/provide a production deploy from `916ae1b` or newer, then rerun the hash-aware live smoke before accepting SITE-P0-5.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 19:22 CEST][P0 SITE][external/live]: Rechecked current `origin/main` `e55ef7f` after later setup/content handoffs landed. GitHub deployments API still reports latest Production deployment `4731442202`, SHA `3be70d4`, created `2026-05-18T16:56:19Z`, target `https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app`. Clean current-main worktree verification passed `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/check-live-site.test.js` 7/7, `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` with 720 questions and 13 chapters, and `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` with 720 questions and `staticSiteQuestionBankParityValidated:true`. The live Production URL is still stale: `SITE_LIVE_TIMEOUT_MS=30000 NODE_OPTIONS='--v8-pool-size=1' npm run test:site-live -- https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app` exits 1 because live serves 715 questions with hash prefix `afb9eec56629`, while current main expects 720 questions with hash prefix `57e05be047f9`; Practice hub, wide layout, Mock route, Ebook renderer, and placeholder-copy asset checks pass. Source delta from the deployed SHA to current main now includes `content/question-bank.csv`, `data/additionalQuestions.ts`, `data/chapters.ts`, `package.json`, `site/app.js`, `site/practice.js`, `site/questions.js`, and `site/styles.css`, so SITE-P0-5 remains external deploy drift, not a local readiness failure. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open; provide a production deploy from `e55ef7f` or newer, then rerun the hash-aware live smoke before accepting SITE-P0-5.
REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 19:48 CEST][P0 SITE][external/live]: Rechecked clean current `origin/main` `655a63a` after Practice result i18n, mobile-nav evidence, and content handoffs advanced main. GitHub deployments API still reports latest Production deployment `4731442202`, SHA `3be70d4`, created `2026-05-18T16:56:19Z`, target `https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app`. Clean current-main worktree verification passed `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/check-live-site.test.js` 7/7, `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` with 720 questions and 13 chapters, `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` with 720 questions and `staticSiteQuestionBankParityValidated:true`, and `git diff --check`. The live Production URL is still stale: `SITE_LIVE_TIMEOUT_MS=30000 NODE_OPTIONS='--v8-pool-size=1' npm run test:site-live -- https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app` exits 1 because live serves 715 questions while current main expects 720, and live hash prefix `afb9eec56629` does not match current prefix `57e05be047f9`; Practice hub, wide layout, Mock route, Ebook renderer, and placeholder-copy asset checks pass. Source delta from deployed SHA `3be70d4` to current main includes `content/question-bank.csv`, `data/additionalQuestions.ts`, `data/chapters.ts`, `package.json`, `site/app.js`, `site/practice.js`, `site/questions.js`, `site/settings.js`, and `site/styles.css`, so SITE-P0-5 remains external deploy drift, not a local readiness failure. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open; provide a production deploy from `655a63a` or newer, then rerun the hash-aware live smoke before accepting SITE-P0-5.

MANAGER-content->VALIDATOR 2026-05-18T19:20:00+02:00
Routing scan baseline was `origin/main` `c03f88f`; require a current-main
recheck before acceptance. Add `CONTENT-AUTHORED-TF-PREFIX-1` to the acceptance
target from PR #524 / `916ae1b`: 11 authored source true/false rows still
store redundant UI-afforded prefixes (`q002`, `q006`, `q023`, `q028`,
`q031`, `q047`, `q049`, `q074`, `q091`, `q094`, `q143`). Acceptance should
require authored data cleanup, regenerated canonical/static mirrors, a guard
rejecting authored `true_false` stems that start with `Sant eller falskt:` /
`True or false:`, preservation of UHR locators and answer keys, export/static
parity, `validate:content`, typecheck, ownership, Prettier/diff checks,
PR/merge evidence, and current-main recheck. Do not accept a data-only branch
that leaves `site/questions.js` stale.

Refresh `REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1` with PR #525 /
`589fda3`: current static output has 155 copies of the source-material fallback
option, and authored data/CSV do not contain that text. Keep it
DATA-INTEGRITY-owned, queued behind or bundled safely with the generated
true/false residual route. CONTENT should capacity-handoff unless an authored
cleanup is explicitly leased.

REVIEWER-SITE-FLAG-PALETTE-DRIFT-1 [product defect][2026-05-18 19:30 CEST][P0 BRAND][site/static-flag]: The static site's visible Swedish flag marks still recolor with user palette choices, violating the fixed-flag-color directive in `codex-tasks/open.txt` and `docs/parallel-sessions/design-tokens.md`. This is distinct from existing blocker `B-UIUX-COMPONENTS-FLAG-CONSTANTS-20260518`, which covers the React Native `SwedishFlagBand`/`lib/theme/flag.ts` prerequisite; the deployed `site/` has its own flag surfaces. Static source evidence on current `origin/main` `c03f88f`: `site/styles.css` renders `.brand__mark` with `background: var(--blue)` and yellow bars with `var(--gold)`, and renders `.hero__cross::before/::after` with `background: var(--blue)`; `site/settings.js` palette choices mutate `--blue` and `--gold` to non-flag palettes. Browser evidence from `python3 -m http.server 8247 --bind 127.0.0.1 --directory site` plus system Chrome/Playwright: after opening Settings and choosing palettes in both light and dark themes, `Sverigeflaggan` stays official (`brandBg rgb(0,106,167)`, bars `rgb(254,204,0)`), but `Midsommar` changes the brand flag and hero cross to green/yellow (`rgb(62,124,82)` / `rgb(244,197,66)`), `Falu` changes them to red/gold (`rgb(140,26,43)` / `rgb(233,185,74)`), `Skargarden` changes them to slate/yellow, and `Norrsken` changes them to teal/mint; browser errors were empty. Manager action: route SITE/SETUP to give the static site immutable flag colors (for example `--flag-blue:#006aa7` and `--flag-yellow:#fecc00`) and use them for every actual flag rendering (`.brand__mark`, `.hero__cross`, mascot/flag easter eggs/favicons as applicable) while keeping normal UI accents palette-driven. Add a static browser guard that cycles all palettes in light and dark mode and fails if visible flag surfaces are not official blue/yellow.
REVIEWER-SITE-PRACTICE-RESULT-I18N-1 update [2026-05-18 19:38 CEST][P0 SITE][language]: Rechecked current `origin/main` `810c091` after the Settings language rerender acceptance and static flag routing update. The defect remains and is broader than the original Swedish-only `70%score` evidence: the completed Practice result breakdown glues numeric values directly to localized labels in both supported languages. Runtime evidence from a clean current-main static serve on `http://127.0.0.1:8257`: Swedish `#/practice?c=mix` completion rendered rows `["4Rätt","6Fel","40%score"]`, with `hasGluedScore:true`, `hasEnglishScoreWord:true`, `hasSwedishScoreWord:false`, `hasGluedCorrect:true`, and `hasGluedWrong:true`; English completion rendered `["6Correct","4Wrong","60%score"]`, with `hasGluedScore:true`, `hasGluedCorrect:true`, and browser console/page errors empty. Source evidence still points to `site/app.js` result markup using `<li><b>${correct}</b>${copy.correctLabel}</li>`, `<li><b>${n - correct}</b>${copy.wrongLabel}</li>`, and `<li><b>${pct}%</b>score</li>`. Existing green checks miss this result screen: `test:static-site-settings-language` 4/4, `test:static-site-question-feedback` 3/3, and `validate:content` at 720 questions all pass. Manager action: keep this as the first active SETUP/site atom; require spaced, localized result labels for correct, wrong, and percent score in SV and EN, plus a static browser/DOM guard that fails on glued strings such as `4Rätt`, `6Correct`, or `40%score`.
REVIEWER-SITE-PRACTICE-RESULT-I18N-1 resolution recheck [2026-05-18 19:40 CEST][P0 SITE][language]: Rechecked current `origin/main` `853a13b` after SETUP commit `d372521` (`setup: localize static practice results`) landed ahead of the reviewer evidence update. This defect now has executable and browser evidence for closure. Clean current-main verification passed `npm run test:static-site-practice-result-i18n` 2/2, `npm run test:static-site-settings-language` 4/4, `npm run test:static-site-question-feedback` 3/3, and `npm run validate:content` with 720 questions and static-site parity true. Independent system-Chrome smoke against a local static serve of `site/` completed `#/practice?c=mix` in both languages: Swedish rows were `["2 Rätt","8 Fel","20% Poäng"]`, English rows were `["3 Correct","7 Wrong","30% Score"]`, with no glued result strings and no browser console/page errors. Manager action: VALIDATOR can accept `REVIEWER-SITE-PRACTICE-RESULT-I18N-1` once the SETUP source commit/handoff is reviewed; remove it from active SETUP routing and keep the next active static-site atoms as flag palette drift, mobile nav reachability, question-count copy, and live deploy freshness.
REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1 update [2026-05-18 19:43 CEST][P0 SITE][mobile-nav]: Rechecked current `origin/main` `58980df` after Practice result i18n acceptance and setup rate-limit routing. The mobile navigation defect remains current. Static source evidence: `site/index.html` has required top-level links for Home, Practice, Mock exam, Ebook, and Support, but `site/styles.css` still hides `.nav a:nth-child(n+3)` inside the `@media (max-width: 720px)` block and no replacement menu control exists. Runtime evidence from a clean current-main static serve at `http://127.0.0.1:8261/` with system Chrome at 390x844: `innerWidth:390`, `documentScrollWidth:448`, `bodyScrollWidth:448`, visible nav `["Home","Practice"]`, hidden required nav `["Mock exam","Ebook","Support"]`, `menuButtonCount:0`, `settingsVisible:true`, and browser console/page errors empty. Focused syntax/parity checks pass (`node --check site/app.js site/practice.js site/settings.js`; `export-site-question-bank --check` with 720 questions), so existing gates still do not cover narrow static-site route reachability. Manager action: keep this as the next SETUP/site atom after flag palette drift; add an accessible mobile nav/menu or equivalent reachable route list, remove horizontal overflow, and add a static <=390px browser guard proving Practice, Mock exam, Ebook, Support, and Settings are reachable.

CEO->VALIDATOR 2026-05-18T19:44:00+02:00
Staffing remains HOLD/add-none. `csup staff --scenario=resume --dry-run`
reported queued work but every configured host held with
`reason=lunarc_requires_slurm_station`; no apply was run. Local supply is still
only `_csup_sentinel_` plus the six-pane `civic-content` session. `csup
factory-audit civic-test` is RED for `missing:project_config`; treat that as a
tooling audit issue before any future factory-run/apply decision.

Acceptance/routing actions:
- Review and accept/reject `REVIEWER-SITE-PRACTICE-RESULT-I18N-1` from the
  SETUP source/handoff evidence; CEO recheck also passed
  `test:static-site-practice-result-i18n`, static export parity, content
  validation, and typecheck.
- Keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open. Current source is ready, but
  production still serves 715 questions/hash `afb9eec56629` while current main
  expects 720/hash `57e05be047f9`.
- Route SETUP next to `REVIEWER-SITE-FLAG-PALETTE-DRIFT-1`, then mobile nav
  reachability, then static question-count copy unless newer reviewer evidence
  raises a stricter P0.
- Keep DATA-INTEGRITY on `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1` from clean
  current main with q201-q720 evidence before unknown-material option cleanup.
- Keep CONTENT handoff-only unless you lease the paired authored true/false
  prefix cleanup with generated CSV/static mirror parity.

REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1 update [2026-05-18 19:56 CEST][P0 SITE][mobile-nav]: Rechecked clean current `origin/main` `db77064` after the static flag palette atom was accepted. The mobile navigation defect remains current and is now the first local SETUP/site source atom. Static source evidence still shows `site/styles.css` hides `.nav a:nth-child(n+3)` inside the `@media (max-width: 720px)` block, with no replacement menu control. Focused checks passed: `node --check site/app.js`, `node --check site/practice.js`, `node --check site/settings.js`, and `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` with 720 questions and 13 chapters. Runtime evidence from `python3 -m http.server 8267 --bind 127.0.0.1 --directory site` plus system Chrome at 390x844 exited 2 by design: `innerWidth:390`, `documentScrollWidth:448`, `bodyScrollWidth:448`, `overflowX:true`, visible nav only `["Home","Practice"]`, hidden required nav `["Mock exam","Ebook","Support"]`, `menuButtonCount:0`, `settingsVisible:false`, and browser console/page errors empty. Manager action: keep this as the next SETUP/site atom; add an accessible mobile nav/menu or equivalent reachable route list, remove horizontal overflow, and add a <=390px browser guard proving Practice, Mock exam, Ebook, Support, and Settings are reachable.

REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1 resolution recheck [2026-05-18 20:04 CEST][P0 SITE][mobile-nav]: Rechecked clean current `origin/main` `bc20f8c` after SETUP commit `8e9b49c` (`setup: fix static mobile nav reachability`) landed. This defect now has executable and browser evidence for closure. `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' npm run test:static-site-mobile-nav` exits 0 with 1/1 passing. Independent system-Chrome smoke at 390x844 against a local static serve of `site/` showed `documentScrollWidth:390`, `bodyScrollWidth:390`, visible `Open navigation` and `Settings` controls, and after opening the menu visible 336x44 links for Home, Practice, Mock exam, Ebook, and Support; browser console/page errors were empty. Manager action: VALIDATOR can accept `REVIEWER-SITE-MOBILE-NAV-REACHABILITY-1` once the SETUP source commit/handoff is reviewed; keep question-count copy and live deploy freshness open.

REVIEWER-SITE-QUESTION-COUNT-COPY-1 update [2026-05-18 20:04 CEST][P0 SITE][copy/content-sync]: Rechecked clean current `origin/main` `bc20f8c`; the user-visible static-site count-copy defect remains open. `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with `Static site question-bank parity OK (720 questions, 13 chapters)`, but runtime system-Chrome smoke against `#/` reports `window.SMT_QUESTIONS.length:720` while the footer still renders `An independent study tool, built by people who've taken the test themselves. Free for the core 500 questions, always.` Source scan also still finds the hardcoded copy in `site/index.html`; no static count-copy guard is present in `package.json` or `scripts/`. Browser console/page errors were empty. Manager action: keep `REVIEWER-SITE-QUESTION-COUNT-COPY-1` queued for SETUP/site or LANGUAGE/site; visible count claims should derive from `window.SMT_QUESTIONS.length` or use non-numeric wording, with a guard rejecting stale hardcoded 500-copy variants.
REVIEWER-SITE-CHAPTER-COUNT-COPY-1 [product defect][2026-05-18 20:14 CEST][P0 SITE][copy/content-sync]: Rechecked current `origin/main` `60f5827` after `setup: fix static question count copy`. The stale 500/500+ question-count copy is gone, but the deployed static Home copy still promises twelve chapters while the generated metadata has 13. Static-site parity is green at 720 questions and 13 chapters; `node --check site/app.js site/practice.js site/settings.js` and `git diff --check` pass. Runtime system-Chrome smoke against a local static serve of `site/` showed English Home renders `Twelve chapters, sized so you can finish one between trams...` and Swedish Home renders `Tolv kapitel, lagom korta...`, while both states report `window.SMT_CHAPTERS_META.length === 13` and no stale `500` claim. Browser console/page errors were empty. Manager action: route SETUP/site or LANGUAGE/site to update static chapter-count copy in `site/index.html`, `site/app.js`, and localized variants, then add a static guard that rejects visible chapter-count claims drifting from `window.SMT_CHAPTERS_META.length`. Keep this separate from the already-landed question-count copy source atom.
REVIEWER-SITE-CHAPTER-COUNT-COPY-1 resolution recheck [2026-05-18 20:25 CEST][P0 SITE][copy/content-sync]: Rechecked clean current `origin/main` `0b26519` after SETUP commit `0b26519` (`setup: fix static chapter count copy`) landed. This defect now has executable and browser evidence for closure. `NODE_OPTIONS='--v8-pool-size=1' npm run test:static-site-chapter-count-copy` exits 0 with 2/2 passing; `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules NODE_OPTIONS='--v8-pool-size=1' node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; `node --check site/app.js site/practice.js site/settings.js scripts/static-site-chapter-count-copy.test.js`, `npm run typecheck -- --pretty false`, and `git diff --check` pass. Independent system-Chrome Home smoke against a local static serve showed `window.SMT_CHAPTERS_META.length:13`, English overview list count `13` ending at `13 Traditions, holidays & everyday culture`, Swedish overview list count `13` ending at `13 Traditioner, helgdagar & vardagskultur`, and no browser console/page errors. Manager action: VALIDATOR can accept `REVIEWER-SITE-CHAPTER-COUNT-COPY-1` once the SETUP source commit/handoff is reviewed; keep live deploy freshness external until production serves current main.

REVIEWER-GENERATED-TF-PREFIX-SURFACE-1 [product defect][2026-05-18 20:31 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Current `origin/main` `50aca39` still publishes redundant true/false prefixes in generated/static question output, violating `docs/content/wording-rules.md` and the project AGENTS.md content bar that says the UI affords true/false mode and stems must not start with `True or false:` / `Sant eller falskt:`. Static output evidence from `site/questions.js`: VM inspection found `299` true-false questions and all `299` start with those prefixes. It also found `11` meta-stem offenders that stack the prefix with `Påståendet är sant:` / `The statement is true:`: `q150`, `q166`, `q234`, `q254`, `q267`, `q330`, `q338`, `q438`, `q506`, `q518`, and `q714`. Source evidence: `content/question-bank.csv` publishes the same prefix/meta stems; `lib/content/derivedQuestions.ts` and the validator mirror in `scripts/validate-content.js` still wrap generated true-false variants with the true/false prefix and build source true/false variants through `sourceStatementJudgement*`. Gate evidence: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 and reports `questionGeneratedTrueFalseNaturalnessValidated: 720`; `npm run test:derived-content` exits 0 with 5/5 passing; static question/chapter count guards also pass. Manager action: route a DATA-INTEGRITY generator/validator/static-mirror atom to strip the redundant true/false prefix from canonical and static true-false stems, collapse `The statement is true:` / `Påståendet är sant:` generated source variants into direct propositions, add a negative guard covering canonical CSV and `site/questions.js`, regenerate exports, and rerun export/static parity, `validate:content`, `test:derived-content`, focused published-question/content gates, typecheck, lint, ownership, Prettier/diff checks, PR/merge evidence, and a current-main spot check for the 11 named meta rows plus total prefix count.

REVIEWER-GENERATED-TF-PREFIX-SURFACE-1 update [2026-05-18 20:37 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `476afdb` after `data-integrity: remove generated true-false meta stems`. The question-stem meta layer is now gone: VM inspection of `site/questions.js` found `questionMetaOffenders:0` for `Påståendet är sant:` / `The statement is true:` in `q.q`. The redundant true/false prefix defect remains fully open: the same inspection found `720` total questions, `299` true-false questions, and `299` prefix offenders beginning with `Sant eller falskt:` / `True or false:`; samples include `q002`, `q006`, `q023`, `q028`, `q031`, `q047`, `q049`, `q074`, `q091`, `q094`, `q143`, `q146`, `q147`, `q150`, and `q151`. `content/question-bank.csv` still publishes the same prefixes. Existing gates miss this wording-rule violation: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated: 720`; `npm run test:derived-content` exits 0 with 5/5 passing; focused published-question/sentence/source tests exit 0 with 23/23 passing; and both export parity checks exit 0 for 720 questions. Manager action: narrow the next DATA-INTEGRITY route to stripping the UI-afforded true/false prefix from canonical and static true-false stems, updating generator + validator mirror coverage, adding a negative guard for generated and authored true-false prefixes across `content/question-bank.csv` and `site/questions.js`, regenerating exports, and rerunning export/static parity plus the content/type/lint/ownership/diff gates before acceptance. Do not treat PR #476afdb as closure for this reviewer defect.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 20:43 CEST][P0 SITE][external/live]: Rechecked clean current `origin/main` `beb6dfa` after the latest setup/content handoff commits. Local readiness remains green: `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; `node --test scripts/check-live-site.test.js` exits 0 with 7/7 passing; `npm run validate:content` exits 0 with 720 questions and `staticSiteQuestionBankParityValidated:true`; and `npm run typecheck -- --pretty false` exits 0 after linking the existing dependency install into the clean temporary worktree. Production is still stale. The current latest-known production URL `https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app` fails `SITE_LIVE_TIMEOUT_MS=30000 NODE_OPTIONS='--v8-pool-size=1' npm run test:site-live -- <url>` on static bank count and hash: expected 720/hash `7a461698b05c`, found 715/hash `afb9eec56629`; Practice, wide layout, Mock, Ebook, and placeholder-copy checks pass. The previous production URL `https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` also fails the same smoke with expected 720/hash `7a461698b05c`, found 715/hash `5d2710bebf7e`. Manager action: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open as external deploy drift; provide or restore a production deployment from `beb6dfa` or newer, then rerun the hash-aware live smoke before accepting SITE-P0-5. Do not treat local green gates as live closure.

MANAGER-content->VALIDATOR 2026-05-18T20:45:00+02:00
Current `origin/main` is `47f3983` after PR #609 refreshed q151-q200 VERIFY.
Use that as the current routing baseline: q151-q200 old splice residuals are
stale, but the global generated true/false prefix surface remains open, and
new current evidence adds generated false-answer explanation mismatches in
q151, q167, q235, q255, q331, q339, q439, q507, q519, and q715. The
negative/not-true stem wording surface also remains current for q151, q167,
q235, q255, q266, q331, q339, q439, q507, q519, and q715, and q666/q667/q699
residual grammar remains open. Route one DATA-INTEGRITY generated
true/false cleanup if possible, with generator/validator/static mirrors and
guards covering prefix removal, false-answer explanations, negative/not-true
stems, and q666/q667/q699 grammar. Keep CONTENT source implementation
handoff-only unless you explicitly lease the paired `CONTENT-AUTHORED-TF-PREFIX-1`
authored-data plus mirror/guard cleanup. Do not reopen unknown-material option
work unless a fresh current-main scan proves regression.

REVIEWER-SITE-ACTIVE-MOCK-DISCLAIMER-1 resolution recheck [2026-05-18 20:48 CEST][P0 SITE][source-citation]: Rechecked clean current `origin/main` `1659ccf` after the SETUP active-Mock disclaimer follow-up and later reviewer/live updates. The focused active Mock residual from `REVIEWER-SITE-QUESTION-CITATION-DISCLAIMER-1` is now green. `npm run test:static-site-question-feedback` exits 0 with 3/3 passing, including active Mock; `node --test tests/content-static-site-source-citation-parity.test.js` exits 0 with 3/3 passing; `npm run validate:content` exits 0 with 720 questions, `questionDisclaimerCopyValidated:true`, and `staticSiteQuestionBankParityValidated:true`; and `npm run typecheck -- --pretty false` exits 0. Independent system-Chrome smoke against locally served current `site/` at `#/mock?run=1` found active Mock stage visible, active question/options present, source citation `Source: Sverige i fokus, Sverige och omvärlden, Nordiskt samarbete, p. 39`, disclaimer `Independent study practice, not a real exam or an official UHR question.`, and no browser console/page errors. Manager action: VALIDATOR can close the active-Mock disclaimer residual using SETUP source evidence plus this reviewer recheck; keep production live deploy freshness and generated true/false prefix wording open separately.

REVIEWER-GENERATED-TF-STANDALONE-Q601-Q650-CURRENT-1 [product defect][2026-05-18 22:30 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `9d40c89` after q451-q500 source repair, q501/q551 reviewer queueing, latest VERIFY/chapter audit commits, and release privacy gate updates. The old q601-q650 VERIFY residual list is now stale or too broad: direct VM inspection of `site/questions.js` found 50 q601-q650 rows, 25 true/false rows, zero `Sant eller falskt:` / `True or false:` prefix offenders, zero true/false stem meta offenders, and zero generated unknown-material fallback options in the slice. Current remaining q601-q650 generated wording defects are seven true/false rows mirrored by the static bank: `q606` has ungrammatical parallel structure in both languages (`protects the right ... and protection ...` / `skyddar rätten ... och skydd ...`); `q607` uses unnatural `protects that the state chooses...` / `skyddar att staten väljer...`; `q622` says Jews gained the right to live in `the country` / `landet` without naming Sweden; `q626` and `q627` say examples are mentioned `in Swedish Muslim congregations` without saying examples of traditions/directions; and `q638`/`q639` use the context-dependent referent `The event from 1523` / `Händelsen från 1523`. Existing gates miss these residuals: `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 30/30 passing; and `npm run typecheck -- --pretty false` exits 0. Manager action: keep q601-q650 behind q451-q500, q501-q550, and q551-q600 source routing unless VALIDATOR reorders, then route DATA-INTEGRITY to a focused q601-q650 naturalness/standalone cleanup for `q606`, `q607`, `q622`, `q626`, `q627`, `q638`, and `q639`, with generator and validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and current-main spot checks proving the seven rows are grammatical, standalone, and referent-clear.

MANAGER-build->VALIDATOR 2026-05-18T20:50:00+02:00
Acceptance guard for `REVIEWER-GENERATED-TF-PREFIX-SURFACE-1`: PR #618 /
`9f007d8` should count only as partial negative/true meta progress, not route
closure. Focused VM inspection after #618 still found `299/299` true/false
stems with `Sant eller falskt:` / `True or false:` prefixes in
`site/questions.js`. VALIDATOR acceptance for the active route requires zero
generated/authored prefix offenders in canonical CSV and static output,
aligned generated false-answer explanations, export/static parity, content
validators, typecheck/lint/ownership, PR/merge evidence, and a clean
current-main recheck.

REVIEWER-GENERATED-TF-PREFIX-SURFACE-1 update [2026-05-18 20:55 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `9f007d8` after `data-integrity: clean generated true-false negative stems`. The stem-level meta wording is improved: VM inspection of `site/questions.js` found `0` true/false stems containing `Påståendet är sant:`, `The statement is true:`, `Det är inte sant att`, or `It is not true that`; the same CSV scan found `metaStemRows:0`. The P0 wording defect remains open and narrower: `site/questions.js` has `720` total questions, `299` true/false questions, and all `299` still begin with `Sant eller falskt:` / `True or false:`; `content/question-bank.csv` likewise has `299` true_false rows and `299` prefix rows. The false-answer explanation mismatch also remains for `q151`, `q167`, `q235`, `q255`, `q331`, `q339`, and `q715`: those rows have `answer:1` / false while their explanations still begin `The statement is true` / `Påståendet är sant`. The q666/q667/q699 residual grammar/naturalness evidence is still current in the question stems (`common to eating`, `common to lighting`, and capitalized `The arrival of spring`). Existing gates still miss these issues: `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated: 720`; `npm run test:derived-content` exits 0 with 6/6 passing; and `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters. Manager action: keep the DATA-INTEGRITY route open but narrow current acceptance to stripping redundant true/false prefixes from canonical/static output, correcting the seven false-answer explanations, fixing q666/q667/q699 stem grammar/capitalization, and adding validator/static guards so these failures cannot remain green.
REVIEWER-SITE-SOURCE-PROVENANCE-COPY-1 [product defect][2026-05-18 21:03 CEST][P0 SITE][legal/copy]: Focused static-site Terms/Sources pass on clean `origin/main` `75310c8` found the deployed source-provenance copy overclaims the current bank. Runtime `/terms` says `Questions are written from public sources - Riksdagen, Skolverket, statistical reports - and reviewed by humans`; runtime `/sources` shows `Primary sources 8` and lists Riksdagen, Regeringen.se, Migrationsverket, Skolverket, SCB, Folkhalsomyndigheten, Nationalencyklopedin, Institutet for sprak och folkminnen, and UHR. The actual shipped static question bank has 720 questions and only one source title: `Sverige i fokus`; Phase-B external-authority work is explicitly deferred in `codex-tasks/open.txt`, so this copy misleads learners and store/legal reviewers about current provenance. Evidence: `node --test tests/content-static-site-question-bank-parity.test.js` passed 2/2; `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` passed with 720 questions and static-site parity true; direct VM scan of `site/questions.js` returned `{"count":720,"titles":["Sverige i fokus"]}`; system-Chrome runtime pass against locally served `site/` returned `termsHasExternalAuthorityClaim:true`, `sourcesPrimarySources:"Primary sources 8"`, the external source-claim list above, `sourceTitleRuntime:["Sverige i fokus"]`, and browser console/page errors `[]`. Queue helper `/home/billy/Desktop/projects/.shared/review-to-queue.sh` is absent, so REVIEWER queued directly here per prior lane fallback. Manager action: assign SETUP/site copy work to make Terms/Sources describe the current UHR/Sverige i fokus provenance honestly, remove or clearly future-label external-authority source families until those questions exist with visible provenance labels, and add a static guard comparing `site/questions.js` source titles to the public source-page claims so green content/static parity cannot mask this mismatch.

CEO->VALIDATOR 2026-05-18T20:58:20+02:00
Staffing remains HOLD/add-none. Fresh `csup staff --scenario=resume --dry-run`
reported queued work (`work=8`, `blockers=8`) for every configured host, but
all hosts held with `reason=lunarc_requires_slurm_station`; no apply was run.
Local supply is still only `_csup_sentinel_` plus the six-pane
`civic-content` session. `csup factory-audit civic-test` remains RED for
`missing:project_config`.

Acceptance/routing actions:
- Treat PR #618 / `9f007d8` as partial DATA-INTEGRITY progress only. Current
  `origin/main` `75310c8` still has 299/299 true/false prefix offenders in
  `site/questions.js`, while stem-level meta offenders are now 0.
- Keep `REVIEWER-GENERATED-TF-PREFIX-SURFACE-1` open until canonical CSV and
  static output have zero `Sant eller falskt:` / `True or false:` prefixes,
  zero meta wrappers, aligned false-answer explanations, regenerated mirrors,
  export/static parity, content validators, typecheck/lint/ownership, PR/merge
  evidence, and a clean current-main recheck.
- Route DATA-INTEGRITY next to the generated/static true/false cleanup bundle
  covering prefix removal, the remaining reviewer/content false-answer
  explanation mismatches, and q666/q667/q699 grammar. Do not reopen
  unknown-material option cleanup without fresh current-main regression
  evidence.
- Keep SETUP source-held unless fresh P0 site evidence appears. The fresh live
  smoke still blocks only on production serving stale 715-question banks while
  current main expects 720/hash `ead3e32bf91d`.
- Keep CONTENT implementation handoff-only unless you explicitly lease the
  paired `CONTENT-AUTHORED-TF-PREFIX-1` cleanup with DATA-INTEGRITY-owned
  generated CSV/static mirror and guard coverage.

REVIEWER-SITE-EBOOK-CH13-COVERAGE-1 [product defect][2026-05-18 21:09 CEST][P0 SITE][content-sync]: Focused static Ebook pass on clean `origin/main` `6c5d2b4` found the Ebook reader still covers only intro plus chapters 1-12 even though the shipped static bank and Home chapter metadata now have 13 chapters. Static evidence: `site/index.html` ebook nav has `data-eb="intro"` through `data-eb="12"` only, `site/ebook.js` `ORDER` is `["intro","1",...,"12"]`, `PRACTICE_LINKS` has no `"13"`, and `tests/content-static-site-ebook-parity.test.js` hardcodes the same intro/1-12 `chapterIds`, so the guard passes while missing chapter 13. Current bank evidence: `node scripts/export-site-question-bank.js --check` reports `720 questions and 13 chapters`, and VM inspection of `site/questions.js` reports `chapterMetaCount:13`, `ch13QuestionCount:145`, with chapter 13 titled `Traditions and holidays` / `Traditioner och högtider`. Runtime evidence from a local static serve plus system Chrome: `#/ebook` showed no chapter-13 nav item, and direct `#/ebook?c=13` fell back to intro (`readerCrumb:"How to read this book"`, active nav `intro`) with no traditions/holidays reader body and no console errors. Existing gates still pass: `npm run test:static-site-chapter-count-copy` 2/2 and `node --test tests/content-static-site-ebook-parity.test.js` 2/2. Manager action: route SETUP/site or CONTENT-supported site work to add an Ebook chapter 13 reader/nav/order/pager/practice link for the current traditions/holidays chapter, update the static ebook parity guard to derive or assert all 13 shipped chapters, and add a runtime/static check so `#/ebook?c=13` cannot silently fall back to intro.

REVIEWER-GENERATED-TF-PREFIX-SURFACE-1 update [2026-05-18 21:18 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `eeeffb9` after `data-integrity: strip true-false stem prefixes` and the follow-up auto WIP flush. The redundant UI-afforded prefix surface is now clear in `site/questions.js`: VM inspection found `720` questions, `299` true-false rows, `0` `Sant eller falskt:` / `True or false:` prefix offenders, and `0` true/false stem meta offenders. The previous q666/q667/q699 grammar/capitalization residual is also clear in the question stems (`q666` now starts `On Easter Saturday, it is common to eat...`, `q667` starts `On Easter Saturday, it is common to light...`, and `q699` starts `Christmas traditionally celebrates the arrival of spring...`). The remaining generated true/false defect is false-answer explanation mismatch: `q151`, `q167`, `q235`, `q255`, `q331`, `q339`, `q439`, `q507`, and `q715` have `answer:1` / False while their explanations still describe the underlying positive statement as true or say the statement is true. Existing gates do not catch this residual: `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated: 720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 25/25 passing. Manager action: treat the prefix-removal and q666/q667/q699 grammar pieces as accepted candidates after DATA-INTEGRITY source/PR review, but keep or split the remaining P0 route as generated false-answer explanation alignment. Acceptance needs generator/validator/static guard coverage proving false-answer true/false variants do not tell the learner that True/Sant is correct, regenerated static output if changed, export/static parity, `validate:content`, `test:derived-content`, typecheck/lint/ownership, PR/merge evidence, and current-main spot checks for the nine named rows.

REVIEWER-SITE-EBOOK-CH13-COVERAGE-1 resolution recheck [2026-05-18 21:26 CEST][P0 SITE][content-sync]: Rechecked clean current `origin/main` `a1d5921` after SETUP landed `setup: add static ebook chapter 13`. The user-visible coverage defect has behavioral closure evidence, but full source acceptance still needs the manager-recorded formatting follow-up in `codex-tasks/setup.txt`. `node --test tests/content-static-site-ebook-parity.test.js` exits 0 with 3/3 passing, including full static chapter coverage. `node scripts/export-site-question-bank.js --check` exits 0 with `720 questions, 13 chapters`; `npm run validate:content` exits 0 with static-site parity true; `npm run typecheck -- --pretty false`, `node --check site/ebook.js site/app.js site/questions.js`, and `git diff --check` all exit 0. Direct static scan reports `chapterMetaCount:13`, `ch13QuestionCount:145`, `indexNavHas13:true`, `ebookOrderHas13:true`, and `practiceLinksHas13:true`. System-Chrome runtime smoke against locally served `#/ebook?c=13` at 390px reports crumb `Chapter 13 - Traditions`, heading `Traditions, holidays, and change.`, active nav id `13`, a `#/practice?c=13` link, reader traditions/holidays body present, no intro fallback in the reader, no horizontal overflow, and browser errors `[]`. Manager action: treat this as reviewer behavioral-resolution evidence only; require the SETUP formatting follow-up and targeted Prettier evidence before fully accepting `REVIEWER-SITE-EBOOK-CH13-COVERAGE-1`. Keep production live deploy freshness and generated false-answer explanation alignment open separately.

REVIEWER-RELEASE-GATES-1 update [2026-05-18 21:34 CEST][P0 COMPLY][release/privacy]: Rechecked clean current `origin/main` `dff1c24` after the recent site and generated-content fixes. The app/source side now has the ad-supported v1.0 primitives (`lib/monetization/ads.ts` has `REAL_ADS_ENABLED` without `REAL_ADS_ENABLED_FOR_V1 = false`, and `app`/`components`/`lib` expose Remove Ads, 29 SEK, restore, and persisted `removeAds` paths), but publishing privacy/data-safety docs and the publishing test still assert the old disabled-ads MVP contract. Evidence: `rg -n "Data Not Collected|No user data collected|No user data shared|real ads disabled|REAL_ADS_ENABLED_FOR_V1|test app IDs|Current MVP|purchase SDK|Current answer" publishing/privacy-labels.md publishing/google-play-data-safety.md scripts/publishing.test.js` finds `publishing/privacy-labels.md` saying `Data Not Collected`, `REAL_ADS_ENABLED_FOR_V1` is `false`, and no tracking request; `publishing/google-play-data-safety.md` says no user data is collected/shared and real ads are disabled; `scripts/publishing.test.js` still requires those stale strings. `npm run test:publishing` exits 0 with 7/7 passing, so the green publishing gate now proves the wrong contract. The broad GOAL step-7 grep still exits 0 only because generic words such as `tracking` / `advertising` are present; it does not verify the required ad-supported + Remove Ads IAP disclosures. Manager action: route RELEASE or SETUP/COMPLY to update `publishing/privacy-labels.md`, `publishing/google-play-data-safety.md`, and `scripts/publishing.test.js` to the current Google Mobile Ads + Remove Ads non-consumable IAP + ATT/UMP consent posture, with a guard that fails on `REAL_ADS_ENABLED_FOR_V1`, `real ads disabled`, or blanket `Data Not Collected`/`No user data collected` claims unless a current binary-reviewed evidence file justifies them. Do not accept release/privacy closure on the current passing `test:publishing`.

REVIEWER-GENERATED-TF-STANDALONE-Q351-Q400-CURRENT-1 update [2026-05-18 21:42 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean source baseline `origin/main` `120d440` after DATA-INTEGRITY landed `data-integrity: clean q301-q350 true-false wording` and earlier false-explanation fixes; subsequent rebases only added queue/journal/verify docs, and a repeated VM spot scan produced the same static-bank counts. The q301-q350 standalone-stem route is green in the static bank: direct VM inspection of `site/questions.js` found the known q301-q350 true/false rows now publish standalone propositions, with zero true/false prefix offenders, zero true/false stem meta offenders, and zero false-answer explanation offenders across all 720 questions. The q351-q400 defect is narrower than the current VERIFY note: q351 now names the UN declaration and is no longer a standalone-referent offender. Remaining current offenders are q358 and q359 in Swedish only (`Det...` does not name violence in close relationships / honour-related violence and oppression) and q398/q399 in both Swedish and English (`De` / `They` does not name trade unions). Existing gates now pass while missing those four learner-visible standalone defects: `npm run validate:content` exits 0 with `questionFalseAnswerExplanationsValidated:720`; `npm run test:derived-content` exits 0 with 6/6; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 27/27; `npm run typecheck -- --pretty false` exits 0; `git diff --check` exits 0 before queue edits. Manager action: accept q301-q350 and false-explanation closure only after DATA-INTEGRITY source/PR evidence review, then route DATA-INTEGRITY to a focused q351-q400 standalone-stem follow-up for q358/q359/q398/q399 with generator + validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and a current-main spot check proving the four rows name their referents directly.

CEO->VALIDATOR 2026-05-18T21:47:00+02:00
Staffing remains HOLD/add-none. Fresh `csup staff --scenario=resume --dry-run`
reported queued work (`work=8`, `blockers=8`) for every configured host, but
all hosts held with `reason=lunarc_requires_slurm_station`; no apply was run.
`csup factory-audit civic-test` is still RED for `missing:project_config`.

Acceptance/routing actions:
- Treat q301-q350 generated wording as accepted by MANAGER-build on
  current `origin/main`; do not duplicate `120d440` unless a fresh regression
  appears.
- Keep DATA-INTEGRITY on the q351-q400 generated follow-up next: current
  reviewer evidence narrows the current route to q358, q359, q398, and q399
  standalone-referent defects.
- Keep SITE-P0-5 open. Current main passes `export-site-question-bank --check`
  and `validate:content`, but both known production URLs still serve 715
  questions while current main expects 720/hash `69bfcfe3cc12`.
- Route `REVIEWER-RELEASE-GATES-1` as the next non-site P0 source atom for
  RELEASE/COMPLY or SETUP/COMPLY: update `publishing/privacy-labels.md`,
  `publishing/google-play-data-safety.md`, and `scripts/publishing.test.js` to
  the current ads + Remove Ads IAP + consent posture.
- Do not restaff source-provenance copy or Ebook chapter 13; both have
  manager acceptance evidence on current main.

REVIEWER-GENERATED-TF-STANDALONE-Q401-Q450-CURRENT-1 [product defect][2026-05-18 21:52 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `e1ae95a` for the next generated true/false slice after the q301-q350 cleanup and q351-q400 narrowing. Most older q401-q450 defects are now clear: q402/q403, q410, q414/q415, q426/q427, q430/q431, q434/q435, q438/q439, and q450 publish direct propositions with answer-aligned explanations. The remaining current q401-q450 true/false offenders are `q406`, `q407`, `q411`, `q446`, and `q447`. `q406` and `q407` start with `One reason is...` / `En anledning är...` without naming labour-market laws, so they are not standalone true/false propositions outside the preceding source question. `q411` is still a fragment (`An authority that judges work environment crimes.` / `En myndighet som dömer i arbetsmiljöbrott.`). `q446` and `q447` start with `One reason...` without naming 19th-century population growth, and `q447` also renders the visible typo `eU membership`. Evidence: direct VM inspection of `site/questions.js` found 720 total questions, 25 q401-q450 true/false rows, and exactly those five current offenders. Existing gates miss this residual: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 with `questionFalseAnswerExplanationsValidated:720` and `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 27/27 passing; and `git diff --check` exits 0 before queue edits. Manager action: route DATA-INTEGRITY to a focused q401-q450 standalone-stem cleanup for q406/q407/q411/q446/q447 with generator and validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and a current-main spot check proving the five rows name their referents directly and `EU` is capitalized correctly.

REVIEWER-GENERATED-TF-STANDALONE-Q351-Q400-CURRENT-1 resolution recheck [2026-05-18 22:00 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `67846c5` after DATA-INTEGRITY landed `data-integrity: clean q351-q400 true-false wording`; this reviewer note was then rebased over MANAGER-build acceptance `18e20de`. The previously narrowed q351-q400 standalone-stem defect is behaviorally closed. Direct VM inspection of `site/questions.js` found 720 total questions, 25 q351-q400 true/false rows, zero `Sant eller falskt:` / `True or false:` prefix offenders in the slice, zero true/false stem meta offenders, and zero false-answer explanation offenders across the full 720-question static bank. Spot checks show q358/q359 now name violence in close relationships and honour-related violence/oppression directly, and q398/q399 now name trade unions directly in both Swedish and English. Gates pass on the clean worktree: `npm run validate:content` exits 0 with `questionFalseAnswerExplanationsValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 28/28 passing, including the residual q351-q400 wording guard; `npm run typecheck -- --pretty false` exits 0; and `git diff --check` exits 0 before queue edits. Manager action: no further q351-q400 routing is needed unless a fresh current-main regression appears; keep the separate q401-q450 standalone-stem route open for q406/q407/q411/q446/q447.

CEO->VALIDATOR 2026-05-18T22:03:51+02:00
Staffing remains HOLD/add-none. This cycle's `csup staff --scenario=resume
--dry-run` reported queued work (`work=9`, `blockers=9`) for every configured
host, but all hosts held with `reason=lunarc_requires_slurm_station`; no apply
was run. `csup factory-audit civic-test` remains RED for
`missing:project_config`.

Acceptance/routing actions:
- q351-q400 DATA-INTEGRITY source repair is accepted by MANAGER-build and
  reviewer resolution evidence is landed on current main. Route DATA-INTEGRITY
  next to q401-q450 standalone cleanup using q401 evidence from
  `3582841`/`1831089`.
- Treat q401-q450 VERIFY/reviewer evidence as current unless rejected; do not
  allow duplicate q401 branches to publish further.
- Keep q451-q500 reserved to Pane 3 recovery and require a current-main recheck
  after `9270ff3` before PR/merge. Route q501-q550 only after q451 lands,
  rejects, or is explicitly abandoned.
- Keep SITE-P0-5 open until production serves current `origin/main` and the
  hash-aware live smoke passes. No Vercel CLI from this repo.

MANAGER-build->VALIDATOR 2026-05-18T22:20:00+02:00
Accept q401-q450 DATA-INTEGRITY source repair on current `origin/main`: PR
#690 / `85fe297` closes `REVIEWER-GENERATED-TF-STANDALONE-Q401-Q450-CURRENT-1`
for q406/q407/q411/q446/q447. Manager recheck passed content validation,
derived-content tests, focused published/static/content-gate tests 27/27,
canonical/static export parity, `node --check site/questions.js`, typecheck,
lint, ownership, targeted Prettier, diff whitespace, and a direct static-bank
spot scan with zero residual referent/fragment/`eU membership` offenders.

Keep q451-q500 as the next DATA-INTEGRITY source route unless Pane 3 has newer
current-main recovery evidence; q501-q550 stays behind q451 landing, rejection,
or explicit abandonment. Keep SITE-P0-5 external until production serves
current `origin/main`; no Vercel CLI from this repo.

REVIEWER-GENERATED-TF-STANDALONE-Q501-Q550-CURRENT-1 [product defect][2026-05-18 22:12 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `8e3aa16` after q401-q450 source repair and q451-q500 VERIFY landed. Older q501-q550 residual lists are now stale or too broad: the slice has 50 rows, 25 true/false rows, zero `Sant eller falskt:` / `True or false:` prefix offenders, and zero true/false stem meta offenders in `site/questions.js`. Current remaining q501-q550 generated true/false wording defects are seven rows mirrored in both `content/question-bank.csv` and `site/questions.js`: `q526` and `q527` say Islam/Judaism is "the second largest in Sweden" without naming the missing referent religion; `q530` and `q531` render double comma punctuation after `31 December`; `q531` is also ungrammatical in both languages (`är det vanligt att stora brasor...` / `it is common to large bonfires...`); `q535` has ungrammatical Swedish (`brukar arbetarrörelsen arrangerar demonstrationer`); and `q542`/`q543` render English typos `spreadinging` and `welcominging`. Existing gates miss these current residuals: `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 29/29 passing; and typecheck exits 0 after linking the existing dependency install into the clean temp worktree. Manager action: keep q501-q550 behind q451-q500 source routing, then route DATA-INTEGRITY to a focused q501-q550 standalone/naturalness cleanup for `q526`, `q527`, `q530`, `q531`, `q535`, `q542`, and `q543`, with generator and validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and current-main spot checks proving the seven rows are grammatical, standalone, and punctuation-clean.

CEO->VALIDATOR 2026-05-18T22:12:23+02:00
Staffing remains HOLD/add-none. Fresh `/home/billy/bin/csup staff
--scenario=resume --dry-run` reported queued work for every configured host
(`work=9`, `blockers=9`), but every host held with
`reason=lunarc_requires_slurm_station`; no apply was run. `csup factory-audit
civic-test` remains RED for `missing:project_config`.

Acceptance/routing actions:
- Treat q401-q450 as accepted by MANAGER-build via `e022455` / source commit
  `85fe297`; do not duplicate q401-q450 source work unless fresh current-main
  regression evidence appears.
- Route DATA-INTEGRITY next to q451-q500 from the current VERIFY evidence:
  `0d58526` for q454/q466/q470/q471/q495 plus `8e3aa16` for the q479
  correction.
- Treat q501-q550 reviewer evidence in `99c0960` as queued but behind the
  q451-q500 source route; do not start duplicate q451 VERIFY branches.
- Keep `REVIEWER-RELEASE-GATES-1` as the next non-site P0 COMPLY atom for
  RELEASE/COMPLY or SETUP/COMPLY. Current `npm run test:publishing` proves a
  stale disabled-ads contract, so privacy/data-safety closure must update the
  docs and guard to the current ads + Remove Ads IAP + consent posture.
- Keep SITE-P0-5 open until production serves current `origin/main` and the
  hash-aware live smoke passes. No Vercel CLI from this repo.

REVIEWER-GENERATED-TF-STANDALONE-Q551-Q600-CURRENT-1 [product defect][2026-05-18 22:23 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `2af6fa7` after q401-q450 source repair, q501-q550 reviewer queueing, and the latest live-deploy recheck. The older q551-q600 VERIFY residual list is now stale or too broad: direct VM inspection of `site/questions.js` found 50 q551-q600 rows, 25 true/false rows, zero `Sant eller falskt:` / `True or false:` prefix offenders, zero true/false stem meta offenders, and zero generated unknown-material fallback options in the slice. Current remaining q551-q600 generated wording defects are four rows mirrored in both `content/question-bank.csv` and `site/questions.js`: `q563` English says `Advent occurs a Saturday...` instead of `on a Saturday`; `q574` English renders lowercase `buddhist and Hindu`; `q598` English has subject/verb disagreement (`Travel to Asia and increased interest... is mentioned...`); and `q599` remains stilted as a learner-facing true/false stem (`That Sweden's first mosques were built... is mentioned...`). Existing gates miss these residuals: `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 29/29 passing; and typecheck exits 0 after linking the existing dependency install into the clean temp worktree. Manager action: keep q551-q600 behind q451-q500 and q501-q550 source routing unless VALIDATOR reorders, then route DATA-INTEGRITY to a focused q551-q600 naturalness cleanup for `q563`, `q574`, `q598`, and `q599`, with generator and validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and current-main spot checks proving the four rows are grammatical and natural.
REVIEWER-GENERATED-TF-STANDALONE-Q551-Q600-CURRENT-1 resolution recheck [2026-05-18 23:04 CEST]: Clean current `origin/main` `d1753ae` closes the q551-q600 naturalness residual after DATA-INTEGRITY cleanup and handoff. Direct CSV/static scan over q562/q563/q574/q575/q598/q599 found 720 static questions, all six CSV rows mirrored in `site/questions.js`, and zero residual matches for `occurs a Saturday`, lowercase `buddhist and Hindu`, subject/verb `interest ... is mentioned`, leading `That ... is mentioned`, or the q698 Jesus capitalization patterns. Spot evidence: q563 now says `Advent occurs on a Saturday...`; q574 capitalizes `Buddhist and Hindu`; q598 uses plural `are mentioned`; q599 is a direct learner-facing false proposition. Gates pass: `npm run validate:content` with 720 questions and static parity true; `npm run test:derived-content` 6/6; `node scripts/export-site-question-bank.js --check` with 720 questions and 13 chapters; focused content/static/test-gate suite 30/30 including the residual q551-q600 wording guard; `npm run typecheck -- --pretty false`; and `git diff --check`. Manager action: accept/close q551-q600 after inspecting the DATA-INTEGRITY source/PR evidence, then route the next current generated residual slice (`REVIEWER-GENERATED-TF-STANDALONE-Q601-Q650-CURRENT-1`) unless VALIDATOR has newer accepted evidence.

MANAGER-build->VALIDATOR 2026-05-18T22:34:00+02:00
Accept q451-q500 DATA-INTEGRITY source repair on current `origin/main`: PR
#703 / `c6299ce` closes `VERIFY-GENERATED-TF-STANDALONE-Q451-Q500-POST642-1`
for q454/q466/q470/q471/q479/q495. Manager recheck passed content validation,
derived-content tests, focused published/static/content-gate tests 28/28,
canonical/static export parity, `node --check site/questions.js`, typecheck,
lint, ownership, targeted Prettier, diff whitespace, and a direct CSV/static
spot scan with zero residual capitalization/reason/adverb/order offenders.

Route DATA-INTEGRITY next to q501-q550 for q526/q527/q530/q531/q535/q542/q543
from reviewer evidence in `99c0960`; q551-q600 stays behind q501-q550 unless
VALIDATOR explicitly reorders. Keep SITE-P0-5 external until production serves
current `origin/main`; no Vercel CLI from this repo.

MANAGER-build->VALIDATOR 2026-05-18T22:43:00+02:00
Accept `REVIEWER-RELEASE-GATES-1` SETUP/COMPLY source guard on current
`origin/main`: PR #707 / `9d40c89` updates the Apple privacy-label draft,
Google Play Data safety draft, and publishing guard for the current Google
Mobile Ads + Remove Ads non-consumable IAP + ATT/UMP consent posture. Manager
recheck passed publishing tests 7/7, release preflight 44/44, typecheck, lint,
ownership, JS syntax, targeted Prettier, diff whitespace, and a stale-copy scan
with zero guarded disabled-ads/no-data phrases.

Keep final store-console submission, binary privacy review, and external
production deploy freshness as separate manual/operator gates. SETUP has no
new local source atom unless REVIEWER/VALIDATOR files fresh site or release
source evidence.

CEO->VALIDATOR 2026-05-18T22:46:00+02:00
Staffing remains HOLD/add-none after current-main advance to `7c690c0`.
q451-q500 is accepted by MANAGER-build, q501-q550 VERIFY has refreshed on
current main, and REVIEWER-RELEASE-GATES-1 is accepted by MANAGER-build. Route
DATA-INTEGRITY next to q501-q550 for q526/q527/q530/q531/q535/q542/q543;
q551-q600 stays queued behind q501-q550 unless VALIDATOR explicitly reorders.
Keep SITE-P0-5 external until production serves current main and the hash-aware
live smoke passes.

REVIEWER-GENERATED-TF-STANDALONE-Q651-Q700-CURRENT-1 [product defect][2026-05-18 22:58 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `b441aed` after q601-q650 reviewer queueing, q451-q500 acceptance, q501-q550 source repair, release privacy acceptance, and the latest live-deploy/content/verify handoff cycle. The old q651-q700 VERIFY defects for q666/q667/q699 are now stale: direct VM inspection of `site/questions.js` found 50 q651-q700 rows, 25 true/false rows, zero `Sant eller falskt:` / `True or false:` prefix offenders, zero true/false stem meta offenders, and q666/q667/q699 now render grammatical standalone stems. Current remaining q651-q700 generated wording defect is `q698`, mirrored in both `content/question-bank.csv` and `site/questions.js`: the true statement lowercases the proper noun in both languages (`Julen firar traditionellt jesu födelse...` / `Christmas traditionally celebrates jesus' birth...`) while the authored source option and explanations correctly use `Jesu födelse` / `Jesus' birth`. Existing gates miss this residual: `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 30/30 passing; and typecheck exits 0 after linking the existing dependency install into the clean temp worktree. Manager action: keep q651-q700 behind q551-q600 and q601-q650 source routing unless VALIDATOR reorders, then route DATA-INTEGRITY to a focused q651-q700 proper-noun capitalization cleanup for `q698`, with generator and validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and current-main spot checks proving `Jesu` and `Jesus'` remain capitalized.

CEO->VALIDATOR 2026-05-18T22:44:40+02:00
Correction on latest observed `origin/main` `58cb0f6`: the lane-specific
manager queues have now been notified, and current routing changed after q501
source cleanup, q551/q651 reviewer evidence, and static ebook verification
landed. Keep q451-q500 and REVIEWER-RELEASE-GATES-1 accepted. Inspect/accept
q501-q550 source cleanup from `b441aed`; if rejected, return only precise
residuals to DATA-INTEGRITY. Keep q551-q600, q601-q650, and q651-q700 behind
q501-q550 acceptance unless VALIDATOR explicitly reorders. Route
`REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1` to SETUP/site with CONTENT support for
wording/provenance. Keep SITE-P0-5 external until production serves current
main and the hash-aware live smoke passes. Staffing remains HOLD/add-none
because every configured host held on `reason=lunarc_requires_slurm_station`,
and `csup factory-audit civic-test` remains RED for `missing:project_config`.

MANAGER-build->VALIDATOR 2026-05-18T23:02:00+02:00
Accept q501-q550 DATA-INTEGRITY source repair on current `origin/main`: PR
#716 / `b441aed` plus handoff commit `557fe95` closes
`VERIFY-GENERATED-TF-Q501-Q550-CURRENT-1` for q526/q527/q530/q531/q535/q542/q543.
Manager recheck passed content validation, derived-content tests, focused
published/static/content-gate tests 29/29, canonical/static export parity,
`node --check site/questions.js`, typecheck, lint, ownership, targeted
Prettier, diff whitespace, serialized `test:content` 314/314, and a direct
CSV/static scan with zero residual q501-q550 wording offenders.

Route DATA-INTEGRITY next to q551-q600 for q563/q574/q598/q599 from reviewer
evidence in `8e46b3b` / `5a9fd78`; q601-q650 stays behind q551-q600 unless
VALIDATOR explicitly reorders, and q651-q700 q698 stays behind q551/q601.
Keep SITE-P0-5 external until production serves current `origin/main`; no
Vercel CLI from this repo.

REVIEWER-GENERATED-JUDGEMENT-Q701-Q720-CURRENT-1 [product defect][2026-05-18 22:49 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `6bac896` for the last generated q701-q720 slice after q501 source cleanup, q551/q601/q651 reviewer evidence, and static ebook verification landed. The old q701-q720 VERIFY note is stale in both directions: direct VM inspection of `site/questions.js` found 20 q701-q720 rows, 10 true/false rows, zero `Sant eller falskt:` / `True or false:` prefix offenders, zero true/false stem meta offenders, and no generated unknown-material fallback options. Current remaining defect is a generated judgement-template surface in `q713` and `q716`, mirrored in both `content/question-bank.csv` and `site/questions.js`: both are `single_choice` rows asking the learner to judge a statement (`Välj rätt alternativ för påståendet...` / `Choose the correct option for the statement...`, `Vilket alternativ stämmer med påståendet?` / `Which option matches the statement?`) while the options are `Sant`, `Falskt`, `Inget av alternativen stämmer`, `Endast ibland` / `True`, `False`, `None of the options is correct`, `Only sometimes`. That recreates a true/false answer-judgement prompt inside a single-choice shell and violates the wording-rule bar for flat, natural learner-facing prompts. Existing gates miss it: `npm run validate:content` exits 0 with `questionJudgementMetaStemsValidated:720` and `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 31/31 passing; and typecheck exits 0 after linking the existing dependency install into the clean temp worktree. Manager action: keep this behind q551-q600, q601-q650, and q651-q700 unless VALIDATOR explicitly reorders, then route DATA-INTEGRITY to replace generated judgement variants for q713/q716 with natural four-option practice questions or suppress the redundant judgement variant, update generator + validator mirror/static guard coverage, regenerate canonical/static output, and provide export/static parity, content/type/lint/ownership gates, and current-main spot checks proving no q701-q720 single-choice row uses True/False plus `None of the options` / `Only sometimes` as filler answers.

MANAGER-build->VALIDATOR 2026-05-18T23:05:00+02:00
Accept `REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1` on current `origin/main`: PR
#721 / `3373a5e` softens unsupported static Home/Ebook source-backed and
every-claim-footnoted copy and adds a guard so those claims cannot return
without ebook citation metadata or Sources-page ebook coverage. Manager recheck
passed route/static-link assertion, ebook parity 4/4, static chapter-count
copy 2/2, static source-provenance copy 2/2, static-bank export parity,
`validate:content`, typecheck, lint, ownership, changed-file JS syntax,
targeted Prettier, and diff whitespace after rebasing the worker branch onto
current main.

Do not duplicate ebook source-coverage work without fresh current-main
provenance evidence. SETUP has no unblocked local source atom; keep
SITE-P0-5 external until production serves current `origin/main` and the
hash-aware live smoke passes. DATA-INTEGRITY remains routed to q551-q600 next.
No Vercel CLI from this repo.

MANAGER-build->VALIDATOR 2026-05-18T23:10:00+02:00
Accept q551-q600 DATA-INTEGRITY source repair on current `origin/main`: PR
#730 / `45d2e2d` plus handoff PR #733 / `d1753ae` closes
`REVIEWER-GENERATED-TF-STANDALONE-Q551-Q600-CURRENT-1` for q563/q574/q598/q599
and the paired Advent occurrence row q562. Manager recheck passed content
validation, derived-content tests, focused published/static/content-gate tests
30/30, canonical/static export parity, `node --check site/questions.js`,
typecheck, lint, ownership, targeted Prettier, diff whitespace for both source
and handoff commits, and a direct q562/q563/q574/q575/q598/q599 static scan
with zero residual offenders.

Route DATA-INTEGRITY next to q601-q650 for q606/q607/q611/q622 from current
VERIFY evidence. q651-q700 q698 and q701-q720 q713/q716 stay behind q601-q650
unless VALIDATOR explicitly reorders. Keep SITE-P0-5 external until production
serves current `origin/main`; no Vercel CLI from this repo.

REVIEWER-SITE-LIVE-DEPLOY-STALE-1 update [2026-05-18 22:58 CEST][P0 SITE][external/live]: Rechecked clean current `origin/main` `cabe389` after ebook source-coverage acceptance and latest content handoffs. Local readiness remains green: `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; `node --test scripts/check-live-site.test.js` exits 0 with 7/7 passing; `npm run validate:content` exits 0 with static-site parity true; `npm run typecheck -- --pretty false` exits 0; and `git diff --check` exits 0 before queue edits. Production is still stale on both known URLs: `https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app` fails the live smoke with expected 720/hash `97ddcda4c5c9`, found 715/hash `afb9eec56629`; `https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` fails with expected 720/hash `97ddcda4c5c9`, found 715/hash `5d2710bebf7e`. Practice hub, wide layout, Mock route, Ebook renderer, and ebook placeholder-copy checks pass on both URLs. Manager action: keep SITE-P0-5 blocked on external production deploy freshness until production serves `cabe389` or newer and the hash-aware live smoke passes. Do not run Vercel CLI from this repo.

REVIEWER-GENERATED-JUDGEMENT-TRUEFALSE-FILLER-ALL-1 [product defect][2026-05-18 23:10 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Focused whole-bank scan on clean current `origin/main` `87bc8b3` found generated `single_choice` judgement variants that recreate true/false prompts inside a four-option single-choice shell. This is broader than the current q701-q720-only reviewer row: `site/questions.js` has 22 `single_choice` rows whose options are `True`, `False`, `None of the options is correct`, and `Only sometimes` / `Sant`, `Falskt`, `Inget av alternativen stämmer`, and `Endast ibland`: `q149`, `q152`, `q165`, `q168`, `q233`, `q236`, `q253`, `q256`, `q265`, `q268`, `q329`, `q332`, `q337`, `q340`, `q437`, `q440`, `q505`, `q508`, `q517`, `q520`, `q713`, and `q716`. The stems ask the learner to choose a correct option for a statement (`Choose the correct option for the statement...` / `Which option matches the statement?...`) instead of asking a natural civic question; this violates the wording-rule bar against answer-judgement templates and gives learners filler answers that are not real distractors. Existing gates miss the pattern: `npm run validate:content` exits 0 with `questionJudgementMetaStemsValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; focused published-question/sentence/source tests exit 0 with 33/33 passing; and `git diff --check` is clean before queue edits. Manager action: route DATA-INTEGRITY to one generator/validator/static-mirror cleanup that replaces or suppresses this generated true/false judgement single-choice variant across the full bank, adds a guard rejecting single-choice rows with True/False plus filler options, regenerates `content/question-bank.csv` and `site/questions.js`, and provides export/static parity, content/type/lint/ownership gates, and current-main scans proving the 22 listed rows no longer use judgement/filler-answer shells.

MANAGER-build->VALIDATOR 2026-05-18T23:14:00+02:00
Accept q601-q650 DATA-INTEGRITY source repair on current `origin/main`: PR
#740 / `8b7c77f` plus handoff PR #743 / `cbaddb0` closes
`VERIFY-GENERATED-TF-Q601-Q650-CURRENT-1` for q606/q607/q611/q622, with q610
kept parallel to q611. Manager recheck passed content validation,
derived-content tests, focused published/static/content-gate tests 31/31,
canonical/static export parity, `node --check site/questions.js`, typecheck,
lint, ownership, targeted Prettier, diff whitespace for both source and
handoff commits, and a direct q606/q607/q610/q611/q622 CSV/static scan with
zero residual offenders. Current `origin/main` added only reviewer/journal
metadata after the recheck; q601-q650 artifacts are unchanged.

Route DATA-INTEGRITY remains on the already-started q651-q700 q698
proper-noun capitalization atom from current reviewer evidence. After q651 is
accepted or rejected, route the broader
`REVIEWER-GENERATED-JUDGEMENT-TRUEFALSE-FILLER-ALL-1` cleanup; it supersedes
the older q701-q720-only generated-judgement route. Keep SITE-P0-5 external
until production serves current `origin/main`; no Vercel CLI from this repo.

MANAGER-build->VALIDATOR 2026-05-18T23:19:00+02:00
Pane 4 / DATA-INTEGRITY is usage-limited after q601-q650 acceptance PR #750 /
`19abd0c`; the Codex UI reported retry availability on May 23, 2026 at 23:11.
Current DATA-INTEGRITY source route remains q651-q700 q698 proper-noun
capitalization from clean current main. Park the stale judgement-filler work
until q651 is accepted or rejected; then route
`REVIEWER-GENERATED-JUDGEMENT-TRUEFALSE-FILLER-ALL-1`, which supersedes the
older q701-q720-only judgement route. SITE-P0-5 remains external; no Vercel CLI
from this repo.

REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1 [product defect][2026-05-18 23:21 CEST][P0 TRANSLATE-COMPLETE][answer-quality][content-sync]: Focused generated-option pass on clean current `origin/main` `8bb8a8b` found the static/user-facing bank still uses generic fallback options instead of real distractors across generated single-choice rows. Browser-backed static inspection of locally served `site/` found 720 questions, 421 `single_choice` rows, and 155 rows whose options include `None of the options is correct` and/or `Only sometimes`; 22 of those are the already-queued true/false judgement shell rows, but 133 are ordinary generated single-choice rows such as q148 `Which answer is correct? Where is Sweden located?` with options `In the Nordic region in northern Europe | In southern Europe | None of the options is correct | Only sometimes`, q156 distance from Treriksroset to Smygehuk, q160 Baltic Sea, q164 largest islands, q172 Kebnekaise, q176 largest lakes, q180 population, and q184 natural resources. These filler choices are not natural answer options for the stems, make the quiz feel generated, and fail the content bar for useful plausible distractors. Existing gates miss the defect: `npm run validate:content` exits 0 with 720 questions, `npm run test:derived-content` exits 0 with 6/6 passing, `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters, `node scripts/export-question-bank.js --check` exits 0, and `git diff --check` is clean before queue edits. Manager action: route DATA-INTEGRITY to replace generic generated single-choice fallback options with stem-specific distractors or suppress the low-quality variant, update generator + validator/static guard coverage, regenerate `content/question-bank.csv` and `site/questions.js`, and provide export/static parity plus content/type/lint/ownership gates. This may be bundled with `REVIEWER-GENERATED-JUDGEMENT-TRUEFALSE-FILLER-ALL-1`, but acceptance must separately prove zero ordinary single-choice rows expose `None of the options is correct` / `Only sometimes` fallback choices and zero true/false judgement-shell rows remain.

REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1 update [2026-05-18 23:31 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `9f585d4` after the q698 handoff landed. This is not a new duplicate route: it tightens acceptance for the already-queued generated single-choice filler-options defect. Direct VM inspection of `site/questions.js` found 720 total questions, 421 `single_choice` rows, and 133 generated ordinary single-choice rows whose stems begin with `Vilket svar är korrekt?` / `Which answer is correct?`; `content/question-bank.csv` has the same 133 rows. All 133 of those meta-stem rows also expose the already-queued generic fallback options, so the repair should remove both surfaces together. Representative rows include `q148`, `q156`, `q160`, `q164`, `q172`, `q176`, `q180`, `q184`, `q188`, `q192`, `q196`, and `q200`; the last rows include `q672`, `q676`, `q680`, `q684`, `q688`, `q692`, `q696`, `q700`, `q704`, `q708`, `q712`, and `q720`. Existing gates still miss the wording surface: `npm run validate:content` exits 0 with `questionJudgementMetaStemsValidated:720`, `generatedPromptTemplateParityValidated:576`, and static parity true; `npm run test:derived-content` exits 0 with 6/6 passing; `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; `node scripts/export-question-bank.js --check` exits 0; focused published-question/sentence/source tests exit 0 with 34/34 passing; `npm run typecheck -- --pretty false` exits 0; and `git diff --check` was clean before queue edits. Manager action: when DATA-INTEGRITY fixes `REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1`, acceptance must separately prove zero ordinary generated single-choice rows retain the `Vilket svar är korrekt?` / `Which answer is correct?` meta-prefix, zero ordinary generated single-choice rows expose `None of the options is correct` / `Only sometimes`, and zero true/false judgement-shell rows remain.
CEO->VALIDATOR 2026-05-18T23:36:34+02:00
Staffing remains HOLD/add-none. Cwd-based `csup staff --scenario=resume
--dry-run` saw queued work on every configured host (`work=9`, `blockers=9`),
but every host held on `reason=lunarc_requires_slurm_station`; explicit
`csup staff civic-test` and `factory-audit civic-test` still expose the
`missing:project_config` supervisor lookup problem. No apply was run.

q698 is already accepted by MANAGER-build as partial q651-q700 closure in
`08f836d`; do not duplicate it. Validate the remaining q651-q700 route only
for q663/q670/q671 current residuals, with q698 kept in spot/regression checks.
After q663/q670/q671 is accepted or rejected, route DATA-INTEGRITY to the
generated single-choice filler bundle. The filler bundle should cover both the
133 ordinary `Which answer is correct?`/fallback rows and the 22 true/false
judgement-shell rows, with separate zero-count proof for each surface.

Keep SITE-P0-5 external until production serves current main and the
hash-aware live smoke passes. Latest live-deploy evidence from SETUP remains
stale-production only; no Vercel CLI from this repo.

REVIEWER-GENERATED-TF-STANDALONE-Q651-Q700-CURRENT-1 follow-up [2026-05-18 23:39 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `09d8cb3` after the q698 proper-noun capitalization fix and later verify handoffs. The q698 portion is closed in current output: `site/questions.js` now renders `Julen firar traditionellt Jesu födelse inom kristendomen.` / `Christmas traditionally celebrates Jesus' birth in Christianity.` The remaining q651-q700 generated true/false wording defects are `q663`, `q670`, and `q671`, mirrored in the static bank: `q663` says the 25 December church service is called `Lucia procession` without a natural English article/context; `q670` awkwardly attaches `med en adventskalender hemma` / `with an Advent calendar at home` to opening calendar doors; `q671` reads as though children light bonfires `with an Advent calendar at home`. Direct VM inspection found 720 total questions, 50 q651-q700 rows, 25 true/false rows, zero true/false prefix offenders, zero true/false meta-stem offenders, and exactly those three residual patterns. Existing gates still miss them: `node scripts/export-site-question-bank.js --check` exits 0 with 720 questions and 13 chapters; `npm run validate:content` exits 0 with `questionGeneratedTrueFalseNaturalnessValidated:720`; `npm run test:derived-content` exits 0 with 6/6 passing; focused published/static/test-gate tests exit 0 with 32/32 passing, including only the now-fixed q698 proper-noun guard; `npm run typecheck -- --pretty false` exits 0; and `git diff --check` was clean before queue edits. Manager action: keep the q698 subcheck closed, but keep the q651-q700 route open for a focused DATA-INTEGRITY follow-up on q663/q670/q671 with generator and validator mirror coverage, regenerated canonical/static output, export/static parity, content/type/lint/ownership gates, and current-main scans proving q663/q670/q671 are natural while q698 remains capitalized.

CEO->VALIDATOR 2026-05-18T23:46:00+02:00
Staffing remains HOLD/add-none. Latest `origin/main` is `d468f31`, and commits
after the accepted q698 source repair are coordination/evidence only. Cwd-based
`csup staff --scenario=resume --dry-run` saw queued work on every configured
host (`work=9`, `blockers=9`) but all hosts held on
`reason=lunarc_requires_slurm_station`; explicit `csup staff civic-test` and
`factory-audit civic-test` still expose `missing:project_config`. No apply was
run.

Keep q698 closed as partial q651-q700 acceptance. Route DATA-INTEGRITY only to
q663/q670/q671, with q698 as a regression spot check. After q663/q670/q671 is
accepted or rejected, route the bundled generated single-choice cleanup:
ordinary meta-stem/fallback rows and true/false judgement-shell rows must each
have separate zero-count proof in canonical CSV and static output. Keep SETUP
source-held, CONTENT VERIFY/report-only unless explicitly leased, and UIUX
blocked until MANAGER-uiux supersedes the stale ResultSummary queue item and
DESIGN-TOKENS publishes `lib/theme/flag.ts`. SITE-P0-5 remains external deploy
freshness; no Vercel CLI from this repo.

REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1 partial resolution recheck [2026-05-19 00:00 CEST][P0 TRANSLATE-COMPLETE][wording-rule][content-sync]: Rechecked clean current `origin/main` `b9116df` after DATA-INTEGRITY commit `e1d04ea` and later UIUX handoff. Current static output now has zero generated single-choice rows with `None of the options is correct` / `Only sometimes` fallback options, and zero true/false judgement-shell single-choice rows. That closes the filler-option and judgement-shell surfaces of the existing reviewer defect if MANAGER-build accepts the source PR evidence. The ordinary generated single-choice meta-stem surface is still open: direct VM inspection of `site/questions.js` found 720 total questions, 421 `single_choice` rows, and 133 generated ordinary single-choice rows still beginning with `Which answer is correct?` / `Vilket svar är korrekt?`; `content/question-bank.csv` mirrors the same 133 rows. Representative survivors include `q148`, `q156`, `q160`, `q164`, `q172`, `q176`, `q180`, and `q184`, and late-bank survivors include `q672`, `q676`, `q680`, `q684`, `q688`, `q692`, `q696`, `q700`, `q704`, `q708`, `q712`, and `q720`. Existing gates stay green despite the meta-prefix surface: `npm run validate:content` exits 0 with `generatedSingleChoiceFillerOptionsValidated:576`, `questionJudgementMetaStemsValidated:720`, and static parity true; `npm run test:derived-content` exits 0 with 6/6 passing; export/static parity checks exit 0; focused published/static/test-gate/source/sentence tests exit 0 with 39/39 passing; `npm run typecheck -- --pretty false` exits 0; and `git diff --check` was clean before queue edits. Manager action: accept the filler-option/judgement-shell subclosure only after source/PR inspection, but keep `REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1` open for DATA-INTEGRITY to remove or rewrite the 133 ordinary `Which answer is correct?` / `Vilket svar är korrekt?` generated meta-stem rows in both canonical CSV and `site/questions.js`.

MANAGER-build->VALIDATOR 2026-05-19T00:18:00+02:00
Accept q651-q700 DATA-INTEGRITY follow-up on current `origin/main`: PR #798 /
`e90f82a` closes the q663/q670/q671 residual wording route while preserving
the accepted q698 capitalization. Manager recheck passed content validation,
derived-content tests, focused published/static/content-gate tests 35/35,
canonical/static export parity, `node --check site/questions.js`, typecheck
after linking dependencies, lint, ownership, targeted Prettier, diff
whitespace, serialized `test:content` 321/321, and a direct CSV/static scan
with zero stale q663/q670/q671/q698 offenders.

Route DATA-INTEGRITY next to the remaining ordinary generated single-choice
meta-stem/explanation surface under
`REVIEWER-GENERATED-SINGLE-CHOICE-FILLER-OPTIONS-1`: prove zero
`Vilket svar är korrekt?` / `Which answer is correct?` generated ordinary
single-choice meta-prefix rows in canonical CSV and static output, keep zero
ordinary filler-option rows and zero true/false judgement-shell rows, align
single-choice explanations such as q149/q152 away from True/False review copy,
and include the current q270/q271 `One reason is...` true/false standalone
defects if the same generator pass can close them cleanly. If not bundled,
route q270/q271 as the next smaller true/false follow-up before the broad
single-choice cleanup. Rerun the standard generator/static/content/type/lint/
ownership gates. Keep SITE-P0-5 external until production serves current main;
no Vercel CLI from this repo.

MANAGER-build acceptance 2026-05-19T04:01+02:00
Accepted UIUX-COMPONENTS PR #909 / `83d668e3` for the shared Button contract:
Button now exports the variant/props contract, documents defaults, and uses
theme tokens for border width, touch target, and pressed feedback. Accepted
DATA-INTEGRITY PR #908 / `ce01c423` for
`QUESTION-BANK-CSV-OPTION-PARITY-1`: `content/question-bank.csv` now exports
`optionSv` and `optionEn` JSON payload columns, with exporter, validator, and
CSV contract-test parity.

Manager recheck on current main after queue-only claims through `5ccf86c6` used a
clean worktree at `/tmp/sct-manager-current-MPSUx3/wt`. Passed
`node scripts/export-question-bank.js --check`; focused CSV/authored/Button/
content-production tests 14/14; `npm run validate:content`; `npm run
test:ui-effects` 50/50; `npm run test:theme-discipline`; `npm run
test:a11y-labels`; typecheck; lint; ownership; targeted Prettier;
`git diff --check 83d668e3^..83d668e3`; `git diff --check
ce01c423^..ce01c423`; current-tree `git diff --check`; Button token scan; and
direct CSV option-payload scan for 720 rows.

Do not duplicate the Button contract or CSV option-payload route without fresh
current-main regression evidence. CSV correct-answer and UHR source-metadata
CSV routes remain separate open work and are not accepted by this note.
SITE-P0-5 remains external production freshness; no Vercel CLI from this repo.

REVIEWER-Q073-Q074-NATURALNESS-1 [product defect][2026-05-20 03:08 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q073/q074 source review found learner-facing explanations still use source-note wording instead of natural explanations. q073 says `UHR-avsnittet Kommunerna har ett stort ansvar...` / `The UHR section Municipalities have a large responsibility says...`; q074 repeats the same `UHR-avsnittet` / `The UHR section... says` pattern. The facts and UHR locators are otherwise present, but the explanation body should teach the learner directly while UHR stays in the source citation line. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '1238,1270p'` shows both q073 and q074 explanation strings; `rg -n "explanationEn|ExplanationPanel|language" app/quiz components/quiz app/'(tabs)'/practice.tsx -S` shows routed quiz and practice render `question.explanationEn` through `ExplanationPanel` when English support is selected; existing queue scan found no q073/q074 reviewer item. Broader validation evidence is limited in this pass because earlier `npm run validate:content` and focused content-test commands were interrupted; do not treat green validators as closing this naturalness gap without source review. Manager action: assign a CONTENT-owned q073/q074 naturalness atom to rewrite the Swedish and English explanations idiomatically while preserving the page 31 municipal welfare and elderly-care facts, then rerun content/export gates.

REVIEWER-Q078-Q081-NATURALNESS-1 [product defect][2026-05-20 04:42 CEST][P0 TRANSLATE-COMPLETE][language]: Focused q078-q081 content review found learner-facing explanations still use citation-scaffold wording instead of natural teaching prose. q078 says `UHR-avsnittet Sveriges väg till demokrati säger...` / `The UHR section Sweden’s path to democracy says...`; q079 says `UHR-avsnittet Folkrörelserna beskriver...` / `The UHR section Popular movements describes...`; q080 says `UHR-avsnittet Demokratins genombrott säger...` / `The UHR section The democratic breakthrough says...`; q081 says `UHR-avsnittet Den svenska modellen säger...` / `The UHR section The Swedish model says...`. Facts, correct answers, and UHR locators are present, but explanation bodies should teach directly while UHR remains in the separate source citation line. Evidence: `nl -ba data/additionalQuestions.ts | sed -n '1328,1420p'` shows the q078-q081 explanation strings; `rg -n "Q078|Q079|Q080|Q081|q078|q079|q080|q081" codex-tasks/validator.txt docs/parallel-sessions/journals/reviewer.md` shows older q078-q081 source-stem findings but no q078-q081 explanation-naturalness item. Validation evidence is intentionally limited to source inspection in this reviewer pass; do not close TRANSLATE-COMPLETE on green validators without checking learner-facing explanation prose. Manager action: assign a CONTENT-owned q078-q081 naturalness atom to rewrite Swedish and English explanations idiomatically while preserving the page 33-35 democracy/popular-movements/suffrage/Swedish-model facts, then rerun content/export gates.
