DASHBOARD-PER-CHAPTER-INPUT-GUARD-1 [P1 dashboard/robustness] lib/learning/dashboardStats.ts + components/dashboard/PerChapterProgressBars.tsx + tests/v1-1-pro-foundations.test.js + tests/content-dashboard-route-accessibility-parity.test.js + scripts/learning.test.js + scripts/validate-content.js: harden Dashboard per-chapter progress selectors and rendering against malformed chapter totals and answer booleans: require finite non-negative integer `chapter.questionCount`, clamp `coverage` to 0..1, count correctness only for strict `isCorrect === true`, and keep chart/link labels from rendering `NaN`, negative, or over-100% percentages; preserve valid per-chapter accuracy, coverage, sorting, last-answer metadata, and localized Dashboard accessibility copy | why: focused REVIEWER dashboard robustness pass on current main found `perChapterProgress()` divides `bucket.questionIds.size / chapter.questionCount` with no finite/range guard and increments `correct` for any truthy `answer.isCorrect`. A direct transpiled probe with three answers returned `coverage:"NaN"` for `questionCount:NaN`, `coverage:-0.6` for `questionCount:-5`, `coverage:1.2` for `questionCount:2.5`, and `accuracy:0.666...` when two of three answers had non-boolean `isCorrect:"yes"`/`1`; those values feed `PerChapterProgressBars` percentages and chapter-link labels. Duplicate search found dashboard daysBack, mock-duration, mock-score, XP-history, and chapter-link a11y tasks plus weak-chapter input guards, but no Dashboard per-chapter selector guard for malformed `questionCount` or non-boolean correctness. Current focused guards miss it because they cover valid selector math and UI copy, not corrupt imported progress/chapter metadata | verify: npm run validate:content && node --test tests/v1-1-pro-foundations.test.js scripts/learning.test.js tests/content-dashboard-route-accessibility-parity.test.js --test-name-pattern "dashboard|Dashboard|perChapter|chapter progress|coverage|accuracy|questionCount|finite|NaN|boolean" && npm run typecheck -- --pretty false; fixtures fail if non-finite, negative, fractional, or undersized `questionCount` can produce `NaN`, negative, or over-1 coverage; if truthy non-boolean `isCorrect` can inflate accuracy or unresolved-mistake summaries; if Dashboard percentage/link/accessibility text can render non-finite values; or if valid 0-question, partial-coverage, full-coverage, sorting, and localized Dashboard behavior regresses
GENERATED-TF-POLITICAL-RIGHTS-INFINITIVE-FRAGMENTS-Q751-Q752-1 [P0 TRANSLATE-COMPLETE/generated-output] lib/content/derivedQuestions.ts + scripts/validate-content.js + tests/content-published-question-types.test.js + tests/content-question-sentence-endings.test.js + content/question-bank.csv + site/questions.js: make generated true/false variants from q146 `What right do people, groups, and parties have in a democracy?` produce standalone bilingual propositions such as `In a democracy, people, groups, and parties may try to persuade others of their political ideas.` and `In a democracy, people, groups, and parties may not stop others from voting.` instead of infinitive answer fragments; regenerate canonical/static banks and add a guard rejecting generated true_false stems that start with bare `Try to persuade...`, `Stop others...`, `Försöka övertyga...`, or `Hindra andra...` without the democracy/right subject | why: focused REVIEWER generated-output pass on clean current `origin/main` found `content/question-bank.csv` and `site/questions.js` publish q751 `Försöka övertyga andra om sina politiska idéer.` / `Try to persuade others of their political ideas.` and q752 `Hindra andra från att rösta.` / `Stop others from voting.` as learner-facing true/false prompts. Source q146's answer options are natural as options, but true/false prompts need complete civic statements. Duplicate search for q146/q751/q752 and the exact fragments found only old true/false-prefix notes plus broader generated-fragment rows, not this political-rights infinitive cluster. Current focused checks miss it: the direct static-bank probe found q146/q751/q752 with UHR ch02 `Demokrati betyder folkstyre` page 10 metadata intact; `npm run validate:content -- --json` is already red on unrelated q021-q040 published/source option parity; and `node --test tests/content-published-question-types.test.js tests/content-question-sentence-endings.test.js scripts/derived-content.test.js --test-name-pattern 'q146|generated|true_false|sentence|published'` showed the generated true/false naturalness fixtures passing before the unrelated q021-q040 gate failures masked the broader run | verify: npm run validate:content && npm run test:derived-content && node --test tests/content-published-question-types.test.js tests/content-question-sentence-endings.test.js tests/content-static-site-question-bank-parity.test.js && npm run content:export && node scripts/export-site-question-bank.js --check && npm run typecheck -- --pretty false; direct scans of content/question-bank.csv and site/questions.js fail on q751/q752 learner-facing stems that are bare infinitive answer fragments, while q146 options remain natural, q751 stays true, q752 stays false, UHR ch02 metadata stays intact, and source/static parity remains in sync
REMOVE-ADS-PREMIUM-BANNER-INFLIGHT-GUARD-1 [P1 ads-iap/robustness] components/monetization/PremiumBanner.tsx + scripts/monetization.test.js + scripts/validate-content.js + tests/content-remove-ads-purchase-runtime-parity.test.js: add the same ref-backed in-flight guard used by `RemoveAdsPlacementCta` to `PremiumBanner` buy/restore actions so a rapid double press cannot start duplicate store calls before React state disables the buttons; keep the existing disabled/busy UI, status messages, runtimeOptions override behavior, and ad-free active state | why: while fixing the default native Remove Ads entitlement runtime, the focused purchase-runtime guard proved the placement CTA is protected by `purchaseActionInFlightRef`, but `PremiumBanner` still relies only on `activeAction` React state before awaiting `buyRemoveAds` or `restoreRemoveAdsPurchase`. That state update is not a synchronous lock, so the profile/home paywall can theoretically dispatch duplicate store requests on rapid repeated activation. Duplicate search of the current queue found no Remove Ads/PremiumBanner in-flight guard task | verify: npm run test:monetization && node --test tests/content-remove-ads-purchase-runtime-parity.test.js --test-name-pattern "Remove Ads|PremiumBanner|in-flight" && npm run typecheck -- --pretty false; fixtures fail if `PremiumBanner` lacks `useRef(false)`, an early `if (purchaseActionInFlightRef.current) return`, a `true` assignment before awaiting the store call, and a `false` reset in `finally`, while `RemoveAdsPlacementCta` remains guarded and existing buy/restore status behavior still passes
COMPLIANCE-FOOTER-TARGET-SIZE-1 [P1 UIUX/a11y] components/compliance/ComplianceLinks.tsx + tests/e2e/compliance-link-targets.spec.ts: make the shared compliance footer links render at least 44px tall on mobile without horizontal overflow on `/profile` and `/settings`, using existing theme tokens and preserving localized labels/order | why: while verifying SOURCES-HOME-BACK-AFFORDANCE-1, the full compliance link target e2e still failed on current main because the shared footer link `Öppna Om provet` measured 16px tall on `/profile` and `/settings`; duplicate search found older target-size claims but no open atom for the shared compliance footer link style itself | verify: CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/compliance-link-targets.spec.ts --workers=1 -g '/profile|/settings' && NODE_OPTIONS='--v8-pool-size=1' npm run test:a11y-labels && NODE_OPTIONS='--v8-pool-size=1' npm run test:theme-discipline && NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false
MOCK-EXAM-LIBRARY-ROUTE-SURFACE-1 [P1 competitive/mock-exam] app/(tabs)/exam.tsx + components/MockExamConfigPanel.tsx + lib/learning/mockExamLibrary.ts + lib/quiz/examGenerator.ts + tests/content-mock-exam-runtime-parity.test.js + tests/e2e/exam-submit-review.spec.ts + scripts/validate-content.js: wire the existing named mock exam library and custom exam config panel into the native Exam route so users can choose a named mock, the random mock, or a bounded custom UHR-based practice exam before starting; preserve the current daily/free/rewarded access gate, timer, no-ads-during-exam guarantee, delayed explanations, score persistence, source-backed review, and Swedish/English copy | why: competitive mining on 2026 App Store citizenship-test prep listings found comparable apps surface multiple realistic mock exams, focused weak-topic/custom-by-chapter practice, and readiness/progress around the test flow; current main already contains `MOCK_EXAM_LIBRARY`/`materializeMock` with 7 mocks and a `MockExamConfigPanel`, but `app/(tabs)/exam.tsx` imports only `defaultMockExamConfig` and calls `generateExam(questions, { questionCount: defaultMockExamConfig.questionCount, sessionId })`. Focused `rg` found `MOCK_EXAM_LIBRARY`, `materializeMock`, and `MockExamConfigPanel` used only by tests/exports/journals, not by the route, leaving the production user with one default random mock despite completed library/component work. Duplicate search of the current queue found dashboard, generated-content, and Remove Ads tasks only, plus old journal notes saying SCREENS can port the mock-exam config flow | verify: npm run validate:content && node --test tests/content-mock-exam-runtime-parity.test.js tests/content-mock-exam-config-schema.test.js tests/v1-1-retention-selectors.test.js --test-name-pattern "mock exam|MockExamConfigPanel|MOCK_EXAM_LIBRARY|materializeMock|route" && npx playwright test tests/e2e/exam-submit-review.spec.ts tests/e2e/exam-answer-options.spec.ts tests/e2e/rewarded-web-preview.spec.ts --project=chromium && npm run typecheck -- --pretty false; fixtures fail if the Exam landing cannot render localized named/random/custom choices, if selecting a named mock does not materialize through `materializeMock`, if custom chapter/question bounds can request unsupported or non-UHR questions, if the default/random path stops working, if rewarded access can be bypassed, or if explanations/ads/source review regress during an active exam
REMOVE-ADS-NATIVE-RECEIPT-PERSISTENCE-REGRESSION-1 [P0 ads-iap/security] lib/monetization/purchases.ts + lib/monetization/useRemoveAdsEntitlements.ts + tests/remove-ads-native-validation.test.js + scripts/monetization.test.js + tests/content-remove-ads-purchase-runtime-parity.test.js + tests/content-remove-ads-hook-parity.test.js + scripts/validate-content.js: restore native Remove Ads receipt validation and relaunch persistence so the native provider does not mark arbitrary local purchase tokens valid, purchase/restore grants require an explicit validated receipt result, and a previously validated `StoredRemoveAdsEntitlementRecord` survives app relaunch instead of being cleared by the default hook path; keep web mock purchases, Android/iOS store product-id mapping, non-consumable finish ordering, `persistence_failed` recovery, and fail-closed pending/read-failure ad gating intact | why: focused REVIEWER ads/IAP security pass on current `origin/main` found `createNativePurchaseProvider().validateRemoveAdsReceipt(...)` returns `{ status: 'valid' }` for a fake local `purchaseToken`/`transactionId` with no validator, so `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules node --test tests/remove-ads-native-validation.test.js` fails expecting `pending`; a direct transpiled probe also showed `getPurchaseEntitlements({ storage })` on `createMemoryPurchaseStorage(true)` returns `adsDisabled:false` and deletes the valid stored record (`after null`) because `useRemoveAdsEntitlements` passes no provider on native (`createDefaultPurchaseRuntimeOptions()` returns `undefined`). This breaks the GOAL.md device gate requiring Remove Ads to persist across relaunch and weakens the purchase trust boundary. Duplicate search found only the stale `REMOVE-ADS-NATIVE-STORE-VALIDATOR-1` claim and the failing test, not a live open queue row | verify: NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules node --test tests/remove-ads-native-validation.test.js && npm run test:monetization -- --test-name-pattern "Remove Ads|receipt|provider|relaunch|SecureStore|getPurchaseEntitlements" && node --test tests/content-remove-ads-purchase-runtime-parity.test.js tests/content-remove-ads-hook-parity.test.js && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if default native receipt validation grants without an explicit validator, if invalid/pending validation can persist `adsDisabled:true`, if `getPurchaseEntitlements` clears a structurally valid stored Remove Ads record during relaunch, if native/web purchase runtimes bypass the shared `purchaseRuntime`, or if pending/read-failure states allow ads before entitlement hydration resolves

CONTENT-Q160-Q169-PUBLISHED-OPTION-PARITY-1 [P0 data-integrity/content] data/questions.ts + data/questionLocalizations.ts + scripts/validate-content.js + tests/content-published-question-types.test.js: restore authored-to-published source option parity for q160-q169 after the localization pilot so localized learner copy does not mutate the canonical source `options` field that `validateAuthoredSourceParity()` compares; keep q160-q169 question/explanation/localization behavior intact and do not touch the separately claimed q021-q040 parity cluster | why: while validating CONTENT-PUBLISHED-NEGATIVE-FIXTURE-ISOLATION-1 on current main, `npm run validate:content` still reported the known q021-q040 parity failures plus new q160-q169 `published source options does not match authored source` failures; duplicate search found a q021-q040 claim but no open/claimed q160-q169 parity atom | verify: NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH npm run validate:content 2>&1 | tee /tmp/validate-content-q160-q169.log; `rg "q16[0-9] published source options does not match authored source" /tmp/validate-content-q160-q169.log` returns no matches, q160-q169 still render their intended localized copy in exported/static banks, and q021-q040 failures remain outside this task unless their claimed fix has landed
NATIVE-SV-MOCKPROV-COPY-GUARD-1 [P1 TRANSLATE-COMPLETE/native-copy] app/(tabs)/home.tsx + app/(tabs)/practice.tsx + tests/content-home-route-header-parity.test.js + tests/content-practice-route-copy-parity.test.js + scripts/validate-content.js: replace Swedish learner-facing `mockprov`/`mock-provet` remnants with `övningsprov`/`övningsprovet` in Home guided-path completed CTAs and accessibility labels plus Practice source-details copy, then add content/test guards rejecting those mixed Swedish-English forms in Swedish native UI copy while preserving English `mock exam` copy | why: focused Swedish wording audit of current main found `app/(tabs)/home.tsx` still renders `Gå till mockprov` and `${title}: gå till mockprov när steget är klart.` for all three completed guided-path stages, while `app/(tabs)/practice.tsx` says `Aldrig en del av mock-provet.` in the Swedish source explanation. Nearby native exam/readiness/static ebook copy already uses `övningsprov`, and current tests cover several Swedish Home/Practice wording regressions but do not fail on these remaining `mockprov` strings. Duplicate search found historical claimed IDs (`SWEDISH-MOCKPROV-COPY-NATURALNESS-1`, `SWEDISH-MOCKPROV-ABOUT-COPY-NATURALNESS-1`, `EXAM-SV-OVNINGSPROV-COPY-GUARD-1`) but no live open row for the still-present Home/Practice native copy | verify: npm run validate:content && node --test tests/content-home-route-header-parity.test.js tests/content-practice-route-copy-parity.test.js tests/content-static-site-ebook-parity.test.js --test-name-pattern "mockprov|mock-provet|övningsprov|home|practice|source" && npx playwright test tests/e2e/home-study-loop-copy.spec.ts tests/e2e/practice-chapter-hub.spec.ts --project=chromium && npm run typecheck -- --pretty false; fixtures fail if Swedish native Home or Practice copy contains `mockprov`/`mock-provet`, if the intended `övningsprov` wording disappears, if English support loses `mock exam`, or if Home/Practice navigation/accessibility labels regress
UI-EFFECTS-RELEASE-GATE-DRIFT-1 [P0 release-tests/a11y] scripts/ui-effects.test.js + components/ui/Button.tsx + components/monetization/LaunchPopupAd.native.tsx + components/quiz/ProvenanceBadge.tsx + app/(tabs)/mistakes.tsx + app/(tabs)/profile.tsx + app/(tabs)/home.tsx + app/quiz/[sessionId].tsx + lib/quiz/examGenerator.ts + focused tests/content-*-parity.test.js: restore the `test:ui-effects` release gate by triaging each failing UI-effects assertion on current main, fixing real product regressions in the named surfaces and updating stale assertions to the current component contracts only where focused content/e2e guards already own the behavior; keep the first-run guide modal accessibility guard green | why: focused accessibility/release pass on current `origin/main` `f08ded30` found `NODE_OPTIONS='--v8-pool-size=1' node --test --test-reporter=dot scripts/ui-effects.test.js` exits 1, and the focused command `NODE_OPTIONS='--v8-pool-size=1' node --test --test-name-pattern "launch popup ad has native app-open implementation|button derives an accessibility label|first-run about modal" scripts/ui-effects.test.js` exits 1 with 2/3 failing. The Button guard still requires `minHeight: space[6]` while `components/ui/Button.tsx` currently exposes the 44px minimum target as `minHeight: 44`; the LaunchPopupAd.native guard still expects the old synchronous app-open cap regex while the current native component gates on consent, `launchPopupLoadInFlight`, loaded/error listeners, and sets `launchPopupShownThisRuntime` inside the LOADED path. The full dot run also reports stale-or-real failures around ProvenanceBadge, onboarding/card scaffolds, routed quiz shuffle, AnswerOption/QuestionCard/ChapterCard accessibility, quiz feedback cards, Mistakes review, Profile/Home copy, and English mock-exam review text. Duplicate search found historical journals where `test:ui-effects` was green after older atoms plus no live open queue row for the current red suite; `npm test` dispatches the full `test:all` path, which includes `npm run test:ui-effects`, so this red suite blocks a release-safe validation run and makes accessibility/static regression signals unreliable | verify: NODE_OPTIONS='--v8-pool-size=1' npm run test:ui-effects && npm run validate:content && node --test tests/content-button-accessibility-parity.test.js tests/content-launch-ad-route-parity.test.js tests/content-question-card-accessibility-parity.test.js tests/content-answer-option-accessibility-parity.test.js tests/content-mistakes-route-copy-parity.test.js tests/content-profile-route-copy-parity.test.js tests/content-home-route-header-parity.test.js tests/content-exam-route-copy-parity.test.js && npm run typecheck -- --pretty false; fixtures fail if `test:ui-effects` remains red, if product regressions are papered over instead of fixed, if stale UI-effects assertions keep enforcing superseded implementation shapes, or if focused parity/e2e guards no longer cover the behavior moved out of `scripts/ui-effects.test.js`
CHAPTER-LOCALIZED-TEXT-SV-EN-TYPECHECK-1 [P0 data-integrity/i18n] data/chapters.ts + types/content.ts + scripts/validate-content.js + tests/content-chapter-schema.test.js + tests/content-type-schema-parity.test.js + scripts/export-site-question-bank.js + site/questions.js: make every chapter `nameText` and `descriptionText` satisfy `LocalizedContentText` by including canonical `sv` and `en` entries alongside the 10 added locale entries, format the generated chapter file, and add a validator/test guard so future chapter localization maps cannot omit `sv`/`en` or drift from `nameSv`/`nameEn` and `descriptionSv`/`descriptionEn`; preserve static export localized chapter names/descriptions | why: localization-completeness pass on current `origin/main` `b619afa2` found the recent 10-locale chapter commits left all 13 `nameText` objects and all 13 `descriptionText` objects without `sv`/`en`. With an ignored symlink to the shared dependency tree in the temp worktree, `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false` exits 2 with repeated `data/chapters.ts` TS2322 errors saying each map is missing properties from `Record<"sv" | "en", string>`; a direct scan prints `{ "nameTextCount": 13, "descriptionTextCount": 13, "nameWithSv": 0, "descWithSv": 0 }`. `npx --no-install prettier --check data/chapters.ts` also exits 1, and source inspection shows generated `},    descriptionSv` / `},    questionCount` formatting drift. Duplicate search found only live-deploy notes involving `data/chapters.ts` plus the unrelated q160 option-parity row, not an open task for broken chapter localized-text typing | verify: npm run typecheck -- --pretty false && npx --no-install prettier --check data/chapters.ts && npm run validate:content && node --test tests/content-chapter-schema.test.js tests/content-type-schema-parity.test.js tests/content-chapter-text-normalization.test.js tests/content-static-site-question-bank-parity.test.js && node scripts/export-site-question-bank.js --check; fixtures fail if any chapter localized text map lacks `sv` or `en`, if `nameText.sv/en` or `descriptionText.sv/en` differ from the canonical `nameSv/nameEn` and `descriptionSv/descriptionEn`, if the generated static chapter bank loses the added locales, or if `data/chapters.ts` is not Prettier-clean
LANGUAGE-PICKER-TRIGGER-HASPOPUP-GUARD-1 [P1 a11y/i18n] components/ui/LanguagePicker.tsx + scripts/accessibility-labels.test.js + tests/e2e/language-picker-modal-accessibility.spec.ts: add `aria-haspopup="menu"` to the topbar language picker trigger and extend the static accessibility guard so the web button advertises the modal menu it opens; preserve existing `aria-expanded`, `accessibilityState`, focus return, disabled-coming-soon rows, keyboard navigation, compact touch target, and Swedish/English trigger copy | why: focused accessibility pass on current main found `tests/e2e/language-picker-modal-accessibility.spec.ts` already asserts the language trigger has `aria-haspopup="menu"` before opening, but `components/ui/LanguagePicker.tsx` only sets `aria-expanded={open}`, `accessibilityRole="button"`, `accessibilityLabel`, and `accessibilityState={{ expanded: open }}` on the trigger. `scripts/accessibility-labels.test.js` guards menu/menuitem row semantics but has no trigger haspopup assertion, so the contract can drift outside the E2E run. Duplicate search found claimed LanguagePicker target-size, disabled-row, and modal-backdrop work, but no live open row for the trigger haspopup gap | verify: node --test scripts/accessibility-labels.test.js --test-name-pattern "LanguagePicker|haspopup" && CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/language-picker-modal-accessibility.spec.ts --workers=1 && npm run typecheck -- --pretty false; fixtures fail if the trigger lacks `aria-haspopup="menu"`, if `aria-expanded` stops reflecting open/closed state, if focus does not return after selecting a language, if disabled locale rows close the menu, or if the compact close target/keyboard navigation regress
PROFILE-BADGE-SUMMARY-LOCALIZATION-PARITY-1 [P1 profile/i18n] app/(tabs)/profile.tsx + lib/learning/badges.ts + tests/content-profile-route-copy-parity.test.js + scripts/validate-content.js: make the Profile badge section expose localized badge summary and empty-state copy through `formatBadges(badges, language, copy.noBadges)` or an equivalent language-keyed helper, add `noBadges` copy for `Inga märken ännu` / `No badges yet`, and keep badge title, description, locked hint, progress hint, and unlocked/locked status labels sourced from the current settings language | why: current broad content validation reports `profile route must define localized badge-title overrides`, `profile badge summary must use localized badge and empty-state copy`, and missing Profile copy for `Inga märken ännu`, `Första övningen`, `Nivå 2`, `Misstagsrepetition`, `Tre dagars svit`, and `No badges yet`; source inspection shows `app/(tabs)/profile.tsx` renders `getAllBadges().map(...)` directly with `BadgeRow` and has no `noBadges` field or localized badge summary path even though `scripts/validate-content.js` already guards for those strings. Duplicate search across `codex-tasks` and `docs/parallel-sessions` found no live profile-badge localization task | verify: npm run validate:content && node --test tests/content-profile-route-copy-parity.test.js tests/content-badge-catalog-schema.test.js tests/content-profile-route-header-parity.test.js --test-name-pattern "profile|badge|No badges|Inga märken|Första övningen|Tre dagars svit|language" && npm run typecheck -- --pretty false; fixtures fail if Profile badge copy bypasses `profileCopy[language]`, if no-badge state is not localized, if badge milestone titles/descriptions/hints drift from `lib/learning/badges.ts`, or if the Profile badge/header layout regresses
PRO-LIFETIME-RECEIPT-PERSISTENCE-GUARD-1 [P0 ads-iap/security] lib/monetization/proLifetimePurchase.ts + lib/monetization/purchases.ts + components/monetization/ProPaywall.tsx + tests/v1-1-pro-iap.test.js + scripts/monetization.test.js + scripts/validate-content.js: replace the Pro Lifetime bare-boolean entitlement with a structured, receipt-validated, store-confirmed record parallel to Remove Ads; make the shared purchase provider validation generic enough for Pro Lifetime, grant Pro only after an explicit valid receipt result, persist before finishing the native transaction, surface a recoverable persistence failure status in the paywall, and reject relaunch grants from forged `monetization.proLifetime.entitled.v1` values | why: focused ads/IAP security pass on current main found `lib/monetization/proLifetimePurchase.ts` defines `const STORED_TRUE = 'true'`, writes that value in `setProLifetimeEntitlement(true)`, and grants full Pro whenever `storedValue === STORED_TRUE`. A direct transpiled probe with `{ [PRO_LIFETIME_STORAGE_KEY]: 'true' }` returned `spacedRepetition:true`, and calling `setProLifetimeEntitlement(true)` with memory storage wrote `"true"` without any purchase or receipt. Source inspection also found no receipt-validation call in the Pro Lifetime buy/restore path, and `buyProLifetime()` calls `provider.finishPurchase?.(purchase)` before persistence, so a storage failure can finish the store transaction without durable local entitlement. Existing `tests/v1-1-pro-iap.test.js` covers happy-path buy/restore but lacks forged-storage, invalid-receipt, pending-receipt, and persistence-failure fixtures. Duplicate search found Remove Ads bare-boolean guards plus Pro scope/product claims, but no live Pro Lifetime receipt/persistence row | verify: node --test tests/v1-1-pro-iap.test.js --test-name-pattern "Pro Lifetime|receipt|persist|spoof|forged|finish|persistence" && npm run test:monetization -- --test-name-pattern "Pro Lifetime|receipt|persistence|finish" && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if a bare `true` Pro Lifetime storage value grants Pro, if `setProLifetimeEntitlement(true)` can persist without a valid receipt record, if invalid or pending Pro receipts grant entitlements, if buy/restore finish the native transaction before durable persistence, if storage failure is collapsed to a generic error instead of a recoverable status, or if Remove Ads receipt validation/product-id behavior regresses
CALIBRATION-CONFIDENCE-RATING-RUNTIME-GUARD-1 [P1 learning/robustness] lib/learning/calibration.ts + lib/storage/progressStore.ts + tests/v1-1-pro-foundations.test.js + tests/content-progress-schema-parity.test.js + scripts/validate-content.js: add exported `isConfidenceRating` and `normalizeConfidenceRating` runtime guards, make `generateCalibration()` skip invalid confidence values before indexing buckets, and make `gradeFromConfidence()` / `lapsePenaltyForWrong()` use safe defaults for invalid ratings while preserving valid 1..5 FSRS mapping and progress-store hydration behavior | why: focused learning robustness pass on current main found `generateCalibration()` directly indexes `buckets[event.confidenceRating - 1]` and `correctByRating[event.confidenceRating]` with no runtime guard even though imported progress or old answer history can contain untyped values. The focused command `node --test --test-name-pattern "generateCalibration: invalid confidence ratings|invalid ratings use safe defaults" tests/v1-1-pro-foundations.test.js` exits 1: invalid ratings throw `Cannot read properties of undefined (reading 'count')`, and invalid correct ratings return grade 4 instead of the expected safe grade 3. The test file already expects `isConfidenceRating()` and `normalizeConfidenceRating()` exports, but `lib/learning/calibration.ts` currently exports neither; duplicate search found no live calibration/confidence-rating open row | verify: node --test --test-name-pattern "generateCalibration: invalid confidence ratings|invalid ratings use safe defaults|gradeFromConfidence|lapsePenaltyForWrong" tests/v1-1-pro-foundations.test.js tests/content-progress-schema-parity.test.js && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if invalid values like `0`, `6`, `NaN`, `Infinity`, strings, null, objects, arrays, or fractional ratings can throw, corrupt bucket counts, grant easy FSRS grades, add lapse penalties, or get persisted by progress hydration, while valid ratings keep the current bucket, grade, and penalty behavior
DASHBOARD-SNAPSHOT-ANSWER-ATTEMPTS-PARITY-1 [P1 dashboard/release-tests] lib/learning/dashboardProgressSnapshot.ts + app/dashboard.tsx + tests/v1-1-pro-foundations.test.js + scripts/learning.test.js + scripts/validate-content.js: preserve the dashboard snapshot per-answer contract by accepting `answerAttempts` as a legacy alias for `answerHistory` before synthesizing fallback attempts from `questionProgress`, then add a guard that per-answer history wins over synthetic progress counts so Dashboard activity, XP sparkline, and per-chapter answers cannot be over-counted during store/schema transitions | why: focused dashboard release-test pass on current main found `buildDashboardProgressSnapshot()` destructures only `answerHistory = []`, but existing `tests/v1-1-pro-foundations.test.js` and older learning helpers still exercise per-answer `answerAttempts`. The focused command `node --test --test-name-pattern "dashboard progress snapshot adapts local store progress" tests/v1-1-pro-foundations.test.js` exits 1 with `4 !== 3`: the provided three per-answer attempts are ignored, so the builder falls back to four synthetic attempts from `questionProgress` (`q1.seenCount:3` plus `q2.seenCount:1`). The live `DASHBOARD-PER-CHAPTER-INPUT-GUARD-1` row covers malformed chapter totals and boolean correctness, not this snapshot input-contract drift; duplicate search found no live `dashboardProgressSnapshot`/`answerAttempts` row | verify: node --test --test-name-pattern "dashboard progress snapshot adapts local store progress|dashboard progress snapshot prefers dated answer history|daily goal prefers per-answer attempts" tests/v1-1-pro-foundations.test.js scripts/learning.test.js && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if `answerAttempts` is ignored, if explicit per-answer history double-counts alongside fallback `questionProgress`, if current `answerHistory` callers regress, or if Dashboard daily activity, per-chapter answer counts, XP sparkline, mock-score summary, or app/dashboard wiring drift
COMPLIANCE-ROUTE-CTA-TARGET-SIZE-1 [P1 UIUX/a11y] app/support.tsx + app/about-the-test.tsx + components/compliance/LegalPage.tsx + tests/e2e/compliance-link-targets.spec.ts: make the remaining route-local compliance CTAs render at least 44px tall on mobile without horizontal overflow on `/support` and `/about-the-test`, using existing theme/shared button or link tokens and preserving localized labels, destinations, and order | why: after PR #1791 fixes shared footer links, the full compliance link target e2e still fails only `/support` because `Öppna den offentliga supportsidan` measures 34px tall and `/about-the-test` because `Öppna övningsläget` measures 33px tall; duplicate search found no open row for these route-local CTA target heights | verify: CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/compliance-link-targets.spec.ts --workers=1 -g '/support|/about-the-test' && NODE_OPTIONS='--v8-pool-size=1' npm run test:a11y-labels && NODE_OPTIONS='--v8-pool-size=1' npm run test:theme-discipline && NODE_OPTIONS='--v8-pool-size=1' npm run typecheck -- --pretty false; fixtures fail if either route has a visible link/button target below 44px tall, if labels/destinations/order drift, or if the shared compliance footer styles regress
PRO-PAYWALL-INFLIGHT-GUARD-1 [P1 ads-iap/robustness] components/monetization/ProPaywall.tsx + tests/v1-1-pro-foundations.test.js + tests/v1-1-pro-iap.test.js + scripts/monetization.test.js + scripts/validate-content.js: add a ref-backed in-flight guard to `runProAction()` so rapid double activation cannot dispatch duplicate Pro Lifetime buy/restore store calls before React state disables the buttons; preserve comparison visibility, busy/disabled accessibility state, restore status messages, runtimeOptions overrides, and separate Remove Ads path copy | why: focused ProPaywall robustness pass on current main found `RemoveAdsPlacementCta` already uses `purchaseActionInFlightRef` as a synchronous lock, and `PremiumBanner` has a live row for the same race, but `components/monetization/ProPaywall.tsx` still relies only on `activeAction` state around `buyProLifetime()` / `restoreProLifetime()`. A rapid double press can enter `runProAction()` twice before the React state update commits, dispatching duplicate Pro store requests. Duplicate search found the Remove Ads/PremiumBanner in-flight task plus the Pro Lifetime receipt/persistence row, but no live ProPaywall in-flight guard | verify: node --test tests/v1-1-pro-foundations.test.js tests/v1-1-pro-iap.test.js --test-name-pattern "ProPaywall|Pro Lifetime|in-flight|buyProLifetime|restoreProLifetime" && npm run test:monetization -- --test-name-pattern "ProPaywall|Pro Lifetime|in-flight" && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if `ProPaywall` lacks a `useRef(false)` lock, an early return while in flight, a true assignment before awaiting store calls, and a false reset in `finally`, or if Pro status copy/runtimeOptions/remove-ads separation regresses
CONTENT-TYPE-SCHEMA-FOCUSED-PARITY-GATE-1 [P1 data-integrity/tests] scripts/validate-content.js + tests/content-type-schema-parity.test.js: add a focused validator mode for content TypeScript schema parity and make the content-type schema parity tests use it so unrelated authored-source option parity failures cannot mask type/interface/union drift assertions; preserve full `npm run validate:content` behavior for complete content validation and keep the existing negative schema-drift fixtures meaningful | why: while validating `CHAPTER-LOCALIZED-TEXT-SV-EN-TYPECHECK-1`, `npm run validate:content` and `node --test tests/content-type-schema-parity.test.js` were red only because the separately claimed q021-q040 and q160-q169 published-source option parity failures run before the schema parity assertions. The focused chapter-localized-text mode made chapter guards actionable under those unrelated blockers; the type-schema parity suite needs the same isolation so future `types/content.ts` drift reports the expected schema mismatch instead of whichever content-data blocker is currently claimed | verify: node scripts/validate-content.js --focus-content-type-schema-parity exits 0 with `contentTypeSchemaParityValidated:true`; node --test tests/content-type-schema-parity.test.js passes when only unrelated authored-source option parity is red; npm run validate:content still runs full validation and reports published-source option parity failures when present; npm run typecheck -- --pretty false && git diff --check
COUNTDOWN-BANNER-UHR-CONFIRMED-COPY-1 [P0 currentness/legal-copy] components/ui/CountdownBanner.tsx + lib/learning/examDate.ts + tests/content-countdown-banner-parity.test.js + scripts/validate-content.js: update the Home countdown banner from tentative `expected in August 2026` / `väntas starta i augusti 2026` wording to UHR-confirmed first-sitting copy for 15 August 2026 in Stockholm, remove or clearly de-emphasize the separate 17 August government development deadline from learner-facing countdown text, and add a guard that rejects reintroducing tentative August-only phrasing once the UHR first-sitting date is known | why: focused REVIEWER currentness pass on current `origin/main` 9e6519fb found `components/ui/CountdownBanner.tsx` still tells users the civic-knowledge test is expected in August 2026 and then appends a government deadline date, while UHR's current FAQ at https://www.uhr.se/medborgarskapsprovet/fragor-och-svar/ (accessed 2026-05-20) says the first civic-knowledge test will be held on 15 August 2026, can only be written in Stockholm in August 2026, and exact place details will come later. The current `app/about-the-test.tsx`, `data/citizenshipRequirements.ts`, and `site/ebook.js` already carry the 15 August/Stockholm fact, so the countdown banner is the stale/confusing surface. Duplicate search found only older `COUNTDOWN-BANNER-TIMELINE-SOURCE-AFFORDANCE-1` and `COUNTDOWN-BANNER-FIRST-SITTING-DATE-PARITY-1` claims, not a live open row for replacing the banner's tentative wording and deadline mix-up | verify: npm run validate:content && node --test tests/content-countdown-banner-parity.test.js tests/content-about-the-test-route-copy-parity.test.js tests/content-citizenship-requirements-parity.test.js scripts/static-site-source-provenance-copy.test.js --test-name-pattern "countdown|15 August|15 augusti|Stockholm|expected|väntas|deadline|UHR" && npm run typecheck -- --pretty false; fixtures fail if the banner still says the test is merely expected in August, if it presents the 17 August development deadline as a learner test date, if the UHR source link disappears, if the 15 August 2026 Stockholm first-sitting fact drifts from app/about/citizenship/static surfaces, or if accessibility/source-link behavior regresses
MOBILE-ADS-CONSENT-HOOK-FOCUSED-PARITY-GATE-1 [P1 ads/privacy release-tests] scripts/validate-content.js + tests/content-mobile-ads-consent-hook-parity.test.js + lib/monetization/useMobileAdsConsent.ts: add a focused `--focus-mobile-ads-consent-hook` validator mode and make the mobile-ads-consent hook parity tests use it, then update the cached-initialization negative fixture to mutate the current platform-aware `cachedInitializationPlatform === platform` branch instead of the removed `if (!entitlements.adsDisabled && cachedInitialization) return cachedInitialization;` string; preserve the full `npm run validate:content` path and the current fail-closed Remove Ads/platform behavior | why: focused REVIEWER monetization/privacy pass on current `origin/main` 12a7e67e found `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-mobile-ads-consent-hook-parity.test.js` exits 1 only because `parseValidationSummary()` runs full `scripts/validate-content.js` and the separately queued q021-q040/q160-q169 authored-source option failures mask the consent-hook assertions. Source inspection also found the second negative fixture replaces `if (!entitlements.adsDisabled && cachedInitialization) return cachedInitialization;`, but `lib/monetization/useMobileAdsConsent.ts` now uses a platform-aware memo branch with `cachedInitializationPlatform === platform`, so once unrelated content blockers clear that mutation becomes a no-op and the negative guard will stop proving cached Remove Ads initialization drift is rejected. Duplicate search found only the broader `CONTENT-TYPE-SCHEMA-FOCUSED-PARITY-GATE-1`, not a mobile ads consent hook focused gate | verify: node scripts/validate-content.js --focus-mobile-ads-consent-hook prints `mobileAdsConsentHookCasesValidated:5` and `mobileAdsConsentHookParityValidated:true` even while unrelated authored-source option parity is red; node --test tests/content-mobile-ads-consent-hook-parity.test.js passes and its two negative fixtures fail for the intended Mobile Ads consent hook messages, including platform-cache drift; npm run validate:content still runs full validation and reports q021/q160 option parity when present; npm run typecheck -- --pretty false && git diff --check
MOCK-EXAM-SELECTION-E2E-COVERAGE-1 [P1 competitive/mock-exam] tests/e2e/exam-selection-config.spec.ts + tests/e2e/exam-submit-review.spec.ts + app/(tabs)/exam.tsx: add focused browser coverage for the native Exam landing selection flow after the mock exam library route is wired: select a named mock other than the default, select the random mock, configure a custom chapter/count/time exam, then start and assert the active timer/count/source review reflects the chosen snapshot while the daily/free/rewarded gate and no-ads-during-exam guarantee still hold | why: PR #1796 wires `MOCK_EXAM_LIBRARY`, `materializeMock`, and `MockExamConfigPanel` into `app/(tabs)/exam.tsx` and keeps the default submit/review E2E green, but the browser suite still only starts the default path. The new library/custom controls are guarded by route/source parity and unit-style checks, not by a user-level click path proving radio/checkbox/stepper interactions survive the web/native accessibility mapping, MMKV-backed settings seeding, access gating, and exam start snapshotting. Duplicate search found the route-surface task itself plus old component journal notes, but no live follow-up dedicated to end-to-end selection coverage | verify: npm run build:web:export && CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/exam-selection-config.spec.ts tests/e2e/exam-submit-review.spec.ts --workers=1 && npm run typecheck -- --pretty false; fixtures fail if named/random/custom selection cannot be changed from the landing page, if a custom zero-chapter or over-limit request can start, if the active exam ignores the selected count/time/chapters, if source-backed review or delayed explanations regress, or if rewarded access can be bypassed
LANGUAGE-PICKER-STATIC-KEYBOARD-GUARD-1 [P1 a11y/i18n] components/ui/LanguagePicker.tsx + scripts/accessibility-labels.test.js + tests/e2e/language-picker-modal-accessibility.spec.ts: extend the static LanguagePicker accessibility guard to reject a full-screen accessible backdrop close target, missing compact close button, missing row tabIndex, or missing web keyboard handlers for Escape/Arrow/Home/End/Enter/Space while preserving the runtime e2e flow | why: while implementing LANGUAGE-PICKER-TRIGGER-HASPOPUP-GUARD-1, the browser spec exposed close-target and keyboard-focus regressions that the lightweight static guard did not catch; keeping these contracts in the static guard makes future a11y drift visible before the heavier e2e run | verify: NODE_OPTIONS="--v8-pool-size=1" npm run test:a11y-labels && CI=1 NODE_OPTIONS="--v8-pool-size=1" npm run test:e2e -- tests/e2e/language-picker-modal-accessibility.spec.ts --workers=1 && npm run typecheck -- --pretty false
DAILY-CHALLENGE-SURFACE-PERSISTENCE-1 [P2 post-v1.0 competitive/daily-practice] app/(tabs)/home.tsx + app/(tabs)/practice.tsx + lib/storage/progressStore.ts + types/progress.ts + tests/v1-1-daily-challenge.test.js + scripts/release-preflight.js: finish or explicitly defer the existing Daily Challenge feature by wiring `buildDailyChallenge()` into Home, launching `/practice?mode=challenge`, constraining Practice to the day-stable five-question challenge with the 60-second timer, recording one completion per local day in progress storage, and keeping the release-preflight v1.1-scope guard honest so this cannot ship half-surfaced before v1.0 Remove Ads gates pass | why: focused REVIEWER competitive/completeness pass on current `origin/main` fa186309 found `lib/learning/dailyChallenge.ts` and `tests/v1-1-daily-challenge.test.js` already define a low-friction Daily Challenge, but `rg` found no open/claimed daily-challenge task and no Home/Practice/storage runtime wiring. The focused command `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test tests/v1-1-daily-challenge.test.js` passes the selector/copy unit cases but fails the two product-surface cases: Home lacks `buildDailyChallenge({ bank: questions })`, `dailyChallengeBannerCopy(...)`, `/practice?mode=challenge`, and `dailyChallengeCompletions`, while progress storage lacks `DailyChallengeProgress`, `dailyChallengeCompletions`, and `recordDailyChallengeCompletion`. The feature is already represented as v1.1 scope in `scripts/release-preflight.js`, so the fix should either complete it after v1.0 or add an explicit deferred guard instead of leaving a red half-feature test | verify: NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules node --test tests/v1-1-daily-challenge.test.js && npm run test:learning -- --test-name-pattern "daily challenge|daily goal|streak" && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if Home does not show localized active/done Daily Challenge copy, if `/practice?mode=challenge` does not use exactly today's deterministic challenge IDs and timer, if completion can be recorded more than once per local day, if persisted progress hydration drops or corrupts challenge completions, or if v1.0 release-preflight scope allows the half-feature to look complete before Remove Ads gates are accepted
ANSWER-OPTION-A11Y-FOCUSED-PARITY-GATE-1 [P1 release-tests/a11y] scripts/validate-content.js + tests/content-answer-option-accessibility-parity.test.js + components/quiz/AnswerOption.tsx: add a focused `--focus-answer-option-accessibility` validator mode and make the AnswerOption parity test use it, so unrelated authored-source option parity failures cannot mask shared answer-option accessibility contract drift; preserve full `npm run validate:content` behavior and current Swedish/English idle plus feedback label coverage | why: while restoring `UI-EFFECTS-RELEASE-GATE-DRIFT-1` on current main, the focused AnswerOption parity assertion had to be updated for `getQuestionOptionText(option, language)`, but `node --test tests/content-answer-option-accessibility-parity.test.js` was not actionable because `parseValidationSummary()` runs full `scripts/validate-content.js` and exits first on separately claimed q021-q040/q160-q169 published-source option parity failures. Duplicate search found broader content-type/mobile-ads focused gates and completed AnswerOption localization work, but no live focused gate for this AnswerOption accessibility suite | verify: NODE_OPTIONS='--v8-pool-size=1' node scripts/validate-content.js --focus-answer-option-accessibility prints `answerOptionAccessibilityParityValidated:true` and a nonzero `answerOptionAccessibilityRulesValidated` count even when unrelated authored-source option parity is red; NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-answer-option-accessibility-parity.test.js passes; `npm run validate:content` still runs full validation and reports q021/q160 option parity when present; `npm run typecheck -- --pretty false` and `git diff --check` pass
MOBILE-ADS-CONSENT-ORDERING-RUNTIME-1 [P0 ads/privacy release-tests] lib/monetization/mobileAdsConsent.ts + lib/monetization/useMobileAdsConsent.ts + scripts/monetization.test.js + tests/content-mobile-ads-consent-hook-parity.test.js: collect Google UMP consent before requesting iOS App Tracking Transparency, then initialize Google Mobile Ads only after both decisions are reflected in `getAdSdkInitializationDecision`; keep Remove Ads and disabled-real-ads short-circuits from showing either prompt, preserve Android UMP-only initialization, and keep the cached hook fail-closed behavior | why: focused REVIEWER monetization/privacy pass on current `origin/main` de3a65b found `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' npm run test:monetization` exits 1 with the consent runtime case failing: `initializeGoogleMobileAdsAfterConsent()` records `[att:get, att:request, ump, ads:init]` while the release test expects `[att:get, ump, att:request, ads:init]`. Source inspection shows `collectMobileAdsConsentState()` awaits `resolveTrackingTransparencyStatus()` before `resolveUmpConsentStatus()`. Google's current AdMob iOS IDFA guidance says the UMP SDK can present an IDFA explainer before the ATT request (https://developers.google.com/admob/ios/privacy/idfa), so the runtime is prompting in the wrong order for a compliant, contextual consent flow. Duplicate search found only `MOBILE-ADS-CONSENT-HOOK-FOCUSED-PARITY-GATE-1`, which isolates validator/test drift around the hook cache, not this native SDK prompt order | verify: NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test --test-name-pattern "native Mobile Ads consent runtime requests ATT and UMP before SDK init|ad consent decision covers ATT and UMP" scripts/monetization.test.js && NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' npm run test:monetization && node --test tests/content-mobile-ads-consent-hook-parity.test.js && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if iOS ATT is requested before UMP, if SDK initialization can happen before both prompt states are known, if Remove Ads users see consent prompts, if Android UMP-only consent regresses, or if initialization cache errors stop failing closed
LAUNCH-POPUP-SUPPRESSED-ROUTES-MONETIZATION-PARITY-1 [P1 ads/release-tests] scripts/monetization.test.js + tests/content-launch-ad-route-parity.test.js + lib/monetization/ads.ts + scripts/validate-content.js: reconcile the monetization test suite's expected `adsConfig.suppressedLaunchPopupRoutes` with the current product and validator route policy, keeping `/exam`, `/practice`, `/quiz`, `/about-the-test`, `/citizenship-requirements`, `/disclaimer`, `/onboarding`, `/privacy`, `/sources`, `/support`, and `/terms` suppressed while `/`, `/home`, `/learn`, `/mistakes`, and `/profile` remain launch-ad eligible; if any listed route should not be suppressed, change `LAUNCH_POPUP_AD_SUPPRESSED_ROUTES`, validator expectations, and focused route parity together instead of leaving split contracts | why: the same focused `npm run test:monetization` run exits 1 on `global launch popup ad is suppressed on active question and compliance routes` because `scripts/monetization.test.js` still expects only eight suppressed routes, while `lib/monetization/ads.ts`, `scripts/validate-content.js`, and `tests/content-launch-ad-route-parity.test.js` already agree on the safer eleven-route list that also suppresses app-open ads on about-the-test, citizenship-requirements, and onboarding surfaces. Duplicate search found the broad `UI-EFFECTS-RELEASE-GATE-DRIFT-1` and the already-correct focused launch route parity test, but no live row for this `test:monetization` route-list drift; leaving it red blocks the v1.0 monetization acceptance command after the consent-order fix | verify: NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test --test-name-pattern "global launch popup ad is suppressed on active question and compliance routes|launch popup ad route suppression" scripts/monetization.test.js tests/content-launch-ad-route-parity.test.js && NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' npm run test:monetization && npm run validate:content && npm run typecheck -- --pretty false; fixtures fail if the route arrays differ between product, validator, monetization, and focused parity tests, if active question/compliance/onboarding/about/requirements routes can show app-open launch ads, or if passive study/profile routes become unintentionally suppressed
PROGRESS-SCHEMA-FOCUSED-PARITY-GATE-1 [P1 learning/tests] scripts/validate-content.js + tests/content-progress-schema-parity.test.js: add a focused validator mode for progress schema/runtime parity and make the progress schema parity tests use it so unrelated authored-source option parity failures cannot mask progress-store and calibration guard assertions; preserve full `npm run validate:content` behavior for complete content validation and keep existing negative progress schema fixtures meaningful | why: while validating CALIBRATION-CONFIDENCE-RATING-RUNTIME-GUARD-1, the direct calibration and hydration tests passed, but the full `tests/content-progress-schema-parity.test.js` failed before its own assertions because separately claimed q021-q040 and q160-q169 published-source option parity failures run first; the suite needs the same isolation pattern as the queued content-type schema focus gate so future progress/calibration regressions report their own failures | verify: node scripts/validate-content.js --focus-progress-schema-parity exits 0 with progress schema/calibration parity summary fields true; node --test tests/content-progress-schema-parity.test.js passes when unrelated authored-source option parity is red; npm run validate:content still runs full validation and reports q021/q160 option parity when present; npm run typecheck -- --pretty false && git diff --check
ABOUT-THE-TEST-FOCUSED-PARITY-GATE-1 [P1 currentness/test-health] scripts/validate-content.js + tests/content-about-the-test-route-copy-parity.test.js: add a focused validator mode for about-the-test route copy/source parity and make the route parity tests use it so unrelated authored-source option parity blockers cannot mask UHR date/currentness assertions | why: while validating COUNTDOWN-BANNER-UHR-CONFIRMED-COPY-1, the about-the-test route tests failed before their currentness assertions because parseValidationSummary() runs full validate:content and current main still has q021-q040/q160-q169 option parity failures; countdown now has a focused mode, but the related about-the-test guard remains noisy | verify: node scripts/validate-content.js --focus-about-the-test-route-copy reports about-the-test route parity fields true/nonzero even while unrelated option parity is red; node --test tests/content-about-the-test-route-copy-parity.test.js passes; npm run validate:content still reports full content blockers; npm run typecheck -- --pretty false && git diff --check
COMPLIANCE-ROUTE-LINK-COMPONENT-GUARD-1 [P1 UIUX/a11y-tests] tests/content-compliance-route-link-parity.test.js + app/support.tsx + app/about-the-test.tsx + app/citizenship-requirements.tsx + components/compliance/LegalPage.tsx: add a lightweight static parity guard requiring route-local legal/compliance CTAs to use `ComplianceActionLink` or `LegalExternalLink` instead of plain styled `Link` text anchors, with an explicit allowlist for non-CTA inline references; preserve the existing browser target-size e2e as the runtime proof | why: while fixing COMPLIANCE-ROUTE-CTA-TARGET-SIZE-1, `/support` and `/about-the-test` only failed at browser time because plain route-local `Link` styles had text-height tap targets. A static guard would catch future legal-route CTAs before the heavier e2e run and should also audit nearby citizenship/about/source routes for the same pattern | verify: node --test tests/content-compliance-route-link-parity.test.js && CI=1 E2E_PORT=4174 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/compliance-link-targets.spec.ts --workers=1 -g '/support|/about-the-test|/citizenship-requirements' && NODE_OPTIONS='--v8-pool-size=1' npm run test:a11y-labels && npm run typecheck -- --pretty false && git diff --check
DASHBOARD-SNAPSHOT-FOCUSED-PARITY-GATE-1 [P1 dashboard/tests] scripts/validate-content.js + tests/content-dashboard-progress-snapshot-parity.test.js + lib/learning/dashboardProgressSnapshot.ts: add a focused `--focus-dashboard-progress-snapshot` validator mode and dedicated dashboard progress snapshot parity test so unrelated authored-source option parity and chapter-localization typecheck blockers cannot mask the `answerHistory`/`answerAttempts` alias contract; preserve full `npm run validate:content` behavior and existing `v1-1-pro-foundations` coverage | why: while validating PR #1801 for `DASHBOARD-SNAPSHOT-ANSWER-ATTEMPTS-PARITY-1`, the focused node tests passed but `npm run validate:content` remained red on q021-q040/q160-q169 authored-source option parity and `npm run typecheck` remained red on the separately claimed chapter localized text blocker. The snapshot contract is now guarded in a broad foundations file, but no focused content/validator path reports only dashboard snapshot drift. Duplicate search found dashboard per-chapter input, content-type schema focus, progress-schema focus, and the current answerAttempts fix, but no dashboard snapshot focused gate | verify: node scripts/validate-content.js --focus-dashboard-progress-snapshot exits 0 with `dashboardProgressSnapshotParityValidated:true`; node --test tests/content-dashboard-progress-snapshot-parity.test.js passes while unrelated content parity is red; full `npm run validate:content` still reports q021/q160 failures when present; focused fixtures fail if `answerAttempts` stops aliasing `answerHistory`, if `answerHistory` loses precedence, or if `questionProgress` fallback double-counts explicit answer history; npm run typecheck -- --pretty false && git diff --check
WEB-EXPORT-BUNDLE-BUDGET-REGRESSION-1 [P1 performance/web-export] app/ + components/ + lib/ + tests/web-export-budget.test.js + scripts/build-config.test.js: reduce the Expo web entry bundle back under the documented mobile budget or explicitly re-baseline the budget with a measured, justified ceiling after identifying the size driver; preserve current web export routing, visual smoke, and release-preflight behavior | why: focused REVIEWER web-export health pass on current `origin/main` after a clean `CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2` and `NODE_OPTIONS='--v8-pool-size=1' node scripts/prepare-web-export.js --check dist-web` found `NODE_OPTIONS='--v8-pool-size=1' npm run test:web-export-budget` exits 1. The generated entry bundle `_expo/static/js/web/entry-80e9fb6350193d73008bec786a30a351.js` measures `rawBytes=2431316`, `gzipBytes=597962`, and `brotliBytes=465992`, exceeding the current documented budget of raw 1650000, gzip 450000, brotli 360000 in `tests/web-export-budget.test.js`; the same export still passes visual smoke and release-preflight tests, so this is a performance budget regression rather than an export crash. Duplicate search found only the existing budget test/build-config references, not a live queue row for restoring or deliberately re-baselining the bundle budget | verify: CI=1 EXPO_NO_TELEMETRY=1 NODE_OPTIONS='--v8-pool-size=1' npm run build:web:export -- --max-workers 2 && NODE_OPTIONS='--v8-pool-size=1' npm run test:web-export-budget && CI=1 NODE_OPTIONS='--v8-pool-size=1' npm run test:e2e -- tests/e2e/visual-smoke.spec.ts --workers=1 && npm run test:release-preflight; fixtures fail if the entry bundle remains over the documented raw/gzip/brotli ceiling without a measured justification, if a re-baseline does not record the measured raw/gzip/brotli values and reason, if route splitting/web export preparation regresses, or if the visual smoke stops rendering primary routes
PRO-FOUNDATIONS-PRICE-SCOPE-DRIFT-1 [P1 ads-iap/tests] tests/v1-1-pro-foundations.test.js + lib/monetization/tierComparison.ts + components/monetization/ProPaywall.tsx: reconcile the broad Pro foundation assertions with current Pro/Remove Ads product copy and tier scope, or split stale price/scope checks into focused tests so ProPaywall-specific guards are not masked by unrelated Pro Lifetime failures; preserve the current separate Remove Ads path and Pro comparison behavior | why: while validating PRO-PAYWALL-INFLIGHT-GUARD-1, the new focused in-flight tests passed, but `node --test --test-name-pattern "ProPaywall|Pro Lifetime|in-flight|buyProLifetime|restoreProLifetime" tests/v1-1-pro-foundations.test.js tests/v1-1-pro-iap.test.js` still failed on existing Pro foundation drift: expected `59 SEK`/`29 SEK` while current Swedish UI uses `kr`, expected `REMOVE_ADS_PRICE_LABEL` in ProPaywall where current copy is literal, and expected older Pro entitlement/table scope. These failures mask unrelated ProPaywall regressions and should be resolved separately from the in-flight lock | verify: node --test tests/v1-1-pro-foundations.test.js --test-name-pattern "tierComparison|paywallCtaLabels|ProPaywall" passes or is split into focused green current-contract tests; node --test tests/v1-1-pro-iap.test.js --test-name-pattern "buyProLifetime|restoreProLifetime" remains green; npm run validate:content still reports only unrelated content-data blockers if present; npm run typecheck -- --pretty false && git diff --check
