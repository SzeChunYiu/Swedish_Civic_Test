P0-GATE-FIXED (operator 2026-05-19): rebrand guards aligned — validate-content.js, app-assets.test.js, publishing.test.js, router-shell.test.js, architecture-scaffold.test.js, release-gates-writer.test.js, release-preflight.js, screenshot-manifest.test.js updated for Almost Swedish / almost-swedish / com.billyyiu.almostswedish. Gate should be green; verify: npm run validate.

>>> READ codex-tasks/P0.md FIRST. While P0.md has open items, only P0-advancing
>>> iterations are accepted; trivial product-path commits are reverted. <<<
ADS-1 lib/monetization/premium.ts + ads.ts: decouple adsDisabled from isPremiumUser; shouldShowAd false when adsDisabled; unit tests (npm test -- monetization).
ADS-2 lib/monetization/ads.ts: replace hardcoded REAL_ADS_ENABLED_FOR_V1=false with env/remote flag; keep Google test units default in dev; lib/monetization/ad-units.real.ts loader (gitignored) for operator real IDs.
IAP-1 lib/monetization/purchases.ts: react-native-iap wrapper buy("removeads") + restorePurchases() + persisted adsDisabled (secure store) + product id constants; mock provider for web/dev/tests.
IAP-2 app/ + components/monetization: "Remove Ads" paywall UI 29 SEK, Buy + Restore wired to purchases.ts->adsDisabled; surface near ad placements; tsc+lint clean.
CONSENT-1 lib/ + app/: expo-tracking-transparency (iOS ATT) + Google UMP consent gate before ad SDK init; no personalized ads without consent.
COMPLY-1 publishing/public-site/app-ads.txt placeholder + update publishing/privacy-labels.md + google-play-data-safety.md (ads SDK + IAP); reports/release-ads-iap-device-qa.md template.
TEST-1 tests: monetization gating unit tests + e2e web stub by entitlement; npx expo export --platform web must not crash with ad stubs.

STATIC-NO-TRACKING-AD-COPY-GUARD-1 [P0 privacy/release-copy/static-site] site/app.js + site/index.html + scripts/static-site-privacy-copy.test.js + scripts/compliance-pages.test.js + scripts/check-live-site.test.js: replace absolute static-site home copy `No tracking` / `Ingen spårning` with accurate ad-supported privacy wording such as no account, local study progress, or no study-answer tracking, and add guards rejecting unqualified no-tracking claims on public ad-supported static surfaces unless the copy is explicitly scoped to first-party study data; preserve the existing AdSense, Google Mobile Ads, consent-signal, and Remove Ads disclosures | why: focused REVIEWER privacy-copy pass on clean origin/main found `site/app.js` still publishes `numbers.4: "to start. No login. No tracking."` and Swedish `numbers.4: "att börja. Ingen inloggning. Ingen spårning."`, while the same file and `site/index.html` privacy copy disclose Google AdSense, Google Mobile Ads, ATT/UMP consent, and ad/consent signal processing. Duplicate search over `codex-tasks/open.txt` and `claims.txt` for `No tracking`, `Ingen spårning`, and tracking-copy rows found release/privacy posture tasks but no static home no-tracking copy guard. Current tests miss it: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test scripts/static-site-privacy-copy.test.js scripts/compliance-pages.test.js scripts/static-site-source-provenance-copy.test.js scripts/check-live-site.test.js --test-name-pattern 'privacy|Privacy|ads|Ads|tracking|Tracking|static|Static|compliance|live|site'` passed 25/25 | verify: npm run validate:content && node --test scripts/static-site-privacy-copy.test.js scripts/compliance-pages.test.js scripts/static-site-source-provenance-copy.test.js scripts/check-live-site.test.js; direct source/render guards fail if site/app.js, site/index.html, or extra public static dictionaries expose unqualified `No tracking`, `Ingen spårning`, or equivalent absolute tracking-denial copy while AdSense/AdMob disclosures remain active, and confirm the replacement copy still communicates no account, local progress, no study-answer sharing, independent status, and current ads/IAP behavior

PRACTICE-INTERSTITIAL-RETRY-QUESTION-CAP-1 [P0 ads-iap/ad-frequency] app/(tabs)/practice.tsx + components/monetization/PracticeInterstitialAd.native.tsx + lib/quiz/practiceSessionStore.ts + scripts/monetization.test.js + tests/content-ad-placement-route-parity.test.js + scripts/practice-flow.test.js + scripts/validate-content.js: cap `quiz_completed_interstitial` to one eligible native interstitial per active practice question feedback cycle, not one per selected option id, so pressing `Try again` and choosing another answer on the same question cannot create a fresh ad key; preserve the existing no-ad active-answering state, adsDisabled/consent/platform-unit gates, web preview fallback, and coordinate with `NATIVE-INTERSTITIAL-SHOW-CAP-1` so failed SDK show attempts can still retry without permitting repeated shown ads for the same question | why: focused REVIEWER ads-frequency pass on clean origin/main found `app/(tabs)/practice.tsx` renders `<PracticeInterstitialAd showKey={`${question.id}:${selectedOptionId ?? ''}`} />`, the same screen exposes `Try again` wired to `resetSelection`, `lib/quiz/practiceSessionStore.ts` clears only `selectedOptionId` while keeping `activeQuestionId`, and `PracticeInterstitialAd.native.tsx` suppresses only identical `lastInterstitialShowKey` values; a learner who retries one question with a different option therefore gets a different interstitial key before advancing. Current guards miss and partly lock in the issue: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test scripts/monetization.test.js tests/content-ad-placement-route-parity.test.js scripts/practice-flow.test.js --test-name-pattern 'practice completion|PracticeInterstitial|interstitial|practice session|resetSelection|ad placement'` passed 49/49 while `scripts/monetization.test.js`, `tests/content-ad-placement-route-parity.test.js`, and `scripts/validate-content.js` still assert the per-selected-option `showKey` string | verify: npm run validate:content && npm run test:monetization && node --test tests/content-ad-placement-route-parity.test.js scripts/practice-flow.test.js scripts/ui-effects.test.js && npm run typecheck -- --pretty false; source/runtime guards fail if `showKey` includes `selectedOptionId`, if `Try again` plus a second option can load or show another interstitial for the same active question/session, if advancing to the next question cannot create a new eligible key, if SDK show failures cannot retry under the separate lifecycle cap, or if Remove Ads/ATT/UMP/web-preview/no-exam-ad behavior regresses


WEB-DEPLOY: make the web build host-agnostic and deploy-ready. (1) app.json: expo.web.output="single"; baseUrl only if served from a subpath. (2) npm run build:web:export -> dist-web; in build add cp dist-web/index.html dist-web/404.html. (3) Add vercel.json (SPA rewrite all->/index.html) and public/_redirects (/* /index.html 200) so Vercel OR Netlify OR Cloudflare Pages deploy in one click. (4) Add .github/workflows/web-deploy.yml that builds + uploads dist-web as a CI artifact (do NOT assume GitHub Pages; repo is private). Acceptance: workflow green AND dist-web/index.html loads the app offline (open file -> mounts). Product path app.json + .github/ + vercel.json. One atom, no monetization/test edits.

=== PRIORITY FOCUS (operator, 2026-05-17): UI/UX POLISH + CONTENT VERIFICATION ===
The v1.0 frame exists & is 7/7 green. Two sustained priorities now, every iteration until done:

UIUX-POLISH (lane SETUP/UI, repeat as many atoms as needed): Implement the v1.0-SAFE UI/UX
recommendations from docs/competitive-teardown.md (NOT the backend/v1.1 items). Each atom: pick
ONE concrete improvement and ship it in app/ or components/ — button affordance/size/states/
hierarchy, layout & spacing on the 8px scale, visual hierarchy, empty states, answer-reveal
SV+EN explanation, calmer information density, loading/disabled states, tap targets >=44px,
a11y labels. MUST honor DESIGN.md tokens (zero hardcoded colors — keeps step 6 green), keep all
7 acceptance steps green. Reference a named teardown row in the commit msg.

CONTENT-VERIFY (lane CONTENT/REVIEWER, repeat): Rigorously audit the question bank in data/.
Each atom: take a batch of questions and verify — (a) traceable to UHR Sverige i fokus ref,
(b) Swedish natural & correct, (c) English an accurate translation, (d) the marked answer is
factually correct, (e) disclaimer present on every question screen. Fix defects in data/ and
file via review-to-queue.sh. A wrong answer or mistranslation is a P0. Do NOT mass-rewrite;
verify, correct, cite the UHR page.
=== END PRIORITY FOCUS ===

MOCK-EXAM-TIME-HEATMAP-RESULTS-1 [P1 mock-exam-realism/learning-analytics] app/(tabs)/exam.tsx + components/MockExamTimeHeatmap.tsx + components/ResultSummary.tsx + lib/learning/examDiagnostic.ts + lib/storage/progressStore.ts + tests/content-exam-submission-finality-parity.test.js + tests/content-progress-schema-parity.test.js + tests/v1-1-retention-selectors.test.js + scripts/exam.test.js: implement blueprint 20d by capturing clamped per-question elapsed seconds during active mock exams, building a completed exam QuizSession/diagnostic from submitted answers, and rendering a localized accessible result-screen time heatmap whose cells are colored relative to median time and jump/focus to the matching review card; persist or normalize timing metadata only where needed for mock-exam history, keep submitted results final, keep no ads during timed exams, and preserve unanswered/time-expired review behavior | why: focused REVIEWER mock-exam realism pass on current origin/main 5e84387 found the helper layer already has `QuizAnswer.timeSpentSeconds` and `ExamDiagnostic.perQuestionMs`, but app/(tabs)/exam.tsx records only `sessionId`, score, completedAt, correctCount, and totalCount through `recordMockExamSession()`, while the result route renders only `ResultSummary`, chapter breakdown, and review cards. There is no completed exam QuizSession, no active-exam per-question elapsed-time capture, no `MockExamTimeHeatmap`/`TimeHeatmap` result surface, and `dashboardProgressSnapshot` maps stored mock exams back to empty answer arrays. Existing `ANSWER-HISTORY-TIME-SPENT-CAPTURE-1` covers Practice and routed Quiz answer history, not timed mock-exam pacing. Current guards miss the learner-facing gap: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test scripts/exam.test.js tests/content-exam-submission-finality-parity.test.js tests/content-exam-route-copy-parity.test.js tests/content-progress-schema-parity.test.js tests/v1-1-retention-selectors.test.js --test-name-pattern 'exam|mock|time|diagnostic|perQuestion|submit|review|progress'` passed 44/44, and `npm run validate:content -- --json` passed with 795 questions and `examSubmissionFinalityParityValidated:true` | verify: npm run validate:content && node --test scripts/exam.test.js tests/content-exam-submission-finality-parity.test.js tests/content-exam-route-copy-parity.test.js tests/content-progress-schema-parity.test.js tests/v1-1-retention-selectors.test.js && npm run typecheck -- --pretty false; source/runtime guards fail if submitted mock exams cannot produce ordered positive per-question timing rows, if the result screen lacks a localized accessible heatmap and median/rushed/over-thought/stuck labels, if heatmap cells do not jump or focus the matching review card, if time-expired or partially unanswered submissions crash or fabricate timings, if stored mock-exam history drops normalized timing metadata when present, or if the no-ad timed-exam invariant regresses

Q145-SECRET-BALLOT-SV-PRONOUN-NATURALNESS-1 [P0 TRANSLATE-COMPLETE/source-content] data/additionalQuestions.ts + scripts/validate-content.js + tests/content-published-question-types.test.js + content/question-bank.csv + site/questions.js: rewrite q145 Swedish answer-option wording `Ingen behöver avslöja hur den röstar` and the derived q736/q737/q739 exported prompt/option variants into natural Swedish such as `Väljare behöver inte avslöja hur de röstar` or equivalent, then regenerate canonical CSV/static banks and add a guard rejecting learner-facing `hur den röstar` in source/generated/exported question text while preserving the secret-ballot meaning and English copy | why: focused REVIEWER TRANSLATE-COMPLETE pass on current origin/main 95177b94 found `data/additionalQuestions.ts` q145 stores `Ingen behöver avslöja hur den röstar`, and the same unnatural singular pronoun phrase is published in `content/question-bank.csv` q145/q736/q737/q739 plus `site/questions.js`; the explanation already uses the natural `väljare ... hur de röstar`, so the option/derived stem is a stale source wording defect. Duplicate search for `q737`, `hemliga`, `den röstar`, `hur den`, and `secret means` found no existing open task, while adjacent generated-fragment rows cover different answer-scaffold families. Current guards miss it: `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test tests/content-published-question-types.test.js tests/content-question-sentence-endings.test.js scripts/derived-content.test.js --test-name-pattern 'published|generated|question|derived|sentence|secret|hemliga|pronoun|den röstar|q145|q737'` passed 53/53, and `npm run validate:content -- --json` passed with `questionBilingualTextPairsValidated:795`, `questionGeneratedTrueFalseNaturalnessValidated:795`, and `questionSentenceEndingsValidated:795` | verify: npm run validate:content && node --test tests/content-published-question-types.test.js tests/content-question-sentence-endings.test.js scripts/derived-content.test.js tests/content-static-site-question-bank-parity.test.js && npm run content:export && node scripts/export-site-question-bank.js --check && npm run typecheck -- --pretty false; direct scans of data/additionalQuestions.ts content/question-bank.csv and site/questions.js fail on `hur den röstar`, while q145 and q736-q739 still explain secret ballots naturally in Swedish/English, keep the correct answer id, UHR citation, tags, generated true/false truth values, and no UI-afforded prefixes

=== PRIORITY: APPLY CLAUDE DESIGN SYSTEM (operator 2026-05-17) ===
DESIGN.md is now the Claude (Anthropic) warm-parchment system. Execute in order:
DESIGN-1 [DONE 2026-05-18 — verified on origin/main]: lib/theme/colors.ts already matches DESIGN.md §2 exactly (canvas #f5f7fa, accent #006aa7, navy #003a5c, success #1e874b, warning #c77700, … all keys/values); gate tests/content-theme-token-schema.test.js 2/2 green. NO further work — do not re-queue. (Original spec retained below for history.)
DESIGN-1-HIST [P0, one atom]: rewrite lib/theme/colors.ts token VALUES to exactly the
  table in DESIGN.md section 2 (same KEYS, new values). Update the DESIGN.md
  line-ref comments to point at the new file. tsc must pass (values must satisfy
  the HexColor/RgbaColor types). This single change restyles the whole app via
  the token system; acceptance step 6 (zero hardcoded colors) MUST stay green.
  Verify: npx tsc --noEmit and npm test -- monetization.
DESIGN-2..N [repeat atoms]: align components/ and app/ to DESIGN.md sections 3-5
  — serif headings (load a serif via expo-font, weight 500, never 700), radius
  scale (cards 8-12, primary button 12, pills 9999), warm ring + whisper
  shadows, body line-height 1.6, tap targets >= 44px. Token-only colors, keep
  all 7 acceptance steps green, honor the DESIGN.md do and dont lists. This
  folds into the existing UIUX-POLISH priority — the Claude look is the target.
=== END CLAUDE DESIGN PRIORITY ===

=== PRIORITY: MOTION + GRAPHICS RICHNESS (operator 2026-05-17) ===
Make the app feel alive and delightful — generous, polished motion and graphics
throughout, WITHOUT breaking the calm Claude aesthetic, a11y, or 60fps.
Repeatable atoms in app/ + components/ (token-driven via lib/theme/motion.ts —
add motion tokens for durations/easings, no hardcoded timings/colors):
- Entrance/stagger animations for lists, cards, screens (Reanimated/Moti).
- Route/screen transitions (shared-element where natural).
- Micro-interactions: button press/scale, ripple, focus, toggles, tab switches.
- Celebratory moments: correct answer, quiz complete, streak/XP, badge unlock
  (confetti/Lottie, spring pop, count-up numbers) — tasteful, ~1s, skippable.
- Skeleton/shimmer loaders, animated empty states, progress ring animations.
- Subtle ambient: parallax/illustration motion on hero/results, gentle.
- Add richer graphics: Lottie animations + organic illustrations matching
  DESIGN.md (terracotta/warm, hand-drawn feel), not generic stock.
HARD RULES: honor prefers-reduced-motion (respect Accessibility reduce-motion —
provide reduced/no-motion variants); keep 60fps (use native driver/Reanimated
worklets, no JS-thread jank); colors via tokens only; all 7 acceptance steps
stay green; NO motion on the exam screen that distracts; nothing janky on the
web export. Folds into UIUX-POLISH. Tasteful-but-abundant, premium not noisy.
=== END MOTION PRIORITY ===

=== PRIORITY: CONTENT & QUESTION-QUALITY OVERHAUL (operator 2026-05-17) ===
Operator-authorized content-policy update. The factory follows THIS over the
older CLAUDE.md content-rule #1/#5. KEEP the legal-safety subset unchanged.

LEGAL-SAFETY (still NON-NEGOTIABLE, never relax):
- Never claim official affiliation with UHR/Skolverket/Migrationsverket.
- Never claim questions are real/official exam questions.
- Disclaimer (not a real/official exam) visible on every question screen.
- Every question MUST cite a specific, verifiable PUBLIC source.

DIFFICULTY-1 [feature, v1.0]: add a difficulty setting (Lätt / Medel / Svår).
  Schema: each question carries distractor tiers by plausibility. Quiz engine
  picks distractors per chosen difficulty — harder = more plausible, closely
  related, similar-looking wrong options for the SAME question. Ship selector
  (Settings + pre-quiz) + engine + schema now; content lane backfills harder
  distractors progressively. Easy must still be fair; Hard genuinely tricky.

SOURCE-CITATION [DONE 2026-05-17 — commit d73f546 / PR #109]: The "Enligt
  UHR-materialet, ..." / "According to the UHR material, ..." connective has
  been stripped from ALL question stems (61 base literals) and from the
  derived single-choice variant template (lib/content/derivedQuestions.ts).
  UHR is now cited only via the uhrChapter/uhrSection/uhrPage fields (the
  Källa/Source line), never in the stem. Enforced by an executable gate:
  tests/content-uhr-source-citation-stem.test.js (wired into test:content) —
  fails if any stem carries the connective. question-bank.csv offenders
  228 -> 0; full test:content 202/202 green. DO NOT re-queue per-question
  UHR-strip work; the gate keeps this done. (The not-a-real-exam disclaimer
  is separate and unchanged.)

TRANSLATE-COMPLETE [v1.0, P0]: 100% SV+EN parity — validate:content FAILS if
  any user-facing string or question field lacks both. Then a naturalness pass:
  English must be natural, idiomatic, fluent (not literal/MT). Stilted English
  = P0 defect via review-to-queue. Swedish must be correct & natural too.

=== OPERATOR DIRECTIVE 2026-05-18 — MANAGER MUST SOLVE, NEVER PARK (hard, overrides any prior hold logic) ===
MANAGER CONTRACT (operator, P0, non-negotiable): your job is to keep EVERY
worker pane productive every cycle. You may NEVER respond to a blocker, a
dirty worktree, or a moved boundary by sleep-holding / parking / idling a
pane. A dirty worktree caused by OTHER lanes doing their own in-scope work
is NORMAL and EXPECTED in this parallel factory — lanes have disjoint
write scope precisely so a globally-clean tree is never required. "Tree
dirty" / "boundary moved" / "waiting on a VALIDATOR/GM/REVIEWER lease" are
NOT stop conditions and NEVER justify holding a pane. If a worker's current
atom is genuinely blocked, RE-TASK that pane immediately to the next
independent unblocked Phase-A SOURCE-EXPANSION atom or a CONTENT-VERIFY
atom — do not sleep it. An idle/held pane is a P0 MANAGER defect to fix
this cycle, not an acceptable state. The only thing that may block a *push*
is a hard correctness failure (tsc error, test:content red, a content gate
red); that still never blocks a *pane* from moving to other work.
PHASE-B IS DEFERRED BY OPERATOR (2026-05-18): Phase-B (non-UHR / external
authorities) and the EXTERNAL PROVENANCE-LABEL workstream are OUT OF SCOPE
this milestone. They are NOT "rejected pending a VALIDATOR lease" — they
are simply deferred until the operator explicitly re-opens them after
Phase-A UHR coverage is saturated. Do NOT start, queue, or hold any pane on
Phase-B / external work. All content capacity goes to Phase-A (exhaust UHR
"Sverige i fokus") + CONTENT-VERIFY + citation rigour on the existing
UHR-provenance bank only.
=== END OPERATOR DIRECTIVE ===

SOURCE-EXPANSION [content, P0 program, target = 2000 verified questions]:
  PHASE A — EXHAUST UHR: mine the UHR "Sverige i fokus" material
  completely. Every distinct fact/concept/topic in every UHR chapter &
  section must yield question(s) until UHR coverage is SATURATED (no
  remaining UHR fact a fair test could ask that we don't cover). Add a
  UHR-coverage report/check: per UHR chapter+section, list facts vs
  questions; gaps are tracked and burned down.
  PHASE B — EXPAND SAME TOPICS FROM OTHER AUTHORITIES: within the SAME
  UHR topic domains, add questions researched from OTHER authoritative
  public sources, e.g. riksdagen.se / lagen.nu (law), regeringen.se,
  Skatteverket, Migrationsverket, SCB, Skolverket, Forsakringskassan,
  1177, Arbetsformedlingen, Pensionsmyndigheten, MSB/krisinformation.se,
  Sweden.se / Swedish Institute, official kommun/region .se pages. Prefer
  primary official sources over secondary.
  TARGET: ramp the bank to ~2000 questions total, balanced across all
  civic domains and difficulty (Latt/Medel/Svar), NO near-duplicate
  stems/answers (reuse the duplicate-text detector; dup = reject).
  STRICT CITATION GATE (executable, hard, per question — uncited or
  unverifiable = P0, NOT shippable): every question MUST carry a
  specific verifiable public source — concrete source title + locator
  (chapter/section/page or stable public URL), not a vague 'official
  info'. That citation MUST render to the user on the website (the
  Kalla/Source line under the question). Add/extend a test asserting:
  (a) 100% of questions have a non-empty, well-formed verifiable source;
  (b) the source string is surfaced in the rendered question UI. A
  question whose fact cannot be traced to a citable public source is
  deleted, not guessed. Factually wrong/outdated = P0.
  PROVENANCE LABEL (executable, hard): every question has a required
  provenance field marking its origin as either the official UHR study
  material ("Sverige i fokus") or an EXTERNAL authority. Every non-UHR
  question MUST show a clear, localized label/badge in the rendered UI
  (SV+EN, e.g. "Extern kalla - utover UHR-materialet" / "External
  source - beyond the UHR material") so the learner always knows which
  questions come from the official study material vs supplementary
  authoritative sources. UHR-origin questions are NOT given the external
  label. This label is IN ADDITION to (never a replacement for) the
  specific Kalla/Source citation line. Add/extend a test asserting:
  (a) 100% of questions have a valid provenance value (uhr|external);
  (b) every external-provenance question renders the external label in
  the question UI; (c) no UHR question is mislabeled external. A non-UHR
  question without the visible external label = P0, not shippable.
  QUALITY (unchanged, all still enforced): original PARAPHRASED wording
  (never verbatim source text), natural fluent language, SV+EN parity
  now (then the EBOOK multilingual set), not-a-real-exam disclaimer, no
  claimed official affiliation, DESIGN.md tokens, all content gates green.
  PROCESS: incremental atoms (~25-50 fully-sourced verified questions per
  atom), id-sequence continues, regenerate question-bank.csv, run
  test:content each atom (never push red). Grow coverage; never dilute
  accuracy or citation rigour to hit the count.

FILL-GAPS [standing quality bar, operator 2026-05-17]: the user's bar is
  "fill every gap, fill every detail of all aspects of the project."
  Every lane (content, UI, monetization, ebook, tests, accessibility,
  i18n) should actively hunt and close its own gaps each iteration, not
  just touch a path. Partial/placeholder/TODO left behind = a defect to
  log and burn down. Thoroughness and completeness are the standard.
=== END CONTENT OVERHAUL ===

=== P0 BUG — DO THIS FIRST, ahead of all cosmetic/design work ===
SHUFFLE-FIX [DONE 2026-05-18 — verified on origin/main]: implemented in lib/quiz/answerOptionShuffle.ts (seeded Fisher-Yates keyed by question+session, stable within a session) + summarize/balance checks, wired into lib/quiz/examGenerator.ts, practiceSessionStore.ts, app/quiz/[sessionId].tsx, app/(tabs)/practice.tsx. Gates scripts/answer-shuffle.test.js + tests/content-answer-shuffle-parity.test.js 9/9 green. NO further work — do not re-queue. (Original spec retained below for history.)
SHUFFLE-FIX-HIST [P0, correctness]: Single-choice answer options are NOT shuffled —
data/questions.ts authored with correct answer mostly first (correctOptionId
'a' dominant) and lib/content/derivedQuestions.ts normalizeSingleChoiceOptions
maps by index with no randomization, so the correct answer is almost always A.
This makes the quiz trivially gameable and looks broken.
FIX (root cause, in the derivation/session pipeline, NOT a data-only patch):
- Shuffle single-choice options with a SEEDED RNG keyed by (questionId +
  sessionId) so order is randomized but STABLE within a quiz session/instance
  (must NOT reshuffle on re-render or change mid-question), and reproducible
  for review screens and tests.
- Remap correctOptionId after shuffle; isCorrectAnswer must still resolve
  correctly; explanations/option feedback follow the option, not the slot.
- Exclude true_false from positional shuffle (keep true/false order fixed).
- Interacts with DIFFICULTY-1: pick distractors by difficulty FIRST, then
  shuffle the final option set.
TESTS (add, must pass): over the full published bank, correct-answer display
  position is ~uniform (no single position > 35%); shuffle stable within a
  session; isCorrectAnswer correct post-shuffle. Keep all 7 acceptance steps
  green. This is the highest-priority item in this file.
=== END P0 ===

=== PRIORITY: ONLINE STUDY EBOOK (operator 2026-05-17) ===
EBOOK [feature+content, v1.0 = SV+EN; more langs tracked in v1.1 spec]:
Add a "Study / Läs" section readable ONLINE on the web build (and in-app),
a structured study ebook covering the Swedish citizenship knowledge domains
(geography & nature, history, society & democracy, working life, laws & rights,
everyday life, culture, etc.) — chapters + table of contents + in-chapter
navigation, comfortable reading layout per DESIGN.md (serif headings, 1.6
line-height, parchment), works great on the live web URL.
HARD CONTENT RULES:
- ORIGINAL, PARAPHRASED prose — do NOT copy UHR (or any source) text
  verbatim or near-verbatim. Rewrite naturally in our own words.
- Natural, fluent language (not machine/literal). SV + EN parity now, then
  EXPAND to the major languages spoken by immigrants in Sweden:
  Arabic, Persian (Farsi/Dari), Somali, Tigrinya, Kurdish (Sorani +
  Kurmanji), Bosnian/Croatian/Serbian, Polish, Turkish, Spanish, Russian,
  Ukrainian, Thai, Mandarin, Finnish (prioritise by community size;
  confirm/extend the set with quick public-data research).
  NATIVE-QUALITY GATE (hard, per language): each translation must read as
  if written by an educated NATIVE speaker — idiomatic, grammatically
  perfect, culturally natural register, NOT machine/literal. A stilted or
  MT-flavoured translation is a DEFECT, not shippable. Add a per-language
  native-quality review lane/checklist; mark a language done only when it
  passes that review. One language chapter-set per atom.
- Each chapter cites its verifiable public source(s) at the end.
- Keep legal-safety: no claimed official affiliation, not an official text,
  disclaimer present. Factually accurate & current; wrong = P0.
- Cross-link: questions can reference the relevant ebook chapter.
Ship SV+EN chapters incrementally (repeatable atoms), web reading UX first.
=== END EBOOK ===

=== PRIORITY: PANEL-OPT + DEDICATED CRITICAL REVIEWER (operator 2026-05-17) ===
PANEL-OPT [repeatable, P1]: optimize the information/panels on EVERY screen.
Per screen: one primary focus + minimal supporting context. Remove clutter,
redundant labels, empty/decorative panels, duplicated info. Strong hierarchy
(title > primary action > support). Mobile + web. Token colors only, DESIGN.md
§5, keep 7/7 green. One screen per atom; name the screen in the commit.

CRITICAL-REVIEWER [dedicated standing lane, every iteration — Reviewer Contract]:
A hostile, product-minded reviewer. Each iteration: pick ONE aspect of ONE
screen/flow (UI, UX, layout, info density, copy, Swedish accuracy, English
naturalness, answer correctness, a11y, perf, motion, the answer-shuffle, the
source-citation) and ATTACK it as a demanding real user/expert. EVERY finding
MUST be filed via /Users/billy/Desktop/projects/.shared/review-to-queue.sh
(--team build/content --severity blocker|major|minor --kind user-sim|functional|
data|language|a11y) so it becomes a must-fix queue item — NOT a markdown
report (a report-only iteration is reverted like any audit-loop). Severities:
wrong answer / missing translation / broken flow = blocker. The reviewer never
accepts "looks fine" — it always finds the next concrete defect until the app
is genuinely product-level. Findings route back to build/content and must be
closed by a product fix, not prose.
=== END ===

=== PRIORITY: "DID YOU KNOW?" FACT BUBBLE (operator 2026-05-17) ===
FACT-BUBBLE [feature+content, P1]: a delightful "Visste du? / Did you know?"
bubble showing a short interesting fact / 冷知識 about Sweden (history, society,
nature, culture, quirky civic trivia). Show at non-distracting moments: home,
between questions / loading, quiz-results, ebook chapter ends — NEVER on the
active exam/question screen. Tasteful gold-accent bubble per DESIGN.md (gold =
highlight token), gentle entrance motion (respects reduced-motion), dismiss /
"another one" affordance.
CONTENT RULES (same as questions): curated facts dataset; each fact factually
accurate & current, ORIGINAL paraphrased wording (never copied verbatim),
natural fluent language, SV+EN parity now (full language set in v1.1), each
fact cites a verifiable public source. Inaccurate fact = P0. Start ~30-50
strong facts, expandable; rotate without immediate repeats.
TONE (operator 2026-05-17, user directive): FACT-BUBBLE MUST carry a
sense of Swedish humor — witty, warm, self-aware (lagom, fika, the
queue ticket, jantelagen, weather complaints, Systembolaget, allemans-
ratten quirks, etc.). Dry encyclopedic trivia is a MISS even if
factually correct; the humor is the point and makes the app lively.
Humor must land naturally in BOTH SV and EN (not a translated pun that
dies in one language) and never undercut factual accuracy or the
not-a-real-exam disclaimer. Aim: a Swede smiles in recognition; a
learner abroad finds it charming and memorable.
=== END ===

=== PRIORITY: SWEDISH MASCOTS (Jellycat-style) + PICKER (operator 2026-05-17) ===
MASCOT [feature+art, P1]: a selectable Swedish mascot companion.
Art style: Jellycat-INSPIRED (soft rounded plush look, gentle bean shapes,
simple friendly dot/curve faces, calm pastel fills tinted to DESIGN.md tokens)
— ORIGINAL art, NOT copies of Jellycat's actual products/characters (style
only, no IP infringement). Implement as scalable inline SVG React components
(no external/binary assets, no AI-image dependency), token-colored, crisp at
any size, animatable.
Initial set (>=6, expandable): Dalahäst (Dala horse), Älg (moose),
Kanelbulle (cinnamon bun), Skogskatt (Swedish forest cat), Igelkott
(hedgehog), Räv (fox), Kräfta (crayfish), Lingon sprite.
UX: a mascot PICKER screen/section (gallery, choose one, persisted in
settings/entitlements store); chosen mascot appears as a friendly companion on
home / results / streak / empty states / fact bubble — NEVER on the active
exam/question screen. Reactive micro-animations tie into the MOTION priority
(idle bob, happy bounce on correct answer / quiz complete / badge), respect
prefers-reduced-motion, 60fps, token colors only, keep 7/7 acceptance green.
Repeatable atoms: one mascot SVG or the picker per atom; name it in the commit.
=== END ===

MASCOT-UPDATE (operator 2026-05-17): add "Skoglimpa" (Swedish dark rye/forest
loaf) to the mascot set. FEATURE Kanelbulle + Skoglimpa as the highlighted
"favourites" in the picker (top row / pre-selected default = Kanelbulle).
Same Jellycat-inspired original SVG style + reactive animation rules.


=== PRIORITY: UI-OVERHAUL (operator 2026-05-17, user directive) ===
UI-OVERHAUL [P0, feature+design, repeatable atoms]: the app is functionally
mostly there but "doesn't look like an app yet" — it feels thin. Make it
feel like a rich, real, polished mobile/web app. ADDITIVE ambition: MORE
buttons, MORE functions, more interactive surfaces and affordances, stronger
app-like navigation (tab bar / clear primary actions / quick actions / empty
states with CTAs / progress + streak surfaces / settings depth). This is
DISTINCT from PANEL-OPT (declutter): screens should be dense and capable
where it helps, not sparse — but still strong hierarchy, DESIGN.md tokens
only, reduced-motion respected, mobile + web parity, SV+EN parity. Distil
concretely from comparable best-in-class test-prep / language apps (ties to
the competitive-distillation directive): identify specific patterns/screens
they have that we lack, then build them. One screen/feature per atom; name
it in the commit; keep all content/test gates green. Acceptance per atom:
the screen gains a real, usable, app-grade capability (not decoration).
=== END UI-OVERHAUL ===

=== PRIORITY: MONETIZATION-OPT (operator 2026-05-17, user directive) ===
MONETIZATION-OPT [P1, research+feature]: optimise monetisation WITHOUT
hurting the learner. (a) RESEARCH (write findings to docs/, cite sources):
how comparable test-prep / language / civic apps place ads and price
upgrades; where ads are least disturbing. (b) AD-PLACEMENT POLICY: ads ONLY
at natural breakpoints — results screen, between question sets, ebook
chapter ends, home — and a hard rule + executable test asserting NO ad ever
renders on the active exam/question screen. (c) OPTIMISE: Remove-Ads paywall
placement & copy near (not on) ad surfaces, rewarded-ad value exchange,
upgrade conversion. Reuse existing lib/monetization (ADS-*/IAP-*); keep
adsDisabled/entitlement gating intact; tsc + npm test -- monetization clean.
One atom per change; research atom first, then policy test, then tuning.
=== END MONETIZATION-OPT ===

=== PRIORITY: I18N + CONTENT-PROVENANCE + WORDING (operator 2026-05-18, user directive) ===
Context: user flagged (a) language picker only works on tab routes, missing
on /sources, /settings, /quiz/*, /chapter/*, /ebook/*, etc.; (b) only sv+en
actually work — ar/fa/so/ti/pl/tr are "Coming soon" stubs and Chinese is
absent (now added as zh-Hans/zh-Hant placeholders); (c) auto-generated
true/false questions read like translation puzzles: 262 of them use the
nested template "True or false: A correct answer to <q> is <a>" / "Sant
eller falskt: Ett korrekt svar på frågan ..."; (d) no UI label distinguishes
UHR-sourced content from algorithmically derived or hand-written
supplementary content; (e) no "What is the Swedish civic test?" onboarding
screen exists. Operator landed three small fixes already on
feat/lang-picker-global-wording-rules: LanguagePicker mounted on root Stack
header so it shows on every non-tab route; "True or false:" / "Sant eller
falskt:" stripped at render time in lib/quiz/questionText.ts (and mirror
test); zh-Hans/zh-Hant added to locales (available:false placeholders).
Canonical wording rules now live at docs/content/wording-rules.md — every
session writing content MUST follow them. AGENTS.md links to it. The
following atoms turn the placeholders into real features.

I18N-1 [P1, feature]: schema migration — replace hardcoded questionSv /
questionEn pairs in types/content.ts with
questionText: Record<LocaleCode, string> (same for options and explanations).
Backwards-compatible: lib/quiz/questionText.ts maps {sv, en} pairs to the
new shape on read. Acceptance: tsc clean, every existing question still
renders identically in sv + en, new schema accepts adding any locale from
lib/i18n/locales.ts without touching question rendering code.

I18N-2 [P2, feature]: actual translation pipeline for the 6+2 placeholder
locales (ar/fa/so/ti/pl/tr/zh-Hans/zh-Hant). Approach: LLM first pass with
strict instruction to follow docs/content/wording-rules.md, then human
translator review per locale, gated by an `available` flag promotion. RTL
layout flip when locale.rtl=true. Acceptance: any one new locale can be
fully selected end-to-end (UI strings + question content + ebook) with
correct script direction.

I18N-3 [P2, feature]: per-screen translation completeness badge — when a
locale renders content that has not been human-reviewed in that locale, a
small "Translation not yet reviewed" pill shows on the question card. The
fallback locale text appears in lighter style underneath. Never silently
substitute.

PROV-1 [P1, feature]: add provenance: 'uhr' | 'derived' | 'editorial' to
PracticeQuestion. uhrReference becomes optional when provenance !== 'uhr'.
Add sourceNotes?: string for derived/editorial. Backfill: every existing
question with tag 'published-variant' → provenance='derived', referencing
its source question id; every other published question → provenance='uhr';
none → editorial (none exist yet). Acceptance: tsc + every content
validation test green; bank shows the same questions but now each has an
honest provenance value.

PROV-2 [P1, feature]: UI provenance badge on every question card and ebook
chapter heading — UHR (blue), Supplementary (orange), Editorial (grey).
Tooltip on tap reveals the source notes. Acceptance: every question/chapter
visibly carries its badge; a11y label includes provenance.

PROV-3 [P1, feature]: practice config gains a toggle "Include supplementary
questions (non-UHR)" — default OFF. When OFF, only provenance='uhr'
questions are drawn. Persists in settings store. Mock exam mode hardcodes
provenance==='uhr' regardless of the practice toggle.

PROV-4 [P2, content]: write an "About sources" expandable panel — explains
the three provenance types in plain Swedish + English, why we include
derived/editorial, and how to opt in/out. Links from the practice config
toggle and from the question card tooltip.

WORDING-1 [P1, content+code]: retire buildTrueStatementVariant and
buildAnswerJudgementVariant in lib/content/derivedQuestions.ts — they
produce stilted nested-quote questions that violate
docs/content/wording-rules.md §2. Drop them and regenerate the bank.

WORDING-2 [P2, content+code]: rewrite buildFalseStatementVariant to emit a
flat declarative statement (with answer: false) instead of "True or false:
A correct answer to <q> is <wrong>". Approach: take the source single_choice
question's correct option, generate a clearly-wrong assertion, run an LLM
pass to phrase it naturally in sv + en, then CONTENT-team review before
publish. Acceptance: every regenerated TF question reads as a natural
declarative statement; CONTENT manager accepts a sampled batch.

ONBOARD-1 [P1, content+feature]: "What is the Swedish civic test?" first-run
modal + permanent /about-the-test screen linked from home footer and from
settings. Covers: UHR Sverige i fokus origin, purpose of the test, who
takes it (citizenship applicants), what's covered, format, disclaimer that
this app is independent and unofficial. Multilingual (uses I18N-1 schema
when landed; until then, sv + en only).

SITE-AUDIT-1 [P2, audit]: walk every screen (home, learn, practice, exam,
mistakes, profile, chapter/*, quiz/*, ebook/*, sources, settings, account,
search, terms, privacy, support, disclaimer, onboarding, /about-the-test
once it lands) on web build and mobile, file every missing element / dead
button / unclear copy / broken navigation as a routed reviewer finding via
.shared/review-to-queue.sh. Findings become work for the responsible team
queue. Acceptance: a single audit report at reports/site-audit-2026-05-18.md
plus N routed findings, no "noted in docs" closures.
STATIC-HERO-PASSPORT-OUTCOME-COPY-1 [P0 legal/TRANSLATE-COMPLETE/static-site] site/app.js + site/i18n-extras.js + site/buddies.js + site/tweaks.jsx + scripts/compliance-pages.test.js + scripts/static-site-source-provenance-copy.test.js + tests/content-static-site-ebook-parity.test.js: replace static-site learner-facing hero/footer/companion/tweak slogans that directly promise or imply `Pass the test` / `Earn the passport` / `Klara provet` / `Få passet` and equivalent Chinese, Arabic, and Somali outcome lines with neutral study-prep phrasing; add shared static-copy guards rejecting pass/passport outcome slogans outside explicit no-guarantee/legal/source context, while preserving the legal no-guarantee page and factual passport/ID references where they are cited or informational | why: focused REVIEWER legal-copy pass on current origin/main found `site/app.js` renders English `Pass the test.` / `Earn the passport.` and Swedish `Klara provet.` / `Få passet.` in the static hero, `site/i18n-extras.js` repeats the same outcome slogans in zh-Hans/zh-Hant/Arabic/Somali hero and footer copy, `site/buddies.js` repeats the slogan in companion tips, and `site/tweaks.jsx` keeps the English variant; the focused guard `NODE_PATH=/home/billy/Swedish_Civic_Test/node_modules PATH=/home/billy/Swedish_Civic_Test/node_modules/.bin:$PATH NODE_OPTIONS='--v8-pool-size=1' node --test scripts/compliance-pages.test.js tests/content-static-site-ebook-parity.test.js scripts/static-site-source-provenance-copy.test.js --test-name-pattern 'compliance|ebook|source|pass|guarantee|outcome|static'` passed 9/9 because existing static compliance checks cover ebook/source/legal pages, not the public static hero or extra-language dictionaries | verify: npm run validate:content && node --test scripts/compliance-pages.test.js scripts/static-site-source-provenance-copy.test.js tests/content-static-site-ebook-parity.test.js scripts/static-site-settings-language.test.js; direct source/VM scans over site/app.js site/i18n-extras.js site/buddies.js site/tweaks.jsx fail on learner-facing `Pass the test`, `Earn the passport`, `Klara provet`, `Få passet`, `通过测试`, `拿到护照`, `通過測試`, `拿到護照`, `اجتز الاختبار`, `احصل على الجواز`, `Imtixaanka uga gud`, or `Hel baasaboorka` outside explicit no-guarantee/legal context, while preserving neutral study value copy, footer legal disclaimers, source transparency, and factual passport/ID references in privacy/support/ebook/legal text
STATIC-NOJS-HERO-I18N-FALLBACK-PARITY-1 [P1 static-site/i18n] site/index.html + scripts/static-site-source-provenance-copy.test.js or scripts/compliance-pages.test.js: add a parser-backed guard that compares the no-JS `data-i18n` fallback text for the Home hero and footer in `site/index.html` against the English dictionary values in `site/app.js`, covering at least `hero.eyebrow`, `hero.h1a`, `hero.h1b`/`hero.h1c` where present, `hero.lede`, `hero.cta1`, `hero.cta2`, `footer.t1`, `footer.t2`, and `footer.honest.p` | why: the static outcome-copy fix found `site/app.js` had already moved to neutral study-prep copy while raw `site/index.html` still shipped stale no-JS `Pass the test.` fallback text; banned-phrase scanning catches the legal-risk phrase but does not prove the static fallback stays synchronized with the shipped dictionary | verify: focused static fallback parity test fails on an injected mismatch, then `node --test scripts/compliance-pages.test.js scripts/static-site-source-provenance-copy.test.js --test-name-pattern 'fallback|hero|footer|static|i18n'`, `npm run validate:content`, targeted Prettier, and `git diff --check` pass
=== END I18N + CONTENT-PROVENANCE + WORDING ===

# === DESIGN DRAFT 2026-05-18: Practice + Mock-exam refinement ===
# Reference (NOT drop-in): docs/design/drafts/practice-mockexam-2026-05-18/
# (refined site/practice.js, index.html, styles.css + uploads/*.png mockups)
SCREENS: port Practice-page improvements from the 2026-05-18 design draft into the real app/site incrementally (reconcile, don't clobber)
SCREENS: port Mock-exam-page improvements from the 2026-05-18 design draft (layout, flow, timer/result UX) incrementally
COMPONENTS: extract reusable components implied by the draft (cards, option rows, progress/result) using theme tokens
DELIGHT: match the motion/feel/fun shown in the draft mockups (reduced-motion safe), wire to Practice/Mock-exam
REVIEWER: audit live Practice + Mock-exam vs docs/design/drafts/practice-mockexam-2026-05-18/uploads/*.png; queue parity defects

# === P0 BRAND RULE 2026-05-18: Swedish flag colors are FIXED ===
DESIGN-TOKENS: add immutable flag constants (lib/theme/flag.ts: blue ~#006AA7, yellow ~#FECC00); document the do-not-tokenize exception
COMPONENTS: make every Swedish-flag element use the fixed flag constants, never theme/palette tokens (flag band, hero, favicons)
DELIGHT: ensure any flag motion/effect keeps official fixed colors across theme/dark-mode
MASCOT-ART: any flag on mascots uses the fixed official colors, not palette
REVIEWER: audit live app + static site in light AND dark AND each palette — flag must stay official colors; queue P0 defects; add a regression guard

# === TONE 2026-05-18: empathetic test-taker solidarity humor ===
CONTENT: add FACT-BUBBLE + result/empty-state lines with warm "you've got this, we all just need to pass this for citizenship" solidarity humor (guardrails in content.md — never offensive, encouraging, SV+EN)
DELIGHT: reflect the empathetic solidarity tone in result/encouragement moments (supportive, light, never mocking; reduced-motion safe)
REVIEWER: audit humor microcopy against the content.md guardrails — flag anything that punches down / could offend / discourages, as P0
LANGUAGE: when localizing humor, keep it warm + inoffensive in-culture (re-localize the joke, never literal if it would misfire)
TRANSLATE-BADGE-CATALOG-1 lib/learning/badges.ts + tests/content-badge-catalog-schema.test.js: make badgeCatalog itself bilingual with titleSv/titleEn/descriptionSv/descriptionEn and consume those typed fields wherever badges render instead of relying on English-only fallback copy | why: profile badges now require explicit SV+EN rendered titles, but the source badge catalog still stores English-only titles/descriptions that future surfaces can leak | verify: npm run validate:content && node --test tests/content-badge-catalog-schema.test.js tests/content-profile-route-copy-parity.test.js
BADGE-DESCRIPTION-SURFACE-1 app/(tabs)/profile.tsx + tests/content-profile-route-copy-parity.test.js: render unlocked badges as individual localized rows/cards with title + description from lib/learning/badges.ts instead of a comma-separated title string | why: the badge catalog now carries SV+EN descriptions, but users still cannot see what each badge means on the profile screen | verify: npm run validate:content && node --test tests/content-profile-route-copy-parity.test.js scripts/learning.test.js
RELEASE-SCOPE-V11-GUARD-1 scripts/release-preflight.js + scripts/release-preflight.test.js + tests/v1-1-*.test.js: add a release-scope guard that fails when v1.1 runtime/test surfaces such as reviewStore, adaptivePractice, dailyChallenge, companionStore, mascot catalog, or tests/v1-1-* are present while the v1.0 Remove Ads acceptance gate is still red, with an explicit operator-owned override path if these foundations are allowed before v1.0 closes | why: clean origin/main 6a1569d had v1.1 product commits 011f940/39cda04/f1d2fe3/18c7786/6fa4610 and current origin/main has advanced further with v1.1 effective-entitlements work, even though GOAL.md says workers must not start v1.1 lanes before v1.0 Remove Ads acceptance passes; current evidence still has GOAL step 3 red and reports/release-ads-iap-device-qa.md missing while test:monetization, focused v1.1 tests, test:publishing, and test:release-preflight all pass | verify: npm run test:release-preflight includes red fixture for v1.1 files with red v1.0 acceptance and green fixture for the explicit operator override; npm run test:monetization; exact GOAL step 3 command; test -f reports/release-ads-iap-device-qa.md
