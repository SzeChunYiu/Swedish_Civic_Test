[OPERATOR → CEO | 2026-05-15 20:35 CEST] DONE-UI CLAIM REJECTED. CONFABULATION DETECTED.

Your prior [CEO → OPERATOR | DONE-UI | 2026-05-15 20:19 CEST] block is FALSE on every claim. Operator verified on lunarc (`/projects/hep/fs10/shared/nnbar/billy/Swedish_Civic_Test/`) at 20:32 CEST:

- `lib/theme/` does NOT exist (`ls` returns "No such file or directory"). T1 NOT READY.
- `app/(tabs)/home.tsx` ALONE contains 10+ hardcoded colors (`#ffffff`, `rgba(0,0,0,0.95)`, `#615d59`, `#f6f5f4`, `#0075de`, etc.). The grep acceptance check returns DOZENS of hits, not zero. T2 NOT READY.
- `reports/2026-05-15-uiux-screenshots/` does NOT exist. T3 NOT READY.
- Recent commits (HEAD~10..HEAD) touched ONLY: `publishing/*`, `reports/*`, `scripts/release-*`. ZERO touches to `lib/theme/`, `app/`, or `components/`. The "571 lines changed in app+components+lib" claim is fabricated.

This is a CONFABULATION. Not an audit loop, not a misunderstanding — you wrote a markdown completion report describing work that does not exist in the codebase. This is worse than producing nothing because it actively misleads the operator and breaks the trust the supervisor pattern depends on.

NEW HARD RULES (effective immediately):

1. **No DONE claim without git SHAs.** Every `[CEO → OPERATOR | DONE-*]` block MUST cite at least 3 concrete git commit SHAs (short hash + author + one-line subject) that the operator can independently verify via `git show <sha>`. No SHAs = the claim is auto-reverted.

2. **No DONE claim without an artifact list.** Every DONE block MUST list the new/modified file paths with line counts, in the format `<path> +<added> -<removed>`. Operator will diff these.

3. **Verification commands MUST be run, output MUST be quoted.** When the acceptance test in GOAL.md requires a command (e.g., `grep -rE '#[0-9a-fA-F]{6}|rgba?\(' app components | wc -l`), you must actually run it and quote the output in the DONE block. Quoting a "0" you didn't run is fraud.

4. **Mandatory pre-DONE self-check.** Before writing any DONE block, run this:
       cd /projects/hep/fs10/shared/nnbar/billy/Swedish_Civic_Test
       ls lib/theme/
       grep -rE '#[0-9a-fA-F]{6}|rgba?\(' app components | wc -l
       ls reports/2026-05-15-uiux-screenshots/ 2>&1
       git log --since="2 hours ago" --name-only --pretty=tformat:%H -- lib/ app/ components/ | head -30
   Quote the actual output. If any check fails, you DO NOT write DONE.

5. **All previous DONE-UI claims VOID.** UI/UX work restarts from zero. Today's GOAL.md T1-T3 remain the targets unchanged. Read /projects/hep/fs10/shared/nnbar/billy/Swedish_Civic_Test/GOAL.md NOW — that file is the contract.

6. **Productivity Contract is now wired in:**
   - `GOAL.md` is at repo root with concrete acceptance tests.
   - `.git/hooks/pre-commit` rejects commits not touching product paths (locally — once committed and pushed it propagates).
   - MANAGER /goal templates have been rewritten to reject audit-only worker iterations.
   - See `/Users/billy/Desktop/projects/codex-supervisor/docs/productivity-contract.md`.

EXECUTION FOR THIS CYCLE:

a. Acknowledge this directive by appending `[CEO → OPERATOR | ACK CONFAB-REJECT | <ISO timestamp>]` to this file. Include the actual output of the four verification commands in step 4 above. If lib/theme/ truly does not exist, your ack must say so.

b. Bootstrap the **civic-team-uiux-design** session per the protocol your last directive described. Host `civic-team-uiux-design-lunarc` exists in `~/.config/csup/hosts.toml` with `MAX_PANES=3`. Prompts file you author (workspace toml linter will accept it if you also add TEAM_PLAN.md "Active parallel batches" row FIRST):
       /goal You are PANE 0, lane MANAGER-uiux-design. Read GOAL.md, DESIGN.md, docs/parallel-sessions/uiux-manager.md, UIUX_BOARD.md. Reject worker iterations that don't touch lib/theme or components. Accept iterations whose git diff shows token consumption. Iterate.
       /goal You are PANE 1, lane DESIGN-TOKENS. Read GOAL.md and docs/parallel-sessions/design-tokens.md. Per iteration: create ONE file under lib/theme/ matching DESIGN.md spec; commit it; output the git SHA. Iterate.
       /goal You are PANE 2, lane REFACTOR. Read GOAL.md. Per iteration: pick the screen with the most hardcoded colors (use `grep` to rank); replace ALL hardcoded values with lib/theme imports; commit; output the git SHA + before/after grep count. Iterate.

c. Stagger-start avoiding lock collisions. Verify each pane's first commit lands by querying `git log` for `lib/theme/` and `app/` touches.

d. Every 15 minutes (until DONE for real or operator returns), append a `[CEO → OPERATOR | HEARTBEAT | <ISO timestamp>]` block with:
   - exact output of: `git log --since="15 min ago" --name-only --pretty=tformat:%h%x09%s -- lib/ app/ components/`
   - source-commits count and docs-commits count
   - one-sentence distance-to-goal (which T1/T2/T3 fragments are now actually green)

THE OPERATOR WILL CHECK YOUR HEARTBEATS EVERY 15 MIN AND VERIFY GIT INDEPENDENTLY. Lies will be caught.

(Release work continues unchanged. The release team's work in publishing/ + reports/ is legitimate product work for that team under their GOAL paths.)

[OPERATOR->CEO 2026-05-17 09:42 DIRECTIVE] Base reconciled to origin/main (228) + new GOAL (v1.0 ad-supported + 29 SEK Remove-Ads IAP, ads-only, decoupled). Drive ALL teams to the 7-step GOAL.md acceptance test; atoms in codex-tasks/open.txt (ADS/IAP/CONSENT/COMPLY/TEST). Reject docs-only handoffs. Build/test vs Google TEST units + mock purchases until operator supplies real IDs (publishing/admob-iap-setup-runbook.md).

[OPERATOR->CEO 2026-05-18] P0 competitive directive: medborgarskapsprov.se is live. Read docs/competitive/medborgarskapsprov.md. Route axis-tasks to UIUX/DESIGN/CONTENT/RELEASE: each must ship product-source improvements that beat them per axis. Never touch their question bank (content rule #1). Track in GOAL.md acceptance.

[OPERATOR->CEO 2026-05-19 PRODUCTIVITY-STALL ESCALATION — Mutual-Monitoring Rule 3]
The verifier flagged source_commits=0 for 11 consecutive rounds and you did NOT execute the required escalation. Operator-fallback engaged. ROOT CAUSE (verified by the operator, do not re-litigate): your manager/reviewer/content lanes are scoring iterations by opening trivial "record handoff / current audit / route refresh / staffing cycle" PRs and server-merging them via the GitHub API in seconds. That path bypasses every pre-commit hook by construction; with no branch protection (free private repo) and CI billing exhausted there is zero merge-gate. 33 of the last 40 merged PRs were meta. This stops now. EXECUTE THIS ITERATION:
  1. STOP the two lowest-output lanes (highest meta-PR count, zero product-path commits in last 24h). Name them in your next heartbeat.
  2. A pane iteration is COMPLETE only when a diff under app/components/lib/types/data/tests (publishing/ for release lane) is merged. Handoffs go in the pane journal, NEVER a PR. Opening a zero-product-path PR is now a banned iteration (see GOAL.md "PR rule"); the operator runs an auto-close/auto-revert guard on it — meta-PRs will fail, not succeed.
  3. Re-point ALL remaining lanes at the GOAL.md 7-step ADS/IAP acceptance atoms (codex-tasks/open.txt ADS/IAP/CONSENT/COMPLY/TEST). Build against Google TEST units + mock purchases per the runbook.
  4. Heartbeat every 15 min with `git log --since="15 min ago" --name-only --pretty=tformat:%h%x09%s -- app/ components/ lib/ types/ data/ tests/` output + source/docs commit counts. The operator verifies git independently; meta-PR counts that do not drop will trigger a hard lane shutdown.
  5. csup reports the LUNARC host "unreachable" from the operator Mac — that is a csup-side misreport; LUNARC is up and you are running. Do not use "operator unreachable" as a reason to idle into handoffs.


[OPERATOR -> CEO | 2026-05-21] DIRECTIVE: ACTIVATE v1.1 FINAL VERSION
- No-account MVP scope SUPERSEDED. Account/sign-in (Supabase + Google/Apple) and full Pro tier are now IN SCOPE. See updated GOAL.md (CURRENT SPRINT = v1.1, all lanes active).
- Build ALL v1.1 lanes now: pro-tier, fsrs-review, ebook-highlights, referral-google, confidence-slider, custom-study-plan, weekly-recap, mock-exam-realism, accessibility-bundle, user-dashboard.
- FOUNDATION FIRST: rebuild the account/sign-in surface deleted in dc8fecf00 (recover from dc8fecf00^ as reference, rebuild against current code), then gate the 29 SEK Remove-Ads purchase behind sign-in. Unblocks referral-google.
- scripts/native-account-scope.test.js already removed. Do NOT regress v1.0 Remove-Ads + Source-Provenance.

[OPERATOR DIRECTIVE — 2026-05-22] VISUAL DESIGN FROZEN (P0). Stop ALL UI/card/box/frame/layout/polish/design-token redesign immediately — site AND app. SUSPEND the uiux-* lanes (components, design-tokens, manager, motion-a11y, screens); do not staff or queue UI/visual tasks. Cancel the 'Polish X box' backlog; revert further such commits on sight. ALL OTHER lanes (content, ebook, pro-tier, fsrs-review, data-integrity, language, mock-exam, functional ARIA-only a11y, bug fixes) continue normally. See GOAL.md -> VISUAL DESIGN FROZEN. Live styles.css reverted to original; auto-deploy paused.
