# P0.md — prioritized blockers. CLEAR THESE FIRST.

ENFORCEMENT (CEO + every MANAGER, every acceptance pass):
- While ANY unchecked P0 item below is open, an iteration is ACCEPTED only if
  its commit demonstrably ADVANCES a P0 item. Trivial product-path commits
  (e.g. "content: polish qNNN wording") do NOT count as progress and are
  REVERTED while P0s are open.
- The watchdog/verifier: if a P0 stays open for >=3 verifier rounds with zero
  P0-advancing commits, STOP the lowest-output lane and append
  [CEO->OPERATOR P0-STALL] to ceo-inbox.txt.
- A P0 is "done" only when its executable check passes (not when prose says so).

## OPEN P0
- [ ] SHUFFLE-FIX: seeded per-session answer-option shuffle (correct no longer
      always 'A'). Done = test: correct-display-position ~uniform (no position
      >35%) over published bank + stability test green. (see open.txt P0 block)
- [ ] TRANSLATE-COMPLETE: 100% SV+EN parity (validate:content FAILS on any gap)
      + English naturalness pass. Done = validate:content enforces it + review.
- [ ] SOURCE-CITATION: remove "Enligt/According to UHR" from stems; source
      shown as citation line below; not-a-real-exam disclaimer kept separate.

## DONE (verify, do not redo)
- [x] DESIGN tokens -> Claude palette (lib/theme/colors.ts; operator applied;
      tsc green). Component-level Claude alignment continues under UIUX-POLISH.

Operator owns this file. CEO/workers may check items only with the passing
executable check named above.

## Standing
- [ ] CRITICAL-REVIEWER lane runs every iteration, files findings via
      review-to-queue.sh (never report-only). Open reviewer blockers are P0.
