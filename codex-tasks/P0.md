# P0.md — prioritized blockers. CLEAR THESE FIRST.

ENFORCEMENT (CEO + every MANAGER, every acceptance pass):
- While ANY unchecked P0 item below is open, an iteration is ACCEPTED only if
  its commit demonstrably ADVANCES a P0 item. Trivial product-path commits
  (e.g. "content: polish qNNN wording") do NOT count as progress and are
  REVERTED while P0s are open.
- The watchdog/verifier: if a P0 stays open for >=3 verifier rounds with zero
  P0-advancing commits, STOP the lowest-output lane and append
  [CEO->OPERATOR P0-STALL] to ceo-inbox.txt.
- A P0 is "done" only when its executable check passes (not when prose says so).

## OPEN P0

None after the 2026-05-22 executable recheck on `origin/main` `6cb09257`.

## DONE (verify, do not redo)
- [x] SHUFFLE-FIX: seeded per-session answer-option shuffle (correct no longer
      always 'A'). Verified 2026-05-22 on `6cb09257` with
      `npm run test:correct-display-position` exit 0.
- [x] TRANSLATE-COMPLETE: 100% SV+EN parity (validate:content FAILS on any
      gap) + English naturalness pass. Verified 2026-05-22 on `6cb09257`
      with `npm run validate:content -- --focus-translate-complete-p0` and
      `node --test tests/content-translate-complete-p0-parity.test.js` exit 0.
- [x] SOURCE-CITATION: remove "Enligt/According to UHR" from stems; source
      shown as citation line below; not-a-real-exam disclaimer kept separate.
      Verified 2026-05-22 on `6cb09257` with
      `node --test tests/content-uhr-source-citation-stem.test.js` and
      `npm run validate:content -- --focus-source-citation-stem` exit 0.
- [x] DESIGN tokens -> Claude palette (lib/theme/colors.ts; operator applied;
      tsc green). Component-level Claude alignment continues under UIUX-POLISH.

Operator owns this file. CEO/workers may check items only with the passing
executable check named above.

## Standing

CRITICAL-REVIEWER lane runs every iteration and files findings via
review-to-queue.sh (never report-only). Open reviewer blockers are P0 when
filed as concrete tasks; the lane cadence itself is not a blocking P0 item.
