1. Create lib/theme/colors.ts with the full Notion palette from DESIGN.md (canvas, surface, text, textMuted, accent, border, success #1aae39, warning #dd5b00, correctBg, incorrectBg). Each as a typed const with a JSDoc citing the DESIGN.md section. Verify with `npx tsc --noEmit`.
2. Create lib/theme/spacing.ts: 8px base scale, export `space[0..10]` mapping to multiples per DESIGN.md.
3. Create lib/theme/typography.ts: Inter weights/sizes/lineHeights per DESIGN.md. Export `text.h1`, `text.h2`, `text.body`, `text.caption`, `text.label` as React Native `TextStyle` objects.
4. Create lib/theme/radius.ts: `card = 8`, `pill = 9999`, `input = 4`.
5. Create lib/theme/shadows.ts: multi-layer sub-0.05 opacity. Export `shadow.card`, `shadow.elevated`.
6. Create lib/theme/motion.ts: durations (`fast: 120`, `base: 200`, `slow: 320`) + easings.
7. Create lib/theme/index.ts: barrel export everything.
