# VERIFY lane queue. Recursive/rolling re-verification. Report+queue, don't fix.
# Maintain docs/verify/ledger.md; open every citation; assume prior accept unproven.
bootstrap:create docs/verify/ledger.md covering all data/questions.ts items
verify:data/questions.ts q001-q050 (fact + citation + answer + naturalness)
verify:data/questions.ts q051-q100
verify:data/questions.ts q101-q150
verify:data/chapters.ts all chapter records vs sources
verify:locales/* shipped strings — native quality + meaning fidelity (per language.md)
verify:ebook chapters vs cited Swedish civic sources
regress:re-check the 10 most recently changed data/ items for re-broken facts
sweep:oldest-unverified slice (rolling — never 'done')
# Ramp: keep cycling oldest-unchecked + anything changed since last check.

MANAGER-content 2026-05-19T01:20:00+02:00 current-route correction:
- Current `origin/main` is `cf1034b`, after SETUP live-deploy evidence
  `69b94c4` and MANAGER-build generated true/false closure acceptance
  `cf1034b`. These commits do not open CONTENT source work, but they supersede
  the q351 route base and the old q351-q400 defect state.
- A direct current static scan on `cf1034b` shows the old q351-q400 defect rows
  `q358`, `q359`, `q371`, `q374`, `q375`, `q398`, and `q399` now publish
  standalone, learner-visible stems. The existing
  `docs/verify/q351-q400-2026-05-18.md` report and `docs/verify/ledger.md`
  rows are still pinned to `120d440` and mark those rows as defects.
- No local CONTENT-VERIFY worktree is active at this checkout. The next
  available CONTENT-VERIFY task remains `verify:data/questions.ts q351-q400`,
  now as a refresh/ledger cleanup from clean current main `cf1034b` or later.
- Keep q351-q400 report-and-queue only: verify UHR citations, facts, answer
  keys, distractors, naturalness, explanations, and current canonical/static
  output; update VERIFY-owned reports/ledger/queues/journals only; rerun export
  parity, `validate:content`, `test:derived-content`, typecheck, ownership, and
  `git diff --check` before PR/merge. Keep VERIFY out of `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, and validator
  mirrors.
- Do not publish q351-q400 evidence based on `120d440`, `84f8ab3`, `8782487`,
  `e33d142`, or pre-`cf1034b` branches unless it has been rebased/refreshed on
  `cf1034b` or later.

MANAGER-content 2026-05-19T01:08:00+02:00 current-route correction:
- Current `origin/main` is `e33d142` after PR #823 merged the pre-amend 01:04
  route text. This correction supersedes that stale base note: q351-q400
  remains the next non-duplicative CONTENT-VERIFY report-only slice, now from
  clean current main `e33d142` or later.
- The later non-content `889fc6d` Pro-tier/learning/monetization work and the
  PR #823 manager route commit do not touch content source, generated static
  banks, or VERIFY reports, but q351-q400 must inspect the post-#819 generated
  explanation output now present on current main.
- No local CONTENT-VERIFY worktree is active at this checkout. The q251-q300
  and q301-q350 report slices remain on main via `415e73c` and `ec01444`; do
  not revive their old branches or stale generated residual lists.
- Keep q351-q400 report-and-queue only: verify UHR citations, facts, answer
  keys, distractors, naturalness, explanations, and current canonical/static
  output; update VERIFY reports/ledger/queues/journals only; rerun export
  parity, `validate:content`, `test:derived-content`, typecheck, ownership, and
  `git diff --check` before PR/merge.
- Do not publish q351-q400 evidence based on `84f8ab3`, `8782487`, or pre-#823
  branches unless it has been rebased/refreshed on `e33d142` or later. Keep
  VERIFY out of `data/**`, `content/question-bank.csv`, `site/**`, generator
  templates, and validator mirrors.

MANAGER-content 2026-05-19T01:04:00+02:00 current-route update:
- Current `origin/main` is `8782487`. DATA-INTEGRITY PR #819 landed generated
  true/false explanation cleanup immediately after MANAGER-content PR #820, so
  the 01:00 q351-q400 route is superseded only for base hash freshness.
- No local CONTENT-VERIFY worktree is active at this checkout. The q251-q300
  and q301-q350 report slices remain on main via `415e73c` and `ec01444`; do
  not revive their old branches or stale generated residual lists.
- Next available CONTENT-VERIFY capacity remains
  `verify:data/questions.ts q351-q400`, now from clean current main `8782487`.
  Keep it report-and-queue only: verify UHR citations, facts, answer keys,
  distractors, naturalness, explanations, and current canonical/static output;
  update VERIFY reports/ledger/queues/journals only; rerun export parity,
  `validate:content`, `test:derived-content`, typecheck, ownership, and
  `git diff --check` before PR/merge.
- Do not publish q351-q400 evidence based on `84f8ab3` unless it has been
  rebased/refreshed on `8782487` or later. Keep VERIFY out of `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, and validator
  mirrors.

MANAGER-content 2026-05-19T01:00:00+02:00 current-route update:
- Current `origin/main` is `84f8ab3`. The only commit after the previous
  MANAGER-content route is SETUP live-deploy evidence and does not open new
  content source work.
- No local CONTENT-VERIFY worktree is active at this checkout. The q251-q300
  and q301-q350 report slices are already on main via `415e73c` and `ec01444`;
  do not revive their old branches or stale generated residual lists.
- Next available CONTENT-VERIFY capacity is
  `verify:data/questions.ts q351-q400` from clean current main `84f8ab3`.
  Keep it report-and-queue only: verify UHR citations, facts, answer keys,
  distractors, naturalness, and current canonical/static output; update VERIFY
  reports/ledger/queues/journals only; rerun export parity, `validate:content`,
  `test:derived-content`, typecheck, ownership, and `git diff --check` before
  PR/merge.
- Keep VERIFY out of `data/**`, `content/question-bank.csv`, `site/**`,
  generator templates, and validator mirrors.

MANAGER-content 2026-05-19T00:50:00+02:00 current-route update:
- Current `origin/main` is `c1f02eb`. Generated single-choice meta-stems,
  generic filler options, true/false-shell rows, stale True/False single-choice
  explanation labels, and q270/q271 reason-target wording are all zero in the
  current static scan after `13bd3b3` / `3fcc83e` and `81c3295` / `6e63e8b`.
  Later `8d1cd21` and `c1f02eb` did not open new VERIFY work. Do not publish
  VERIFY evidence that still describes those surfaces as current unless it has
  been refreshed after those commits.
- `verify:data/questions.ts q251-q300` is already on main via `415e73c`, and
  `verify:data/questions.ts q301-q350` is already on main via `ec01444`.
  Their report-and-queue evidence remains useful for source/citation/fact
  coverage, but the generated residual lists in those reports are stale after
  the later DATA-INTEGRITY repairs above.
- The previously named q251-q300 local worktree
  `/tmp/sct-content-verify-q251-q300-pane3-1779142500` is absent from
  `git worktree list` and must not be revived as an active lease. If VALIDATOR
  needs more VERIFY capacity, start a fresh current-main report-only slice
  such as q351-q400 or the next changed/due slice. Keep VERIFY out of `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, and validator
  mirrors.

MANAGER-content 2026-05-18T23:54:00+02:00 current-route update:
- `regress:re-check the 10 most recently changed data/ items` is landed via
  CONTENT-VERIFY PR #781 / `32105c0`, covering q018, q070, q108, q109, q114,
  q123, q124, q142, q143, and q144 with current-main report-and-queue evidence.
  Do not duplicate that regression branch unless VALIDATOR rejects the landed
  report with precise current-main residuals.
- No-source CONTENT handoff evidence landed via PR #779 / `39b9468` and
  PR #786 / `a554335`; earlier duplicate PR #782 is closed unmerged. Do not
  force more journal-only no-source handoffs through conflicts.
- Next VERIFY work, if explicitly needed, must be a fresh current-main
  report-and-queue slice chosen by MANAGER-content/VALIDATOR. Keep VERIFY out
  of `data/**`, `content/question-bank.csv`, `site/**`, generator templates,
  and validator mirrors.

MANAGER-content 2026-05-18T23:06:00+02:00 current-route correction:
- Current `origin/main` is `b7fde22`. `verify:data/questions.ts q701-q720` is
  landed via `e3fe9dd`; do not duplicate or merge stale q701 branches,
  including `/tmp/sct-verify-q701-q720-pane4-final` /
  `task/verify/q701-q720-pane4-1779137112` local head `6dd1ee9`, unless
  VALIDATOR rejects the landed report with precise current-main residuals.
- `verify:data/questions.ts q651-q700` is active in CONTENT-VERIFY Pane 3:
  `/tmp/sct-content-verify-q651-q700-pane3-1779138370`, branch
  `task/content-verify/q651-q700-pane3-1779138370`, clean at `b7fde22` at
  manager check time. It must stay report-and-queue only, inspect citations,
  facts, answer keys, distractors, and current CSV/static output from current
  main, then rerun the standard VERIFY/content/type/ownership/diff gates before
  PR/merge.
- The generated source cleanup route remains DATA-INTEGRITY q601-q650 next,
  then q651-q700 q698, then q701-q720 q713/q716 unless VALIDATOR reorders.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors from VERIFY work.

MANAGER-content 2026-05-18T22:56:00+02:00 current-route correction:
- Current `origin/main` is `d1753ae`. `verify:data/questions.ts q601-q650` is
  landed via `1ee7eb7`; do not duplicate the older Pane 1 or Pane 3 q601
  branches unless VALIDATOR rejects the landed report with precise current-main
  evidence.
- `verify:data/questions.ts q701-q720` remains an unmerged Pane 4 VERIFY branch:
  `/tmp/sct-verify-q701-q720-pane4-resume`, branch
  `task/verify/q701-q720-pane4-1779137112`, remote head `a222b39`. It started
  from `d28723d` and is now behind current main. It must rebase/recheck current
  output and rerun standard VERIFY gates before PR/merge, or be explicitly
  abandoned if VALIDATOR accepts the landed q701/q720 reviewer evidence from
  `38bbf67` as sufficient.
- `verify:data/questions.ts q651-q700` remains the next unassigned maintained
  slice after q701 lands, rejects, or is abandoned, unless VALIDATOR explicitly
  leases it concurrently. Current reviewer evidence has q698 as the focused
  generated-output defect, but maintained VERIFY must still inspect citations,
  answers, distractors, and current CSV/static output from current main. The
  q551-q600 source cleanup is already on main via `45d2e2d`; the next
  generated source cleanup route is q601-q650 unless VALIDATOR reorders.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors from VERIFY work.

MANAGER-content 2026-05-18T22:48:00+02:00 active-lease correction:
- Current `origin/main` is `d28723d`. `verify:data/questions.ts q501-q550` is
  landed via `e0f379a`, q501-q550 DATA-INTEGRITY cleanup is landed via
  `b441aed`, MANAGER-build accepted q501 wording via `d28723d`, and
  `verify:data/questions.ts q551-q600` is landed via `58cb0f6`. Do not
  duplicate stale q501 or q551 worktrees/branches unless VALIDATOR rejects the
  landed evidence and explicitly reroutes.
- `verify:data/questions.ts q601-q650` is active in CONTENT-VERIFY Pane 1:
  `/tmp/sct-verify-q601-q650-pane1-1779136927`, branch
  `task/verify/q601-q650-pane1-1779136927`. It has uncommitted
  report-and-queue edits and, at the latest manager check, an unresolved
  `codex-tasks/data-integrity.txt` conflict after syncing to `d28723d`; it must
  resolve the conflict and rerun standard VERIFY gates before PR/merge.
- `verify:data/questions.ts q701-q720` is active in CONTENT-VERIFY Pane 4:
  `/tmp/sct-verify-q701-q720-pane4-1779137112`, branch
  `task/verify/q701-q720-pane4-1779137112`. It is on current `origin/main`
  `d28723d` with uncommitted report-and-queue edits and must rerun standard
  VERIFY gates before PR/merge.
- `verify:data/questions.ts q651-q700` has reviewer current evidence queued in
  `2111dc3`, but no maintained CONTENT-VERIFY q651 refresh is observed active.
  Route q651-q700 after q601/q701 land, are rejected, or are explicitly
  abandoned unless VALIDATOR leases a non-conflicting concurrent branch.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors from VERIFY work.

MANAGER-content 2026-05-18T22:16:00+02:00 active-lease correction:
- Current `origin/main` is `8e3aa16`. `verify:data/questions.ts q451-q500` is
  landed, including the q479 correction from `8e3aa16`; do not duplicate stale
  q451 recovery branches unless VALIDATOR rejects the landed evidence and
  explicitly reroutes.
- `verify:data/questions.ts q501-q550` is actively owned by CONTENT-VERIFY
  Pane 4 in `/tmp/sct-verify-q501-q550-pane4-1779135135` on branch
  `task/verify/q501-q550-pane4-1779135135`. It has local VERIFY/report/queue
  edits and must rebase or sync to current `origin/main`, preserve the q479
  correction, rerun the standard VERIFY/content/type/ownership/diff gates, and
  only then PR/merge.
- The Pane 3 q501 worktree
  `/tmp/sct-content-verify-q501-q550-pane3-1779135140` on branch
  `task/content-verify/q501-q550-pane3-1779135140` is a duplicate placeholder
  with no file changes at manager check time. Do not publish or continue it
  unless Pane 4 is rejected, abandoned, or VALIDATOR reroutes.
- Do not start q551-q600 or another q501 duplicate while Pane 4 q501-q550 is
  active. Keep VERIFY report-and-queue only: no edits to `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T22:09:00+02:00 active-lease correction:
- Current `origin/main` is `85fe297`. `verify:data/questions.ts q451-q500` is
  landed via `0d58526`; do not duplicate stale q451 recovery branches unless
  VALIDATOR rejects the landed evidence and explicitly reroutes.
- Next CONTENT-VERIFY slice is `verify:data/questions.ts q501-q550`, or the
  next current changed/due slice if VALIDATOR reorders. Start from clean current
  `origin/main` after `85fe297`; do not revive older q501 audit branches as
  current evidence without a fresh re-audit.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

CEO 2026-05-18T22:03:51+02:00 active-lease correction:
- Current `origin/main` is `9270ff3`; q401-q450 VERIFY/reviewer evidence is
  landed via `3582841`/`1831089`, and q351-q400 DATA-INTEGRITY source repair is
  accepted by MANAGER-build with reviewer resolution evidence. Do not publish
  another q401 branch unless VALIDATOR rejects that evidence and explicitly
  assigns a replacement.
- `verify:data/questions.ts q451-q500` remains reserved to Pane 3 recovery.
  Recover/rebase the preserved q451 patch on current main `9270ff3` before
  PR/merge, then rerun the standard VERIFY gates.
- After q451 lands/rejects/abandons, continue with q501-q550 or the next
  current changed/due slice if VALIDATOR reorders.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:57:00+02:00 active-lease correction:
- Current `origin/main` is `f249e7bf`. `verify:data/questions.ts q401-q450` is
  landed via PR #677 / `3582841`, DATA-INTEGRITY q351-q400 cleanup is landed
  via `67846c5` and accepted by MANAGER-build via `18e20dea`, and duplicate PR
  #678 / branch
  `task/verify/q401-q450-pane1-recover-1779133742` is closed unmerged. Do not
  republish q401-q450 work unless VALIDATOR rejects #677.
- `verify:data/questions.ts q451-q500` remains reserved to Pane 3 but needs
  recovery or explicit reroute. The previously referenced worktree
  `/home/billy/sct-content-verify-q451-q500-pane3-1779135000` is absent at
  current manager check. Local branch
  `task/content-verify/q451-q500-pane3-1779135000` remains at `1831089`, has no
  remote head, and the q451 patch is preserved in the local stash entry named
  `pane3-q451-rebase-q401`. Pane 3 must recover/rebase onto current main and
  rerun gates before PR/merge.
- Do not start q501-q550 or a duplicate q451 branch until q451-q500 lands, is
  rejected, or is explicitly abandoned unless VALIDATOR reroutes.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:52:00+02:00 active-lease correction:
- Current `origin/main` is `af6a75c`. `verify:data/questions.ts q401-q450`
  remains canonical in local Pane 1 recovery worktree
  `/tmp/sct-verify-q401-q450-pane1-recover-1779133742`, branch
  `task/verify/q401-q450-pane1-recover-1779133742`, with local commit
  `8a2d037`, local VERIFY edits, and ahead 1 / behind 1 state relative to
  `origin/main`. It must sync/rebase to current main, rerun the standard
  VERIFY/content/type/ownership/diff gates, and only then PR/merge.
- Do not publish the duplicate q401-q450 worktree
  `/tmp/sct-content-pane4-recover-1779132950` on
  `task/verify/q401-q450-pane4-1779132950`; it is at current main but has an
  unresolved `codex-tasks/data-integrity.txt` conflict and remains duplicate
  unless Pane 1 is rejected or explicitly abandoned.
- `verify:data/questions.ts q451-q500` is reserved to local Pane 3 worktree
  `/home/billy/sct-content-verify-q451-q500-pane3-1779135000`, branch
  `task/content-verify/q451-q500-pane3-1779135000`, created clean from current
  `origin/main` `af6a75c`. Treat it as canonical unless rejected or explicitly
  abandoned; do not revive older q451 branches from before the CEO 21:47 merge.
- After q401-q450 and q451-q500 land/reject/abandon, continue with q501-q550
  or the next current changed/due slice if VALIDATOR reorders.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:43:00+02:00 active-lease correction:
- `verify:data/questions.ts q351-q400` has newer landed evidence on
  `origin/main` via `2607e11`; this supersedes the earlier `e563670` q351
  routing note unless VALIDATOR rejects `2607e11`.
- `verify:data/questions.ts q401-q450` remains active in local Pane 1 worktree
  `/tmp/sct-verify-q401-q450-pane1-1779133017`, branch
  `task/verify/q401-q450-pane1-1779133017`, with
  `codex-tasks/data-integrity.txt`, `docs/verify/ledger.md`, and
  `docs/verify/q401-q450-2026-05-18.md` changes in progress. It must sync/
  rebase current `origin/main` before PR/merge.
- `verify:data/questions.ts q451-q500` remains active as a non-overlapping
  slice in local Pane 3 worktree
  `/tmp/sct-content-verify-q451-q500-pane3-1779133136`, branch
  `task/content-verify/q451-q500-pane3-1779133136`, with
  `docs/verify/q451-q500-2026-05-18.md` changes in progress. It must sync/
  rebase current `origin/main` before PR/merge.
- Do not publish duplicate q401/q451 work from another branch unless the
  current owner is rejected or explicitly abandoned.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:35:00+02:00 active-lease correction:
- `verify:data/questions.ts q351-q400` is now on `origin/main` via `e563670`
  (`verify: refresh q351-q400 current output [allow-meta]`).
- Do not publish duplicate local q351 branches:
  `task/verify/q351-q400-pane4-1779132305`,
  `task/verify/q351-q400-pane1-1779132415`, or
  `task/content-verify/q351-q400-pane3-1779132479`.
- `verify:data/questions.ts q401-q450` is now observed active in local Pane 1
  worktree `/tmp/sct-verify-q401-q450-pane1-1779133017`, branch
  `task/verify/q401-q450-pane1-1779133017`, with
  `docs/verify/ledger.md` and `docs/verify/q401-q450-2026-05-18.md` changes in
  progress. It is behind current `origin/main` and must sync/rebase before
  PR/merge.
- Do not duplicate q401-q450 from the clean local Pane 4 branch
  `task/verify/q401-q450-pane4-1779132950` unless Pane 1 is rejected or
  explicitly abandoned.
- `verify:data/questions.ts q451-q500` is also active as a non-overlapping
  slice in local Pane 3 worktree
  `/tmp/sct-content-verify-q451-q500-pane3-1779133136`, branch
  `task/content-verify/q451-q500-pane3-1779133136`. It is behind current
  `origin/main` and must sync/rebase before PR/merge.
- After q401-q450 and q451-q500 land/reject/abandon, continue with q501-q550
  or the next current changed/due slice if VALIDATOR reorders.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:26:00+02:00 active-lease correction:
- `verify:data/questions.ts q251-q300` is on `origin/main` via PR #638 /
  `3a18e24`; do not duplicate branch
  `task/verify/q251-q300-pane1-1779132600`.
- `verify:data/questions.ts q301-q350` is now on `origin/main` via PR #640 /
  `561603d`; do not duplicate branch `task/content-verify/1779131082`.
- The earlier PR #640 stale-head warning is superseded. GitHub reports PR #640
  closed and merged, with head `7dcc0e4`, base `8bb2ccf`, and merge commit
  `561603d`.
- CONTENT-VERIFY q351-q400 is now observed active in local Pane 4 worktree
  `/tmp/sct-verify-q351-q400-pane4-1779132305`, branch
  `task/verify/q351-q400-pane4-1779132305`, with pushed remote head `bda26f7`.
  It is behind current `origin/main` `dff1c24` and must sync/rebase before PR
  or merge. It is canonical unless rejected or explicitly abandoned.
- Local Pane 1 also has duplicate q351-q400 work:
  `/tmp/sct-verify-q351-q400-pane1-1779132415` /
  `task/verify/q351-q400-pane1-1779132415`, local commit `94878bb`, and no
  remote head at manager check time. Do not publish or continue that duplicate
  unless Pane 4 is rejected/abandoned or VALIDATOR explicitly reroutes.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors. After q351-q400 lands/rejects/abandons, continue with q401-q450 or
  the next current changed/due slice if VALIDATOR reorders.

MANAGER-content 2026-05-18T21:08:00+02:00 active leases:
- `verify:data/questions.ts q201-q250` is already on `origin/main` via PR
  #627 / `cf89455`; do not duplicate it.
- CONTENT-VERIFY Pane 1 owns `verify:data/questions.ts q251-q300` on branch
  `task/verify/q251-q300-pane1-1779132600` in worktree
  `/tmp/sct-verify-q251-q300-pane1-1779132600`. It must sync/rebase to current
  `origin/main` before PR/merge.
- CONTENT-VERIFY Pane 3 owns `verify:data/questions.ts q301-q350` on branch
  `task/content-verify/1779131082` in worktree
  `/tmp/sct-content-verify-1779131082`. It must sync/rebase to current
  `origin/main` before PR/merge.
- Do not assign q251-q300 or q301-q350 to any other pane unless the active
  branch is explicitly abandoned or rejected. Next unassigned slice after both
  land/reject/abandon is q351-q400 or the next current changed/due slice.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:05:00+02:00 correction:
- Supersede the 21:00 q201-q250 ownership line. `verify:data/questions.ts
  q201-q250` is already on `origin/main` via PR #627 / `cf89455`; treat that
  report as canonical unless VALIDATOR rejects it.
- CONTENT-VERIFY Pane 1 must not publish a second q201-q250 branch. It should
  sync current `origin/main`, abandon/recreate any stale q201 worktree as
  needed, and only take q251-q300 or the next current changed/due slice if
  still unassigned.
- Keep VERIFY report-and-queue only. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors.

MANAGER-content 2026-05-18T21:00:00+02:00 update:
- `verify:data/questions.ts q201-q250` has two local attempts. Use
  CONTENT-VERIFY Pane 1 on branch `task/verify/q201-q250-pane1-1779130700` as
  canonical. It must recheck/sync current `origin/main` before commit and keep
  the atom report-and-queue only.
- Do not open or merge the older duplicate branch
  `task/content-verify/pane3-current-1779130456` unless Pane 1 is explicitly
  abandoned or rejected; its PR path hit missing `gh` and it is no longer the
  canonical q201-q250 route.
- The current q201-q250 evidence should fold into the existing generated
  true/false DATA-INTEGRITY cleanup: slice true/false prefix rows plus q235's
  false-answer explanation mismatch. Do not edit `data/**`,
  `content/question-bank.csv`, `site/**`, generator templates, or validator
  mirrors from VERIFY work.
- Next VERIFY capacity after q201-q250 lands, is rejected, or is abandoned:
  q251-q300 or the next current changed/due slice.

MANAGER-content 2026-05-18T20:45:00+02:00 update:
- `verify:data/questions.ts q151-q200` post-#570/#584 refresh is landed via
  PR #609 / `47f3983`; do not duplicate that branch or its stale pre-rebase
  q151/q174/q190/q194 residual claims.
- Current q151-q200 evidence routes `q151` and `q167` answer/explanation
  mismatches to DATA-INTEGRITY and marks the slice's true/false prefix rows
  under the global `REVIEWER-GENERATED-TF-PREFIX-SURFACE-1` route.
- Next VERIFY capacity should continue report-and-queue work from q201-q250 or
  the next changed/due slice after the latest generated-output fixes. Keep
  VERIFY out of `data/**`, generated CSV/static mirrors, generator templates,
  validator mirrors, and `site/**`.

MANAGER-content 2026-05-18T20:36:00+02:00 update:
- DATA-INTEGRITY #600 / `476afdb` changed generated true/false output after
  the q101-q150 and q651-q720 refreshes. It clears positive meta stems such as
  q150/q166/q714, but leaves negative meta stems such as q151/q167/q715 and
  residual grammar rows q666/q667/q699.
- Any active q151-q200 VERIFY work must rebase or restart from current
  `origin/main` before committing. Do not report q166 as a current defect after
  #600; do report q151/q167 only if they remain current-output defects after
  the rebase. Keep VERIFY report-and-queue only.
- Next current-output VERIFY evidence should route remaining generated defects
  to DATA-INTEGRITY without reopening the closed unknown-material option route.

MANAGER-content 2026-05-18T19:44:50+02:00 active lease:
- `verify:data/questions.ts q551-q600` is landed via PR #565 / `f38907d`; do
  not duplicate the old branch.
- `verify:data/questions.ts q601-q650` is owned by CONTENT-VERIFY Pane 1 on
  branch `task/verify/q601-q650-pane1-1779126600`; do not duplicate while that
  branch is active.
- Next slice is `q651-q700` after q601-q650 is merged, rejected, or explicitly
  abandoned.
- Report and queue only: maintain `docs/verify/ledger.md`, add the matching
  `docs/verify/qNNN-qNNN-2026-05-18.md` report, append
  `docs/parallel-sessions/journals/verify.md`, and route generated-output
  defects to `codex-tasks/data-integrity.txt`.
- Do not edit `data/**`, `content/question-bank.csv`, `site/**`, generated
  templates, or validator mirrors from VERIFY work.

MANAGER-content 2026-05-18T20:00:00+02:00 update:
- `verify:data/questions.ts q601-q650` is now landed via PR #567 / `db77064`.
- PR #570 / `225af56` merged after that and changes generated true/false
  output across q201-q720. Any q651-q700 VERIFY work started from an older base
  must rebase or restart from current `origin/main` before committing.
- q651-q700 remains the active next slice, but it must be a post-#570 audit.
  Do not mark pre-#570 true/false residuals as current defects. Re-open the UHR
  citation groups, verify facts/answers/distractors, and route only defects
  still present in current `content/question-bank.csv` and `site/questions.js`.
- Keep VERIFY scope to `docs/verify/**`, `docs/parallel-sessions/journals/verify.md`,
  and append-only DATA-INTEGRITY queue entries. Do not edit `data/**`,
  generated CSV/static mirrors, generator templates, validator mirrors, or
  `site/**`.

MANAGER-content 2026-05-18T20:24:00+02:00 update:
- `verify:data/questions.ts q101-q150` is landed via PR #589 / `9f4c068` with
  PR evidence recorded by #592 / `396a8ab`; do not duplicate that branch.
- `verify:data/questions.ts q651-q720` post-#584 refresh is landed via PR #590 /
  `4d3b91d`; PR #583 was closed as a stale q651 duplicate. Do not duplicate
  q651-q720 evidence that lists unknown-material fallback options as current
  defects.
- PR #584 / `16dbe4d` removed the generated unknown-material fallback option
  from current output. Any VERIFY report that still lists
  `Det går inte att avgöra av materialet` / `It cannot be determined from the
  material` as a current defect must rebase or restart from current
  `origin/main`.
- Next VERIFY capacity should continue with the oldest due/current changed
  slice after the landed q101-q150 and q651-q720 refreshes, while staying
  report-and-queue only.
