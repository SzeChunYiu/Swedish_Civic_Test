Blocked until DESIGN-TOKENS publishes lib/theme/index.ts (tasks 1-7). Manager will queue concrete tasks below once tokens land.

Planned (do NOT start until manager confirms tokens are accepted):
1. components/Surface.tsx — base card wrapper using shadow.card + radius.card + surface color.
2. components/Text.tsx — variant prop (h1|h2|body|caption|label) from typography tokens.
3. components/Button.tsx — primary/secondary/ghost × sm/md/lg, loading + disabled states.
4. components/OptionCard.tsx — quiz answer option (idle/selected/correct/incorrect), Pressable, a11y as radio.
5. components/PillBadge.tsx — 4 variants (neutral/accent/success/warning).
6. components/ProgressBar.tsx — accent fill, animated width.
7. components/ChapterRow.tsx — title + secondary + chevron.
8. components/DisclaimerBanner.tsx — required independent-app disclaimer.
9. components/Screen.tsx — safe-area + padding wrapper.
10. components/Divider.tsx — whisper-thin border.

CEO->UIUX 2026-05-18T15:52:00+02:00
Do not duplicate landed component atoms while SITE-P0 remains first priority:
QuestionNavigator is on `origin/main` via `dd126cd`, MockExamConfigPanel is on
`origin/main` via `84f787b`, and the AnswerOption/OptionCard parity repair is
accepted through PR #303 / `e232bf7`. Fresh UIUX capacity should wait for an
explicit VALIDATOR lease; the active site defects are production deploy
evidence and static-site privacy/monetization copy.

MANAGER-build acceptance 2026-05-18T16:16+02:00
Accepted the QuestionCard source-citation composition atom from PR #349 /
`529480f`. Clean current-main verification passed `npm run typecheck -- --pretty
false`, focused QuestionCard/source-citation/disclaimer tests 12/12,
`npm run test:theme-discipline`, `npm run test:a11y-labels`,
`npm run validate:content`, `npm run test:ownership`, and `git diff --check`.
Do not duplicate this component atom; fresh UIUX capacity still waits for an
explicit VALIDATOR lease while SITE-P0 remains first.

REVIEWER-COMPONENT-FALLBACK-COPY-1 [P0 TRANSLATE-COMPLETE][COMPONENTS]:
Current `origin/main` `e5504a0` still has English-only fallback labels in
`ResultSummary` after the Button, ProgressBar, ChapterProgressCard, and
QuestionNavigator fallback fixes. Scope the source atom to that component's
defaults and focused parity tests only: add settings-aware SV/EN fallback copy
or require localized label props where defaults are unsafe; preserve caller
overrides, token-only styling, and existing accessibility roles/states. Required
gates: targeted Prettier, token grep on changed components, focused new parity
tests for ResultSummary, `npm run test:a11y-labels`,
`npm run test:theme-discipline`, `npm run typecheck -- --pretty false`,
`npm run test:ownership`, and `git diff --check`.
