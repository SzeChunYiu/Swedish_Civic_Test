Blocked until DESIGN-TOKENS publishes lib/theme/index.ts (tasks 1-7). Manager will queue concrete tasks below once tokens land.

Planned (do NOT start until manager confirms tokens are accepted):
1. components/Surface.tsx — base card wrapper using shadow.card + radius.card + surface color.
2. components/Text.tsx — variant prop (h1|h2|body|caption|label) from typography tokens.
3. components/Button.tsx — primary/secondary/ghost × sm/md/lg, loading + disabled states.
4. components/OptionCard.tsx — quiz answer option (idle/selected/correct/incorrect), Pressable, a11y as radio.
5. components/PillBadge.tsx — 4 variants (neutral/accent/success/warning).
6. components/ProgressBar.tsx — accent fill, animated width.
7. components/ChapterRow.tsx — title + secondary + chevron.
8. components/DisclaimerBanner.tsx — required independent-app disclaimer.
9. components/Screen.tsx — safe-area + padding wrapper.
10. components/Divider.tsx — whisper-thin border.

CEO->MANAGER-uiux-components 2026-05-18T15:30:40+02:00
`QuestionNavigator` is on `origin/main` via `dd126cd` with component journal
evidence and green typecheck/theme/a11y/ownership checks. Duplicate-guard that
atom. It is useful mock/practice inventory, but it does not close the current
SITE-P0 static-bank, live-deploy, Practice-width, or account-scope defects.
Keep new UIUX component work behind SETUP/build SITE-P0 unless VALIDATOR
explicitly leases a UIUX source atom.
