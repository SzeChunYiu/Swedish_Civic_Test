BLOCKERS — cleared & reset by operator 2026-05-18, at origin/main HEAD 83b6bbb.

All prior REVIEWER-BLOCKED-DIRTY-WORKTREE / "stop until reconcile main" /
q023/q102/q103 external-source-lease entries were STALE (pinned to long-dead
HEADs from the abandoned Phase-B / 520-question divergence) and are deleted.
There are NO active product blockers.

STAFFING / RATE-LIMIT NOTES (do not stop unblocked active panes):
/goal [BID=B-CONTENT-RL-20260518] [type=infra] [blocks=A8] [owner=MANAGER-content] Pane 2 and Pane 4 in `civic-content` hit usage limits on 2026-05-18 while q016 was being assigned; q016 is unstaffed until a fresh content worker is available. Read docs/parallel-sessions.md and docs/parallel-sessions/AI_FACTORY.md first.
/goal [BID=B-UIUX-COMP-VERCEL-QUOTA-20260518] [type=external] [blocks=U12] [owner=VALIDATOR] PR #232 for `DisclaimerBanner` is open and mergeable with local component gates green, but Vercel failed with daily deployment quota `api-deployments-free-per-day`; merge after quota reset or explicit deployment-status deferral.

STANDING POLICY (operator, hard — read every cycle):
1. A dirty worktree from OTHER lanes' in-scope work is NORMAL in this
   parallel factory and is NOT a blocker. Lanes have disjoint write scope.
   Never record a "tree dirty / boundary moved" blocker and never stop or
   park a pane for it.
2. Phase-B / external-provenance work is DEFERRED (see open.txt OPERATOR
   DIRECTIVE). Do not file blockers about Phase-B leases.
3. A real blocker is ONLY a hard correctness failure on Phase-A work
   (tsc error, test:content red, a content gate red) that prevents a
   *push*. Even then the affected pane RE-TASKS to another unblocked
   Phase-A atom — it never idles.
4. If you would otherwise write "stop X until Y reconciles": instead
   re-task X to the next independent Phase-A atom and log that, not a stop.

# === P0 STOP-THE-LINE 2026-05-18: deployed site/ is starved ===
# Vercel serves ONLY site/. Factory built into app/ + docs/design/drafts/,
# so the LIVE site lacked Practice & Mock-exam and a thin ebook. This is
# what users actually see. Highest priority until site/ is the full product.
SITE-P0-1: wire site/practice.js + the Practice route into site/index.html so the Practice page is reachable and styled (reconcile with #154, don't regress ebook)
SITE-P0-2: build the Mock-exam page into site/ (timed flow + result UX) per docs/design/drafts/practice-mockexam-2026-05-18 + uploads/*.png, reachable from index.html
SITE-P0-3: sync site/questions.js from data/questions.ts (canonical bank) and keep them from drifting
SITE-P0-4: ebook page in site/ — keep the richer #154 site/ebook.js, improve writing quality per CONTENT/LANGUAGE voice; never regress
SITE-P0-5: REVIEWER audit the LIVE deployed site vs the draft mockups; queue parity defects until site/ is the full well-written product
