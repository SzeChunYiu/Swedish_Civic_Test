BLOCKERS — cleared & reset by operator 2026-05-18, at origin/main HEAD 83b6bbb.

All prior REVIEWER-BLOCKED-DIRTY-WORKTREE / "stop until reconcile main" /
q023/q102/q103 external-source-lease entries were STALE (pinned to long-dead
HEADs from the abandoned Phase-B / 520-question divergence) and are deleted.
Content-specific q016 staffing is resolved. SITE-P0 deployed-site blockers
below are active and outrank new non-site content routing.

STAFFING / RATE-LIMIT NOTES (do not stop unblocked active panes):
/goal [BID=B-CONTENT-RL-20260518] [type=infra][resolved] [owner=MANAGER-content] Superseded 2026-05-18T13:39:36+02:00: q016 landed via PR #225 / `31d9828`, q017 landed via PR #222 / `fc19b58`, and duplicate q016 draft PR #230 was closed unmerged. Do not treat q016 as unstaffed. Fresh capacity should go to VALIDATOR for SITE-P0 lease/staffing before new non-site content.
/goal [BID=B-CONTENT-Q114-STATIC-MIRROR-GATE-20260518] [type=approval] [blocks=A6] [owner=VALIDATOR] q114 PR #352 is the canonical CONTENT-VERIFY path, but a rebased CONTENT-only diff fails `validate:content` because generated `site/questions.js` becomes stale and CONTENT is barred from `site/**`. Decide whether SETUP/build or DATA-INTEGRITY performs a companion static mirror sync, or explicitly authorize a bundled cross-lane fix. Read docs/parallel-sessions.md and docs/parallel-sessions/AI_FACTORY.md first.
/goal [BID=B-SETUP-RL-20260518T1525] [type=infra][resolved] PANE 2's earlier account-scope cap is superseded by PR #359 / `392b84e`. Do not duplicate the static-site account cleanup.

RESOLVED 2026-05-18T15:15+02:00:
The `ANSWER-OPTION-OPTIONCARD-PARITY-1` stop-line gate is green on current
`origin/main` `ff117b1` after PR #303 / `e232bf7`. Manager verification passed
`npm run validate:content`, focused AnswerOption parity tests, typecheck,
ownership, and `git diff --check`.

RESOLVED 2026-05-18T15:41+02:00:
`REVIEWER-SITE-Q142-QUESTION-BANK-DRIFT-1` static-bank sync is green on current
`origin/main` `d4ffd91` after PR #324. `site/questions.js` now exports 715
questions and passes static-bank parity. The remaining count issue is the
separate live-smoke default in `scripts/check-live-site.js`.

RESOLVED 2026-05-18T15:47+02:00:
`REVIEWER-SITE-LIVE-SMOKE-COUNT-1` is green on current `origin/main` `11b2b96`
after PR #328. `scripts/check-live-site.js` now derives the default expected
question count from local `site/questions.js` while preserving the explicit
`SITE_LIVE_REQUIRED_QUESTION_COUNT` override. Manager verification passed the
focused live-site test suite, local default current-site smoke at 715 questions,
and a stale-705 synthetic deploy rejection.

RESOLVED 2026-05-18T16:24+02:00:
`REVIEWER-SITE-PRACTICE-WIDTH-PARITY-1` is green after PR #338 / `c7227b6`.
`REVIEWER-SITE-Q018-QUESTION-BANK-DRIFT-1` is green after PR #355 / `1376794`.
Clean manager verification passed static-bank parity, local `test:site-live`
including the wide Practice layout, `validate:content`, typecheck, ownership,
and `git diff --check`.

RESOLVED 2026-05-18T16:28+02:00:
Static-site `REVIEWER-ACCOUNT-SCOPE-1` is green after PR #359 / `392b84e`.
Focused guard, site JS syntax checks, typecheck, ownership, and whitespace
checks passed; broader native account/dependency decisions need a separate
VALIDATOR route if still in P0 scope.

RESOLVED 2026-05-18T16:32+02:00:
The live question-bank hash guard is green after PR #361 / `701a181`. Keep it
as part of live deploy closure so same-count content drift cannot pass.

RESOLVED 2026-05-18T16:36+02:00:
`REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1` is green after PR #368 /
`c118147`. Clean manager verification passed `npm run
test:static-site-privacy-copy` 2/2, site/test JS syntax checks, source
spot-checks for AdSense/AdMob/consent/local-progress/29 SEK Remove Ads copy in
English and Swedish, typecheck, ownership, targeted Prettier, and `git diff
--check`.

STANDING POLICY (operator, hard — read every cycle):
1. A dirty worktree from OTHER lanes' in-scope work is NORMAL in this
   parallel factory and is NOT a blocker. Lanes have disjoint write scope.
   Never record a "tree dirty / boundary moved" blocker and never stop or
   park a pane for it.
2. Phase-B / external-provenance work is DEFERRED (see open.txt OPERATOR
   DIRECTIVE). Do not file blockers about Phase-B leases.
3. A real blocker is ONLY a hard correctness failure on Phase-A work
   (tsc error, test:content red, a content gate red) that prevents a
   *push*. Even then the affected pane RE-TASKS to another unblocked
   Phase-A atom — it never idles.
4. If you would otherwise write "stop X until Y reconciles": instead
   re-task X to the next independent Phase-A atom and log that, not a stop.

# === P0 STOP-THE-LINE 2026-05-18: deployed site/ is starved ===
# Vercel serves ONLY site/. Factory built into app/ + docs/design/drafts/,
# so the LIVE site lacked Practice & Mock-exam and a thin ebook. SITE-P0-1 is
# accepted via PR #242 / c4404d8; SITE-P0-2 is accepted via PR #252 / 5e834b9;
# SITE-P0-3 is accepted via PR #264 / 19a420e; SITE-P0-4 is accepted via
# PR #279 / 9def23a plus handoff PR #280 / 41a6bcb. SITE-P0-5 reviewer audit
# is complete via PR #296 / 82291b1 and queued the concrete defects below.
REVIEWER-SITE-LIVE-DEPLOY-STALE-1: production deploy is stale; PR #321 added the harness, PR #328 fixed current-count smoke behavior, and PR #361 added the live hash guard, but closure still needs production deploy evidence from current `origin/main`. Latest scheduled deploy runs fail before job start because the account payment/spending limit blocks GitHub Actions runner execution; restore Actions capacity or provide an operator-verified production deployment before accepting SITE-P0-5.
