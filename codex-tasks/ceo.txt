
staffing request.
Manager updates sent: content manager received a direct tmux update to treat
q010/q011 as landed and route q037 next; validator was queued to accept only
PR-backed P0 atoms and keep expansion teams unstaffed until acceptance rows and
leases exist.

Cycle update 2026-05-18T12:53:42+02:00
Objective: keep the factory on P0 burn-down. TRANSLATE-COMPLETE and
SOURCE-CITATION remain open; q037 is on `origin/main` via PR #189 / `cd89504`,
q071 is on `origin/main` via PR #192 / `4e1faf9`, and manager dispatch on
`origin/main` (`022cffe`) has q072 assigned to pane 1 and q021 assigned to pane
3. Pane 4 is visibly working q073; do not duplicate it.
Teams: hold local `civic-content` only. Do not start build, UIUX, QA, release,
research, verify, delight, mascot, language, or LUNARC teams this cycle.
Data-integrity has a valid P0 generated-judgement prompt queue item, but no
safe staffed build lane is active locally and remote staffing returned no
usable queued work.
Priorities: finish q072, q021, and the in-flight q073 content atom as
one-question UHR-only TRANSLATE-COMPLETE/SOURCE-CITATION work. Next visible
content naturalness defects are q012, q013, q014, q015, q016, q017, and q020
unless VALIDATOR has already accepted them from newer PR evidence. Keep
DATA-INTEGRITY generated-judgement work queued for the next build-capable
staffing window.
Staffing evidence: `/home/billy/bin/csup staff --scenario=resume --dry-run`
returned STAFF-DOWN/no queued work for all configured remote hosts and repeated
that the remote fs10/fs9 checkouts have no task queue files. Local tmux shows
only `civic-content` active with six panes. Decision: HOLD/add-none; no
`--apply` and no stop without manager confirmation.
Escalations: none requiring human approval. Remote queue absence is a staffing
constraint, not a product blocker; `codex-tasks/blockers.txt` still says there
are no active blockers.
Manager updates sent: appended directives to `codex-tasks/content.txt` and
`codex-tasks/validator.txt`; direct tmux notice sent to MANAGER-content.

Cycle update 2026-05-18T13:22:00+02:00
Objective: keep the factory on P0 burn-down. TRANSLATE-COMPLETE and
SOURCE-CITATION remain the clearest content work; the generated judgement prompt
fix is now on `origin/main` and needs VALIDATOR acceptance before the P0 row can
be treated as covered. Expansion work stays secondary.
Teams: HOLD/add-none. Live tmux showed only `_csup_sentinel_` and
`civic-content` with six panes. `/home/billy/bin/csup staff
--scenario=resume --dry-run` returned queued work across configured hosts, but
every LUNARC/remote line held with `reason=lunarc_requires_slurm_station`; no
`--apply` was run.
Priorities: `origin/main` advanced to `ee4ddb4` after #202, the
DATA-INTEGRITY generated-stem fix #198 / `02193fb`, and q073. q072 is on
`origin/main` via PR #194 / `6fc47b4`; q073 is on `origin/main` via
`ee4ddb4`. q021 is active in `/tmp/sct-q021-pane3` with PR #203 open at
observed pane state; q012 status is superseded by the correction below. After
q021, route q013, q014, q015, q016, q017, q020, then q027/q028 unless VALIDATOR
has newer accepted evidence. Do not restaff DATA-INTEGRITY generated-judgement
work unless VALIDATOR rejects the #198 evidence.
Escalations: none requiring human approval. Station gating is a staffing
constraint, not a product blocker. The local `gh` CLI is missing, so this CEO
coordination update is prepared from a separate `origin/main` worktree to avoid
polluting the active content branch.
Manager updates sent: queued directives in `codex-tasks/content.txt` and
`codex-tasks/validator.txt`; direct tmux input was intentionally skipped because
the content panes were actively running worker prompts and tests.

Cycle correction 2026-05-18T13:27:00+02:00
q012 landed on `origin/main` after the 13:22 CEO note, via PR #204 /
`2c7c02f`. Treat any routing that still calls q012 active as stale. PR #206 is
an open MANAGER-content dispatch against `codex-tasks/content.txt`; it assigns
q012 and must be rebased/reconciled before merge. q021 remains open in PR #203
and not mergeable at connector check time. Current content order after q021 is
q013, q014, q015, q016, q017, q020, then q027/q028 unless VALIDATOR has newer
accepted evidence.

Cycle update 2026-05-18T15:52:00+02:00
Objective: keep the factory on SITE-P0 burn-down. Required CEO/operating docs
were read earlier this cycle from the supervisor repo fallback plus this board;
the requested project-local operating docs and `/home/billy/docs/...` CEO path
were absent. Decision baseline included q108 on `origin/main` via `d86717e`;
`origin/main` then advanced through component-only `9023506` before this CEO
update merged. Static site question-bank drift is accepted via PR #324 /
`d4ffd91`, and live-smoke count derivation is accepted via PR #328 /
`11b2b96`. Do not reopen those atoms or duplicate q108.
Teams: HOLD/add-none. Live tmux still shows only `_csup_sentinel_` plus
`civic-content` with six panes. `/home/billy/bin/csup staff --scenario=resume
--dry-run` reports queued work for configured hosts, but every LUNARC/remote
line holds with `reason=lunarc_requires_slurm_station`; no `--apply` was run.
Priorities: production deploy evidence for current `origin/main` is first, then
the static-site privacy/monetization copy defect. Practice width parity is now
accepted via PR #338, and static-site account scope via PR #359. Treat
`REVIEWER-SITE-PRIVACY-MONETIZATION-COPY-1` as active SITE-P0 because
VALIDATOR has concrete runtime/source evidence and the blocker/setup queues did
not yet surface it.
Escalations: no new human approval request this cycle. The production deploy
blocker still needs a successful scheduled/manual deploy and a current-count
live smoke against the production URL; if the deploy fails because of external
billing/cap/credential state, escalate that evidence to the operator instead of
closing the atom from local smoke.
Manager updates queued: VALIDATOR must keep deploy-stale open until production
evidence exists, SETUP gets the privacy-copy atom after account-scope, BLOCKERS
now lists the privacy-copy defect, UIUX is duplicate-guarded for landed
QuestionNavigator/MockExamConfigPanel work, and CONTENT is duplicate-guarded
for q108 while SITE-P0 outranks fresh non-site content.

Cycle update 2026-05-18T17:19:08+02:00
Objective: keep the factory on P0 burn-down, with SITE-P0-5 production deploy
freshness as the only active site blocker and TRANSLATE-COMPLETE plus
SOURCE-CITATION defects still open for generated and authored content.
Required docs: read `docs/parallel-sessions.md`,
`docs/parallel-sessions/TEAM_PLAN.md`, `codex-tasks/ceo.txt`,
`codex-tasks/blockers.txt`, and the supervisor fallback copies of
`ceo-executive.md`, `company-operating-model.md`, `ai-factory.md`, and
`ceo-staffing.md`. The requested `/home/billy/docs/...` path and project-local
operating docs are absent.
Teams: HOLD/add-none. Live tmux shows only `_csup_sentinel_` and
`civic-content` with six panes; no local build, validator, UIUX, QA, or release
manager pane is active. `/home/billy/bin/csup staff --scenario=resume
--dry-run` reported STAFF-UP queued work for every configured host but held
each with `reason=lunarc_requires_slurm_station`; no `--apply` was run.
Evidence: clean `origin/main` `de64960` passes `node --test
scripts/check-live-site.test.js` 7/7, `npm run validate:content` with 715
questions and static parity true, and `node scripts/export-site-question-bank.js
--check`. The production URL still fails `node scripts/check-live-site.js
https://dist-jgsjooi52-billy10384-5430s-projects.vercel.app` on static
question-bank hash (`expected cbb24a108686`, `found 5d2710bebf7e`).
Priorities: keep `REVIEWER-SITE-LIVE-DEPLOY-STALE-1` open until production
serves current `origin/main` and the hash-aware live smoke passes. While
external deploy capacity is blocked, route SETUP to the next unblocked source
atom `REVIEWER-VISUAL-SMOKE-LAUNCH-OVERLAY-1` because production submit guard
is accepted via PR #407 / `0572be8`; route DATA-INTEGRITY first to
`REVIEWER-GENERATED-TF-NATURALNESS-2`, then q124 data/static-mirror cleanup
with CONTENT owning only authored wording. q123 source-section cleanup is now on
`origin/main` via `f15404a`; VALIDATOR should accept/reject that evidence and
duplicate-guard q123. Do not accept data-only q124 branches that leave
`site/questions.js` stale.
Escalations: human/operator action is required for GitHub Actions
payment/spending capacity or an operator-verified production deployment. Also
reconcile the current scheduled-deploy workflow with the repo rule that local
panes must not run Vercel CLI; no pane should run `vercel` from this checkout.
Manager updates queued: appended CEO directives to `codex-tasks/validator.txt`,
`codex-tasks/setup.txt`, `codex-tasks/data-integrity.txt`,
`codex-tasks/content.txt`, and `codex-tasks/blockers.txt`. Direct tmux input was
skipped because no local build/validator manager pane exists and the only local
session is active content panes.

Cycle update 2026-05-18T19:44:00+02:00
Objective: keep the factory on P0 burn-down with SITE-P0-5 production
freshness as the only external release blocker, while source-side work closes
the remaining static-site surface defects and generated-content wording
residuals.
Required docs read: `docs/parallel-sessions.md`,
`docs/parallel-sessions/AI_FACTORY.md`, `docs/parallel-sessions/TEAM_PLAN.md`,
project queue files, and supervisor fallback copies of
`ceo-executive.md`, `company-operating-model.md`, `ai-factory.md`, and
`ceo-staffing.md`. The requested `/home/billy/docs/...` path and project-local
operating-doc paths remain absent.
Teams: HOLD/add-none. `/home/billy/bin/csup staff --scenario=resume --dry-run`
reported queued work (`work=7`, `blockers=7`) for configured hosts, but every
host held with `reason=lunarc_requires_slurm_station`; no `--apply` was run.
Local tmux still shows only `_csup_sentinel_` and `civic-content` with six
panes. `csup factory-audit civic-test` is RED for `missing:project_config`, so
do not use factory-run/apply as a launch signal until that audit is reconciled.
Evidence: latest observed `origin/main` is `d72fe29`; its delta over the
checked source commit is coordination/evidence-only. Current source checks passed
`npm run validate:content` at 720 questions, `node
scripts/export-site-question-bank.js --check`, `npm run
test:static-site-practice-result-i18n`, and `npm run typecheck -- --pretty
false`. Production still fails `npm run test:site-live -- https://dist-3u8o5zl6a-billy10384-5430s-projects.vercel.app`
because live serves 715 questions with hash `afb9eec56629` while current main
expects 720 and hash `57e05be047f9`.
Priorities: VALIDATOR can accept Practice result i18n after reviewing the
SETUP source/handoff evidence and should keep SITE-P0-5 open until production
serves current main. Next SETUP order is static flag palette drift, mobile nav
reachability, then static question-count copy. DATA-INTEGRITY remains first on
`REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1` from clean current main with q201-q720
evidence; unknown-material option cleanup stays behind it unless VALIDATOR
bundles safely. CONTENT should hand off capacity unless VALIDATOR leases the
paired authored true/false prefix cleanup with DATA-INTEGRITY mirrors. UIUX is
held behind explicit validator lease and the flag-constants blocker.
Escalations: human/operator deployment capacity or an operator-verified
production deploy remains required for SITE-P0-5. Direct tmux input was skipped
because no local build/validator/UIUX manager pane is active and content has no
new implementation instruction beyond the queued handoff.

Cycle update 2026-05-18T20:58:20+02:00
Objective: keep the factory on P0 burn-down. SITE-P0-5 remains external
deploy-capacity work; the active source route is now the generated/static
true/false wording-rule surface after the partial negative-meta cleanup.
Required docs read: `docs/parallel-sessions.md`,
`docs/parallel-sessions/AI_FACTORY.md`, `docs/parallel-sessions/TEAM_PLAN.md`,
project queue files, and supervisor fallback copies of `ceo-executive.md`,
`company-operating-model.md`, `ai-factory.md`, and `ceo-staffing.md`. The
requested `/home/billy/docs/parallel-sessions/ceo-executive.md` and
project-local operating-doc paths remain absent.
Teams/staffing: HOLD/add-none. Fresh `/home/billy/bin/csup staff
--scenario=resume --dry-run` reported queued work for all configured hosts
(`work=8`, `blockers=8`), but every host held with
`reason=lunarc_requires_slurm_station`; no `--apply` was run. Local tmux still
shows only `_csup_sentinel_` plus the six-pane `civic-content` session.
`csup factory-audit civic-test` remains RED for `missing:project_config`.
Current evidence: latest observed `origin/main` is `75310c8`. Source-relevant
DATA-INTEGRITY work is PR #618 / `9f007d8`; later commits are reviewer/content
coordination or recheck artifacts. Direct VM inspection of current
`site/questions.js` found 720 questions, 299 true/false rows, 299 redundant
`Sant eller falskt:` / `True or false:` prefix offenders, 0 meta-stem
offenders, remaining grammar offenders `q666`, `q667`, `q699`, and remaining
false-answer explanation mismatches in the latest reviewer/content handoffs.
Current static bank hash prefix is `ead3e32bf91d`.
Priorities: VALIDATOR should treat PR #618 as partial progress only and keep
`REVIEWER-GENERATED-TF-PREFIX-SURFACE-1` open. DATA-INTEGRITY next owns one
generated/static cleanup bundle to remove all UI-afforded true/false prefixes,
fix the remaining false-answer explanations, cover q666/q667/q699 grammar, and
regenerate canonical/static mirrors with guard coverage. SETUP remains
source-hold unless fresh P0 site evidence appears. CONTENT remains
verify/handoff-only unless VALIDATOR explicitly leases the paired authored
true/false prefix cleanup with DATA-INTEGRITY mirror ownership.
Escalations: operator deployment capacity or an operator-verified production
deploy is still required for SITE-P0-5. Fresh current-main live smokes against
both known production URLs failed only on stale static-bank freshness: expected
720/hash `ead3e32bf91d`, found 715/hash `afb9eec56629` or `5d2710bebf7e`.
Do not run Vercel CLI from this repo. Manager updates were queued in
`codex-tasks/validator.txt`, `codex-tasks/data-integrity.txt`,
`codex-tasks/setup.txt`, `codex-tasks/content.txt`, and
`codex-tasks/blockers.txt`; MANAGER-content also received a direct tmux notice
because it is the only active local manager lane.

Cycle update 2026-05-18T21:16:59+02:00
Objective: keep the factory on P0 burn-down. Current `origin/main` is
`268cc9d` (`manager-build: record ebook format followup`), so earlier q011 and
generated-judgement routing is superseded by the current generated true/false
cleanup lane plus SITE-P0 deploy freshness.
Read: `docs/parallel-sessions.md`, `AI_FACTORY.md`, `TEAM_PLAN.md`, `gm.md`,
GM journal, meeting sheet, `AGENTS.md`, `CLAUDE.md`, company operating model,
and staffing policy.
Teams/staffing: HOLD/add-none. Live tmux shows only `_csup_sentinel_` and
`civic-meta-local`; no local build/content/uiux/qa/release worker pane is
active. Clean-worktree `csup factory-audit` reports YELLOW for disabled/fs10
host configs and GREEN/no queued local fs9 host lanes. Clean-worktree
`csup staff /projects/hep/fs9/shared/nnbar/billy/civic-lunarc/_ceo-cycle-20260518T211659
--scenario=resume --dry-run` reports STAFF-UP with queued work/blockers for
configured hosts, but every host HOLDS with `reason=lunarc_requires_slurm_station`;
no `--apply` was run.
Priorities: DATA-INTEGRITY has fresh implementation evidence in `25655ba`
(`data-integrity: align false true-false explanations`) that needs
VALIDATOR/manager acceptance against the generated true/false cleanup route.
SITE-P0 production deploy freshness remains external/operator evidence; do not
close it from local source readiness alone and do not run Vercel CLI from this
repo. Do not stop the meta pane.
Escalations: usable worker capacity or operator-verified production deployment
evidence is still required. The active thread goal remains open.

Cycle correction 2026-05-18T21:31:00+02:00
`origin/main` advanced before PR #653 merged. Current `origin/main` is
`dff1c24`, and generated true/false cleanup acceptance remains recorded by
`a269684` (`manager-build: accept generated true-false cleanup`). The 21:16
DATA-INTEGRITY acceptance-needed line is superseded by that manager-build
acceptance. HOLD/add-none remains the staffing decision because the clean
staffing gate still holds configured hosts behind SLURM station requirements,
and SITE-P0 deploy freshness still needs operator/deploy evidence.

Cycle update 2026-05-18T21:47:00+02:00
Objective: keep the factory on P0 burn-down. Current `origin/main` is
`e1ae95a`; the latest source-side DATA-INTEGRITY work, including q301-q350
wording cleanup in `120d440`, is accepted by MANAGER-build evidence. The
current source-side route is the narrowed q351-q400 standalone follow-up.
Read: project `docs/parallel-sessions.md`, `AI_FACTORY.md`, `TEAM_PLAN.md`,
`gm.md`, meeting sheet, `AGENTS.md`, `CLAUDE.md`, current queue files, and
supervisor fallback copies of `ceo-executive.md`,
`company-operating-model.md`, `ai-factory.md`, and `ceo-staffing.md`. The
requested `/home/billy/docs/...` and project-local operating-doc paths remain
absent; fallback docs were used.
Teams/staffing: HOLD/add-none. Fresh `/home/billy/bin/csup staff
--scenario=resume --dry-run` reported queued work for every configured host
(`work=8`, `blockers=8`), but each host held with
`reason=lunarc_requires_slurm_station`; no `--apply` was run. `csup
factory-audit civic-test` remains RED with `missing:project_config` even
though the repo contains `.codex-supervisor.toml`; treat this as tooling/config
reconciliation before any factory-run/apply decision. Local tmux still shows
only `_csup_sentinel_` plus `civic-content` with six panes.
Evidence: clean current-main temp worktree passed
`node scripts/export-site-question-bank.js --check` with 720 questions/13
chapters and `npm run validate:content` with static-site parity true,
`questionFalseAnswerExplanationsValidated:720`, and
`questionGeneratedTrueFalseNaturalnessValidated:720`. Production still fails
the hash-aware live smoke at both known URLs: expected 720/hash
`69bfcfe3cc12`, found 715/hash `afb9eec56629` at `dist-3u8o5zl6a` and
715/hash `5d2710bebf7e` at `dist-jgsjooi52`.
Priorities: DATA-INTEGRITY next owns the narrowed q351-q400 standalone
follow-up for q358/q359/q398/q399 from current main. SETUP has no new local
source atom after source-provenance copy and Ebook chapter 13 acceptance; keep
SITE-P0-5 external until production serves current main. RELEASE/COMPLY is the
new source-side P0:
`REVIEWER-RELEASE-GATES-1` still says publishing privacy and data-safety
docs/tests assert the old disabled-ads contract despite the current ads/IAP
posture.
Escalations: operator deployment capacity or operator-verified production
deploy evidence remains required for SITE-P0-5. Supervisor config/audit
reconciliation is also required before any non-dry-run staffing. No Vercel CLI
was run.

Cycle update 2026-05-18T22:03:51+02:00
Objective: keep the factory on P0 burn-down. Current `origin/main` is
`9270ff3`; q351-q400 DATA-INTEGRITY source repair is accepted by MANAGER-build
and reviewer resolution evidence is landed, while q401-q450 VERIFY/reviewer
evidence is landed via `3582841`/`1831089`. q451-q500 remains active but in
recovery under the content manager. Treat older q401 branches and PRs as
duplicate unless VALIDATOR explicitly rejects the landed evidence.
Required docs read: supervisor fallback copies of `ceo-executive.md`,
`company-operating-model.md`, `ai-factory.md`, and `ceo-staffing.md`, plus
current `TEAM_PLAN.md` and queue files. The requested `/home/billy/docs/...`
and project-local operating-doc paths remain absent.
Teams/staffing: HOLD/add-none. This cycle's `/home/billy/bin/csup staff
--scenario=resume --dry-run` reported queued work for configured hosts
(`work=9`, `blockers=9`), but every host held with
`reason=lunarc_requires_slurm_station`; no `--apply` was run. `csup
factory-audit civic-test` remains RED for `missing:project_config`. Local tmux
shows only `_csup_sentinel_` plus `civic-content` with six panes.
Priorities: DATA-INTEGRITY moves next to q401-q450 standalone cleanup using the
landed q401 evidence. CONTENT/VERIFY keeps q451 as the only active VERIFY
slice and routes q501-q550 only after q451 lands/rejects/abandons. SETUP
remains source-held except for fresh P0 site evidence or explicitly assigned
RELEASE/COMPLY privacy/data-safety gate work. SITE-P0-5 remains external
deployment evidence only; no pane should run Vercel CLI.
Escalations: operator deployment capacity or operator-verified production
deploy evidence remains required for SITE-P0-5. Supervisor config/audit
reconciliation remains required before treating factory-run/apply as safe.

Cycle update 2026-05-18T22:12:23+02:00
Objective: keep the factory on P0 burn-down without duplicating accepted
generated wording or static-site atoms. Latest observed `origin/main` is
`99c0960` (`reviewer: queue q501 q550 current defects`); q401-q450 is now
accepted by MANAGER-build via `e022455` / source commit `85fe297`.
Required docs were read from the supervisor fallback copies plus
`docs/parallel-sessions.md`, `docs/parallel-sessions/AI_FACTORY.md`,
`docs/parallel-sessions/TEAM_PLAN.md`, and the current queue files; the
requested `/home/billy/docs/...` paths are absent on this machine.

Teams/staffing: HOLD/add-none. Local tmux shows only `_csup_sentinel_` plus
`civic-content` with six panes; no local build, validator, uiux, qa, or release
manager pane is active. `/home/billy/bin/csup staff --scenario=resume
--dry-run` reported STAFF-UP with queued work for every configured host
(`work=9`, `blockers=9`), but every host held with
`reason=lunarc_requires_slurm_station`; no `--apply` was run.
`/home/billy/bin/csup factory-audit civic-test` remains RED for
`missing:project_config`, so do not treat factory-run/apply as safe until the
project config audit is reconciled.

Priorities: do not duplicate q401-q450 source work unless fresh current-main
regression evidence appears. DATA-INTEGRITY next owns q451-q500 generated
wording defects from current VERIFY evidence: `0d58526` for q454, q466, q470,
q471, and q495 plus `8e3aa16` for q479. CONTENT-VERIFY should not duplicate
q401 or q451 evidence; q501-q550 reviewer evidence is queued in `99c0960` but
stays behind q451-q500 source routing. Keep
`REVIEWER-RELEASE-GATES-1` as the next non-site P0 COMPLY source atom for
RELEASE/COMPLY or SETUP/COMPLY: update the publishing privacy/data-safety docs
and `scripts/publishing.test.js` to match the current ads, Remove Ads IAP, and
consent posture. SETUP remains source-held unless VALIDATOR assigns that COMPLY
atom or fresh P0 site source evidence appears.

Escalations: SITE-P0-5 still needs production to serve current `origin/main`
and pass the hash-aware live smoke; no pane should run Vercel CLI from this
repo. Usable LUNARC/station capacity or reconciled factory project config is
needed before adding remote workers.

Cycle update 2026-05-18T22:46:00+02:00
Current `origin/main` is `7c690c0`. q451-q500 DATA-INTEGRITY cleanup is
accepted via PR #703 / `c6299ce`, q501-q550 VERIFY is refreshed, and
REVIEWER-RELEASE-GATES-1 is accepted via PR #707 / `9d40c89`. HOLD/add-none
remains the staffing decision; host staffing is still gated by
`reason=lunarc_requires_slurm_station`, and factory audit remains RED for
`missing:project_config`.
Priorities: DATA-INTEGRITY next owns q501-q550 for
q526/q527/q530/q531/q535/q542/q543. q551-q600 stays behind q501-q550 unless
VALIDATOR reorders. SETUP has no new local source atom unless fresh site or
release source evidence appears; SITE-P0-5 remains external deploy evidence.

Cycle update 2026-05-18T22:44:40+02:00
Correction on latest observed `origin/main` `58cb0f6`. The earlier CEO note
landed before all lane-specific manager queues were notified, before static
ebook verification queued a fresh SETUP/site defect, and before q501-q550
source cleanup landed. Manager updates are now queued for VALIDATOR,
DATA-INTEGRITY, SETUP, CONTENT, and the blocker ledger.

Teams/staffing: HOLD/add-none remains the staffing decision. Required CEO and
operating docs were read from supervisor fallback copies plus project
parallel-session docs and current queues because `/home/billy/docs/...` and
project-local operating-doc paths are absent on this machine.
`/home/billy/bin/csup staff --scenario=resume --dry-run` reported queued work,
but every configured host held with `reason=lunarc_requires_slurm_station`; no
apply was run. `csup factory-audit civic-test` remains RED for
`missing:project_config`, so do not treat factory-run/apply as safe until the
supervisor config audit is reconciled.

Priorities: q451-q500 DATA-INTEGRITY cleanup and RELEASE-GATES source work are
accepted and should not be duplicated. q501-q550 source cleanup is now on main
via `b441aed`; VALIDATOR/MANAGER-build should recheck and accept or send only
precise residuals back. Keep q551-q600, q601-q650, and q651-q700 evidence
queued behind q501-q550 acceptance unless VALIDATOR explicitly reorders. SETUP
now has a fresh site/source atom from `e74d624`: route
`REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1` to SETUP/site with CONTENT support for
source wording, while keeping SITE-P0-5 external deploy freshness separate.

Escalations: SITE-P0-5 remains external deploy capacity/operator evidence; the
latest live-deploy recheck still shows both known production URLs stale against
expected 720/hash `f25e0b9a06c3`. No pane should run Vercel CLI from this repo.
Remote staffing remains gated on usable station/LUNARC capacity and reconciled
factory project config.

Cycle update 2026-05-18T23:36:34+02:00
Objective: keep the factory on P0 burn-down without duplicating accepted
generated wording or static-site atoms. Latest observed `origin/main` is
`4fee33f`; commits after q698 source repair are coordination/evidence only.
q698 is accepted as partial q651-q700 closure by MANAGER-build in `08f836d`,
while q663/q670/q671 remain the next DATA-INTEGRITY source residuals.

Required docs read: supervisor fallback copies of `ceo-executive.md`,
`company-operating-model.md`, `ai-factory.md`, and `ceo-staffing.md`, plus
`GOAL.md`, `docs/parallel-sessions.md`, current `TEAM_PLAN.md`, blockers, and
manager queue files. The requested `/home/billy/docs/...` and project-local
operating-doc paths remain absent.

Teams/staffing: HOLD/add-none. Local tmux still shows only `_csup_sentinel_`
plus `civic-content` with six panes. Explicit `csup staff civic-test
--scenario=resume --dry-run` still fails with `no .codex-supervisor.toml found
for project 'civic-test'`; cwd-based `csup staff --scenario=resume --dry-run`
reports STAFF-UP on every configured host (`work=9`, `blockers=9`) but every
host holds on `reason=lunarc_requires_slurm_station`; no `--apply` was run.
`csup factory-audit civic-test` remains RED for `missing:project_config`.

Evidence: the clean current-main source state was verified after q698 with
`validate:content`, `test:derived-content`, static export parity, focused
published/static/test-gate tests 32/32, typecheck, and diff whitespace.
`git diff c26e7c1..4fee33f --name-only` shows only coordination/evidence
files after the q698 source commit. Fresh live smokes still fail only on stale
production static-bank freshness: current main expects 720/hash
`d7ab78d7fffc`, while both known production URLs serve stale 715-question
banks.

Priorities: DATA-INTEGRITY next owns q651-q700 q663/q670/q671, preserving the
accepted q698 capitalization guard. After that is accepted or rejected, route
the broader generated single-choice filler cleanup: 133 ordinary
`Which answer is correct?` / fallback-option rows plus 22 true/false
judgement-shell rows. SETUP remains source-held unless fresh site/release
source evidence appears. CONTENT remains VERIFY/report-and-queue only unless
VALIDATOR explicitly leases authored content.

Escalations: production deploy freshness still requires operator deployment
capacity or operator-verified deploy evidence. Supervisor project-config lookup
and usable LUNARC/station capacity remain staffing blockers. No pane should run
Vercel CLI from this repo.

Cycle update 2026-05-18T23:46:00+02:00
Objective: keep the factory on P0 product burn-down while preventing duplicate
coordination loops after the q698 partial repair and later evidence-only
commits. Required CEO docs were read from supervisor fallback copies of
`ceo-executive.md`, `company-operating-model.md`, `ai-factory.md`,
`ceo-staffing.md`, plus `GOAL.md`, project parallel-session docs, current
`TEAM_PLAN.md`, and queue files; the requested `/home/billy/docs/...` and
project-local operating-doc paths remain absent.

Teams/staffing: HOLD/add-none. Latest observed `origin/main` is `d468f31`;
changes after the q698 source commit are coordination, verification, setup
evidence, and UIUX blocked-audit artifacts only. Local tmux still shows only
`_csup_sentinel_` plus `civic-content` with six panes. Cwd-based
`/home/billy/bin/csup staff --scenario=resume --dry-run` reported queued work
on every configured host (`work=9`, `blockers=9`) but every host held on
`reason=lunarc_requires_slurm_station`; no apply was run. Explicit
`/home/billy/bin/csup staff civic-test --scenario=resume --dry-run` failed
with `no .codex-supervisor.toml found for project 'civic-test'`, while
`factory-audit civic-test` remains RED for `missing:project_config`.

Priorities: q698 remains accepted as partial q651-q700 closure; DATA-INTEGRITY
next owns only q663/q670/q671, preserving q698 as a regression spot check.
After that route is accepted or rejected, queue the bundled generated
single-choice cleanup covering 133 ordinary meta-stem/fallback rows plus 22
true/false judgement-shell rows, with separate zero-count proof for each
surface. SETUP stays source-held unless fresh site/release source evidence
appears. CONTENT stays VERIFY/report-only unless VALIDATOR grants a specific
authored-content lease. UIUX should stop rerouting the satisfied ResultSummary
fallback-copy atom; DESIGN-TOKENS must publish `lib/theme/flag.ts` before
COMPONENTS takes the SwedishFlagBand fixed-flag-colors source atom. SITE-P0-5
remains external deploy freshness; no pane should run Vercel CLI from this
repo.

Escalations: production deploy freshness still requires operator deployment
capacity or operator-verified deploy evidence. Supervisor project-config lookup
and usable LUNARC/station capacity remain staffing blockers.
