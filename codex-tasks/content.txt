MANAGER-content current-route update - 2026-05-19T01:04+02:00

Current `origin/main` is `8782487` after DATA-INTEGRITY PR #819 landed generated
true/false explanation cleanup immediately after MANAGER-content PR #820. This
supersedes the 01:00 route note's base hash only: q351-q400 remains the next
non-duplicative CONTENT-VERIFY report-only slice, but it must start from clean
current `origin/main` `8782487`, not `84f8ab3`, and must inspect the post-#819
generated explanation output. No local CONTENT-VERIFY worktree is active.

CONTENT source implementation remains handoff-only. `CONTENT-AUTHORED-TF-PREFIX-1`
is still unleased and must not become a data-only branch. CONTENT must not edit
`data/**`, `content/question-bank.csv`, `site/**`, generator templates, or
validator mirrors without an explicit bundled lease.

Fresh CONTENT-VERIFY capacity may take `verify:data/questions.ts q351-q400`
from clean current `origin/main` `8782487`. Keep it report-and-queue only:
inspect UHR citations, facts, answer keys, distractors, naturalness,
explanations, and current CSV/static output; update only VERIFY-owned
reports/ledger/queues/journals; then rerun export parity, `validate:content`,
`test:derived-content`, typecheck, ownership, and diff whitespace gates before
PR/merge. Do not publish q351-q400 evidence based on `84f8ab3` unless it has
been rebased/refreshed on `8782487` or later.

MANAGER-content current-route update - 2026-05-19T01:00+02:00

Current `origin/main` is `84f8ab3`. The only newer commit after the last
MANAGER-content route is a SETUP live-deploy recheck, so it opens no CONTENT
source work. `git worktree list --porcelain` shows no active local
CONTENT-VERIFY worktree, and tmux shows only the `civic-content` node pane plus
the supervisor sentinel. Do not revive q251-q300 or q301-q350 branches: those
are on main via `415e73c` and `ec01444`, and their generated residual lists are
stale after the accepted generated-output fixes.

CONTENT source implementation remains handoff-only. `CONTENT-AUTHORED-TF-PREFIX-1`
is still unleased and must not become a data-only branch. CONTENT must not edit
`data/**`, `content/question-bank.csv`, `site/**`, generator templates, or
validator mirrors without an explicit bundled lease.

Fresh CONTENT-VERIFY capacity may take `verify:data/questions.ts q351-q400`
from clean current `origin/main` `84f8ab3`. Keep it report-and-queue only:
inspect UHR citations, facts, answer keys, distractors, naturalness, and current
CSV/static output; update only VERIFY-owned reports/ledger/queues/journals; then
rerun export parity, `validate:content`, `test:derived-content`, typecheck,
ownership, and diff whitespace gates before PR/merge. If q351-q400 is rejected
or explicitly deferred, choose the next current changed/due slice rather than a
stale q251/q301 residual branch.

MANAGER-content current-route update - 2026-05-19T00:50+02:00

Current `origin/main` is `c1f02eb`. The generated single-choice cleanup is now
manager-accepted via PR #805 / `3fcc83e` and `13bd3b3`: current canonical/static
output has zero ordinary `Vilket svar är korrekt?` / `Which answer is correct?`
meta-stem rows, zero generic filler-option rows, zero true/false-shell rows,
and zero stale True/False single-choice explanation-label rows. Do not route
or duplicate old generated single-choice filler/meta-stem/judgement-shell work
unless VALIDATOR rejects the accepted evidence with precise current-main
residuals.

The q651-q700 residuals are also manager-accepted via `e90f82a` and `ad3f270`;
do not reopen q663/q670/q671 or q698 without fresh current-main evidence.
DATA-INTEGRITY source commit `81c3295` plus handoff `6e63e8b` landed the q270/
q271 reason-target wording repair on current main; treat it as a
MANAGER-build/VALIDATOR acceptance candidate, not a CONTENT source route.
Later `8d1cd21` and `c1f02eb` did not open new CONTENT work.

CONTENT source implementation remains handoff-only. `CONTENT-AUTHORED-TF-PREFIX-1`
is still unleased and must not become a data-only branch. CONTENT must not edit
`data/**`, `content/question-bank.csv`, `site/**`, generator templates, or
validator mirrors without an explicit bundled lease. The named q251-q300
CONTENT-VERIFY worktree is no longer present locally; q251-q300 landed via
`415e73c`, q301-q350 landed via `ec01444`, and their previously listed
generated residuals are stale after `13bd3b3` / `81c3295`. If VALIDATOR wants
more CONTENT-VERIFY capacity, start a fresh report-and-queue slice from current
`origin/main`, likely q351-q400 or the next changed/due slice, and keep it
report-only.

MANAGER-content current-route update - 2026-05-18T23:54+02:00

Current `origin/main` is `a554335`. The distinct CONTENT-VERIFY recent-data
regression artifact is landed via PR #781 / `32105c0`; do not duplicate that
branch or reopen the same ten-record regression check unless VALIDATOR rejects
the landed report with precise current-main residuals. No-source handoff
evidence also landed via PR #779 / `39b9468` and PR #786 / `a554335`; the
earlier duplicate Pane 1 PR #782 remains closed unmerged. Do not spend more
CONTENT capacity forcing additional journal-only no-work branches through
conflicts.

CONTENT source implementation remains handoff-only. `CONTENT-AUTHORED-TF-PREFIX-1`
is still unleased and must not become a data-only branch. Current generated
output ownership remains DATA-INTEGRITY/VALIDATOR: q663/q670/q671 first, then
the generated single-choice filler/judgement bundle with separate zero-count
proof for ordinary meta-stems, generic filler options, and true/false
judgement-shell rows. If VALIDATOR wants more CONTENT capacity before
DATA-INTEGRITY resumes, route a fresh report-and-queue VERIFY slice from clean
current main; otherwise stop duplicate no-work handoffs.

MANAGER-content current-route confirmation - 2026-05-18T23:52+02:00

Latest route baseline observed is `f4746f2`. Later handoff-only commits may
exist, but this does not open new CONTENT source implementation work. q601-q650
DATA-INTEGRITY cleanup is accepted; q651-q700,
q701-q720, q151-q200, and q201-q250 VERIFY evidence is landed; q698 is closed
as part of the q651-q700 route; and the generated single-choice filler bundle
is queued. Do not duplicate q601, q651, q698, q701, q151-q200, q201-q250, or
stale VERIFY branches unless VALIDATOR rejects landed evidence with precise
current-main residuals.

CONTENT remains handoff-only for source implementation. The only CONTENT-shaped
source candidate is still `CONTENT-AUTHORED-TF-PREFIX-1`, and it remains
unleased until VALIDATOR grants a bundled authored-data plus generated
CSV/static mirror plus guard cleanup. Current generated-output ownership stays
with DATA-INTEGRITY/VALIDATOR: q663/q670/q671 remain the focused q651-q700
follow-up, then the generated single-choice filler bundle should cover both
the 133 ordinary `Which answer is correct?`/fallback rows and the 22
true/false judgement-shell rows with separate zero-count proof for each
surface.

Do not edit `data/**`, `content/question-bank.csv`, `site/**`, generator
templates, validator mirrors, or regenerated static mirrors from CONTENT
without an explicit bundled lease. If VALIDATOR needs more content capacity
before DATA-INTEGRITY resumes from its usage-limit window, route a fresh
report-and-queue VERIFY/regression slice from clean current main.

MANAGER-content current-route correction - 2026-05-18T23:25+02:00

Current `origin/main` is `9f585d4`. q601-q650 DATA-INTEGRITY cleanup is
accepted, q651-q700 VERIFY is landed via `58732d6`, q701-q720 VERIFY is landed
via `e3fe9dd`, q698 capitalization source repair plus handoff are now on main
via `c26e7c1` and `9f585d4`, and the broad generated judgement/filler plus
single-choice filler-options reviewer queues are landed via `0e94a31` and
`ffb2ba2`. Do not duplicate q601, q651, q698, or q701 branches unless
VALIDATOR rejects the landed evidence with precise current-main residuals.

CONTENT remains handoff-only for source implementation. DATA-INTEGRITY is
currently usage-limited per `39585c0`; q698 should be inspected/accepted or
rejected from `c26e7c1`/`9f585d4`, while any q663/q670/q671 follow-up from the
landed q651 VERIFY evidence remains DATA-INTEGRITY/VALIDATOR-owned, not
CONTENT-owned.
The whole-bank generated judgement/filler cleanup follows q651 acceptance or
rejection and supersedes the older q701-only judgement route; the ordinary
single-choice filler-options cleanup can be bundled with it only if VALIDATOR
keeps the acceptance checks separate.

Do not edit `data/**`, `content/question-bank.csv`, `site/**`, generator
templates, validator mirrors, or regenerated static mirrors from CONTENT.
`CONTENT-AUTHORED-TF-PREFIX-1` remains unleased and may only proceed as an
explicit bundled authored-data plus generated CSV/static mirror plus guard
cleanup.

MANAGER-content current-route correction - 2026-05-18T23:06+02:00

Current `origin/main` is `b7fde22`. q701-q720 maintained VERIFY is now on
main via `e3fe9dd`, and q551-q600 resolution evidence is on main via
`b7fde22`; supersede the 22:56 line that treated Pane 4's q701 branch as
unmerged. Do not publish or merge stale q701 branches, including
`/tmp/sct-verify-q701-q720-pane4-final` /
`task/verify/q701-q720-pane4-1779137112` local head `6dd1ee9`, unless
VALIDATOR rejects the landed `e3fe9dd` report with precise current-main
residuals.

CONTENT-VERIFY Pane 3 now owns the maintained q651-q700 refresh in
`/tmp/sct-content-verify-q651-q700-pane3-1779138370`, branch
`task/content-verify/q651-q700-pane3-1779138370`, clean at `b7fde22` at
manager check time. Do not duplicate q651-q700 unless that branch is rejected,
abandoned, or VALIDATOR explicitly grants a replacement lease.

DATA-INTEGRITY source cleanup order remains q601-q650 next for
q606/q607/q611/q622, then q651-q700 q698, then q701-q720 q713/q716 unless
VALIDATOR reorders. CONTENT source implementation remains handoff-only unless
VALIDATOR explicitly leases authored-content work. VERIFY remains
report-and-queue only: no edits to `data/**`, `content/question-bank.csv`,
`site/**`, generator templates, or validator mirrors from CONTENT/VERIFY work.

MANAGER-content current-route correction - 2026-05-18T22:56+02:00

Current `origin/main` is `d1753ae`. q601-q650 maintained VERIFY is now on
main via `1ee7eb7`, and the separate q701/q720 reviewer judgement-template
defect is queued via `38bbf67`; do not duplicate q601-q650 VERIFY or q701/q720
reviewer evidence.

CONTENT-VERIFY Pane 4's maintained q701-q720 branch remains unmerged:
`/tmp/sct-verify-q701-q720-pane4-resume`,
`task/verify/q701-q720-pane4-1779137112`, remote head `a222b39`. It was based
on `d28723d` and is behind current main after q601 VERIFY, reviewer q701/q720,
ebook copy, manager-build acceptance, content handoff, q551-q600 cleanup,
live-deploy evidence, and q551 handoff commits. Do not merge or publish that
branch as current evidence until it is rebased/rechecked on current main, or
VALIDATOR explicitly abandons it in favor of the landed reviewer evidence.

q651-q700 remains the next unassigned maintained CONTENT-VERIFY slice only
after the q701-q720 branch lands, is rejected, or is explicitly abandoned,
unless VALIDATOR grants a non-conflicting concurrent lease. q551-q600 source
cleanup is now on main via `45d2e2d`; keep generated source cleanup order with
DATA-INTEGRITY/VALIDATOR at q601-q650 next, then q651-q700 q698, then
q701-q720 q713/q716 unless VALIDATOR reorders.

CONTENT source implementation remains handoff-only unless VALIDATOR explicitly
leases authored-content work. VERIFY remains report-and-queue only: no edits
to `data/**`, `content/question-bank.csv`, `site/**`, generator templates, or
validator mirrors from CONTENT/VERIFY work.

MANAGER-content active-lease correction - 2026-05-18T22:48+02:00

Current `origin/main` is `d28723d`. q501-q550 VERIFY is on main via `e0f379a`,
q501-q550 DATA-INTEGRITY cleanup is on main via `b441aed`, MANAGER-build
accepted q501 wording via `d28723d`, and q551-q600 current-output VERIFY is
now on main via `58cb0f6`. Do not publish stale q501 or q551
worktrees/branches unless VALIDATOR rejects the landed evidence and explicitly
reroutes.

CONTENT-VERIFY Pane 1 has active q601-q650 work in
`/tmp/sct-verify-q601-q650-pane1-1779136927`, branch
`task/verify/q601-q650-pane1-1779136927`. It currently has uncommitted
report-and-queue edits in `docs/parallel-sessions/journals/verify.md`,
`docs/verify/ledger.md`, and `docs/verify/q601-q650-2026-05-18.md`. At the
latest manager check it had rebased/synced to `d28723d` but still had an
unresolved `codex-tasks/data-integrity.txt` conflict from that sync. Pane 1
must resolve the conflict before PR/merge and rerun the standard
VERIFY/content/type/ownership/diff gates.

CONTENT-VERIFY Pane 4 has active q701-q720 work in
`/tmp/sct-verify-q701-q720-pane4-1779137112`, branch
`task/verify/q701-q720-pane4-1779137112`. It currently has uncommitted
report-and-queue edits in `docs/parallel-sessions/journals/verify.md`,
`docs/verify/ledger.md`, and `docs/verify/q701-q720-2026-05-18.md`. At the
latest manager check it was on current `origin/main` `d28723d` with those
uncommitted edits. Pane 4 must rerun the standard
VERIFY/content/type/ownership/diff gates before PR/merge.

q651-q700 has reviewer current evidence queued in `2111dc3`, but no maintained
CONTENT-VERIFY q651 refresh is observed active. Route q651-q700 only after the
active q601 and q701 work lands, is rejected, or is explicitly abandoned,
unless VALIDATOR grants a non-conflicting concurrent lease. VERIFY remains
report-and-queue only: no edits to `data/**`, `content/question-bank.csv`,
`site/**`, generator templates, or validator mirrors. CONTENT source
implementation remains handoff-only unless VALIDATOR explicitly leases
authored-content work.

MANAGER-content active-lease correction - 2026-05-18T22:16+02:00

Current `origin/main` is `8e3aa16`. q451-q500 VERIFY remains landed and now
includes the q479 correction from `8e3aa16`; do not publish stale q451
recovery branches unless VALIDATOR rejects the landed evidence and explicitly
reroutes.

q501-q550 is actively owned by CONTENT-VERIFY Pane 4 in worktree
`/tmp/sct-verify-q501-q550-pane4-1779135135`, branch
`task/verify/q501-q550-pane4-1779135135`. That worktree has current VERIFY
edits in `docs/verify/ledger.md`, `docs/verify/q501-q550-2026-05-18.md`,
`codex-tasks/data-integrity.txt`, and `docs/parallel-sessions/journals/verify.md`,
but it started before `38cb692`/`e022455`/`8e3aa16` and must rebase/sync to
current `origin/main`, preserve the q479 landed correction, rerun the standard
VERIFY/content/type/ownership/diff gates, and only then PR/merge.

The local Pane 3 q501 worktree
`/tmp/sct-content-verify-q501-q550-pane3-1779135140`, branch
`task/content-verify/q501-q550-pane3-1779135140`, is a duplicate placeholder
with no file changes at manager check time. Do not publish or continue that
duplicate unless Pane 4 is rejected, abandoned, or VALIDATOR explicitly
reroutes.

Do not route q551-q600 or another q501 duplicate while Pane 4 q501-q550 is
active. VERIFY remains report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors. CONTENT source implementation remains handoff-only unless VALIDATOR
explicitly leases authored-content work.

MANAGER-content active-lease correction - 2026-05-18T22:09+02:00

Current `origin/main` is `85fe297`; q451-q500 VERIFY is landed via
`0d58526` (`verify: refresh q451-q500 current output`) and should not be
duplicated. It supersedes the 22:03 CEO hold and the 21:57 manager recovery
plan for q451; do not publish stale q451 recovery branches, including
`task/content-verify/q451-q500-pane3-1779135000`,
`task/content-verify/q451-q500-pane3-1779135000-commit`,
`task/content-verify/q451-q500-pane3-current-1779141000`, or
`task/verify/q451-q500-pane1-current-1779134457`, unless VALIDATOR rejects the
landed q451 evidence and explicitly reroutes.

Next CONTENT-VERIFY capacity is `verify:data/questions.ts q501-q550`, or the
next current changed/due slice if VALIDATOR reorders. Start only from clean
current `origin/main` after `85fe297`; older q501 generated/residual audit
branches predate the current generated-output fixes and must not be revived as
current evidence without a fresh re-audit.

VERIFY remains report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors. CONTENT source implementation remains handoff-only unless VALIDATOR
explicitly leases authored-content work.

CEO->MANAGER-content 2026-05-18T22:03:51+02:00
Current `origin/main` is `9270ff3`. q401-q450 VERIFY/reviewer evidence has
landed via `3582841`/`1831089`, and q351-q400 DATA-INTEGRITY source repair is
accepted by MANAGER-build with reviewer resolution evidence. Do not publish
further q401 duplicate branches or PRs unless VALIDATOR rejects that landed
evidence and explicitly assigns a replacement.

Keep q451-q500 as the only active VERIFY slice and follow the 21:57 manager
recovery plan. The Pane 3 patch must be recovered/rebased onto current
`origin/main` `9270ff3`, rerun VERIFY/content/type/ownership/diff gates, and
only then publish/merge. Do not revive older q451 branches.

After q451 lands, is rejected, or is abandoned, route q501-q550 or the next
current changed/due VERIFY slice. VERIFY remains report-and-queue only: no
edits to `data/**`, `content/question-bank.csv`, `site/**`, generator
templates, or validator mirrors. CONTENT source implementation remains
handoff-only unless VALIDATOR explicitly leases authored-content work.

MANAGER-content active-lease correction - 2026-05-18T21:57+02:00

Current `origin/main` is `f249e7bf`. q401-q450 VERIFY is landed via PR #677 /
`3582841`, DATA-INTEGRITY q351-q400 cleanup is landed via `67846c5` and
accepted by MANAGER-build via `18e20dea`, and duplicate PR #678 / branch
`task/verify/q401-q450-pane1-recover-1779133742` is closed unmerged. Do not
republish q401-q450 branches unless VALIDATOR rejects the landed #677
evidence.

q451-q500 remains reserved to Pane 3 but needs recovery or explicit reroute.
The previously referenced worktree
`/home/billy/sct-content-verify-q451-q500-pane3-1779135000` is absent at
current manager check. Local branch
`task/content-verify/q451-q500-pane3-1779135000` remains at `1831089`, has no
remote head, and the q451 patch is preserved in the local stash entry named
`pane3-q451-rebase-q401`. Treat Pane 3 q451-q500 recovery as canonical unless
rejected or explicitly abandoned; Pane 3 must recover/rebase onto current main,
rerun VERIFY/content/type/ownership/diff gates, and only then publish/merge.

Do not route q501-q550 or a duplicate q451 branch while Pane 3 q451 recovery is
unresolved, unless VALIDATOR explicitly reroutes after rejection or abandonment.
VERIFY remains report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors. CONTENT source implementation remains handoff-only unless VALIDATOR
explicitly leases authored-content work.

MANAGER-content active-lease correction - 2026-05-18T21:52+02:00

Current `origin/main` is `af6a75c`. The CEO 21:47 staffing-cycle note is now
merged, q301-q350 generated wording is accepted, and CONTENT remains
verify/handoff-only unless VALIDATOR explicitly leases authored source work.

q401-q450 remains the active CONTENT-VERIFY slice, now recovered in local Pane
1 worktree `/tmp/sct-verify-q401-q450-pane1-recover-1779133742`, branch
`task/verify/q401-q450-pane1-recover-1779133742`. It has local commit
`8a2d037`, local VERIFY edits, and is ahead 1 / behind 1 relative to
`origin/main`. Treat this Pane 1 recovery branch as canonical unless rejected
or explicitly abandoned. It must sync/rebase onto current `origin/main`, rerun
the VERIFY/content/type/ownership/diff gates, and only then publish/merge.

Do not publish the duplicate q401-q450 worktree
`/tmp/sct-content-pane4-recover-1779132950` /
`task/verify/q401-q450-pane4-1779132950`; it is at current main but has an
unresolved `codex-tasks/data-integrity.txt` conflict and is duplicate unless
Pane 1 is rejected or explicitly abandoned.

q451-q500 is reserved to local Pane 3 worktree
`/home/billy/sct-content-verify-q451-q500-pane3-1779135000`, branch
`task/content-verify/q451-q500-pane3-1779135000`, created clean from current
`origin/main` `af6a75c`. Treat it as canonical unless rejected or explicitly
abandoned. Do not revive older q451 branches from before the CEO 21:47 merge.

After q401-q450 and q451-q500 land, are rejected, or are explicitly abandoned,
route q501-q550 or the next current changed/due slice. VERIFY remains
report-and-queue only: no edits to `data/**`, `content/question-bank.csv`,
`site/**`, generator templates, or validator mirrors.

MANAGER-content active-lease correction - 2026-05-18T21:43+02:00

Current `origin/main` is `caa4fd9`. q351-q400 VERIFY has a newer landed
refresh via `2607e11`, which supersedes the earlier `e563670` routing note and
the local q351 duplicate warnings below. Treat q351-q400 as closed for current
routing unless VALIDATOR rejects the `2607e11` evidence.

q401-q450 remains active in local Pane 1 worktree
`/tmp/sct-verify-q401-q450-pane1-1779133017`, branch
`task/verify/q401-q450-pane1-1779133017`, with `codex-tasks/data-integrity.txt`,
`docs/verify/ledger.md`, and `docs/verify/q401-q450-2026-05-18.md` changes in
progress. q451-q500 remains active in local Pane 3 worktree
`/tmp/sct-content-verify-q451-q500-pane3-1779133136`, branch
`task/content-verify/q451-q500-pane3-1779133136`, with
`docs/verify/q451-q500-2026-05-18.md` changes in progress. Both must
sync/rebase current `origin/main` before PR/merge.

Do not publish duplicate q401/q451 work from any other branch unless those
owners are rejected or explicitly abandoned. After both slices land/reject/
abandon, route q501-q550 or the next current changed/due slice.

MANAGER-content active-lease correction - 2026-05-18T21:35+02:00

Current `origin/main` is `84e340d`. q351-q400 VERIFY is now landed via
`e563670` (`verify: refresh q351-q400 current output [allow-meta]`). This
supersedes the 21:26 q351 active-lease note.

Do not publish duplicate local q351 branches:
- `/tmp/sct-verify-q351-q400-pane4-1779132305` /
  `task/verify/q351-q400-pane4-1779132305` is now behind `origin/main`.
- `/tmp/sct-verify-q351-q400-pane1-1779132415` /
  `task/verify/q351-q400-pane1-1779132415` is now behind `origin/main`.
- `/tmp/sct-content-verify-q351-q400-pane3-1779132479` /
  `task/content-verify/q351-q400-pane3-1779132479` is now behind
  `origin/main`.

The observed active CONTENT-VERIFY capacity is q401-q450 in local Pane 1
worktree `/tmp/sct-verify-q401-q450-pane1-1779133017`, branch
`task/verify/q401-q450-pane1-1779133017`, with `docs/verify/ledger.md` and
`docs/verify/q401-q450-2026-05-18.md` changes in progress. It is behind
current `origin/main` and must sync/rebase before PR/merge. Do not duplicate
q401 with the clean local Pane 4 branch `task/verify/q401-q450-pane4-1779132950`
unless Pane 1 is rejected or explicitly abandoned.

q451-q500 is also active as a non-overlapping VERIFY slice in local Pane 3
worktree `/tmp/sct-content-verify-q451-q500-pane3-1779133136`, branch
`task/content-verify/q451-q500-pane3-1779133136`. It is behind current
`origin/main` and must sync/rebase before PR/merge.

After q401-q450 and q451-q500 land, are rejected, or are abandoned, next
capacity is q501-q550 or the next current changed/due slice if VALIDATOR
reorders. VERIFY remains report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors.

CONTENT implementation remains handoff-only unless VALIDATOR explicitly leases
the paired authored true/false prefix cleanup named by
`CONTENT-AUTHORED-TF-PREFIX-1`.

MANAGER-content active-lease correction - 2026-05-18T21:26+02:00

Current `origin/main` is `dff1c24`. q251-q300 VERIFY is landed via PR #638 /
`3a18e24`; q301-q350 VERIFY is now landed via PR #640 / `561603d`. Do not
duplicate branches `task/verify/q251-q300-pane1-1779132600` or
`task/content-verify/1779131082`.

The prior PR #640 stale-head warning is superseded: GitHub reports PR #640
closed and merged, with head `7dcc0e4`, base `8bb2ccf`, and merge commit
`561603d`.

The observed active CONTENT-VERIFY capacity is q351-q400 in local Pane 4
worktree `/tmp/sct-verify-q351-q400-pane4-1779132305`, branch
`task/verify/q351-q400-pane4-1779132305`. It has pushed remote head `bda26f7`,
but it is behind current `origin/main` and must sync/rebase before PR/merge.
Treat it as canonical unless rejected or explicitly abandoned.

Local Pane 1 also has duplicate q351-q400 work in:
`/tmp/sct-verify-q351-q400-pane1-1779132415` /
`task/verify/q351-q400-pane1-1779132415`, local commit `94878bb`, and no remote
head at manager check time. Do not publish or continue that duplicate unless
Pane 4 is rejected/abandoned or VALIDATOR explicitly reroutes.

VERIFY remains report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors. After q351-q400 lands, is rejected, or is abandoned, next capacity is
q401-q450 or the next current changed/due slice if VALIDATOR reorders.

Generated/static true/false prefixes remain clean after PR #642; the remaining
prefix surface is source-data-only in the 11 authored rows named by
`CONTENT-AUTHORED-TF-PREFIX-1`. CONTENT implementation remains handoff-only
unless VALIDATOR explicitly leases that paired authored-data plus generated
CSV/static-mirror cleanup.

MANAGER-content active-lease update - 2026-05-18T21:08+02:00

Current `origin/main` is `b6638f4`. q201-q250 VERIFY is already canonical via
PR #627 / `cf89455`; do not duplicate it. Active CONTENT-VERIFY leases now
observed in local tmux/worktrees:
- Pane 1 owns `verify:data/questions.ts q251-q300` in
  `/tmp/sct-verify-q251-q300-pane1-1779132600` on branch
  `task/verify/q251-q300-pane1-1779132600`. It must rebase/sync to current
  `origin/main` before PR/merge and keep the atom report-and-queue only.
- Pane 3 owns `verify:data/questions.ts q301-q350` in
  `/tmp/sct-content-verify-1779131082` on branch `task/content-verify/1779131082`.
  It started from `e0ba44c` while `origin/main` has advanced, so it must
  rebase/sync before PR/merge and route only current-output defects.

Do not assign another pane to q251-q300 or q301-q350 unless one of those
branches is explicitly abandoned or rejected. Next available CONTENT-VERIFY
capacity after both slices land/reject/abandon is q351-q400 or the next current
changed/due slice. VERIFY remains report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors. CONTENT implementation stays handoff-only unless VALIDATOR explicitly
leases `CONTENT-AUTHORED-TF-PREFIX-1` as a paired authored-data plus generated
CSV/static-mirror cleanup.

MANAGER-content correction - 2026-05-18T21:05+02:00

Supersede the 21:00 q201-q250 duplicate-guard ownership line: q201-q250 VERIFY
is already landed on current `origin/main` via PR #627 / `cf89455`, using the
former Pane 3 branch `task/content-verify/pane3-current-1779130456`. Treat that
evidence as canonical unless VALIDATOR rejects it. Pane 1 must not publish the
local q201-q250 duplicate and should abandon/recreate any stale q201 worktree
after syncing to current `origin/main`.

Next available CONTENT-VERIFY capacity moves to q251-q300 or the next current
changed/due slice, unless a newer manager/VALIDATOR route assigns otherwise.
Keep VERIFY report-and-queue only: no edits to `data/**`,
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors.

MANAGER-content current-route update - 2026-05-18T21:00+02:00

Duplicate guard for q201-q250 VERIFY: CONTENT-VERIFY Pane 1 is the canonical
owner for the current-output q201-q250 refresh in worktree
`/tmp/sct-verify-q201-q250-pane1-1779130700`, branch
`task/verify/q201-q250-pane1-1779130700`. The older Pane 3 branch
`task/content-verify/pane3-current-1779130456` is duplicate/stale after its PR
publish path hit missing `gh`; do not open, merge, or duplicate that branch
unless Pane 1 is explicitly abandoned or rejected.

Pane 1 must sync/recheck current `origin/main` before committing, keep scope to
`docs/verify/ledger.md`, `docs/verify/q201-q250-2026-05-18.md`, and
`docs/parallel-sessions/journals/verify.md` unless a newly confirmed current
defect is not already covered by the active DATA-INTEGRITY route. The current
q201-q250 findings are still generated-output work: true/false prefix rows and
the q235 false-answer explanation mismatch. CONTENT source implementation
remains handoff-only unless VALIDATOR explicitly leases
`CONTENT-AUTHORED-TF-PREFIX-1` as a paired data/export/static/guard cleanup.
After q201-q250 lands, is rejected, or is abandoned, next VERIFY capacity moves
to q251-q300 or the next current changed/due slice.

MANAGER-content current-route update - 2026-05-18T20:45+02:00

Current `origin/main` includes PR #609 / `47f3983`, the post-#570/#584
q151-q200 VERIFY refresh. Treat the older q151-q200 splice-residual evidence
as stale: q174/q190/q194-style splice defects are no longer current in that
slice. The current learner-visible generated true/false defects are now:
- the global redundant true/false prefix surface (`299` true_false rows in
  `content/question-bank.csv` and `site/questions.js`);
- negative/not-true false-statement stems in q151, q167, q235, q255, q266,
  q331, q339, q439, q507, q519, and q715;
- false-answer explanation mismatches in q151, q167, q235, q255, q331, q339,
  q439, q507, q519, and q715;
- residual English grammar/naturalness in q666, q667, and q699.

All four are generated-output or generated/static-mirror work and remain
DATA-INTEGRITY-owned. CONTENT source implementation remains handoff-only unless
VALIDATOR explicitly leases `CONTENT-AUTHORED-TF-PREFIX-1` as a paired
authored-data plus generated CSV/static-mirror cleanup. Do not duplicate
q151-q200 VERIFY, do not reopen unknown-material option evidence, and do not
publish a data-only authored-prefix branch.

MANAGER-content current-route update - 2026-05-18T20:36+02:00

Current `origin/main` includes DATA-INTEGRITY #600 / `476afdb`, which clears
the positive generated true/false meta stems such as `Påståendet är sant:` /
`The statement is true:` for rows including q150, q166, and q714. Treat that
as a partial generated-output repair, not full route closure.

Remaining generated-output follow-ups are DATA-INTEGRITY-owned:
- negative generated meta stems still publishing `Det är inte sant att` /
  `It is not true that` in current output, including q151, q167, q235, q255,
  q266, q331, q339, q439, q507, q519, and q715;
- residual grammar/naturalness rows q666, q667, and q699 from the accepted
  post-#584 VERIFY refresh.

CONTENT remains handoff-only unless VALIDATOR explicitly leases
`CONTENT-AUTHORED-TF-PREFIX-1` as a paired authored-data plus generated
CSV/static-mirror cleanup. Do not reopen the closed unknown-material option
route, and do not treat #600 as closure for q715 or the negative meta-stem
pattern.

MANAGER-content current-route update - 2026-05-18T20:24+02:00

Current `origin/main` includes #584 / `16dbe4d` and #590 / `4d3b91d`, so
`REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1` is closed for current routing.
Fresh CONTENT capacity must not write another unknown-material capacity handoff
using the old 155-copy evidence. Exact-main verification found zero old
`materialet` / `from the material` generated option copies.

No standalone CONTENT source atom is leased. `CONTENT-AUTHORED-TF-PREFIX-1`
still requires an explicit paired CONTENT/DATA-INTEGRITY/static-mirror lease
before source edits. Remaining generated true/false naturalness/meta-stem
follow-ups are DATA-INTEGRITY-owned (`q150`, `q666`, `q667`, `q699`, `q714`,
`q715`); static-site count/deploy work remains SETUP/build-owned; VERIFY work
remains report-and-queue only.

P0-first rule: while `codex-tasks/P0.md` has unchecked items, accept only
content work that advances TRANSLATE-COMPLETE, SOURCE-CITATION, or Phase-A
SOURCE-EXPANSION. Keep all content panes on UHR-only atoms; do not read, copy,
paraphrase, or compare against any competitor question bank.

Landed:
- q005/q006 are on `origin/main` via PR #157.
- q007 is on `origin/main` as `c48945c` via PR #163.
- q008 is on `origin/main` via PR #172.
- q009 is on `origin/main` as `734c75c` via PR #177.
- q010 is on `origin/main` via PR #182.
- q011 is on `origin/main` as `793ec73` via PR #180.
- q012 is on `origin/main` as `2c7c02f` via PR #204.
- q013 is on `origin/main` as `782027c` via PR #209.
- q014 is on `origin/main` as `ac5b2c6` via PR #211, with the missing
  journal handoff corrected by PR #214 / `863ce49`.
- q015 is on `origin/main` as `e0e39a5` via PR #218.
- q016 is on `origin/main` as `31d9828` via PR #225, with merge evidence
  recorded by PR #231 / `af56e53`.
- q017 is on `origin/main` as `fc19b58` via PR #222, with merge evidence
  recorded by PR #226 / `d827682`.
- q019 is on `origin/main` as `67b6005` via PR #259.
- q020 is on `origin/main` as `b35ef8f` via PR #262.
- q021 is on `origin/main` as `0bb6cc4` via PR #203.
- q037 is on `origin/main` as `cd89504` via PR #189.
- q071 is on `origin/main` as `4e1faf9` via PR #192.
- q072 is on `origin/main` as `6fc47b4` via PR #194.
- q073 is on `origin/main` as `ee4ddb4` via PR #199.
- q074 is on `origin/main` as `7f3c84b` via PR #270, with merge evidence
  recorded by `fecc520`.
- q078 is on `origin/main` as `2176b29` via PR #271, with merge evidence
  recorded by `3862d7b`.
- q079 is on `origin/main` as `afb12f9` via PR #273.
- q080 is on `origin/main` as `5f0ff48` via PR #284.
- q081 is on `origin/main` as `ff9bb56` via PR #282.
- q070 is on `origin/main` as `6d2bd46` via PR #308, with merge evidence
  recorded by `cb8bdf4`.
- q142 is on `origin/main` as `4636321` via PR #295, with merge evidence
  recorded by PR #311 / `2b3fab0`.
- q143 is on `origin/main` as `98bc031` via PR #316.
- q109 is on `origin/main` as `b2c4e5d` via PR #318.
- q108 is on `origin/main` as `d86717e` via PR #335.
- q018 is on `origin/main` as `76b3bde` via PR #317.
- q114 is on `origin/main` as `f5454ba` via PR #352, with the required static
  mirror update included in the same landed source state.
- q123 source-section audit is on `origin/main` as `2010bc8` via PR #386; it
  is an audit/routing artifact only, not the q123 data/static-mirror cleanup.

Duplicate guard:
- PR #178 and branch `task/content/q009-naturalness-pane1-1779100218` were
  closed/deleted as duplicate q009 work. Do not reopen or republish q009.
- PR #201 and branch `task/content/q073-naturalness-pane4-1779101509` were
  closed as duplicate q073 work. Do not reopen or republish q073.
- q012 is landed via PR #204. Do not reopen or republish q012.
- q013 and q014 are landed. Do not reopen or republish q013/q014.
- q015, q016, q017, and q021 are landed. Do not reopen or republish
  q015/q016/q017/q021.
- q019 is landed via PR #259 / `67b6005`. Do not reopen or republish q019;
  `/tmp/sct-q019-pane1-1779106498` and `/tmp/sct-q019-pane2` are stale after
  the merge.
- q020 is landed via PR #262 / `b35ef8f`. Do not reopen or republish q020;
  `/tmp/sct-pane4-q020` is stale after the merge.
- Pane 2's duplicate q015 worktree `/tmp/sct-q015-pane2` is abandoned; do not
  commit, push, or PR that duplicate q015 diff.
- Duplicate q016 draft PR #230 was closed unmerged, and branch
  `task/content/q016-naturalness-pane1-1779103769` was deleted. Do not reopen
  or republish that duplicate q016 diff.
- q074, q078, q079, q080, and q081 are landed. Do not reopen or republish
  stale q074/q078/q079/q080/q081 worktrees or branches.
- q070, q109, q142, and q143 are landed. Do not reopen or republish stale
  q070/q109/q142/q143 branches or worktrees.
- q108 is landed via PR #335 / `d86717e`. Do not reopen or republish stale
  q108 duplicate branches, including
  `task/content/q108-naturalness-pane3-1779112067`.
- q018 is landed via PR #317 / `76b3bde`; duplicate PR #329 was closed
  unmerged. Do not reopen or republish stale q018 branches or worktrees. The
  follow-up static-site generated bank drift is build/DATA-INTEGRITY-owned;
  CONTENT must not edit `site/**`.
- q114 is landed via `f5454ba` / PR #352. Do not reopen stale q114 branches,
  do not rerun the prior static-mirror companion route, and keep q114
  duplicate-guarded unless VALIDATOR records a concrete evidence failure.
- q144 is open in PR #331 on branch
  `task/content/q144-source-expansion-pane1-1779114000`, but latest CEO
  routing holds fresh non-site content behind SITE-P0 unless VALIDATOR
  explicitly leases it. Do not publish the stale local pane-4 q144 duplicate
  branch.
- q123 is now audit-guarded via PR #386 / `2010bc8`. Do not open a
  CONTENT-only q123 data branch: the learner-visible source-section wording is
  present in both `data/additionalQuestions.ts` and generated
  `site/questions.js`, so closure needs an explicitly leased bundled
  DATA-INTEGRITY/static-mirror atom or another VALIDATOR-approved cross-lane
  path.

Active dispatch:
- q016, q017, q018, q019, q020, q070, q074, q078, q079, q080, q081, q108,
  q109, q114, q142, and q143 are landed. The earlier q016/q018/q019/q020/q070/
  q074/q078/q079/q080/q081/q108/q109/q114/q142/q143 assignments are
  superseded.
- Operator directive in `codex-tasks/open.txt` supersedes the earlier idle-hold
  behavior: managers must not park CONTENT panes while build-capable SITE-P0
  staffing is unavailable. Current gate reality: data-changing CONTENT atoms
  can stale the generated static-site mirror, so fresh CONTENT capacity must
  not open duplicate q114/q144 data PRs or hand-edit generated output. It may
  audit/file concrete UHR-only defects or report to MANAGER/VALIDATOR for an
  explicit lease.
- SITE-P0 work remains build-owned; CONTENT must not edit `site/**` and must
  not present content atoms as SITE-P0 progress.
- Manager-content audit on current `origin/main` `97f3096` filed
  `REVIEWER-GENERATED-TF-NATURALNESS-2` in `codex-tasks/validator.txt`: the
  published CSV still has generated true/false grammar defects such as
  `Det att`, `describes that`, `It is correct that the answer is`, and
  `Svaret är`. Do not hand-edit authored CONTENT rows or `content/question-bank.csv`
  for this; the fix belongs to DATA-INTEGRITY because it must change
  `lib/content/derivedQuestions.ts`, the validator mirror, generated exports,
  and any required static mirror in one gated atom.
- Manager-content audit on current `origin/main` `2010bc8` routes
  `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1` to DATA-INTEGRITY/VALIDATOR:
  q123 still says `avsnittet` / `this section` in authored explanations and in
  the static-site mirror. CONTENT must not hand-edit `site/**` or open a
  data-only q123 branch that would immediately stale the mirror.

Current `origin/main` now includes the q018 and q114 source-note cleanups plus
the q114 static mirror update. Treat q114 as landed/duplicate-guarded unless
VALIDATOR records a concrete evidence failure. CONTENT still must not edit
`site/**`; future static-site bank syncs remain SETUP/build or
DATA-INTEGRITY-owned.

Next content work:
1. Keep q018 and q114 duplicate-guarded as landed; do not open new q018/q114
   data or static-mirror work unless VALIDATOR rejects the current evidence.
2. Keep `REVIEWER-GENERATED-TF-NATURALNESS-2` routed to DATA-INTEGRITY;
   CONTENT must not hand-edit generated CSV rows, generated templates, or the
   validator mirror.
3. Keep `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1` routed to VALIDATOR /
   DATA-INTEGRITY for an explicitly leased bundled data + static mirror fix.
   CONTENT may audit the defect, but must not publish a data-only q123 branch
   or touch `site/**`.
4. Keep q144 duplicate-guarded in PR #331, but do not advance, duplicate, or
   replace it until VALIDATOR explicitly leases non-site content or CEO routing
   releases CONTENT. If it is later released, the q144 owner must rebase/verify
   against then-current `origin/main` before merge and pass export parity,
   `validate:content`, focused content tests, typecheck, ownership,
   Prettier/diff checks, and a content journal handoff with PR/merge evidence.
5. Fresh CONTENT capacity currently reports to MANAGER/VALIDATOR/CEO for
   SITE-P0 staffing/release or bounded UHR-only audit findings instead of
   opening new non-site content. CONTENT still must not edit `site/**`; any
   static-site bank sync or generated true/false template repair remains
   SETUP/build or DATA-INTEGRITY-owned.

MANAGER-content 2026-05-18T16:56:00+02:00
`origin/main` includes q123 audit evidence via PR #386 / `2010bc8`. Route
change: q123 source-section wording is now a concrete bundled data/static-mirror
cleanup candidate, not a CONTENT-only data atom. `CONTENT-Q123-SOURCE-SECTION-
STATIC-MIRROR-1` is filed to VALIDATOR/DATA-INTEGRITY; CONTENT must not edit
`site/**` or publish a data-only q123 branch. Keep q144 held unless VALIDATOR
explicitly leases non-site content, and keep generated true/false naturalness
DATA-INTEGRITY-owned.

MANAGER-content 2026-05-18T16:51:00+02:00
`origin/main` now includes the q144 hold audit and q114 landed source/static
mirror evidence. Route change: q114 is duplicate-guarded as landed, so fresh
CONTENT capacity must not reopen PR #352 or rerun the q114 static mirror.
`REVIEWER-GENERATED-TF-NATURALNESS-2` remains DATA-INTEGRITY-owned, and q144
stays held in PR #331 unless VALIDATOR explicitly leases non-site content.
CONTENT must not edit `site/**`, generated CSV rows, generated templates, or
build-owned queues.

Every released atom must preserve UHR locator evidence, run export parity,
`validate:content`, focused content tests, typecheck, ownership,
Prettier/diff checks, and write a journal handoff naming the PR or merge
evidence.

Acceptance guard: every content handoff must name the question id, changed
artifact, UHR locator, verification commands, PR number or merge commit, and
the P0 item advanced. Do not touch competitor question-bank content.

CEO->MANAGER-content 2026-05-18T13:22:00+02:00
Staffing remains HOLD/add-none. The CEO reran `/home/billy/bin/csup staff
--scenario=resume --dry-run`; queued work exists across configured hosts, but
every LUNARC/remote line held with `reason=lunarc_requires_slurm_station`, so no
`--apply` was run. Keep using only the current local `civic-content` panes.

Updated landed evidence:
- q072 is on `origin/main` via PR #194 / `6fc47b4`.
- q073 is on `origin/main` via PR #199 / `ee4ddb4`.
- q012 is on `origin/main` via PR #204 / `2c7c02f`.
- `origin/main` was `4f222ee` when this queue was updated after q012 and the
  CEO hold cycle; this does not change the P0 content priority.

Active duplicate guard:
- q012 and q073 are landed; close/delete duplicate q012/q073 branches or PRs
  and do not assign them again.
- q021 is active in `/tmp/sct-q021-pane3` with PR #203 open at observed pane
  state. Do not duplicate q021 until VALIDATOR resolves that evidence.

Next content order after q021: q013, q014, q015, q016, q017, q020, then q027
and q028 unless VALIDATOR has newer accepted evidence. Keep every atom
UHR-only, one question where possible, with preserved UHR locator, export
parity, `validate:content`, focused content tests, typecheck, ownership,
Prettier/diff checks, and a journal handoff naming the PR or merge evidence.

CEO->MANAGER-content correction 2026-05-18T13:27:00+02:00
q012 is now landed on `origin/main` via PR #204 / `2c7c02f`; do not assign or
merge any routing that treats q012 as still active. PR #206 is open and touches
this queue, but its q012 assignment is stale and must be rebased/reconciled
before merge. q021 remains isolated to PR #203 and is not mergeable at connector
check time; rebase/update that PR before acceptance. Next content order after
q021 is q013, q014, q015, q016, q017, q020, then q027/q028 unless VALIDATOR has
newer accepted evidence.

MANAGER-content 2026-05-18T13:06:39+02:00
q012 is now landed via PR #204 / `2c7c02f`; do not duplicate q012. Keep q021
isolated to PR #203. Route available local content panes to q013, q014, and
q015 next, then continue q016, q017, q020, q027, and q028 unless VALIDATOR
accepts newer evidence first.

MANAGER-content 2026-05-18T13:22:00+02:00
q013 landed via PR #209 / `782027c`; q014 landed via PR #211 / `ac5b2c6` and
the missing q014 journal artifact landed via PR #214 / `863ce49`. Keep q013
and q014 duplicate-guarded. Pane 1 owns q015 in `/tmp/sct-q015-pane1`; Pane 2
must abandon duplicate q015 work in `/tmp/sct-q015-pane2` and take q016 after
syncing; Pane 3 owns q021 via PR #203 and must rebase/update against current
`origin/main` before merge. Continue q017, q020, q027, and q028 after active
q015/q016/q021 unless VALIDATOR accepts newer evidence first.

MANAGER-content 2026-05-18T13:26:00+02:00
q015 landed via PR #218 / `e0e39a5`; q021 landed via PR #203 / `0bb6cc4`.
Pane 2 and Pane 4 are usage-limited and cannot take q016 right now. Leave q016
as the next content atom for the next available fresh content worker, followed
by q017, q020, q027, and q028 unless VALIDATOR accepts newer evidence first.

CEO->MANAGER-content 2026-05-18T13:32:00+02:00
Supersedes the 13:26 q016 staffing note. q017 landed via PR #222 / `fc19b58`;
q016 landed via PR #225 / `31d9828`, with merge evidence recorded on
`origin/main`. Duplicate q016 draft PR #230 is closed and its remote branch is
deleted. Do not route q020/q027/q028 automatically after q016. SITE-P0
deployed-site blockers are active in `codex-tasks/blockers.txt`, so escalate
fresh capacity to VALIDATOR for SITE lease/staffing before assigning any new
non-site content.

MANAGER-content validation hold 2026-05-18T15:01:10+02:00
Clean `origin/main` at `82291b1` / `1e4445e` fails
`npm run validate:content` before any CONTENT diff with upstream
`components/quiz/AnswerOption.tsx` accessibility parity errors: missing shared
Button import, explicit button role, feedback-aware visible label, and
tone-to-variant mapping. This is outside the CONTENT lease.

Hold CONTENT PR #295 / q142 and the in-flight q018 atom unless VALIDATOR
explicitly allows a content merge over the red base or the COMPONENTS/UIUX
gate is fixed. Do not duplicate q142/q018, do not start another
`data/additionalQuestions.ts` count-gate atom, and do not edit `components/**`
from CONTENT.

MANAGER-content release 2026-05-18T15:11:28+02:00
`origin/main` is now `e232bf7` after the DATA-INTEGRITY
`ANSWER-OPTION-OPTIONCARD-PARITY-1` fix, and
`NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` is green on current
main. Release the red-base hold, but do not duplicate existing work: Pane 4's
remote branch `task/content/q142-source-expansion-pane4-1779108610` remains the
canonical q142 path and must be rebased/verified against `e232bf7` before PR
merge. Other CONTENT panes should not start a second q142 or q018; fresh
capacity waits for q142 merge evidence or an explicit VALIDATOR/manager
assignment.

CEO->MANAGER-content 2026-05-18T15:52:00+02:00
q108 is on current `origin/main` via `d86717e`; do not duplicate it. The
static-bank sync, live-smoke count, live hash guard, Practice width, and q018
static-bank drift defects are already accepted, so CONTENT must not edit
`site/**` or rerun those atoms. Fresh capacity stays behind SITE-P0 deploy
evidence and static-site privacy/monetization copy unless VALIDATOR explicitly
leases new non-site content.

MANAGER-content 2026-05-18T17:07:33+02:00
q124 option-naturalness audit is landed via PR #398 / `ec0f0b3`. Route
`CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1` as a P0 TRANSLATE-COMPLETE
data/static-mirror cleanup with the same split ownership pattern as q123:
CONTENT owns only the authored q124 option wording in
`data/additionalQuestions.ts`, preserving the UHR `Traditioner och högtider` /
`Sveriges nationaldag`, printed page 46 locator; DATA-INTEGRITY owns the
generated `site/questions.js` mirror and parity evidence. Do not open a
CONTENT-only q124 branch that leaves static-site parity red, and do not
duplicate q123/q124 from other panes.

CEO->MANAGER-content 2026-05-18T17:19:08+02:00
Keep local content panes on P0 evidence only. q123 cleanup is now on current
main via `f15404a`; do not duplicate it unless VALIDATOR rejects that evidence.
Do not duplicate q124 cleanup until VALIDATOR explicitly leases the split path:
CONTENT may own authored wording in `data/additionalQuestions.ts`, but
DATA-INTEGRITY must own generated `site/questions.js`, CSV/static parity,
validator/export evidence, and handoff. Generated true/false naturalness is
DATA-INTEGRITY-owned, not CONTENT hand editing. Fresh content audits are useful
only when they file bounded P0 defects with UHR locator evidence and do not
create data-only branches that leave the static site stale.

MANAGER-content 2026-05-18T18:00:55+02:00
Current `origin/main` includes manager-build acceptance of
`CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1` via PR #456 / `67847d4`, with
acceptance evidence recorded by `340e6f7`. Treat q124 as accepted and
duplicate-guarded: the authored q124 option and generated `site/questions.js`
mirror no longer contain the rejected `Midsommarafton började alltid firas på
en fredag` / `Midsummer Eve began always being celebrated on a Friday` wording,
and the UHR `Traditioner och högtider` / `Sveriges nationaldag`, printed page
46 locator is preserved. Do not open another q124 data or static-mirror branch.
Fresh CONTENT capacity should not implement generated-output fixes; the active
non-content implementation route is `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`
for DATA-INTEGRITY, followed only by bounded current-main P0 audits if they add
new evidence with UHR locators and no `site/**` or generated-template edits.

MANAGER-content 2026-05-18T18:22:23+02:00
Current `origin/main` is `673e87a`. q123 and q124 are accepted and
duplicate-guarded; do not reopen data/static branches for either unless
VALIDATOR rejects the accepted evidence. CONTENT still has no safe standalone
authored-content implementation atom: remaining learner-visible defects are
generated/static output issues. Keep `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`
with DATA-INTEGRITY after the `204e522` partial repair, now including VERIFY
q251-q300 evidence from `961607f`, q301-q350 evidence from `5364104` /
`6fda9b8`, q351-q400 evidence from `73ef69b`, q451-q500 evidence from
`673e87a`, and current-main scan hits such as q206, q237, q238, q270, q297,
and q298 in
`content/question-bank.csv` and `site/questions.js`. Also route newly observed
generated unknown-option wording
`Det går inte att avgöra av materialet` / `It cannot be determined from the
material` to DATA-INTEGRITY as `REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1`:
the root is `UNKNOWN_OPTION` in `lib/content/derivedQuestions.ts` plus the
validator mirror in `scripts/validate-content.js`, and current static output
contains 154 copies. CONTENT must not edit `site/**`, generated CSV rows,
generator templates, or validator mirrors; fresh authored content stays behind
explicit VALIDATOR lease.

MANAGER-content 2026-05-18T19:05:00+02:00
Current `origin/main` is `332bfc9`, with q551-q600 residual audit evidence on
main via PR #505 / `7d77249`, q601-q650 evidence on main via PR #503 /
`95aa330`, q651-q700 evidence on main via PR #506 / `eb10dc5`, and q701-q720
evidence on main via PR #512 / `2d41d8f`. Treat q401-q720 residual evidence as
DATA-INTEGRITY-owned generated-template/static-mirror work, not as a CONTENT
implementation lease. CONTENT must not hand-edit generated rows, generator
templates, validator mirrors, regenerated CSV/static mirrors, or `site/**`.
Do not duplicate q401-q450, q501-q550, q551-q600, q601-q650, q651-q700, or
q701-q720 audit branches. No generated residual CONTENT audit slice remains;
CONTENT should capacity-handoff unless VALIDATOR explicitly leases authored
content. q123/q124 remain accepted and duplicate-guarded, q144 is now on main
via PR #513 / `38e4a45`, and generated unknown-material option wording remains
DATA-INTEGRITY-owned.

MANAGER-content 2026-05-18T19:20:00+02:00
Routing scan baseline was `origin/main` `c03f88f`; require a current-main
recheck before implementation. Route `CONTENT-AUTHORED-TF-PREFIX-1` from PR
#524 / `916ae1b` as the next authored-content cleanup only if VALIDATOR leases
the paired CONTENT/DATA-INTEGRITY path. CONTENT owns removing redundant
`Sant eller falskt:` / `True or false:` prefixes from authored source stems in
`data/questions.ts` (`q002`, `q006`) and `data/additionalQuestions.ts` (`q023`,
`q028`, `q031`, `q047`, `q049`, `q074`, `q091`, `q094`, `q143`) while
preserving facts, answer keys, UHR locators, and natural SV/EN wording.
Do not publish a data-only fix. The accepted atom must include generated
canonical/static mirror updates, an authored true_false prefix guard, export
and static parity, `validate:content`, typecheck, ownership, Prettier/diff
checks, PR/merge evidence, and a current-main recheck.

Generated true/false residuals and generated unknown-material option wording
remain DATA-INTEGRITY-owned. Current unknown-option evidence is refreshed by
PR #525 / `589fda3`: `site/questions.js` contains 155 copies, while authored
data and `content/question-bank.csv` contain no copies of the fallback option
text. CONTENT should otherwise capacity-handoff unless VALIDATOR explicitly
leases authored content.

CEO->MANAGER-content 2026-05-18T19:44:00+02:00
CONTENT remains handoff-only for generated residuals: do not hand-edit
generated rows, generator templates, validator mirrors, regenerated CSV/static
mirrors, or `site/**`. The open generated true/false residual route and
unknown-material option route are DATA-INTEGRITY-owned.

The only visible authored-content implementation candidate is
`CONTENT-AUTHORED-TF-PREFIX-1`, and only if VALIDATOR leases the paired
CONTENT/DATA-INTEGRITY path. CONTENT would own removal of redundant
`Sant eller falskt:` / `True or false:` prefixes from the named authored source
stems while preserving UHR locators and answer keys; DATA-INTEGRITY must own
regenerated canonical/static mirrors and guards. Otherwise, keep capacity
available for evidence/handoff and avoid new data-only branches.

MANAGER-content 2026-05-18T19:44:50+02:00
Current `origin/main` includes PR #555 / `98e7268` identifying the remaining
VERIFY gap and PR #565 / `f38907d` landing the q551-q600 maintained VERIFY
report. Supersede the stale "capacity-handoff unless leased" behavior for
available CONTENT capacity: the operator directive in `codex-tasks/open.txt`
says managers must not park panes while P0 work exists. The lease-safe P0 work
is CONTENT-VERIFY, not source implementation.

Lease order:
1. q551-q600 is landed via PR #565 / `f38907d`; do not duplicate the old
   `task/verify/q551-q600-pane3-1779126183` branch.
2. CONTENT-VERIFY Pane 1 owns `verify:data/questions.ts q601-q650` on branch
   `task/verify/q601-q650-pane1-1779126600`; do not duplicate it unless that
   branch is explicitly abandoned or rejected.
3. Next available CONTENT-VERIFY capacity takes `q651-q700` after q601-q650 is
   merged, rejected, or explicitly abandoned.

Scope for these VERIFY atoms: maintain `docs/verify/ledger.md`,
`docs/verify/qNNN-qNNN-2026-05-18.md`,
`docs/parallel-sessions/journals/verify.md`, and append to
`codex-tasks/data-integrity.txt` only for newly confirmed generated-output
defect routes. They must open/check the UHR citation groups, verify facts,
answer keys, distractors, and SV/EN naturalness, and run export/static parity,
`validate:content`, typecheck, ownership, and diff checks. They must not edit
`data/**`, `content/question-bank.csv`, `site/**`, generator templates, or
validator mirrors.

`CONTENT-AUTHORED-TF-PREFIX-1` remains a paired CONTENT/DATA-INTEGRITY cleanup
only if VALIDATOR explicitly leases the data/export/static/guard bundle. Do not
publish a data-only prefix branch.

MANAGER-content 2026-05-18T20:00:00+02:00
PR #570 / `225af56` has merged the DATA-INTEGRITY generated true/false
residual repair after the q601-q650 VERIFY merge. Treat any in-flight
q651-q700 VERIFY work that was started before #570 as stale until it is rebased
or restarted from current `origin/main`.

Current CONTENT-VERIFY direction:
1. q551-q600 and q601-q650 maintained VERIFY reports are landed via #565 and
   #567; do not duplicate them.
2. q651-q700 remains the next VERIFY slice only as a post-#570 current-main
   audit. Recheck the generated CSV/static mirror before writing ledger/report
   rows, and do not queue pre-#570 true/false residual defects as current.
3. If q651-q700 still has generated-output defects on current main, route only
   the remaining current defects to DATA-INTEGRITY. Unknown-material option
   wording remains DATA-INTEGRITY-owned until accepted separately.

CONTENT source implementation remains handoff-only unless VALIDATOR leases the
paired `CONTENT-AUTHORED-TF-PREFIX-1` data/export/static/guard cleanup. CONTENT
must not edit generated rows, generator templates, validator mirrors,
regenerated static mirrors, or `site/**`.

CEO->MANAGER-content 2026-05-18T20:58:20+02:00
Latest observed `origin/main` is `75310c8`. DATA-INTEGRITY #618 / `9f007d8`
partially cleared generated meta/negative wording, but current static output
still has 299 true/false prefix offenders and DATA-INTEGRITY owns the next
generated/static cleanup bundle.

CONTENT source implementation remains handoff-only unless VALIDATOR explicitly
leases the paired `CONTENT-AUTHORED-TF-PREFIX-1` cleanup. If leased, CONTENT
owns only the authored source rows already identified (`q002`, `q006`, `q023`,
`q028`, `q031`, `q047`, `q049`, `q074`, `q091`, `q094`, `q143`) while
preserving UHR locators, facts, answer keys, and natural SV/EN wording. Do not
publish a data-only branch and do not edit generated rows, generator templates,
validator mirrors, regenerated static mirrors, or `site/**`.

VERIFY may continue report-and-queue work only from the next manager/validator
approved current-main slice. Route confirmed generated-output defects to
DATA-INTEGRITY and avoid duplicating landed q151-q200 or q201-q250 evidence.

CEO->MANAGER-content 2026-05-18T21:47:00+02:00
Latest observed `origin/main` is `e1ae95a`. CONTENT source implementation
remains handoff-only unless VALIDATOR explicitly leases authored-content work.
Generated true/false prefix, false-explanation, and q301-q350 generated
wording cleanup are accepted by MANAGER-build; the narrowed q351-q400
standalone cleanup is DATA-INTEGRITY-owned.

VERIFY may continue only report-and-queue work from the next
manager/validator-approved current-main slice. Route confirmed generated-output
defects to DATA-INTEGRITY, and do not edit `data/**`, generated
`content/question-bank.csv`, `site/**`, generator templates, or validator
mirrors from CONTENT/VERIFY work.

CEO->MANAGER-content 2026-05-18T22:44:40+02:00
Current route correction after latest observed `origin/main` `58cb0f6`.
CONTENT should not implement q501-q550 generated fixes; that source cleanup is
already on main and awaits VALIDATOR/MANAGER-build acceptance. q551-q600,
q601-q650, and q651-q700 evidence is queued but stays behind q501-q550
acceptance unless VALIDATOR explicitly reorders.

CONTENT may support SETUP on `REVIEWER-SITE-EBOOK-SOURCE-COVERAGE-1` only if
VALIDATOR leases wording/provenance help: review the ebook source-backed /
footnoted claims, keep provenance labels honest under `docs/content/wording-rules.md`,
and avoid source edits to generated rows, generator templates, validator
mirrors, regenerated static mirrors, or unrelated `site/**` surfaces. Otherwise
continue VERIFY/report-and-queue work only from current main.

CEO->MANAGER-content 2026-05-18T23:36:34+02:00
CONTENT remains VERIFY/report-and-queue only unless VALIDATOR explicitly leases
authored-content source work. Do not implement generated fixes from CONTENT:
q698 is accepted as partial DATA-INTEGRITY closure, q663/q670/q671 remain
DATA-INTEGRITY-owned, and the later generated filler-option cleanup is also
DATA-INTEGRITY-owned.

If local content capacity is available, use it only for current-main VERIFY or
precise report-and-queue work approved by the manager. Do not edit `data/**`,
generated `content/question-bank.csv`, `site/**`, generator templates, or
validator mirrors without an explicit lease.

CEO->MANAGER-content 2026-05-18T23:46:00+02:00
CONTENT remains VERIFY/report-and-queue only unless VALIDATOR explicitly
leases authored-content source work. Do not implement generated fixes from
CONTENT: q698 is accepted, q663/q670/q671 remain DATA-INTEGRITY-owned, and the
later generated single-choice filler cleanup is DATA-INTEGRITY-owned. If local
capacity is available, use it only for current-main VERIFY or precise
report-and-queue work approved by the manager, with no edits to `data/**`,
generated CSV/static mirrors, generator templates, validator mirrors, or
`site/**` without an explicit lease.
