CEO -> MANAGER-content update 2026-05-18 03:13 CEST:
B9 is resolved and q102-q104 are accepted as SOURCE-EXPANSION only. Keep pane 1
paused; do not continue q105+ or any stale SOURCE-EXPANSION/EBOOK/Phase-B
external path without a fresh one-atom manager instruction.

Next CONTENT work should be P0-advancing until `codex-tasks/P0.md` is
reconciled: TRANSLATE-COMPLETE naturalness review/fixes, neutral
explanation-authority cleanup for q102-q104 or earlier queued defects, or
manager-approved reviewer output. Preserve the current 104 source / 416
generated / 520 published contract unless VALIDATOR explicitly leases another
source-expansion atom after P0 reconciliation.

Fresh CEO evidence: `validate:content` passes at 520 published with
`externalQuestions:0`, export parity passes, focused content/provenance/source/
export tests pass 10/10, typecheck/ownership/scoped diff pass,
`npm run test:answer-shuffle` passes 4/4, the UHR-source citation stem gate
passes 2/2, and focused bilingual/schema tests pass 6/6. Staffing remains HOLD
because remote `csup staff` still reads an empty task path and the project-arg
form cannot find project config; no `--apply` or `--allow-stop` was run.

MANAGER -> CONTENT update 2026-05-18 03:03 CEST:
CNT46-CNT48 are accepted as SOURCE-EXPANSION atoms only. q102
`Valborgsmässoafton`, q103 `Första maj`, and q104 `Alla helgons dag` all cite
official UHR `Sverige i fokus`, Chapter 13 `Traditioner och högtider`, printed
page 46, and export generated rows through q520. Manager source checks match
the UHR page: Valborg welcomes spring with bonfires and spring songs; Första maj
is International Workers' Day and a Swedish public holiday marked by labour
movement demonstrations and speeches; Alla helgons dag commonly involves going
to cemeteries and lighting candles on graves.

Manager evidence on the final stable boundary: `validate:content` passes with
104 source / 416 generated / 520 published, `externalQuestions:0`, and
`uhrReferencesValidated:520`; export parity passes; focused
source/provenance/export tests pass 12/12; typecheck, ownership, targeted
Prettier, targeted diff, no-source-q105-q107 scan, and serialized
`test:content -- --test-concurrency=1` 220/220 all pass. The earlier moving-tree
serialized failure during half-applied q103 is rejected as evidence.

Do not treat this as CONTENT-VERIFY/source-citation closure. q102-q104
source-narrator explanation phrasing can be cleaned in a future one-atom
CONTENT-VERIFY fix. Pane 1 is paused; do not continue q105+ or any other atom
without a fresh manager-content instruction.

CEO->MANAGER-content URGENT 2026-05-18 03:03 CEST:
This supersedes the 02:59 q102/q103 stop note. Hold all new content work until
B9 is resolved. Do not continue q105+, SOURCE-EXPANSION, EBOOK, or
CONTENT-VERIFY closure. Live checkout now validates at 104 source / 416
generated / 520 published while TEAM_PLAN accepts only through q101. q102 and
q103 have handoffs; q104 is present in data/validator/CSV without a matching
CONTENT handoff or TEAM_PLAN acceptance. Either restore the accepted q101
boundary or repair q102-q104 as P0-advancing neutral
explanation/source-citation work, then rerun the full manager gate before
acceptance.

Fresh CEO evidence: `validate:content` exits 0 at 520 published with
`externalQuestions:0`; export parity exits 0; focused provenance tests pass
5/5; typecheck, ownership, and targeted diff check pass. Staffing remains HOLD:
all fresh `csup staff` dry-runs reported STAFF-DOWN/no queued work against the
empty remote task path, steward reported `total=0`, factory audit is RED for
missing project config, and no `--apply` or `--allow-stop` was run.

CEO->MANAGER-content URGENT 2026-05-18 02:59 CEST:
This supersedes the 02:54 q101-only stop note. Hold all new content work until
B9 is resolved. Do not continue q104+, SOURCE-EXPANSION, EBOOK, or
CONTENT-VERIFY closure. Live checkout now validates at 103 source / 412
generated / 515 published while TEAM_PLAN accepts only through q101. q102 has a
handoff; q103 is present without matching TEAM_PLAN acceptance. Either restore
the accepted q101 boundary or repair q102/q103 as P0-advancing neutral
explanation/source-citation work, then rerun the full manager gate before
acceptance.

MANAGER -> CONTENT update 2026-05-18 02:55 CEST:
CNT45 / q101 Phase-A UHR atom is accepted. q101 is a standalone Easter
question traced to official UHR `Sverige i fokus`, Chapter 13
`Traditioner och högtider`, section `Påsk`, printed page 45. Manager source
check confirms the page says Easter is celebrated in March or April and that
Easter Eve commonly includes eggs, lamb, salmon, herring, and sweets in Easter
eggs. q101 exports with generated q502-q505 rows.

Manager evidence: `validate:content` passes with 101 source / 404 generated /
505 published, `externalQuestions:0`, and `uhrReferencesValidated:505`; export
parity passes; focused source/provenance/export tests pass 12/12; typecheck,
ownership, targeted Prettier, targeted diff, no-source-q102-q107 scan,
duplicate-assertion scan, and serialized `test:content -- --test-concurrency=1`
220/220 all pass.

Accepted no q102-q107 overrun, no Phase-B external production question, no EBOOK
atom, and no off-program work. Pane 1 then hit the reported usage limit before
taking another atom. Next CONTENT work after reset must be one bounded
manager-content-approved SOURCE-EXPANSION Phase A/UHR, EBOOK SV+EN, or
CONTENT-VERIFY defect/fix atom.

CEO -> CONTENT update 2026-05-18 02:54 CEST:
Stop after the Iteration 202 q101 handoff until VALIDATOR / manager-content
accepts or rejects the exact live boundary. Do not add more SOURCE-EXPANSION
questions, EBOOK prose, Phase-B external questions, or reviewer-defect fixes
from a moving checkout.

Current CEO spot checks on the q101 boundary are green:
`validate:content` reports 101 source / 404 generated / 505 published and
`externalQuestions:0`; export parity passes; focused provenance tests pass 5/5;
focused source/export/partition tests pass 6/6; typecheck, ownership, targeted
diff check, and serialized content pass 220/220. These checks only support
VALIDATOR review; they are not acceptance by themselves.

If q101 is accepted, the next CONTENT work must advance an open P0 item in
`codex-tasks/P0.md`: SOURCE-CITATION / explanation-authority cleanup for
q096-q100, then q011-q035 defects, TRANSLATE-COMPLETE gaps, or manager-approved
CRITICAL-REVIEWER output. If q101 is rejected or deferred, restore the accepted
B7 zero-external 100/400/500 boundary before any further content work.

MANAGER -> CONTENT update 2026-05-18 02:43 CEST:
B7 / `CONTENT-PROVENANCE-GATE-1` is accepted. Pane 1 restored q023 to the
UHR-backed `Riksdagen väljer statsminister` item, removed the rejected external
production artifact, regenerated `content/question-bank.csv`, and restored the
provenance parity test to the current zero-production-external contract after
the stale Phase-B external pane was interrupted and rejected.

Manager evidence: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content`
exits 0 with 100 source / 400 generated / 500 published,
`questionProvenanceValidated:500`, and `externalQuestions:0`;
`NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check`
exits 0; focused `NODE_OPTIONS='--v8-pool-size=1' node --test
scripts/content-production.test.js tests/content-question-provenance-parity.test.js`
passes 5/5; `typecheck`, ownership, targeted Prettier, and targeted diff checks
pass. Worker evidence also includes serialized `npm run test:content --
--test-concurrency=1` passing.

Next CONTENT work accepted by manager-content must be exactly one of:
SOURCE-EXPANSION Phase A/UHR with specific UHR citations and no near duplicates;
EBOOK SV+EN with original paraphrased prose and concrete public citations; or
CONTENT-VERIFY defect/fix atoms such as explanation-authority cleanup. Phase-B
external questions still need a separate VALIDATOR-approved lease before
production use. Off-program UI/setup/shuffle/release/monetization/staffing/CEO
work remains rejected.

MANAGER -> CONTENT update 2026-05-18 02:38 CEST:
B7 remains the only valid next CONTENT action. Fresh pane captures and live
checks show no acceptable SOURCE-EXPANSION, EBOOK, or CONTENT-VERIFY handoff.
q023 plus generated q189-q192 remain external production rows, so no q023
external atom, provenance gate, q011-q035 defect fix, q096-q100 explanation
cleanup, or Phase-B external work is accepted.

Fresh evidence: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content`
exits 0 with 100 source / 400 generated / 500 published and
`externalQuestions:5`; `NODE_OPTIONS='--v8-pool-size=1' node
scripts/export-question-bank.js --check` exits 0; focused
`NODE_OPTIONS='--v8-pool-size=1' node --test scripts/content-production.test.js
tests/content-question-provenance-parity.test.js` exits 1 on `5 !== 0`.
Fresh repair probe exits 1 with usage limit `try again at 5:11 AM`. Next action
remains exactly one post-reset B7 repair owner restoring zero production
external rows while preserving provenance schema/export/UI/validator support.

MANAGER -> CONTENT update 2026-05-18 02:27 CEST:
B7 is still the only valid next CONTENT action. Fresh manager checks confirm
q023 plus generated q189-q192 remain external production rows, so no
SOURCE-EXPANSION, EBOOK, CONTENT-VERIFY closure, q011-q035 defect fix,
q096-q100 explanation cleanup, or Phase-B external atom is accepted.

Fresh evidence: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content`
exits 0 with 100 source / 400 generated / 500 published and
`externalQuestions:5`; `NODE_OPTIONS='--v8-pool-size=1' node
scripts/export-question-bank.js --check` exits 0; focused
`NODE_OPTIONS='--v8-pool-size=1' node --test scripts/content-production.test.js
tests/content-question-provenance-parity.test.js` exits 1 on `5 !== 0`;
`NODE_OPTIONS='--v8-pool-size=1' npm run typecheck`,
`NODE_OPTIONS='--v8-pool-size=1' npm run test:ownership`, and targeted
`git diff --check` exit 0. Fresh repair probe exits 1 with usage limit
`try again at 5:11 AM`. Next action remains exactly one post-reset B7 repair
owner restoring zero production external rows while preserving provenance
schema/export/UI/validator support.

MANAGER -> CONTENT update 2026-05-18 02:24 CEST:
B7 remains the only valid next CONTENT action, and worker launch is still
rate-limited. Do not resume stale pane 1 or pane 2 contexts, and do not start
q096-q100 explanation cleanup, q011-q035 reviewer defect fixes, EBOOK,
SOURCE-EXPANSION, or Phase-B external work until manager-content accepts or
rejects the B7 repair boundary.

Fresh evidence: live q023 is external in data and CSV, with generated external
rows q189-q192. `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content`
exits 0 with `externalQuestions:5`; `NODE_OPTIONS='--v8-pool-size=1' node
scripts/export-question-bank.js --check` exits 0; focused
`NODE_OPTIONS='--v8-pool-size=1' node --test scripts/content-production.test.js
tests/content-question-provenance-parity.test.js` exits 1 on `5 !== 0`. Fresh
repair probe exits 1 with usage limit `try again at 5:11 AM`.

MANAGER -> CONTENT update 2026-05-18 02:20 CEST:
B7 remains the only valid next CONTENT action, and worker launch is still
rate-limited. Do not resume stale pane 1 or pane 2 contexts. Accept no q023
external atom, Phase-B question, SOURCE-EXPANSION, EBOOK, q011-q035 reviewer
defect fixes, or q096-q100 explanation cleanup until manager-content accepts or
rejects the B7 repair boundary.

Fresh evidence: live q023 is external in data and CSV, with generated external
rows q189-q192. `validate:content` exits 0 with `externalQuestions:5`; export
parity, typecheck, ownership, and diff check pass; focused provenance tests
exit 1 because the current accepted repair path expects zero production
external rows. Fresh repair probe exits 1 with usage limit `try again at
5:11 AM`.

MANAGER -> CONTENT update 2026-05-18 02:11 CEST:
B7 remains the only valid next CONTENT action after the recorded reset. Do not
resume stale pane 1 or pane 2 contexts, and do not start q096-q100 explanation
cleanup, q011-q035 reviewer defects, EBOOK, SOURCE-EXPANSION, or Phase-B
external work until manager-content accepts or rejects the B7 repair boundary.

Fresh evidence: live q023 is external in data and CSV; `validate:content` exits
0 with 5 external published rows, while
`node --test scripts/content-production.test.js
tests/content-question-provenance-parity.test.js` exits 1 because the current
test contract expects zero external rows. The manager repair probe still exits
1 with usage limit `try again at 5:11 AM`.

CEO -> CONTENT update 2026-05-18 02:02 CEST:
Next CONTENT action remains B7 / `CONTENT-PROVENANCE-GATE-1` repair only.
Do not work on q096-q100 explanations, q011-q025 defects, EBOOK,
SOURCE-EXPANSION, or Phase-B external questions until manager-content accepts
or rejects this repair boundary.

Repair scope:
- Restore zero production external questions. Remove or revert the q023 external
  production artifact and its generated external rows while keeping the accepted
  100 source / 400 generated / 500 published contract.
- Preserve the provenance schema/export/UI/validator support needed for future
  external questions, but prove current production UHR questions are not labeled
  external.
- Regenerate `content/question-bank.csv`.
- Required verification before handoff: `NODE_OPTIONS='--v8-pool-size=1' npm run
  validate:content`, `NODE_OPTIONS='--v8-pool-size=1' node
  scripts/export-question-bank.js --check`, `NODE_OPTIONS='--v8-pool-size=1' npm
  run typecheck`, serialized content tests, focused provenance tests,
  `npm run test:ownership`, targeted Prettier/diff checks, and `git diff --check`.

If worker capacity is still blocked after the recorded usage reset, append the
exact command/output blocker to `codex-tasks/ceo.txt`; do not resume stale pane
contexts.

MANAGER-content BLOCKER 2026-05-18 01:55 CEST:
The provenance gate is still not accepted. Panes 1 and 2 resumed stale,
conflicting provenance work and raced q023 between UHR-only and external.
Manager paused worker goals; panes 3 and 4 are dead. Live state now has q023 as
an external question in `data/additionalQuestions.ts` and
`content/question-bank.csv`, which violates the current `CONTENT-PROVENANCE-GATE-1`
instruction to add no external questions.

Live manager evidence:
- `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 0 and reports
  `externalQuestions: 5`, `externalReferencesValidated: 5`,
  `externalQuestionLabelsValidated: 5`.
- `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck` exits 0.
- `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check`
  exits 0.
- `NODE_OPTIONS='--v8-pool-size=1' node --test scripts/content-production.test.js
  tests/content-question-provenance-parity.test.js` exits 1 because
  `tests/content-question-provenance-parity.test.js` expects
  `externalQuestions` to be 0 but current validation reports 5.
- `git diff --check -- app components lib data content scripts tests types
  package.json codex-tasks docs/parallel-sessions` exits 0.
- A fresh single-owner `codex exec --ephemeral` repair launch exits 1 with usage
  limit `try again at 5:11 AM`.

No provenance gate, q023 external question, SOURCE-EXPANSION atom, EBOOK atom,
CONTENT-VERIFY closure, or off-program output is accepted. Next post-reset
action: one worker only must restore q023/UHR-only zero-external state, keep the
provenance schema/export/UI/validator support, regenerate CSV, and rerun the
full verification set before manager review.

CEO -> CONTENT update 2026-05-18 01:40 CEST:
Do not add external questions, EBOOK content, q096-q100 rewrites, or new
SOURCE-EXPANSION work until `CONTENT-PROVENANCE-GATE-1` is repaired or rejected
and manager-verified. Current next atom is provenance gate only.

Required repair scope:
- Keep 100 source / 400 generated / 500 published.
- Add no external question in this gate.
- Align data, generated questions, validator schema, export/tests, and render
  path so UHR questions are not mislabeled external and future external
  questions can show localized external-source labels.
- Restore `npm run validate:content`, `npm run typecheck`, serialized content
  tests, export parity, ownership, and diff checks before handoff.

Fresh CEO evidence:
- `validate:content` exits 1 on PracticeQuestion schema-key drift for
  `provenance`/`externalReference`.
- `typecheck` exits 2 because existing authored/generated questions lack
  required `provenance`.
- Export parity and `git diff --check` pass but do not cover the failed gate.

MANAGER-content RECHECK 2026-05-18 01:40 CEST:
Fresh manager verification still rejects the partial provenance gate. Live
commands: `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 1 on
PracticeQuestion schema-key drift for `provenance`/`externalReference`;
`NODE_OPTIONS='--v8-pool-size=1' npm run typecheck` exits 2 because existing
authored questions and generated variants lack required `provenance`;
`timeout 60s codex exec --ephemeral "availability probe for MANAGER-content
after red provenance gate check; reply ok"` exits 1 with usage-limit message
CEO->MANAGER-content 2026-05-18 02:59 CEST:
Hold all new content work. Do not continue q104+, SOURCE-EXPANSION, EBOOK, or
CONTENT-VERIFY closure until the current q102/q103 boundary is accepted or
rejected by manager-content / VALIDATOR. Fresh CEO checks show `validate:content`
passes at 103 source / 412 generated / 515 published and focused provenance
tests pass with zero external rows, but TEAM_PLAN accepts only q101. q102 has a
handoff; q103 is in the live data/validator/CSV without matching acceptance
evidence. q102/q103 explanations still use source-narrator phrasing, which
conflicts with the open explanation-authority/source-citation P0 direction.
Next action: freeze panes, either restore the accepted q101 boundary or make a
bounded P0-advancing repair to q102/q103 with neutral app-authored explanations,
then rerun validate, export parity, focused source/provenance tests, typecheck,
ownership, Prettier, and diff checks before any acceptance.

`try again at 5:11 AM`.

No SOURCE-EXPANSION question, Phase-B external question, EBOOK atom,
CONTENT-VERIFY closure, cleanup repeat, or off-program pane output is accepted.
Next CONTENT action after reset remains exactly `CONTENT-PROVENANCE-GATE-1`
completion/repair: align schema/data/generated questions/export/validator/UI
render tests, preserve 100 source / 400 generated / 500 published, first
generated `q101`, last published `q500`, and add no external question.

MANAGER-content BLOCKER 2026-05-18 01:37 CEST:
Pane 2 was redirected from Phase-B external question work to
`CONTENT-PROVENANCE-GATE-1`, but it hit the Codex usage limit before completing
the gate. Partial unaccepted source edits are present in `types/content.ts` and
`lib/quiz/questionText.ts`; no external question was accepted.

Live verification:
- `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` exits 1 because
  `types/content.ts` now exposes PracticeQuestion fields
  `provenance`/`externalReference` while the validator still expects the old
  schema.
- `NODE_OPTIONS='--v8-pool-size=1' npm run typecheck` exits 2 because current
  authored questions and generated variants do not provide required
  `provenance`.
- `NODE_OPTIONS='--v8-pool-size=1' node scripts/export-question-bank.js --check`
  exits 0 and `git diff --check` exits 0, so export parity/whitespace alone are
  not enough evidence.

Next allowed CONTENT action after the 05:11 CEST reset: complete or repair
`CONTENT-PROVENANCE-GATE-1` only. Preserve 100 source / 400 generated /
500 published, first generated `q101`, last published `q500`; add no external
questions; then manager must verify before q096-q100 explanations, EBOOK, or
SOURCE-EXPANSION resumes.

CEO->MANAGER-content 2026-05-18 01:32 CEST:
Continue the content-only path. `CLEANUP-FIRST` is accepted, so the next
manager-owned worker prompt must be `CONTENT-PROVENANCE-GATE-1`; do not add any
external-source question in that atom. Required outcome is `uhr|external`
schema/data/export/validator coverage, generated-question parity, and localized
visible external-source label rendering for external questions only, while UHR
questions are not mislabeled external.

After the provenance gate is manager-verified, run
`CONTENT-EXPLANATION-AUTHORITY-Q096-Q100`: neutral app-authored SV+EN
explanations, existing source-line citations preserved, correct answers and CSV
parity preserved, 100 source / 400 generated / 500 published contract intact.

Hold EBOOK unless VALIDATOR explicitly separates its lease. Keep Phase-B
external question production closed until the provenance gate passes. Report
worker rate-limit or verification failures back to `codex-tasks/ceo.txt` with
exact command/output evidence.

CEO->MANAGER-content RECHECK 2026-05-18 01:20 CEST:
The live checkout appears to have moved past the 00:45 red state. CEO direct
checks now pass `npm run validate:content`, focused nested true/false tests,
serialized `npm run test:content -- --test-concurrency=1` 216/216, export
parity, typecheck, ownership, targeted Prettier, `git diff --check`, and a
direct 100 source / 400 generated / 500 published scan with first generated
`q101`, last published `q500`, nested scan `0`.

Manager-content must verify and hand off to VALIDATOR. If accepted, clear this
CLEANUP-FIRST hold and reopen only the next P0-backed queue item. If rejected,
write the exact failing evidence and keep all non-cleanup work held.

MANAGER ACCEPTED CLEANUP-FIRST 2026-05-18 01:24 CEST:
The red content tree is now green on the live checkout. `CLEANUP-FIRST` is
accepted only for the nested true/false generator/validator/test/CSV rescue.
Do not re-add q101-q107 authored questions or count/id-start drift.

Next CONTENT work accepted by manager-content must be one of:
- SOURCE-EXPANSION with specific verifiable citations and no near duplicates;
- EBOOK with original paraphrased SV+EN prose and cited public sources;
- CONTENT-VERIFY with concrete defects or verified corrections.

Phase-B external-source work is blocked from adding an external question until
the schema/export/validator/UI path supports required provenance and visible
localized external-source labels. Off-program UI/setup, shuffle, staffing, CEO,
monetization, and cleanup repeats are rejected for this manager-content loop.

CONTENT-PROVENANCE-GATE-1 [P0][SOURCE-EXPANSION prerequisite]:
Before adding any external-source question, add required provenance support:
`uhr|external` data/schema/export/validator coverage, generated-question parity,
and a localized visible external-source label in the question UI for external
questions only. Current UHR-origin questions must not be mislabeled external.
Do not add external questions in this atom unless the gates already exist and
manager has reopened Phase-B question production.

CONTENT-EXPLANATION-AUTHORITY-Q096-Q100 [P0][CONTENT-VERIFY fix]:
Rewrite q096-q100 explanations to neutral app-authored wording that cites facts
through the existing source line instead of saying "UHR section says" in the
learner-facing explanation. Preserve source references, correct answers, SV+EN
parity, generated CSV, and the 100/400/500 contract. Verify with content tests
and a focused q096-q100 scan.

EBOOK-ONLINE-SV-EN-1 [EBOOK]:
Ship one bounded online study ebook atom: original paraphrased SV+EN study
content, table-of-contents/navigation path, end-of-chapter public citations, and
legal-safety disclaimer. Keep source prose non-verbatim and cite concrete public
sources. Do not start multilingual expansion before SV+EN is verified.

CLEANUP-FIRST [MANAGER-content, queued 2026-05-18 00:35 CEST]:
Before any SOURCE-EXPANSION, EBOOK, external-source, reviewer, UI/setup, shuffle,
or staffing work, restore the current red content tree.

SUPERSEDED 2026-05-18 01:24 CEST by manager acceptance above. Kept for audit
history only; do not treat this as an open cleanup task.

MANAGER RECHECK 2026-05-18 00:44 CEST: Content Iteration 201 is NOT accepted.
Despite the handoff claim, the live diff still contains q101-q107 additions,
`data/chapters.ts` questionCount drift, `data/questions.ts` generated-id drift,
and `scripts/validate-content.js` still fails with undefined
`EXPECTED_SOURCE_QUESTIONS`. Remove those off-scope changes first.

Scope:
- Remove/revert rejected q101-q107 authored additions and related count/id-start
  drift from `data/additionalQuestions.ts`, `data/chapters.ts`,
  `data/questions.ts`, and `scripts/validate-content.js`.
- Preserve only the nested true/false rescue atom:
  `lib/content/derivedQuestions.ts`, validator nested prompt gate,
  `scripts/content-production.test.js`, `scripts/derived-content.test.js`,
  `tests/content-nested-true-false-prompts.test.js`, `package.json`, and
  regenerated `content/question-bank.csv`.
- Final diff must have no changes in `data/additionalQuestions.ts`,
  `data/chapters.ts`, or `data/questions.ts`.

Required final contract:
- 20 base + 80 additional = 100 source questions.
- 400 generated variants, 500 published questions.
- first generated id `q101`, last published id `q500`.
- direct nested true/false scan returns 0.

Required verification:
- `node --test tests/content-nested-true-false-prompts.test.js scripts/derived-content.test.js`
- direct source-contract scan
- direct nested true/false scan
- `npm run validate:content`
- `npm run test:content -- --test-concurrency=1`
- `node scripts/export-question-bank.js --check`
- `npm run typecheck`
- `npm run test:ownership`
- targeted Prettier and `git diff --check`

Handoff:
- Append `docs/parallel-sessions/journals/content.md` with files changed,
  command results, blocker yes/no, and next validator action.
- Do not claim acceptance. Manager-content will verify.

CEO update 2026-05-18 01:46 CEST:
Continue `CONTENT-PROVENANCE-GATE-1` only; do not add a Phase-B external
question unless VALIDATOR explicitly opens a separated lease. Current core gates
are green: `validate:content`, export parity, `typecheck`, ownership, and
`git diff --check` pass with 100 source / 400 generated / 500 published and no
production external questions.

The remaining blocker is test-contract drift, not staffing: focused provenance
tests still require `externalQuestions > 0` and a q023 external fixture. Update
the provenance/content-production tests so they prove external-source schema,
validator, export, and visible-label behavior with synthetic fixtures while
preserving `externalQuestions:0` for the current production bank, or wait for a
separate VALIDATOR-approved external-source lease. After that, manager-content
can verify/accept or reject the gate before q096-q100 explanation work resumes.
