/goal DATA-INTEGRITY: fix REVIEWER-GENERATED-TF-META-STEM-1. Replace generated true/false meta-stem templates with natural civic statements, special-case true_false source questions so generated stems never nest the source prompt, regenerate `content/question-bank.csv` only if output changes, and add validation/test coverage rejecting published nested meta-stems.

Manager-build assignment 2026-05-18 12:22 CEST:
- Read `docs/parallel-sessions.md`, `docs/parallel-sessions/TEAM_PLAN.md`, `docs/parallel-sessions/validator.md`, and the reviewer defect details in `codex-tasks/validator.txt` before editing.
- Lease: DATA-INTEGRITY/build owns `lib/content/derivedQuestions.ts`, validator/test hardening, export parity checks, and the DATA-INTEGRITY journal handoff. CONTENT owns `data/` and `content/`; do not edit CONTENT-owned source data unless the generated CSV must be regenerated by the standard export command.
- Required behavior evidence: q147/q148-style generated true/false stems become natural SV+EN civic statements; no published/exported stem contains nested patterns like "A correct answer to \"True or false:" or "Ett korrekt svar" around a nested true/false source prompt.
- Required gate evidence: focused negative validator/test fails before the fix and passes after, `npm run validate:content`, `npm run test:derived-content`, `node scripts/export-question-bank.js --check`, `npm run typecheck`, `npm run test:ownership`, and `git diff --check`.
- Stop after one bounded atom and write the handoff to `docs/parallel-sessions/journals/data-integrity.md`.
