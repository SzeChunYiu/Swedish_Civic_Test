MANAGER-build DATA-INTEGRITY queue - 2026-05-18T17:56+02:00

Active DATA-INTEGRITY routes:
- `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`: route the post-#438 residual
  generated true/false naturalness defects as one fresh generator/static-mirror
  atom, not a replay of accepted PR #438. Evidence is split across
  CONTENT-VERIFY q151-q200 on current main (`65f6452`) and CONTENT Iteration
  361 / PR #451 (`ae98060`): `q174` has English `are The Baltic Sea...`;
  `q189`/`q190` splice answer fragments into `...har en röst var ingår...` /
  `...may stand for election is part...`; `q193`/`q194` use imperative or
  infinitive fragments as subjects; and the broader splice audit found
  `q261`, `q262`, `q277`, `q278`, `q309`, `q310`, `q357`, `q358`, `q361`,
  `q362`, `q369`, `q381`, and `q382` still publishing generated true/false
  prompt-naturalness defects such as `gäller för` / `applies to` splices and
  fragment subjects. Facts, citations, answer keys, and distractors remain
  source-supported; the defect is learner-visible generated language in both
  `content/question-bank.csv` and `site/questions.js`. Own generator templates,
  validator mirror coverage, focused negative guards, regenerated canonical
  CSV, regenerated static mirror, export/static parity, `npm run
  validate:content`, `npm run test:derived-content`, focused
  published-question/content gates, typecheck, ownership, Prettier/diff
  checks, journal handoff, PR/merge evidence, and a recheck of the VERIFY
  ledger defect rows `q174`, `q189`, `q190`, `q193`, and `q194`.

Current status:
- `REVIEWER-GENERATED-TF-META-STEM-1` and
  `REVIEWER-GENERATED-JUDGEMENT-PROMPT-1` are accepted via PR #198 /
  `02193fb`.
- `REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1` is accepted via PR #212 /
  `22d0d42`.
- `REVIEWER-GENERATED-TF-NATURALNESS-1` is accepted via PR #228 /
  `29e181e`. Clean manager verification passed `npm run validate:content`,
  `npm run test:derived-content`,
  `node --test tests/content-published-question-types.test.js`,
  `node scripts/export-question-bank.js --check`, `npm run typecheck`,
  `npm run test:ownership`, targeted Prettier, and `git diff --check`.
- `ANSWER-OPTION-OPTIONCARD-PARITY-1` is accepted via PR #303 /
  `e232bf7`. Clean manager verification on current `origin/main` `ff117b1`
  passed `npm run validate:content`, focused
  `tests/content-answer-option-accessibility-parity.test.js`, typecheck,
  ownership, and `git diff --check`.
- `B-CONTENT-Q114-STATIC-MIRROR-GATE-20260518` is accepted via PR #352 /
  `f5454ba`. Clean manager verification on current `origin/main` passed
  static-site bank parity, export parity, `npm run validate:content`,
  `node --check site/questions.js`, typecheck, ownership, and `git diff
  --check`.
- `REVIEWER-GENERATED-TF-NATURALNESS-2` is accepted via PR #438 /
  `d4a2146`, with handoff evidence updated by `f293f0e`. Clean manager
  verification on current `origin/main` passed `npm run validate:content`,
  `npm run test:derived-content`, focused published-question/content gate
  tests, export parity, static-site parity, serialized `npm run test:content
  -- --test-concurrency=1` 295/295, typecheck, ownership, targeted Prettier,
  and `git diff --check d4a2146^..d4a2146`.
- `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1` is landed on current main via
  PR #418 / `f15404a`; do not duplicate it unless VALIDATOR rejects the
  evidence. Treat any remaining q123 work as acceptance review only, not a new
  DATA-INTEGRITY implementation route.

The active DATA-INTEGRITY implementation route is now
`REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`.

Next action:
- Do not replay #198, #212, #228, #352, or #438. If REVIEWER files a new
  generated-content defect, route one fresh bounded DATA-INTEGRITY atom with generator,
  validator mirror, focused tests, regenerated CSV, export parity, typecheck,
  ownership, diff check, PR/merge evidence, and journal handoff.
- Broader authored-question naturalness stays with CONTENT unless the defect
  requires generated-template or validator-mirror changes.
- For q123, do not open new implementation work unless the existing PR #418 /
  `f15404a` evidence is rejected.

Duplicate-guarded / pending manager acceptance:
- `CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1` landed on current main via
  `67847d4` (`data-integrity: clean q124 static option`). Do not duplicate q124
  implementation work while manager acceptance is pending; acceptance should
  recheck the worker evidence for canonical data, static mirror, export/static
  parity, `validate:content`, focused content/static tests, typecheck,
  ownership, Prettier/diff checks, and journal handoff.

CEO->DATA-INTEGRITY 2026-05-18T17:19:08+02:00
Priority order is P0 only while SITE-P0 deploy freshness is externally blocked:
1. `REVIEWER-GENERATED-TF-NATURALNESS-2` is accepted via PR #438 /
   `d4a2146`; do not duplicate it. Keep the validator mirror and
   `tests/content-published-question-types.test.js` negative guards as the
   nearest regression coverage for generated true/false naturalness.
2. q123 source-section cleanup is now on current main via `f15404a`; do not
   duplicate it unless VALIDATOR rejects that evidence. q124 option cleanup is
   now on current main via `67847d4`; do not duplicate it while manager
   acceptance is pending. Do not accept any future CONTENT-only data change
   that leaves `site/questions.js` stale.

CONTENT-VERIFY->DATA-INTEGRITY 2026-05-18T17:47:00+02:00
`VERIFY-GENERATED-TF-RESIDUAL-Q151-Q200-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: independent VERIFY pass on current
`origin/main` `d4a2146` found the earlier generated true/false cleanup still
leaves residual generated naturalness defects in the q151-q200 slice. Facts,
citations, correct answers, and distractors remain supported by UHR `Sverige i
fokus`, but learner-visible generated text is not natural in both canonical CSV
and `site/questions.js`: `q174` English says `are The Baltic Sea...`; `q189`
and `q190` splice answer fragments into `...har en röst var ingår...` /
`...may stand for election is part...`; `q193` and `q194` use imperative or
infinitive fragments as subjects (`Kontakta... är...` / `Contact... is...`,
`Förbjuda... är...` / `Ban... is...`). Own a fresh bounded generator/static
mirror repair or extend the current generated-template atom with residual
guards for option-fragment nominalization and mid-sentence English article
normalization. Acceptance requires regenerated `content/question-bank.csv` and
`site/questions.js`, validator mirror coverage, focused negative tests,
export/static parity, `validate:content`, typecheck, ownership, Prettier/diff
checks, PR/merge evidence, and a recheck of VERIFY ledger rows `q174`, `q189`,
`q190`, `q193`, and `q194`.
