/goal DATA-INTEGRITY: fix REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1. Stop generated `section-practice` single-choice variants from wrapping true/false source prompts with `Vilket svar stämmer bäst? Sant eller falskt:` / `Which answer best matches? True or false:`.

MANAGER-build assignment 2026-05-18 13:10 CEST:
- Read `docs/parallel-sessions.md`, `docs/parallel-sessions/TEAM_PLAN.md`, `docs/parallel-sessions/validator.md`, and the reviewer defect details in `codex-tasks/validator.txt` before editing.
- Context: PR #198 / `02193fb` is merged and MANAGER-build accepted the generated-stem root-cause atom for `REVIEWER-GENERATED-TF-META-STEM-1` and `REVIEWER-GENERATED-JUDGEMENT-PROMPT-1`; do not replay that work.
- Lease: DATA-INTEGRITY/build owns `lib/content/derivedQuestions.ts`, `scripts/validate-content.js`, focused generated-content tests, export parity checks, regenerated `content/question-bank.csv`, and the DATA-INTEGRITY journal handoff.
- Required behavior evidence: no published/exported generated prompt contains `Vilket svar stämmer bäst? Sant eller falskt:` or `Which answer best matches? True or false:`; q146/q162/q230-style rows become natural study prompts or the invalid derived variant is skipped by a deliberate generator rule.
- Guard against regressions from PR #198: no old nested true/false meta-stems and no `correct judgment of the statement` / `rätt bedömning av påståendet` scaffolding returns.
- Scope boundary: cover `REVIEWER-GENERATED-TF-NATURALNESS-1` in this same atom only if the fix remains one bounded generator/validator/CSV/test pass; otherwise leave the broader grammar-splice naturalness queue as the next DATA-INTEGRITY atom.
- Required gate evidence: focused negative guard rejects the current nested single-choice true/false scaffolding before the fix and passes after; `npm run validate:content`; `npm run test:derived-content`; `node --test tests/content-published-question-types.test.js`; `node scripts/export-question-bank.js --check`; `npm run typecheck`; `npm run test:ownership`; and `git diff --check`.
- Stop after one bounded atom and write the handoff to `docs/parallel-sessions/journals/data-integrity.md`.
