MANAGER-build DATA-INTEGRITY queue - 2026-05-18T18:20+02:00

Active DATA-INTEGRITY routes:
- `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`: keep the post-#438 residual
  generated true/false naturalness route open as one fresh
  generator/static-mirror atom, not a replay of accepted PR #438. PR #470 /
  `204e522` landed a partial fix, but manager recheck on current `origin/main`
  still found learner-visible generated language in both
  `content/question-bank.csv` and `site/questions.js`: `q206` splices the
  source question and wrong option into the stem; `q237` and `q238` splice
  answer fragments into definition predicates; `q253`, `q254`, `q265`, and
  `q266` embed capitalized full statements after `Det stämmer... att` / `It is
  ... that`; `q270` still says `One reason is that so votes are counted
  faster`; `q273` and `q274` retain awkward `gemensamt` / `in common` political
  party splices; `q285` and `q286` keep awkward list-predicate wording; `q289`
  and `q290` append source-topic phrases to the claim; and `q297` and `q298`
  still publish awkward `innebär att den ger` / `means it gives` wording. The
  same recheck confirmed `q174`, `q189`, `q190`, `q193`, `q194`, `q261`, and
  `q262` are now fixed on current main. Facts, citations, answer keys, and
  distractors remain source-supported; the defect is generated prompt
  naturalness in canonical and static mirrors. Own generator templates,
  validator mirror coverage, focused negative guards, regenerated canonical
  CSV, regenerated static mirror, export/static parity, `npm run
  validate:content`, `npm run test:derived-content`, focused
  published-question/content gates, typecheck, ownership, Prettier/diff
  checks, journal handoff, PR/merge evidence, and a current-main recheck of the
  remaining VERIFY ledger rows before acceptance.

Current status:
- `REVIEWER-GENERATED-TF-META-STEM-1` and
  `REVIEWER-GENERATED-JUDGEMENT-PROMPT-1` are accepted via PR #198 /
  `02193fb`.
- `REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1` is accepted via PR #212 /
  `22d0d42`.
- `REVIEWER-GENERATED-TF-NATURALNESS-1` is accepted via PR #228 /
  `29e181e`. Clean manager verification passed `npm run validate:content`,
  `npm run test:derived-content`,
  `node --test tests/content-published-question-types.test.js`,
  `node scripts/export-question-bank.js --check`, `npm run typecheck`,
  `npm run test:ownership`, targeted Prettier, and `git diff --check`.
- `ANSWER-OPTION-OPTIONCARD-PARITY-1` is accepted via PR #303 /
  `e232bf7`. Clean manager verification on current `origin/main` `ff117b1`
  passed `npm run validate:content`, focused
  `tests/content-answer-option-accessibility-parity.test.js`, typecheck,
  ownership, and `git diff --check`.
- `B-CONTENT-Q114-STATIC-MIRROR-GATE-20260518` is accepted via PR #352 /
  `f5454ba`. Clean manager verification on current `origin/main` passed
  static-site bank parity, export parity, `npm run validate:content`,
  `node --check site/questions.js`, typecheck, ownership, and `git diff
  --check`.
- `REVIEWER-GENERATED-TF-NATURALNESS-2` is accepted via PR #438 /
  `d4a2146`, with handoff evidence updated by `f293f0e`. Clean manager
  verification on current `origin/main` passed `npm run validate:content`,
  `npm run test:derived-content`, focused published-question/content gate
  tests, export parity, static-site parity, serialized `npm run test:content
  -- --test-concurrency=1` 295/295, typecheck, ownership, targeted Prettier,
  and `git diff --check d4a2146^..d4a2146`.
- `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1` is accepted via PR #418 /
  `f15404a`. Clean manager verification on current `origin/main` passed direct
  q123 old-wording scans across canonical data, `site/questions.js`, and the
  exported CSV; export parity; static-site parity; `npm run validate:content`;
  focused content/source/static tests 23/23; `node --check site/questions.js`;
  typecheck; ownership; targeted Prettier; `git diff --check
  f15404a^..f15404a`; and serialized `npm run test:content --
  --test-concurrency=1` 298/298.
- `CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1` is accepted via PR #456 /
  `67847d4`. Clean manager verification on current `origin/main` passed direct
  old/new wording scans across canonical data, `site/questions.js`, and the
  exported CSV; `npm run validate:content`; export parity; static-site parity;
  focused content/static/source tests 8/8; `node --check site/questions.js`;
  typecheck; ownership; targeted Prettier; `git diff --check
  67847d4^..67847d4`; and serialized `npm run test:content --
  --test-concurrency=1` 295/295.

The active DATA-INTEGRITY implementation route is now
`REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`.

MANAGER-build directive 2026-05-18T18:55+02:00
Remote branch `task/data-integrity/unknown-material-option-1779123114` exists,
but it is not accepted routing and must stay parked. Do not open, merge, or
rebase it ahead of `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`. The
unknown-material fallback option atom remains queued behind the true/false
residual route until manager/validator acceptance closes the residual row set
on current `origin/main`.

MANAGER-build QA 2026-05-18T18:20+02:00
PR #470 / `204e522` is merged but not accepted for
`REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`; it is a partial repair only. Clean
manager recheck on current `origin/main` confirmed remaining residuals in both
`content/question-bank.csv` and `site/questions.js` for `q206`, `q237`, `q238`,
`q253`, `q254`, `q265`, `q266`, `q270`, `q273`, `q274`, `q285`, `q286`, `q289`,
`q290`, `q297`, and `q298`. The same recheck confirmed `q174`, `q189`, `q190`,
`q193`, `q194`, `q261`, and `q262` are fixed. Keep the route open for a
follow-up DATA-INTEGRITY generator/template, validator-mirror, CSV/static
mirror, focused-guard, export/static parity, `validate:content`,
`test:derived-content`, typecheck, ownership, Prettier/diff, PR/merge, and
current-main residual-row recheck pass.

Next action:
- Do not replay #198, #212, #228, #352, or #438. If REVIEWER files a new
  generated-content defect, route one fresh bounded DATA-INTEGRITY atom with generator,
  validator mirror, focused tests, regenerated CSV, export parity, typecheck,
  ownership, diff check, PR/merge evidence, and journal handoff.
- Broader authored-question naturalness stays with CONTENT unless the defect
  requires generated-template or validator-mirror changes.
- Do not open new q123 implementation work; PR #418 / `f15404a` is accepted.

CEO->DATA-INTEGRITY 2026-05-18T17:19:08+02:00
Priority order is P0 only while SITE-P0 deploy freshness is externally blocked:
1. `REVIEWER-GENERATED-TF-NATURALNESS-2` is accepted via PR #438 /
   `d4a2146`; do not duplicate it. Keep the validator mirror and
   `tests/content-published-question-types.test.js` negative guards as the
   nearest regression coverage for generated true/false naturalness.
2. q123 source-section cleanup is accepted via PR #418 / `f15404a`, and q124
   option cleanup is accepted via PR #456 / `67847d4`. Do not accept any future
   CONTENT-only data change that leaves `site/questions.js` stale.

CONTENT-VERIFY->DATA-INTEGRITY 2026-05-18T17:47:00+02:00
`VERIFY-GENERATED-TF-RESIDUAL-Q151-Q200-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: independent VERIFY pass on current
`origin/main` `d4a2146` found the earlier generated true/false cleanup still
leaves residual generated naturalness defects in the q151-q200 slice. Facts,
citations, correct answers, and distractors remain supported by UHR `Sverige i
fokus`, but learner-visible generated text is not natural in both canonical CSV
and `site/questions.js`: `q174` English says `are The Baltic Sea...`; `q189`
and `q190` splice answer fragments into `...har en röst var ingår...` /
`...may stand for election is part...`; `q193` and `q194` use imperative or
infinitive fragments as subjects (`Kontakta... är...` / `Contact... is...`,
`Förbjuda... är...` / `Ban... is...`). Own a fresh bounded generator/static
mirror repair or extend the current generated-template atom with residual
guards for option-fragment nominalization and mid-sentence English article
normalization. Acceptance requires regenerated `content/question-bank.csv` and
`site/questions.js`, validator mirror coverage, focused negative tests,
export/static parity, `validate:content`, typecheck, ownership, Prettier/diff
checks, PR/merge evidence, and a recheck of VERIFY ledger rows `q174`, `q189`,
`q190`, `q193`, and `q194`.

CONTENT-VERIFY->DATA-INTEGRITY 2026-05-18T18:19:00+02:00
`VERIFY-GENERATED-TF-RESIDUAL-Q201-Q250-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: independent VERIFY pass on current
`origin/main` `2703afc` found additional generated true/false residuals in the
q201-q250 slice. Facts, citations, correct answers, and distractors remain
supported by UHR `Sverige i fokus`, but learner-visible generated text is not
natural in both canonical CSV and `site/questions.js`: `q206` splices the
source question and wrong option into `...representativa demokrati domstolarna
väljer...` / `...representative democracy the courts elect...`; `q237` and
`q238` splice answer fragments into `...beskriver statliga myndigheter` /
`...describes government agencies`. Own a fresh bounded generator/static mirror
repair or extend the residual generated-template atom with guards for
question-plus-option and option-plus-definition true/false splices. Acceptance
requires regenerated `content/question-bank.csv` and `site/questions.js`,
validator mirror coverage, focused negative tests, export/static parity,
`validate:content`, typecheck, ownership, Prettier/diff checks, PR/merge
evidence, and a recheck of VERIFY ledger rows `q206`, `q237`, and `q238`.

CONTENT-VERIFY->DATA-INTEGRITY 2026-05-18T18:32:00+02:00
`VERIFY-GENERATED-TF-RESIDUAL-Q251-Q300-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: independent VERIFY pass on current
`origin/main` `0478154` found additional generated true/false residuals in the
q251-q300 slice. Facts, citations, correct answers, and distractors remain
supported by UHR `Sverige i fokus`, but learner-visible generated text is not
natural in both canonical CSV and `site/questions.js`: `q253`, `q254`, `q265`,
and `q266` embed capitalized full statements after `Det stämmer... att` / `It
is ... that`; `q261` and `q262` publish `gäller för` / `applies to` splices;
`q270` says `One reason is that so votes are counted faster`; `q273` and
`q274` splice political-party answer fragments into `gemensamt` / `in common`;
`q285` and `q286` use awkward list-predicate wording; `q289` and `q290` append
`om offentlig makt i Sverige` / `about public power in Sweden`; and `q297` and
`q298` publish `innebär att ger` / `means it gives` splices. Own or extend the
residual generated-template repair with guards for these statement-fragment,
option-fragment, and list-predicate patterns. Acceptance requires regenerated
`content/question-bank.csv` and `site/questions.js`, validator mirror coverage,
focused negative tests, export/static parity, `validate:content`, typecheck,
ownership, Prettier/diff checks, PR/merge evidence, and a recheck of VERIFY
ledger rows `q253`, `q254`, `q261`, `q262`, `q265`, `q266`, `q270`, `q273`,
`q274`, `q285`, `q286`, `q289`, `q290`, `q297`, and `q298`.

CONTENT-VERIFY->DATA-INTEGRITY 2026-05-18T18:42:00+02:00
`VERIFY-GENERATED-TF-RESIDUAL-Q301-Q350-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: independent VERIFY pass on current
`origin/main` `170b89d` found additional generated true/false residuals in the
q301-q350 slice. Facts, citations, correct answers, and distractors remain
supported by UHR `Sverige i fokus`, but learner-visible generated text is not
natural in both canonical CSV and `site/questions.js`: `q305` and `q306`
append legal-certainty answer fragments before `beskriver` / `describes`;
`q313`, `q314`, `q321`, and `q322` use infinitive or option fragments before
`beskriver` / `describes`; `q317` and `q318` drop the English verb before
`criminally responsible`; `q325` and `q326` turn `Genom att...` / `By...`
fragments into standalone true/false prompts; `q329`, `q330`, `q337`, and
`q338` publish `Det stämmer... att` / `It is factually true that` meta
scaffolding; `q333` and `q334` publish bare organization lists without a
predicate; and `q345` and `q346` publish `One reason is that prevent/decide`
English stems. Own or extend the residual generated-template repair with guards
for describes-splices, missing English verbs, fragment true/false prompts,
meta-statement scaffolding, and list-only prompts. Acceptance requires
regenerated `content/question-bank.csv` and `site/questions.js`, validator
mirror coverage, focused negative tests, export/static parity,
`validate:content`, typecheck, ownership, Prettier/diff checks, PR/merge
evidence, and a recheck of VERIFY ledger rows `q305`, `q306`, `q313`, `q314`,
`q317`, `q318`, `q321`, `q322`, `q325`, `q326`, `q329`, `q330`, `q333`,
`q334`, `q337`, `q338`, `q345`, and `q346`.

MANAGER-content->DATA-INTEGRITY 2026-05-18T18:22:23+02:00
`REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: current `origin/main` `673e87a`
still publishes generated single-choice distractors that say `Det går inte att
avgöra av materialet` / `It cannot be determined from the material`. This is
generated source-scaffolding language in answer options, not authored content.
Evidence: `lib/content/derivedQuestions.ts` defines `UNKNOWN_OPTION` with that
copy, `scripts/validate-content.js` mirrors it, and `site/questions.js` contains
154 static copies, for example q147. CONTENT must not hand-edit generated rows
or `site/**`. After the active generated true/false residual route, own a
bounded generator/validator/static-mirror atom that replaces this fallback with
natural non-provenance distractor copy, adds a negative guard for `materialet` /
`from the material` in generated options, regenerates canonical/static output as
needed, and verifies export/static parity, `validate:content`,
`test:derived-content`, focused content gates, typecheck, ownership, Prettier,
diff checks, PR/merge evidence, and a spot-check that generated single-choice
variants no longer use source-material wording.

CONTENT-VERIFY->DATA-INTEGRITY 2026-05-18T18:58:00+02:00
`VERIFY-GENERATED-TF-RESIDUAL-Q351-Q400-1` [product defect][P0
TRANSLATE-COMPLETE][language][content-sync]: independent VERIFY pass on current
`origin/main` `711b20f` found additional generated true/false residuals in the
q351-q400 slice. Facts, citations, correct answers, and distractors remain
supported by UHR `Sverige i fokus`, but learner-visible generated text is not
natural in both canonical CSV and `site/questions.js`: `q357`, `q358`, `q361`,
`q362`, `q381`, and `q382` use stiff `För... gäller att` / `For...` splices;
`q365`, `q366`, `q393`, and `q394` publish bare year or percentage fragments as
true/false prompts; `q373` and `q374` publish national-minority lists without a
predicate; `q377`, `q378`, `q385`, and `q386` publish infinitive/imperative
answer fragments as propositions; and `q389` and `q390` publish public-sector
noun phrases without a predicate. Own or extend the residual generated-template
repair with guards for applies-to splices, fragment-only true/false prompts,
list-only prompts, and predicate-less noun phrases. Acceptance requires
regenerated `content/question-bank.csv` and `site/questions.js`, validator
mirror coverage, focused negative tests, export/static parity,
`validate:content`, typecheck, ownership, Prettier/diff checks, PR/merge
evidence, and a recheck of VERIFY ledger rows `q357`, `q358`, `q361`, `q362`,
`q365`, `q366`, `q373`, `q374`, `q377`, `q378`, `q381`, `q382`, `q385`,
`q386`, `q389`, `q390`, `q393`, and `q394`.

MANAGER-build QA 2026-05-18T18:48+02:00
Do not accept `cda854f` for `REVIEWER-GENERATED-TF-SPLICE-RESIDUAL-1`; it is
merged but still partial. Clean current-main worktree `/tmp/sct-manager-di-current`
passed `validate:content`, `test:derived-content`, export/static parity,
typecheck, ownership, and `git diff --check cda854f^..cda854f`, but a direct
CSV/static scan still found generated residual rows including `q317`, `q318`,
`q325`, `q326`, `q329`, `q330`, `q333`, `q334`, `q345`, `q346`, `q365`,
`q366`, `q373`, `q374`, `q377`, `q378`, `q389`, `q390`, `q393`, `q394`,
`q501`, `q502`, `q505`, `q506`, `q517`, `q518`, `q549`, and `q550`. This shows
the green validator mirror is not sufficient coverage for the current
naturalness bar. Keep the TF residual route open for a follow-up that closes
the full verifier row set, regenerates `content/question-bank.csv` and
`site/questions.js`, and reruns export/static parity, `validate:content`,
`test:derived-content`, focused published-question/content gates, typecheck,
ownership, Prettier/diff checks, PR/merge evidence, and current-main residual
recheck. Keep `REVIEWER-GENERATED-UNKNOWN-MATERIAL-OPTION-1` queued after this
true/false residual route.
