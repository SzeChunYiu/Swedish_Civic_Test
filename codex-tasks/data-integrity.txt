MANAGER-build DATA-INTEGRITY queue - 2026-05-18T15:07+02:00

Active stop-the-line atom:
- `ANSWER-OPTION-OPTIONCARD-PARITY-1`
  - Scope: `scripts/validate-content.js`,
    `tests/content-answer-option-accessibility-parity.test.js`, and
    `docs/parallel-sessions/journals/data-integrity.md`.
  - Problem: current `origin/main` `d42cb51` fails
    `NODE_OPTIONS='--v8-pool-size=1' npm run validate:content` after
    `components/quiz/AnswerOption.tsx` moved from the shared `Button` wrapper
    to `OptionCard`.
  - Required behavior: update the DATA-INTEGRITY validator/test mirror to the
    new `OptionCard` contract, preserving localized SV/EN labels, result-label
    accessibility, selected/disabled forwarding, and tone/state mapping; or
    reject the component change with concrete evidence.
  - Required gates: `npm run validate:content`, focused
    `node --test tests/content-answer-option-accessibility-parity.test.js`,
    `npm run typecheck -- --pretty false`, `npm run test:ownership`, and
    `git diff --check`.
  - Do not work on static account-scope, live deploy, or generated-content
    follow-ups until this red content gate is green.

Current status:
- `REVIEWER-GENERATED-TF-META-STEM-1` and
  `REVIEWER-GENERATED-JUDGEMENT-PROMPT-1` are accepted via PR #198 /
  `02193fb`.
- `REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1` is accepted via PR #212 /
  `22d0d42`.
- `REVIEWER-GENERATED-TF-NATURALNESS-1` is accepted via PR #228 /
  `29e181e`. Clean manager verification passed `npm run validate:content`,
  `npm run test:derived-content`,
  `node --test tests/content-published-question-types.test.js`,
  `node scripts/export-question-bank.js --check`, `npm run typecheck`,
  `npm run test:ownership`, targeted Prettier, and `git diff --check`.

No active DATA-INTEGRITY generated-prompt atom is queued right now.

Next action:
- Do not replay #198, #212, or #228. If REVIEWER files a new generated-content
  defect, route one fresh bounded DATA-INTEGRITY atom with generator,
  validator mirror, focused tests, regenerated CSV, export parity, typecheck,
  ownership, diff check, PR/merge evidence, and journal handoff.
- Broader authored-question naturalness stays with CONTENT unless the defect
  requires generated-template or validator-mirror changes.
