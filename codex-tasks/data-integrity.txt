MANAGER-build DATA-INTEGRITY queue - 2026-05-18T16:56+02:00

Active DATA-INTEGRITY route:
- `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1`: Manager-content audit PR
  #386 / `2010bc8` found q123 still has learner-visible source-section wording
  in both `data/additionalQuestions.ts` and generated `site/questions.js`:
  Swedish `avsnittet` and English `this section`. This is P0
  TRANSLATE-COMPLETE / SOURCE-CITATION naturalness work, but it must be handled
  as one explicitly leased data + static mirror atom so the canonical content,
  generated CSV, static mirror, validators, focused tests, typecheck,
  ownership, Prettier, diff checks, and PR/merge evidence all move together.
  CONTENT must not hand-edit `site/**` or publish a data-only q123 branch.

Current status:
- `REVIEWER-GENERATED-TF-META-STEM-1` and
  `REVIEWER-GENERATED-JUDGEMENT-PROMPT-1` are accepted via PR #198 /
  `02193fb`.
- `REVIEWER-GENERATED-SINGLE-CHOICE-TF-STEM-1` is accepted via PR #212 /
  `22d0d42`.
- `REVIEWER-GENERATED-TF-NATURALNESS-1` is accepted via PR #228 /
  `29e181e`. Clean manager verification passed `npm run validate:content`,
  `npm run test:derived-content`,
  `node --test tests/content-published-question-types.test.js`,
  `node scripts/export-question-bank.js --check`, `npm run typecheck`,
  `npm run test:ownership`, targeted Prettier, and `git diff --check`.
- `ANSWER-OPTION-OPTIONCARD-PARITY-1` is accepted via PR #303 /
  `e232bf7`. Clean manager verification on current `origin/main` `ff117b1`
  passed `npm run validate:content`, focused
  `tests/content-answer-option-accessibility-parity.test.js`, typecheck,
  ownership, and `git diff --check`.
- `B-CONTENT-Q114-STATIC-MIRROR-GATE-20260518` is accepted via PR #352 /
  `f5454ba`. Clean manager verification on current `origin/main` passed
  static-site bank parity, export parity, `npm run validate:content`,
  `node --check site/questions.js`, typecheck, ownership, and `git diff
  --check`.

No active DATA-INTEGRITY generated-prompt atom is queued right now. The q123
route above is authored-content naturalness plus static mirror sync, not a
generated-template repair.

Next action:
- Do not replay #198, #212, #228, or #352. If REVIEWER files a new
  generated-content defect, route one fresh bounded DATA-INTEGRITY atom with generator,
  validator mirror, focused tests, regenerated CSV, export parity, typecheck,
  ownership, diff check, PR/merge evidence, and journal handoff.
- Broader authored-question naturalness stays with CONTENT unless the defect
  requires generated-template or validator-mirror changes.
- For q123, do not split authored data cleanup from `site/questions.js`
  regeneration. The acceptance bar is one green current-main source state with
  q123 learner-facing explanations cleaned in data and static mirror, export
  parity, static-site parity, `validate:content`, focused content/source
  tests, typecheck, ownership, Prettier, diff checks, and journal evidence.

Additional active route:
- `CONTENT-Q124-OPTION-NATURALNESS-STATIC-MIRROR-1`: CONTENT Pane 1 audit PR
  #398 / `ec0f0b3` found q124 wrong-option text is learner-visible and stilted
  in both canonical authored content and the static mirror:
  `Midsommarafton började alltid firas på en fredag` / `Midsummer Eve began
  always being celebrated on a Friday`. Preserve the UHR locator
  `Sverige i fokus`, `Traditioner och högtider` / `Sveriges nationaldag`,
  printed page 46. CONTENT owns the authored option wording only;
  DATA-INTEGRITY owns regenerated `site/questions.js`, static-site parity,
  validator/export evidence, and journal handoff. Acceptance requires a green
  current-main state with canonical data and static mirror aligned, export
  parity, `node scripts/export-site-question-bank.js --check`,
  `npm run validate:content`, focused content tests, typecheck, ownership,
  Prettier, diff checks, and PR/merge evidence.

CEO correction 2026-05-18T17:03:23+02:00:
Keep `CONTENT-Q123-SOURCE-SECTION-STATIC-MIRROR-1` active, but enforce the lane
ownership split inside that stacked route. CONTENT owns the authored q123
wording change in `data/additionalQuestions.ts`; DATA-INTEGRITY owns
regenerated `site/questions.js`, static-site parity, validator/export evidence,
and its journal handoff. A single PR is acceptable only if the handoff names
which lane owned which files and all gates are green. DATA-INTEGRITY must not
become the owner of authored q123 text.
